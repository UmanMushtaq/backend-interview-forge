import type { QuizQuestion } from '../../types';

export const postgresql: QuizQuestion[] = [
  {
    id: 'pg-isolation-001',
    category: 'postgresql',
    subcategory: 'isolation-levels',
    difficulty: 'core',
    question: 'Under PostgreSQL READ COMMITTED, which anomaly is still possible?',
    options: [
      'Dirty reads',
      'Non-repeatable reads',
      'Reading uncommitted data from other transactions',
      'Nothing — it is fully serializable',
    ],
    correctIndex: 1,
    explanation:
      'READ COMMITTED (the Postgres default) prevents dirty reads, so you never see another transaction\'s uncommitted changes. However each statement sees a fresh snapshot, so re-reading the same row can return different values if another transaction committed in between — a non-repeatable read. To prevent that you need REPEATABLE READ, and SERIALIZABLE to also prevent phantoms and write skew.',
    interviewTip: 'Default is READ COMMITTED; jump to SERIALIZABLE only when write skew matters.',
  },
  {
    id: 'pg-nplus1-001',
    category: 'postgresql',
    subcategory: 'n-plus-1',
    difficulty: 'core',
    question: 'An ORM loads 100 orders then lazily loads each order\'s customer. What is the problem and fix?',
    options: [
      'No problem — lazy loading is always optimal',
      'N+1 queries; fix with a JOIN or an eager/batched relation load',
      'Too much memory; fix by adding an index',
      'A deadlock; fix by lowering the isolation level',
    ],
    correctIndex: 1,
    explanation:
      'This is the classic N+1 problem: 1 query for the orders plus 1 query per order for its customer, so 101 round trips. The fix is to fetch the related rows in a single query (a JOIN, or the ORM\'s eager/relations loading, or a batched IN query). Watch your query logs in development — N+1 is invisible until you count the statements.',
    interviewTip: 'Always say you watch the SQL log to catch N+1 early.',
  },
  {
    id: 'pg-money-001',
    category: 'postgresql',
    subcategory: 'data-types',
    difficulty: 'foundation',
    question: 'Which type should store monetary amounts in a fintech database?',
    options: [
      'double precision (float8)',
      'real (float4)',
      'numeric/decimal with a fixed scale',
      'text, formatted as currency',
    ],
    correctIndex: 2,
    explanation:
      'Floating point cannot represent values like 0.10 exactly, so sums drift and reconciliations fail — unacceptable for money. Use numeric/decimal with an explicit precision and scale (or store integer minor units like cents). The trade-off is that numeric math is slower than float, but correctness wins for currency.',
    interviewTip: 'Say numeric(precision, scale) or integer cents — never float for money.',
  },
  {
    id: 'pg-index-gin-001',
    category: 'postgresql',
    subcategory: 'indexes',
    difficulty: 'expert',
    question: 'You frequently filter rows by keys inside a JSONB column. Which index helps most?',
    options: [
      'A B-tree index on the whole JSONB column',
      'A GIN index on the JSONB column',
      'A hash index on the table primary key',
      'No index can help JSONB queries',
    ],
    correctIndex: 1,
    explanation:
      'GIN (Generalized Inverted Index) is built for containment and key/element lookups, which is exactly how JSONB and arrays are queried (the @> operator, key existence, etc.). A plain B-tree only helps equality/range on a scalar value, not membership inside a document. The trade-off is that GIN indexes are larger and slower to update, so index only the access patterns you actually run.',
  },
  {
    id: 'pg-acid-001',
    category: 'postgresql',
    subcategory: 'acid',
    difficulty: 'foundation',
    question: 'What does the "D" in ACID stand for, and what does PostgreSQL do to guarantee it?',
    options: [
      'Distribution — data is replicated to standby nodes',
      'Durability — committed transactions survive crashes because changes are written to the WAL before the transaction confirms',
      'Determinism — the same query always returns the same row order',
      'Decoupling — tables are isolated in separate files',
    ],
    correctIndex: 1,
    explanation:
      'Durability means that once a COMMIT returns success, the data will survive a crash. PostgreSQL achieves this through Write-Ahead Logging (WAL): every change is appended to the WAL on durable storage before the transaction is acknowledged. On recovery after a crash, Postgres replays the WAL to restore any changes that were in flight. Replication and backups add further safety but are separate concerns.',
    interviewTip: 'WAL is also the foundation for logical and streaming replication, so mention it broadly.',
  },
  {
    id: 'pg-acid-002',
    category: 'postgresql',
    subcategory: 'acid',
    difficulty: 'core',
    question: 'Which ACID property does a BEGIN...COMMIT block enforce when two statements must succeed or both be rolled back?',
    options: [
      'Consistency',
      'Isolation',
      'Atomicity',
      'Durability',
    ],
    correctIndex: 2,
    explanation:
      'Atomicity means all operations in a transaction are treated as a single unit: either all commit or all roll back. If an error occurs midway, any partial writes are undone automatically. Consistency means constraints are not violated, Isolation controls visibility between concurrent transactions, and Durability ensures committed data persists.',
  },
  {
    id: 'pg-locking-001',
    category: 'postgresql',
    subcategory: 'locking',
    difficulty: 'expert',
    question: 'What is the difference between optimistic and pessimistic locking?',
    options: [
      'Optimistic locking uses SELECT FOR UPDATE; pessimistic uses version columns',
      'Optimistic locking detects conflicts at commit time using a version/timestamp; pessimistic locking blocks other readers or writers at query time',
      'They are equivalent; the terms are just convention',
      'Pessimistic locking is always safer and should be the default choice',
    ],
    correctIndex: 1,
    explanation:
      'Pessimistic locking (SELECT FOR UPDATE) acquires a row-level lock at read time, preventing concurrent modification until the transaction ends — good for high-contention scenarios. Optimistic locking adds a version counter to the row; each UPDATE checks the version and fails if another writer changed it, then the application retries — better for low-contention workloads where full blocking is wasteful. PostgreSQL supports both.',
    interviewTip: 'In interview: pick optimistic by default, pessimistic when contention is consistently high.',
  },
  {
    id: 'pg-explain-001',
    category: 'postgresql',
    subcategory: 'query-performance',
    difficulty: 'core',
    question: 'What additional information does EXPLAIN ANALYZE provide compared to plain EXPLAIN?',
    options: [
      'It shows the table schema alongside the query plan',
      'It actually executes the query and shows real row counts and timing next to the estimated values',
      'It generates an index recommendation',
      'It rewrites the query to a more efficient form',
    ],
    correctIndex: 1,
    explanation:
      'Plain EXPLAIN shows the planner\'s estimated cost and row counts without running the query. EXPLAIN ANALYZE actually executes the query and appends real timing (ms) and actual row counts to each node, letting you spot where estimates are wildly off. A huge gap between estimated and actual rows usually means stale statistics — run ANALYZE to fix it. Remember that EXPLAIN ANALYZE on a write query will actually mutate data, so wrap it in a transaction you roll back.',
    interviewTip: 'Look for Seq Scan on large tables and large estimate-vs-actual row gaps as the main red flags.',
  },
  {
    id: 'pg-index-partial-001',
    category: 'postgresql',
    subcategory: 'indexes',
    difficulty: 'expert',
    question: 'You have a table of 10 million tasks but only ever query the ~5 000 that have status = \'pending\'. What index type is most efficient?',
    options: [
      'A full B-tree index on the status column',
      'A GIN index on all columns',
      'A partial index: CREATE INDEX ON tasks (id) WHERE status = \'pending\'',
      'No index — a small result set is always a full scan',
    ],
    correctIndex: 2,
    explanation:
      'A partial index includes only the rows matching the WHERE predicate, so it is tiny compared to a full index over 10 million rows. Postgres uses it automatically when the query\'s WHERE clause matches the index predicate. This saves storage and keeps the index in memory. A full B-tree on a low-cardinality column like status can actually hurt because the planner may not use it when the value is common.',
  },
  {
    id: 'pg-vacuum-001',
    category: 'postgresql',
    subcategory: 'maintenance',
    difficulty: 'core',
    question: 'Why does PostgreSQL need VACUUM, and what happens if it is neglected for a long time?',
    options: [
      'VACUUM rebuilds all indexes nightly; without it queries slow down by 50%',
      'PostgreSQL uses MVCC and marks deleted/updated rows as dead tuples rather than removing them; without VACUUM dead tuples accumulate, bloating tables, and transaction ID wraparound can eventually make the database refuse writes',
      'VACUUM is only needed on tables larger than 1 GB',
      'VACUUM flushes the WAL to disk; without it data is lost on crash',
    ],
    correctIndex: 1,
    explanation:
      'MVCC keeps old row versions visible to concurrent transactions, so deleted or updated rows become "dead" rather than immediately reclaimed. VACUUM reclaims that space and updates visibility maps. Neglecting it causes table bloat and, crucially, transaction ID (XID) wraparound: PostgreSQL uses 32-bit XIDs and if the gap reaches ~2 billion without VACUUM, it will freeze the database to prevent data corruption. Autovacuum handles this automatically in most cases.',
    interviewTip: 'Mention XID wraparound — it shows you understand MVCC deeply.',
  },
  {
    id: 'pg-window-001',
    category: 'postgresql',
    subcategory: 'window-functions',
    difficulty: 'core',
    question: 'How is a window function different from GROUP BY aggregation?',
    options: [
      'Window functions always run faster than GROUP BY',
      'Window functions compute an aggregate value alongside each row without collapsing rows into groups',
      'GROUP BY keeps all rows; window functions reduce the result set',
      'They are identical; window functions are syntactic sugar for GROUP BY',
    ],
    correctIndex: 1,
    explanation:
      'GROUP BY collapses many rows into one per group, discarding individual row data. A window function (OVER clause) computes an aggregate across a partition but keeps every original row in the output. This makes it easy to answer questions like "rank each sale within its region" or "running total per month" without losing row-level detail. Common window functions include ROW_NUMBER, RANK, LAG, LEAD, and SUM/AVG OVER.',
    interviewTip: 'Give a concrete example: rank employees by salary within their department using RANK() OVER (PARTITION BY dept ORDER BY salary DESC).',
  },
  {
    id: 'pg-pooling-001',
    category: 'postgresql',
    subcategory: 'connection-pooling',
    difficulty: 'core',
    question: 'Why should a high-traffic Node.js API use a connection pool (like PgBouncer or a driver pool) instead of opening a new database connection per request?',
    options: [
      'PostgreSQL only allows one connection at a time',
      'Each new connection forks a process on the Postgres side, so hundreds of connections waste memory and CPU; a pool reuses a fixed set of connections',
      'Connection pools add retry logic that raw connections lack',
      'Node.js cannot open TCP connections directly to PostgreSQL',
    ],
    correctIndex: 1,
    explanation:
      'PostgreSQL spawns a backend process for every connection, consuming roughly 5-10 MB of memory each. With hundreds of short-lived requests per second, connection setup overhead and memory pressure become serious bottlenecks. A pool keeps a small fixed number of connections open and queues requests, drastically reducing OS resources. PgBouncer sits in front of Postgres for transaction-mode pooling, which is the most efficient but restricts session-level features.',
    interviewTip: 'Distinguish application-level pools (pg.Pool in node-postgres) from middleware pools (PgBouncer) — they solve the same problem at different layers.',
  },
  {
    id: 'pg-migration-001',
    category: 'postgresql',
    subcategory: 'migrations',
    difficulty: 'foundation',
    question: 'Why should database migrations be kept in version control alongside application code?',
    options: [
      'Postgres requires migration files to be committed before applying them',
      'So that schema changes are tracked, reproducible, reviewed in pull requests, and can be rolled back to any previous state in any environment',
      'Version control compresses migration files automatically',
      'Migration files are auto-generated from git history by ORM tools',
    ],
    correctIndex: 1,
    explanation:
      'Treating migrations as code means every schema change has a history, can be reviewed, and can be replayed in CI, staging, or a fresh developer machine. Without version-controlled migrations it is easy for environments to drift, making "it works on my machine" bugs hard to diagnose. Tools like Flyway, Liquibase, and TypeORM migrations track which files have been applied via a migrations table.',
    interviewTip: 'Mention that migration files should be immutable once merged — never edit an already-applied migration.',
  },
  {
    id: 'pg-btree-001',
    category: 'postgresql',
    subcategory: 'indexes',
    difficulty: 'foundation',
    question: 'Which operator class is a B-tree index optimised for in PostgreSQL?',
    options: [
      'Full-text and document containment operators (@>, @@)',
      'Equality and range operators (=, <, >, BETWEEN, ORDER BY)',
      'Geometric and spatial operators',
      'Pattern matching with LIKE "%suffix"',
    ],
    correctIndex: 1,
    explanation:
      'B-tree is the default and most general-purpose index type in Postgres. It supports equality and the six comparison operators, making it ideal for primary keys, foreign keys, and any column used in ORDER BY or range queries. It cannot accelerate full-text search (use GIN with tsvector) or containment operators on JSONB (use GIN with jsonb_ops). A LIKE prefix search (\'foo%\') can use a B-tree with the right operator class; a suffix or infix pattern cannot.',
  },
  {
    id: 'pg-isolation-002',
    category: 'postgresql',
    subcategory: 'isolation-levels',
    difficulty: 'expert',
    question: 'Two concurrent transactions both read a counter, each add 1, and write back. Under SERIALIZABLE, what happens to the second writer?',
    options: [
      'Both writes succeed and the counter is incremented by 2',
      'The second transaction\'s commit fails with a serialization error and must retry',
      'The second transaction\'s write is silently discarded',
      'PostgreSQL merges the two writes by averaging them',
    ],
    correctIndex: 1,
    explanation:
      'SERIALIZABLE uses Serializable Snapshot Isolation (SSI), which detects that the two transactions\' read/write patterns would produce an outcome impossible in any serial execution. The second committer receives a serialization_failure error (SQLSTATE 40001) and must retry the entire transaction. This is safer than just using READ COMMITTED (lost update) or SELECT FOR UPDATE (explicit lock), but requires application retry logic.',
    interviewTip: 'Serialization failures are expected in normal operation — design retry loops with exponential back-off.',
  },
  {
    id: 'pg-explain-002',
    category: 'postgresql',
    subcategory: 'query-performance',
    difficulty: 'expert',
    question: 'In an EXPLAIN ANALYZE output, what does a large difference between "rows=estimate" and "actual rows" usually indicate?',
    options: [
      'The query is correct and the planner is conservative on purpose',
      'Table statistics are stale; running ANALYZE on the table will update the planner\'s row estimates',
      'The index is corrupt and must be rebuilt',
      'The WHERE clause has a syntax error that is silently ignored',
    ],
    correctIndex: 1,
    explanation:
      'The query planner builds its execution plan using statistics stored in pg_statistic (updated by ANALYZE or autovacuum). When the statistics are stale — for example after a large data load — the planner can badly misestimate row counts, choose a nested loop instead of a hash join, and run orders of magnitude slower. Running ANALYZE refreshes those statistics without locking the table. For extremely non-uniform distributions, consider increasing the statistics target with ALTER COLUMN ... SET STATISTICS.',
  },
  {
    id: 'pg-window-002',
    category: 'postgresql',
    subcategory: 'window-functions',
    difficulty: 'expert',
    question: 'What is the purpose of the PARTITION BY clause inside an OVER window definition?',
    options: [
      'It physically splits the table into separate storage partitions',
      'It divides rows into independent groups within which the window function is reset and computed separately',
      'It filters out rows before the window function runs, like WHERE',
      'It orders rows so the window function processes them in a defined sequence',
    ],
    correctIndex: 1,
    explanation:
      'PARTITION BY is the window analogue of GROUP BY: it tells the window function to treat each partition as an independent scope without collapsing rows. For example, RANK() OVER (PARTITION BY department ORDER BY salary DESC) computes a fresh rank within each department. Without PARTITION BY the window spans all rows. ORDER BY inside OVER controls the frame ordering, not filtering.',
  },
  {
    id: 'pg-locking-002',
    category: 'postgresql',
    subcategory: 'locking',
    difficulty: 'core',
    question: 'You want to lock a row for update and skip it if it is already locked. Which SQL clause achieves this?',
    options: [
      'SELECT ... FOR UPDATE WAIT 0',
      'SELECT ... FOR UPDATE SKIP LOCKED',
      'SELECT ... FOR SHARE',
      'SELECT ... NOWAIT only',
    ],
    correctIndex: 1,
    explanation:
      'SELECT ... FOR UPDATE SKIP LOCKED atomically acquires a row lock and skips any rows currently locked by other transactions rather than waiting or erroring. This is the standard building block for work queues in PostgreSQL: multiple workers can each grab a batch of unlocked tasks without stepping on each other. FOR UPDATE NOWAIT is similar but raises an error on contention instead of skipping.',
    interviewTip: 'SKIP LOCKED is the idiomatic Postgres job-queue primitive — mention it when asked about queue implementations.',
  },
];
