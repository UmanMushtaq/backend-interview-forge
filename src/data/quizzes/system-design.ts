import type { QuizQuestion } from '../../types';

export const systemDesign: QuizQuestion[] = [
  {
    id: 'sd-cap-001',
    category: 'system-design',
    subcategory: 'cap-theorem',
    difficulty: 'core',
    question: 'During a network partition, CAP says you must choose between which two properties?',
    options: [
      'Consistency and Availability',
      'Consistency and Partition tolerance',
      'Availability and Partition tolerance',
      'Latency and Throughput',
    ],
    correctIndex: 0,
    explanation:
      'Partition tolerance is non-negotiable in a distributed system  -  networks fail  -  so the real choice during a partition is between Consistency and Availability. A CP system rejects or blocks requests it cannot make consistent; an AP system keeps serving but may return stale data and reconcile later. Most real systems pick per-operation: strong consistency for money movements, availability for feeds.',
    interviewTip: 'Frame CAP as a per-operation choice, not a whole-system label.',
  },
  {
    id: 'sd-cqrs-001',
    category: 'system-design',
    subcategory: 'cqrs',
    difficulty: 'expert',
    question: 'What problem does CQRS primarily solve?',
    options: [
      'It removes the need for a database',
      'It lets the read model and write model scale and be shaped independently',
      'It guarantees ACID across microservices',
      'It eliminates eventual consistency',
    ],
    correctIndex: 1,
    explanation:
      'CQRS (Command Query Responsibility Segregation) splits the write path from the read path so each can use a model and store optimized for its job  -  for example a normalized write store and denormalized, fast read projections. This shines when read and write loads are very asymmetric. The cost is added complexity and usually eventual consistency between the command side and the query projections.',
    interviewTip: 'Only reach for CQRS when read/write needs genuinely diverge.',
  },
  {
    id: 'sd-circuit-001',
    category: 'system-design',
    subcategory: 'resilience',
    difficulty: 'core',
    question: 'What does a circuit breaker protect against?',
    options: [
      'SQL injection',
      'Cascading failures by stopping calls to a failing dependency and failing fast',
      'Data corruption during writes',
      'Unauthorized API access',
    ],
    correctIndex: 1,
    explanation:
      'A circuit breaker tracks failures to a downstream dependency and, once a threshold is crossed, opens to fail fast instead of piling up slow calls and exhausting threads or connections  -  preventing one sick service from dragging down its callers. After a cooldown it goes half-open to test recovery before closing again. It is usually paired with timeouts, retries with back-off, and a fallback response.',
    interviewTip: 'Closed -> Open (fail fast) -> Half-open (probe) -> Closed.',
  },
  {
    id: 'sd-db-per-service-001',
    category: 'system-design',
    subcategory: 'microservices',
    difficulty: 'expert',
    question: 'Why adopt database-per-service in a microservice architecture?',
    options: [
      'To allow services to share tables and join freely',
      'To enforce loose coupling and independent deployability, at the cost of cross-service queries and transactions',
      'Because one database cannot hold multiple schemas',
      'To guarantee global ACID transactions',
    ],
    correctIndex: 1,
    explanation:
      'Giving each service its own database prevents hidden coupling through shared tables, so teams can evolve schemas and deploy independently  -  a core goal of microservices. The trade-off is that you lose cross-service JOINs and single-database transactions, so you reach for API composition, data replication, and Sagas instead. If services constantly need each other\'s tables, that is a signal the boundaries are drawn wrong.',
    interviewTip: 'Private data per service is what makes services independently deployable.',
  },
  {
    id: 'sd-ddd-layers-001',
    category: 'system-design',
    subcategory: 'ddd',
    difficulty: 'core',
    question: 'In Domain-Driven Design, which layer is responsible for business rules and invariants?',
    options: [
      'Infrastructure layer',
      'Application layer',
      'Domain layer',
      'Presentation layer',
    ],
    correctIndex: 2,
    explanation:
      'The Domain layer contains entities, value objects, aggregates, domain services, and domain events  -  it is the heart of the business logic and enforces invariants independently of infrastructure concerns like databases or HTTP. The Application layer orchestrates use cases by coordinating domain objects and calling infrastructure ports. The Infrastructure layer implements those ports with real technology (databases, message brokers, HTTP clients). Keeping business rules out of the Application and Infrastructure layers makes them testable without external dependencies.',
    interviewTip: 'Domain layer has zero infrastructure dependencies; test it with pure unit tests.',
  },
  {
    id: 'sd-hexagonal-001',
    category: 'system-design',
    subcategory: 'hexagonal-architecture',
    difficulty: 'core',
    question: 'What is the central idea behind hexagonal architecture (Ports and Adapters)?',
    options: [
      'All services must be hexagonal in shape on the deployment diagram',
      'The application core defines interfaces (ports) that the outside world implements (adapters), keeping the core independent of frameworks and infrastructure',
      'Business logic is spread across six equal layers',
      'Only six external dependencies are allowed per service',
    ],
    correctIndex: 1,
    explanation:
      'Hexagonal architecture (also called Ports and Adapters) isolates the application core behind technology-neutral interfaces called ports. Adapters implement those ports for specific technologies  -  a REST controller, a Kafka consumer, a PostgreSQL repository  -  and can be swapped without changing core logic. This makes the domain logic testable with in-memory adapters, portable across frameworks, and easy to evolve when infrastructure choices change.',
    interviewTip: 'Core defines interfaces (ports); adapters implement them  -  dependency points inward.',
  },
  {
    id: 'sd-event-sourcing-001',
    category: 'system-design',
    subcategory: 'event-sourcing',
    difficulty: 'expert',
    question: 'What is the fundamental storage mechanism in event sourcing?',
    options: [
      'The current state of each aggregate is stored and updated in place',
      'An immutable, append-only log of domain events from which current state is derived by replaying them',
      'A snapshot of the database taken every hour',
      'A graph of related entities stored in a document database',
    ],
    correctIndex: 1,
    explanation:
      'In event sourcing, you never update records in place. Instead you append domain events (e.g. OrderPlaced, ItemShipped) to an event store, and the current state of an aggregate is derived by replaying all its events from the beginning (or from a snapshot). This gives you a complete audit trail for free, the ability to reconstruct state at any point in time, and a natural event stream to feed downstream projections and process managers.',
    interviewTip: 'Event sourcing = the event log IS the source of truth; state is a projection.',
  },
  {
    id: 'sd-event-sourcing-002',
    category: 'system-design',
    subcategory: 'event-sourcing',
    difficulty: 'expert',
    question: 'What is the purpose of a snapshot in an event-sourced system?',
    options: [
      'To replace the event log with a database backup',
      'To periodically capture the current aggregate state so replays only need to process events after the snapshot',
      'To compress events into a smaller format',
      'To synchronize data across microservices',
    ],
    correctIndex: 1,
    explanation:
      'Replaying every event from the beginning to load an aggregate becomes slow when event counts are large. A snapshot captures the aggregate state at a specific event sequence number, so subsequent loads only replay events after that position rather than the entire history. Snapshots are an optimization and do not replace the event log  -  you can always rebuild them by deleting the snapshot and replaying from event zero.',
    interviewTip: 'Snapshots are a performance optimization; the event log remains the source of truth.',
  },
  {
    id: 'sd-monolith-vs-microservices-001',
    category: 'system-design',
    subcategory: 'microservices',
    difficulty: 'core',
    question: 'What is the strongest argument for starting with a monolith rather than microservices for a new product?',
    options: [
      'Monoliths are always faster to run in production',
      'Boundaries and domain understanding are unclear early on; a monolith lets you refactor freely before committing to service contracts',
      'Microservices require Kubernetes which is too expensive',
      'Monoliths eliminate the need for a message broker',
    ],
    correctIndex: 1,
    explanation:
      'Getting service boundaries wrong in microservices is expensive  -  you end up with chatty services, distributed monolith coupling, or cross-service transactions. In the early stages of a product the domain model is still being discovered, so a well-structured modular monolith lets you iterate on boundaries cheaply. Once traffic and team structure demands it, you can extract services along the boundaries you have already validated.',
    interviewTip: 'Martin Fowler: "Don\'t start with microservices, start with a monolith and extract."',
  },
  {
    id: 'sd-horizontal-scaling-001',
    category: 'system-design',
    subcategory: 'scalability',
    difficulty: 'foundation',
    question: 'What is the difference between horizontal and vertical scaling?',
    options: [
      'Horizontal scaling adds more CPUs to an existing machine; vertical scaling adds more machines',
      'Horizontal scaling adds more instances (machines); vertical scaling increases the resources of an existing instance',
      'Horizontal scaling is for databases only; vertical scaling is for application servers',
      'They are synonymous terms for the same concept',
    ],
    correctIndex: 1,
    explanation:
      'Vertical scaling (scale up) means giving one machine more CPU, RAM, or disk. It is simple but has a hard ceiling and a single point of failure. Horizontal scaling (scale out) means adding more instances and distributing load across them with a load balancer. Horizontal scaling is generally preferred for stateless application tiers because it has no hard ceiling and improves availability through redundancy. Stateful services require more care  -  sticky sessions or shared state stores.',
    interviewTip: 'Design stateless application tiers so horizontal scaling is as simple as adding instances.',
  },
  {
    id: 'sd-load-balancing-001',
    category: 'system-design',
    subcategory: 'scalability',
    difficulty: 'foundation',
    question: 'Which load-balancing algorithm sends each new request to the instance with the fewest active connections?',
    options: [
      'Round Robin',
      'Least Connections',
      'IP Hash',
      'Random',
    ],
    correctIndex: 1,
    explanation:
      'Least Connections routes each incoming request to the backend instance that currently has the smallest number of open connections, making it adaptive when requests have widely varying processing times. Round Robin distributes requests sequentially and works well when requests are homogeneous. IP Hash routes based on the client IP hash, providing session stickiness. For variable-duration workloads, Least Connections (or Least Response Time) usually outperforms Round Robin.',
    interviewTip: 'Least Connections adapts to uneven request durations; use it for heterogeneous workloads.',
  },
  {
    id: 'sd-idempotency-keys-001',
    category: 'system-design',
    subcategory: 'reliability',
    difficulty: 'core',
    question: 'A client retries a payment API call after a network timeout. How do you prevent a double charge?',
    options: [
      'Use a unique auto-increment ID generated by the server on first receipt',
      'Require the client to include a client-generated idempotency key; the server stores it and returns the cached result on duplicate',
      'Disable retries on the client side',
      'Use optimistic locking on the payment record',
    ],
    correctIndex: 1,
    explanation:
      'An idempotency key is a unique identifier (e.g. a UUID) the client generates before the first attempt and includes in every retry. The server stores the key alongside the result when the operation completes, so subsequent requests with the same key return the stored outcome without re-executing. The key must be stored atomically with the operation (e.g. in the same database transaction) to avoid race conditions. This pattern is used extensively in payment APIs like Stripe.',
    interviewTip: 'Client generates the key before first attempt; server stores key + result atomically.',
  },
  {
    id: 'sd-outbox-pattern-001',
    category: 'system-design',
    subcategory: 'reliability',
    difficulty: 'expert',
    question: 'What problem does the Transactional Outbox pattern solve?',
    options: [
      'It speeds up database writes by batching them',
      'It atomically persists a database change and the corresponding message in the same transaction, then reliably publishes the message asynchronously',
      'It provides a dead-letter queue for failed HTTP calls',
      'It replicates data across multiple database regions',
    ],
    correctIndex: 1,
    explanation:
      'Saving to a database and publishing a message to a broker are two separate operations, and network failures can cause one to succeed while the other fails  -  resulting in lost or duplicate events. The Outbox pattern writes the outgoing message to an outbox table in the same local database transaction as the business state change. A separate relay process (polling or CDC via Debezium) reads the outbox and publishes to the broker, guaranteeing at-least-once delivery without a distributed transaction.',
    interviewTip: 'Outbox + CDC = atomic write + reliable publish without 2PC.',
  },
  {
    id: 'sd-cdc-001',
    category: 'system-design',
    subcategory: 'reliability',
    difficulty: 'expert',
    question: 'What is Change Data Capture (CDC) and why is it used with the Outbox pattern?',
    options: [
      'CDC is a caching strategy that captures cache misses for later population',
      'CDC reads the database transaction log to detect and stream row-level changes to downstream consumers, avoiding polling overhead',
      'CDC compresses database backups for faster restore',
      'CDC is a Kafka-specific feature for consumer group coordination',
    ],
    correctIndex: 1,
    explanation:
      'Change Data Capture (tools like Debezium) tails the database write-ahead log (WAL or binlog) to detect inserts, updates, and deletes and stream them to a message broker in near real-time. When combined with the Outbox pattern, CDC eliminates the need for a polling loop  -  as soon as a row is written to the outbox table, the WAL change is captured and published. This gives lower latency, lower database load, and exactly the at-least-once guarantee the Outbox provides.',
    interviewTip: 'Debezium + Outbox = event publishing without polling and without 2PC.',
  },
  {
    id: 'sd-api-gateway-001',
    category: 'system-design',
    subcategory: 'microservices',
    difficulty: 'foundation',
    question: 'What cross-cutting concerns does an API Gateway typically handle?',
    options: [
      'Business logic and domain validation for each service',
      'Authentication, rate limiting, routing, SSL termination, and request aggregation at the edge',
      'Database schema migrations across all services',
      'Consumer group offset management for Kafka topics',
    ],
    correctIndex: 1,
    explanation:
      'An API Gateway sits at the perimeter and handles concerns that all services share: authenticating requests (validating JWTs), enforcing rate limits, routing to downstream services, terminating TLS, and optionally aggregating multiple backend calls into one response for the client. This keeps individual services lean and avoids duplicating cross-cutting code. The risk is making the gateway too smart  -  business logic should live in services, not the gateway.',
    interviewTip: 'Gateway = thin infrastructure concerns at the edge; keep business logic in services.',
  },
  {
    id: 'sd-saga-vs-2pc-001',
    category: 'system-design',
    subcategory: 'distributed-transactions',
    difficulty: 'expert',
    question: 'Why is two-phase commit (2PC) rarely used in microservice architectures?',
    options: [
      'Because 2PC does not support SQL databases',
      'Because 2PC requires all participants to hold locks and remain available during the commit phase, creating tight coupling and availability risk',
      'Because 2PC produces duplicate messages',
      'Because 2PC only works with a single database engine',
    ],
    correctIndex: 1,
    explanation:
      '2PC requires a coordinator to drive all participants through a prepare phase (lock resources) and then a commit phase. If the coordinator or any participant becomes unavailable during the protocol, all participants are blocked holding locks  -  the blocking problem. In microservices with independent deployments and network unreliability, this blocking is unacceptable. The Saga pattern replaces ACID atomicity with eventual consistency and compensating transactions, accepting weaker isolation in exchange for availability and loose coupling.',
    interviewTip: '2PC = blocking + coordinator SPOF; Saga = eventual consistency + compensating actions.',
  },
  {
    id: 'sd-service-mesh-001',
    category: 'system-design',
    subcategory: 'microservices',
    difficulty: 'expert',
    question: 'What does a service mesh provide that a plain API Gateway does not?',
    options: [
      'Public internet routing and DNS management',
      'East-west (service-to-service) observability, mTLS, retries, and circuit breaking without application code changes via sidecar proxies',
      'Schema validation for Kafka messages',
      'Horizontal scaling of database read replicas',
    ],
    correctIndex: 1,
    explanation:
      'An API Gateway handles north-south traffic (clients to services). A service mesh (e.g. Istio, Linkerd) addresses east-west traffic between services by injecting a sidecar proxy alongside each service. The sidecars enforce mTLS for mutual authentication, collect distributed traces and metrics, implement retries with back-off, and provide circuit breaking  -  all without changing application code. This is powerful in large microservice environments but adds significant operational complexity.',
    interviewTip: 'API Gateway = client-to-service (north-south); Service Mesh = service-to-service (east-west).',
  },
  {
    id: 'sd-eventual-consistency-001',
    category: 'system-design',
    subcategory: 'consistency',
    difficulty: 'core',
    question: 'Which statement best describes eventual consistency?',
    options: [
      'Data is always consistent at the moment it is read',
      'If no new updates are made, all replicas will converge to the same value given sufficient time',
      'The system rejects reads until all replicas are synchronized',
      'Data is replicated synchronously to all nodes before a write is acknowledged',
    ],
    correctIndex: 1,
    explanation:
      'Eventual consistency (an AP property in CAP) means replicas may return stale reads in the short term but will converge to the latest value once propagation completes and no new writes occur. This is the consistency model for systems that prioritize availability and partition tolerance  -  DNS, shopping carts, social media feeds, and most CQRS read projections. The key engineering tasks are handling read-your-writes expectations and reconciling conflicts when they arise.',
    interviewTip: 'Eventual consistency: stale reads OK, all nodes converge if writes stop.',
  },
];
