import type { QuizQuestion } from '../../types';

export const redis: QuizQuestion[] = [
  {
    id: 'redis-lock-001',
    category: 'redis',
    subcategory: 'distributed-locks',
    difficulty: 'core',
    question: 'Which command acquires a simple distributed lock that auto-expires?',
    options: [
      'SET lock:key token NX EX 30',
      'SET lock:key token',
      'GET lock:key then SET lock:key token',
      'INCR lock:key',
    ],
    correctIndex: 0,
    explanation:
      'SET key value NX EX 30 sets the key only if it does not already exist (NX) and attaches a 30s expiry (EX) atomically, which is exactly what a lock needs. The GET-then-SET option is a race condition because two clients can both read "empty" and both write. Always store a unique token as the value so the owner can safely release it with a compare-and-delete Lua script.',
    interviewTip: 'NX gives mutual exclusion; EX gives a safety timeout if the holder crashes.',
  },
  {
    id: 'redis-cache-aside-001',
    category: 'redis',
    subcategory: 'caching',
    difficulty: 'foundation',
    question: 'In the cache-aside pattern, what happens on a cache miss?',
    options: [
      'The cache loads the value itself from the database automatically',
      'The application reads from the database, returns it, and writes it back into the cache',
      'The request fails until the cache is warmed',
      'The database is bypassed entirely',
    ],
    correctIndex: 1,
    explanation:
      'With cache-aside (lazy loading), the application is in charge: on a miss it fetches from the source of truth, returns the value, and populates the cache with a TTL for next time. The cache itself is passive — it does not know about your database. The main risks are stale data (mitigated by TTL and invalidation on write) and the thundering-herd of many misses at once.',
    interviewTip: 'Contrast cache-aside (app loads) with read-through (cache loads).',
  },
  {
    id: 'redis-eviction-001',
    category: 'redis',
    subcategory: 'eviction',
    difficulty: 'core',
    question: 'Redis is used purely as a cache and is full. Which maxmemory-policy fits best?',
    options: [
      'noeviction',
      'allkeys-lru',
      'volatile-ttl only on keys you forgot to expire',
      'It is impossible to evict in Redis',
    ],
    correctIndex: 1,
    explanation:
      'For a pure cache you want Redis to drop the least valuable data when memory is full, and allkeys-lru evicts the least-recently-used key across the whole keyspace. noeviction would instead start rejecting writes, which is wrong for a cache. If only some keys have TTLs and others are persistent state, a volatile-* policy may fit better, but a dedicated cache usually uses allkeys-lru or allkeys-lfu.',
  },
  {
    id: 'redis-threading-001',
    category: 'redis',
    subcategory: 'architecture',
    difficulty: 'expert',
    question: 'Redis executes commands on a single thread. What is the key implication?',
    options: [
      'It can only handle one client connection at a time',
      'Each command is atomic, but one slow command (e.g. KEYS *) blocks everything',
      'It cannot use more than one CPU core for anything',
      'Transactions are impossible',
    ],
    correctIndex: 1,
    explanation:
      'Because the command-execution loop is single-threaded, individual commands run to completion without interleaving, which is why operations are atomic and you rarely need locks for a single key. The flip side is that an O(N) command like KEYS * or a big Lua script stalls every other client until it finishes. That is why you use SCAN instead of KEYS and keep scripts short.',
    interviewTip: 'Single-threaded = free atomicity, but never run O(N) commands in production.',
  },
  {
    id: 'redis-stampede-001',
    category: 'redis',
    subcategory: 'caching',
    difficulty: 'expert',
    question: 'What is a cache stampede and how do you prevent it?',
    options: [
      'A stampede occurs when Redis runs out of memory; prevent it by increasing maxmemory',
      'When a popular key expires, many concurrent requests simultaneously miss the cache and all query the database; prevent it with probabilistic early expiry, a mutex lock on the DB fetch, or background refresh',
      'A stampede is when too many keys are written simultaneously; prevent it with pipelining',
      'It is a Redis Cluster split-brain scenario; prevent it with Sentinel',
    ],
    correctIndex: 1,
    explanation:
      'Cache stampede (thundering herd) happens when a hot key expires and a flood of requests all find a miss at the same moment, each independently hitting the database. Prevention strategies include: (1) probabilistic early expiry — recompute the cache before it fully expires with some probability; (2) a per-key mutex so only one request rebuilds the cache while others wait; (3) keeping a stale value visible while one worker refreshes in the background. The right choice depends on your staleness tolerance.',
    interviewTip: 'Interviewers love this because it shows you think about failure modes, not just the happy path.',
  },
  {
    id: 'redis-eviction-002',
    category: 'redis',
    subcategory: 'eviction',
    difficulty: 'core',
    question: 'How does allkeys-lfu differ from allkeys-lru as an eviction policy?',
    options: [
      'LFU evicts the most recently used key; LRU evicts the least frequently used',
      'LFU evicts the key accessed least frequently over time; LRU evicts the key not accessed for the longest time',
      'They are identical; lfu is just an alias for lru',
      'LFU requires a sorted set; LRU works with any data type',
    ],
    correctIndex: 1,
    explanation:
      'LRU (Least Recently Used) evicts the key whose last access time is oldest, so a popular key that was not touched in the last minute could be dropped. LFU (Least Frequently Used) tracks access frequency and prefers to evict keys that are rarely accessed overall, even if they were just accessed once. LFU is often better for workloads with a stable hot set, while LRU handles recency-heavy access patterns.',
  },
  {
    id: 'redis-persistence-001',
    category: 'redis',
    subcategory: 'persistence',
    difficulty: 'core',
    question: 'What is the key trade-off between RDB snapshots and AOF persistence?',
    options: [
      'RDB is safer; AOF is only for Redis Cluster',
      'RDB writes a point-in-time snapshot periodically, risking data loss since the last snapshot; AOF logs every write command for durability but produces a larger file and slower recovery',
      'AOF only works with string values; RDB supports all data types',
      'They are equivalent; choose based on which has a shorter config file',
    ],
    correctIndex: 1,
    explanation:
      'RDB takes fork-based snapshots at configurable intervals (e.g. every 5 minutes), so a crash can lose up to that interval of writes. It produces a compact binary file and restores quickly. AOF appends every write command and can be synced to disk every second (or every write), greatly reducing potential data loss. AOF files grow large but can be rewritten (BGREWRITEAOF). Many production setups enable both for safety.',
    interviewTip: 'Mention that AOF with appendfsync everysec loses at most ~1 second of writes, a common acceptable trade-off.',
  },
  {
    id: 'redis-pubsub-001',
    category: 'redis',
    subcategory: 'pub-sub',
    difficulty: 'foundation',
    question: 'What is a key limitation of Redis Pub/Sub compared to Redis Streams?',
    options: [
      'Pub/Sub supports only one channel; Streams support many',
      'Messages published to a Pub/Sub channel are not persisted; if a subscriber is offline it misses the message permanently',
      'Pub/Sub requires cluster mode; Streams work on standalone Redis',
      'Pub/Sub only delivers messages to the publisher itself',
    ],
    correctIndex: 1,
    explanation:
      'Redis Pub/Sub is fire-and-forget: messages are delivered to currently connected subscribers and immediately discarded. A subscriber that reconnects after a disconnect has no way to replay missed messages. Redis Streams (XADD/XREAD) persist messages in an append-only log with consumer groups, acknowledged delivery, and the ability to read from any offset — making them suitable for reliable message queues. Pub/Sub is fine for real-time notifications where dropping a message is acceptable.',
    interviewTip: 'Use Streams when you need at-least-once delivery; use Pub/Sub when missing messages is acceptable.',
  },
  {
    id: 'redis-rate-limit-001',
    category: 'redis',
    subcategory: 'rate-limiting',
    difficulty: 'core',
    question: 'How can you implement a sliding-window rate limiter in Redis?',
    options: [
      'INCR a counter key with no expiry and check its value',
      'Use a Sorted Set where each request is a member with its timestamp as score; trim members older than the window and check the count — all in a Lua script for atomicity',
      'Use SUBSCRIBE to count incoming messages per second',
      'Store request timestamps in a Redis List and use LLEN to count them',
    ],
    correctIndex: 1,
    explanation:
      'A sorted set with timestamps as scores lets you ZADD the current request, ZREMRANGEBYSCORE to drop entries older than now minus window, and ZCARD to count remaining entries — all in a single atomic Lua script. If the count exceeds the limit, reject the request. This gives true sliding-window semantics. A simple INCR+EXPIRE is easier but gives fixed-window semantics, which can allow double the limit at window boundaries.',
    interviewTip: 'Mention that wrapping multi-command patterns in Lua scripts preserves atomicity in Redis.',
  },
  {
    id: 'redis-redlock-001',
    category: 'redis',
    subcategory: 'distributed-locks',
    difficulty: 'expert',
    question: 'What problem does the Redlock algorithm solve that a single-node Redis lock does not?',
    options: [
      'Redlock enables locks to last longer than 30 seconds',
      'Redlock provides mutual exclusion even if one Redis node fails, by requiring a majority quorum of independent nodes to grant the lock',
      'Redlock automatically retries failed lock acquisitions indefinitely',
      'Redlock replaces SET NX with a more efficient HSETNX command',
    ],
    correctIndex: 1,
    explanation:
      'A single-instance Redis lock fails if that instance goes down or is partitioned: the lock is lost and two clients can acquire it. Redlock acquires locks on N independent Redis nodes (usually 5) and considers the lock held only when a majority (floor(N/2)+1) grant it within a short deadline. A minority of failed nodes cannot break mutual exclusion. Redlock is still debated (Martin Kleppmann\'s critique is worth reading) but is better than a single-node lock for availability-critical cases.',
    interviewTip: 'Mention the quorum requirement (majority of N nodes) — that is the core of the algorithm.',
  },
  {
    id: 'redis-pipeline-001',
    category: 'redis',
    subcategory: 'performance',
    difficulty: 'core',
    question: 'What benefit does Redis pipelining provide?',
    options: [
      'It makes commands execute in parallel on multiple CPU cores',
      'It batches multiple commands into a single network round trip, dramatically reducing latency when sending many commands in sequence',
      'It guarantees that all commands succeed or all fail atomically',
      'It compresses the data transferred to reduce bandwidth',
    ],
    correctIndex: 1,
    explanation:
      'Without pipelining, each command waits for a round-trip acknowledgement before the next is sent (RTT per command). Pipelining buffers many commands client-side, flushes them in a single write, and reads all responses together, so you pay the RTT only once per batch. This is purely a network optimization — commands still execute sequentially and there is no atomicity guarantee. For atomicity across commands, use MULTI/EXEC transactions or Lua scripts.',
    interviewTip: 'Pipelining cuts latency; MULTI/EXEC gives atomicity — they are different tools for different problems.',
  },
  {
    id: 'redis-streams-001',
    category: 'redis',
    subcategory: 'streams',
    difficulty: 'expert',
    question: 'What is the purpose of a consumer group in Redis Streams?',
    options: [
      'It limits how many messages a single consumer can read per second',
      'It allows multiple independent consumers to share the work of processing a stream, with each message delivered to exactly one consumer in the group and acknowledgement-based redelivery on failure',
      'It publishes messages to multiple Redis nodes simultaneously',
      'It encrypts stream messages at rest',
    ],
    correctIndex: 1,
    explanation:
      'A consumer group distributes stream messages among its members so each message is processed by one consumer (competing consumers). Each consumer calls XREADGROUP to claim new messages and XACK to acknowledge completion. Unacknowledged messages remain in the Pending Entries List (PEL) and can be reclaimed by another consumer after a timeout with XAUTOCLAIM. This gives reliable, at-least-once delivery semantics similar to Kafka consumer groups.',
  },
  {
    id: 'redis-setnx-001',
    category: 'redis',
    subcategory: 'distributed-locks',
    difficulty: 'foundation',
    question: 'Why is it unsafe to use the old SETNX + EXPIRE two-command sequence instead of SET NX EX?',
    options: [
      'SETNX is deprecated and removed in modern Redis',
      'If the process crashes between SETNX and EXPIRE, the key is set without an expiry and the lock is held forever',
      'EXPIRE does not work on keys created by SETNX',
      'SETNX sets the value to 1 regardless of the value argument',
    ],
    correctIndex: 1,
    explanation:
      'SETNX and EXPIRE are two separate commands, so the pair is not atomic. If the process crashes, is killed, or loses connectivity after SETNX succeeds but before EXPIRE runs, the key exists with no TTL and the lock will never be automatically released. The atomic SET key value NX EX seconds command, introduced in Redis 2.6.12, eliminates this window entirely by setting the key and its expiry in one atomic operation.',
    interviewTip: 'Always use the single SET NX EX form — the two-command form is a well-known antipattern.',
  },
  {
    id: 'redis-data-types-001',
    category: 'redis',
    subcategory: 'data-structures',
    difficulty: 'foundation',
    question: 'Which Redis data structure is most appropriate for storing a leaderboard sorted by score?',
    options: [
      'A List, using LPUSH for each new score',
      'A Sorted Set (ZADD/ZRANGE/ZREVRANGE), where the score is the sort key',
      'A Hash, with one field per player',
      'A String, JSON-encoded',
    ],
    correctIndex: 1,
    explanation:
      'Redis Sorted Sets store members with a floating-point score and maintain them in sorted order. ZADD adds or updates a member\'s score in O(log N), ZREVRANGE retrieves the top-N members in order, and ZRANK returns a member\'s position. This makes leaderboard operations — update score, top-10 list, rank lookup — all O(log N) or O(log N + M) without any sorting at read time.',
    interviewTip: 'Sorted Sets are the canonical Redis leaderboard structure — being able to name the specific commands shows depth.',
  },
  {
    id: 'redis-architecture-001',
    category: 'redis',
    subcategory: 'architecture',
    difficulty: 'core',
    question: 'What does Redis Sentinel provide that a standalone Redis instance does not?',
    options: [
      'Horizontal sharding across multiple nodes',
      'Automatic failover: Sentinel monitors the primary, and if it becomes unavailable, promotes a replica and updates clients via service discovery',
      'Encryption of data at rest',
      'A query language for filtering keys by pattern',
    ],
    correctIndex: 1,
    explanation:
      'Redis Sentinel is a high-availability solution for non-clustered Redis. A quorum of Sentinel processes monitors the primary; if it is unreachable for a configurable timeout, Sentinel elects a new primary from the replicas and notifies clients of the new address. This gives automatic failover with no manual intervention. Redis Cluster, by contrast, provides both high availability and horizontal sharding.',
    interviewTip: 'Distinguish Sentinel (HA, no sharding) from Cluster (HA + sharding) — interviewers test this.',
  },
  {
    id: 'redis-lru-impl-001',
    category: 'redis',
    subcategory: 'eviction',
    difficulty: 'expert',
    question: 'Redis does not implement a strict LRU algorithm. What approximation does it use and why?',
    options: [
      'It uses a doubly-linked list sorted by access time, which is O(1) and exact',
      'It samples a small pool of random keys and evicts the one with the oldest last-access time, trading a tiny amount of accuracy for much lower memory and CPU overhead',
      'It uses a max-heap ordered by access time, rebuilt on every eviction',
      'It delegates eviction decisions to the operating system\'s page cache',
    ],
    correctIndex: 1,
    explanation:
      'A true LRU requires a data structure (often a linked hash map) that adds per-key overhead. Redis instead samples a configurable number of random keys (default 5, tunable with maxmemory-samples) and evicts the one with the oldest last-access timestamp. Increasing the sample size improves accuracy at the cost of more CPU per eviction. Redis 4.0+ also offers a true LFU approximation using a logarithmic counter in the same access-time field.',
    interviewTip: 'Knowing Redis uses approximate rather than strict LRU signals you have read the internals.',
  },
  {
    id: 'redis-cache-invalidation-001',
    category: 'redis',
    subcategory: 'caching',
    difficulty: 'core',
    question: 'You update a user record in the database. Which cache invalidation strategy is safest to avoid serving stale data?',
    options: [
      'Never invalidate; rely on TTL expiry alone',
      'Delete (or update) the cache key immediately after the database write succeeds, within the same transaction boundary where possible',
      'Invalidate the key before the database write to make room',
      'Let a background job rebuild the cache every 10 minutes',
    ],
    correctIndex: 1,
    explanation:
      'Deleting or updating the cache entry after a successful database write keeps the cache consistent. Invalidating before the write leaves a window where a concurrent read misses the cache, fetches the old value from the database, and re-populates the cache with stale data just before the write completes. A TTL-only approach tolerates stale data for the full TTL duration, which may be unacceptable. Distributed systems can still have edge cases, so a short TTL as a backstop is a sensible belt-and-suspenders measure.',
  },
  {
    id: 'redis-pipeline-002',
    category: 'redis',
    subcategory: 'performance',
    difficulty: 'expert',
    question: 'What is the difference between Redis pipelining and a MULTI/EXEC transaction?',
    options: [
      'Pipelining guarantees atomicity; MULTI/EXEC only batches network calls',
      'Pipelining batches network round trips without atomicity; MULTI/EXEC groups commands that execute atomically without interruption, but queued commands cannot branch on intermediate results',
      'MULTI/EXEC is faster because it uses a separate connection',
      'They are identical; MULTI/EXEC internally uses pipelining',
    ],
    correctIndex: 1,
    explanation:
      'Pipelining is a pure client-side network optimization: commands are sent in a batch to reduce RTT, but other clients can run commands between them on the server. MULTI/EXEC marks a transaction block on the server; all queued commands execute sequentially without interleaving from other clients, giving atomicity. However, commands inside MULTI/EXEC are queued blindly — you cannot inspect intermediate values and branch, which is why complex conditional logic typically requires Lua scripts (which also execute atomically).',
    interviewTip: 'Pipelining = network efficiency; MULTI/EXEC = atomicity on the server side — separate concerns.',
  },
];
