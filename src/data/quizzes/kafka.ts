import type { QuizQuestion } from '../../types';

export const kafka: QuizQuestion[] = [
  {
    id: 'kafka-partition-001',
    category: 'kafka',
    subcategory: 'partitions',
    difficulty: 'core',
    question: 'In Kafka, what does message ordering actually guarantee?',
    options: [
      'Global ordering across the entire topic',
      'Ordering only within a single partition',
      'Ordering only if there is exactly one consumer',
      'No ordering is ever guaranteed',
    ],
    correctIndex: 1,
    explanation:
      'Kafka guarantees order within a partition, not across the whole topic. Messages with the same key hash to the same partition, so per-key ordering holds  -  which is why you key by entity id (e.g. walletId) when order matters. If you need strict global ordering you must use a single partition, sacrificing parallelism.',
    interviewTip: 'Key by entity id to keep per-entity order while still scaling partitions.',
  },
  {
    id: 'kafka-consumer-group-001',
    category: 'kafka',
    subcategory: 'consumer-groups',
    difficulty: 'core',
    question: 'How are partitions distributed among consumers in the same consumer group?',
    options: [
      'Every consumer reads every partition',
      'Each partition is assigned to exactly one consumer in the group at a time',
      'Partitions are shared randomly per message',
      'Only the group leader consumes messages',
    ],
    correctIndex: 1,
    explanation:
      'Within a consumer group, each partition is owned by exactly one consumer, which is how Kafka scales out work while preserving per-partition order. This means group parallelism is capped by the partition count  -  more consumers than partitions leaves some idle. Different groups, however, each get their own full copy of the stream, which is how you fan out to independent pipelines.',
    interviewTip: 'Max useful consumers in a group equals the number of partitions.',
  },
  {
    id: 'kafka-vs-rmq-001',
    category: 'kafka',
    subcategory: 'comparison',
    difficulty: 'expert',
    question: 'When is Kafka a better fit than RabbitMQ?',
    options: [
      'When you need complex per-message routing and short-lived task queues',
      'When you need a durable, replayable, high-throughput event log retained for many consumers',
      'When you never need to re-read messages',
      'When you require synchronous request/response',
    ],
    correctIndex: 1,
    explanation:
      'Kafka is a distributed, append-only log: messages are retained by time/size and consumers track their own offset, so multiple independent pipelines can read and replay the same stream at high throughput. RabbitMQ is a smarter broker for routing and task distribution where a message is typically consumed once and then gone. Choose Kafka for event streaming/analytics and event sourcing; choose RabbitMQ for command queues and rich routing.',
    interviewTip: 'Kafka = replayable log; RabbitMQ = smart broker for transient tasks.',
  },
  {
    id: 'kafka-acks-001',
    category: 'kafka',
    subcategory: 'producer-acks',
    difficulty: 'expert',
    question: 'A producer sets acks=all. What does it buy you, and at what cost?',
    options: [
      'Lowest latency, but messages can be lost on broker failure',
      'The leader and all in-sync replicas must acknowledge, maximizing durability at the cost of latency',
      'It disables replication entirely',
      'It guarantees exactly-once with no other configuration',
    ],
    correctIndex: 1,
    explanation:
      'acks=all means the leader waits for every in-sync replica to persist the record before acknowledging, so a single broker failure will not lose the write  -  the strongest durability setting. The cost is higher latency and lower throughput compared to acks=1 or acks=0. For true exactly-once you additionally enable the idempotent producer and transactions; acks=all alone only protects against loss.',
  },
  {
    id: 'kafka-offset-001',
    category: 'kafka',
    subcategory: 'offsets',
    difficulty: 'foundation',
    question: 'What is a Kafka consumer offset?',
    options: [
      'The byte position of the message on disk',
      'A sequential integer that marks which message in a partition a consumer has read up to',
      'A hash of the message key used for partition assignment',
      'The replication factor of the topic',
    ],
    correctIndex: 1,
    explanation:
      'An offset is a monotonically increasing integer assigned to each message within a partition. Consumers commit their current offset to the __consumer_offsets topic, so after a crash or restart they resume from where they left off. Because consumers own their offsets, multiple independent consumer groups can read the same partition at completely different positions  -  enabling replayability and fan-out without copying data.',
    interviewTip: 'Offset ownership by the consumer is what makes replay and fan-out to many groups possible.',
  },
  {
    id: 'kafka-replay-001',
    category: 'kafka',
    subcategory: 'offsets',
    difficulty: 'core',
    question: 'A new analytics service needs to process all historical events in a Kafka topic. What is the correct approach?',
    options: [
      'Create a new topic and republish historical events',
      'Reset the consumer group offset to the earliest available position and consume from there',
      'Increase the partition count so historical data spreads out',
      'Use a DLQ to replay messages',
    ],
    correctIndex: 1,
    explanation:
      'Because Kafka retains messages on disk (up to the configured retention period or size), a consumer group can simply seek to offset 0 (earliest) and replay everything from the beginning. This is fundamentally different from traditional message queues where consumed messages are deleted. It means you can bootstrap new services, reprocess on bug fixes, and build new projections without any special re-publish step.',
    interviewTip: 'Kafka retention + consumer-controlled offset = free replay for any new consumer.',
  },
  {
    id: 'kafka-log-compaction-001',
    category: 'kafka',
    subcategory: 'log-compaction',
    difficulty: 'expert',
    question: 'What does Kafka log compaction guarantee about a compacted topic?',
    options: [
      'Messages are compressed with gzip to save disk space',
      'For each message key, only the most recent value is retained; older values for the same key are removed',
      'All messages older than the retention period are deleted regardless of key',
      'Duplicate messages with the same value are merged into one',
    ],
    correctIndex: 1,
    explanation:
      'Log compaction removes older records for a given key, keeping only the latest value  -  similar to a key-value store changelog. A message with a null value (tombstone) signals that the key should be deleted entirely. Compacted topics are ideal for materializing state: a consumer bootstrapping from offset 0 gets the current value of every key without processing the full history. Time-based retention and compaction can coexist on the same topic.',
    interviewTip: 'Compacted topic = changelog/snapshot; deletion marker = null-value tombstone.',
  },
  {
    id: 'kafka-retention-001',
    category: 'kafka',
    subcategory: 'retention',
    difficulty: 'foundation',
    question: 'Which two Kafka topic configurations control when old messages are eligible for deletion?',
    options: [
      'retention.ms and retention.bytes',
      'max.message.bytes and segment.ms',
      'replication.factor and min.insync.replicas',
      'acks and linger.ms',
    ],
    correctIndex: 0,
    explanation:
      'retention.ms sets how long (in milliseconds) Kafka keeps messages before they are eligible for deletion, and retention.bytes caps the total size of the partition log. When either limit is exceeded, the oldest log segments are deleted. These settings let you balance storage cost against the replay window: a longer retention costs more disk but allows consumers further behind to still catch up.',
    interviewTip: 'Size and time retention are independent  -  whichever limit is hit first triggers cleanup.',
  },
  {
    id: 'kafka-idempotent-producer-001',
    category: 'kafka',
    subcategory: 'exactly-once',
    difficulty: 'expert',
    question: 'What problem does the idempotent producer setting (enable.idempotence=true) solve?',
    options: [
      'It prevents consumers from reading the same message twice',
      'It deduplicates retried produce requests so a network retry does not create a duplicate message in the partition',
      'It ensures messages are written to all replicas before the producer continues',
      'It enables cross-partition transactions',
    ],
    correctIndex: 1,
    explanation:
      'When a producer retries a send after a network timeout, it may not know whether the original request was received, so without idempotence a duplicate record could appear. With enable.idempotence=true, Kafka assigns each producer a unique producer ID and tracks sequence numbers per partition  -  if the broker receives a duplicate sequence it silently discards it. This gives you exactly-once produce semantics per partition with no application code changes.',
    interviewTip: 'Idempotent producer = safe retries; add transactions for cross-partition exactly-once.',
  },
  {
    id: 'kafka-exactly-once-001',
    category: 'kafka',
    subcategory: 'exactly-once',
    difficulty: 'expert',
    question: 'What is required to achieve end-to-end exactly-once semantics in Kafka (read-process-write)?',
    options: [
      'acks=all on the producer is sufficient',
      'Idempotent producer + Kafka transactions + isolation.level=read_committed on the consumer',
      'A single-partition topic with one consumer',
      'Setting max.in.flight.requests.per.connection=1',
    ],
    correctIndex: 1,
    explanation:
      'True end-to-end exactly-once requires three cooperating pieces: the idempotent producer prevents duplicate writes, Kafka transactions allow atomic multi-partition writes that are committed or aborted together, and the consumer must use isolation.level=read_committed so it only sees records from committed transactions. Setting acks=all alone prevents loss but not duplication; a single partition with one consumer removes parallelism without addressing re-delivery.',
    interviewTip: 'Remember the three parts: idempotent producer, transactions, read_committed consumer.',
  },
  {
    id: 'kafka-rebalancing-001',
    category: 'kafka',
    subcategory: 'consumer-groups',
    difficulty: 'core',
    question: 'What triggers a consumer group rebalance in Kafka?',
    options: [
      'Committing an offset to a new partition',
      'A consumer joining, leaving, or failing to send a heartbeat within session.timeout.ms',
      'The leader broker restarting',
      'A new message arriving in a partition',
    ],
    correctIndex: 1,
    explanation:
      'A rebalance is triggered when the group membership changes: a consumer joins (scale-out), leaves gracefully, or is presumed dead because it missed its heartbeat deadline within session.timeout.ms. During a rebalance, all consumption in the group pauses while partitions are reassigned  -  a stop-the-world event that hurts throughput and can cause duplicate processing if offsets were not committed before the reassignment. Incremental cooperative rebalancing (introduced in Kafka 2.4) reduces this impact by only moving the affected partitions.',
    interviewTip: 'Tune session.timeout.ms and heartbeat.interval.ms, and use cooperative rebalancing to reduce pauses.',
  },
  {
    id: 'kafka-ordering-by-key-001',
    category: 'kafka',
    subcategory: 'partitions',
    difficulty: 'core',
    question: 'You need all events for a given user to be processed in order. What is the correct Kafka design?',
    options: [
      'Use a single-partition topic',
      'Publish events with the user ID as the message key so they always hash to the same partition',
      'Use consumer group isolation.level=read_committed',
      'Enable log compaction on the topic',
    ],
    correctIndex: 1,
    explanation:
      'Kafka\'s default partitioner hashes the message key to deterministically select a partition, so messages sharing the same key always go to the same partition and are consumed in order by one consumer in the group. Using userId as the key keeps per-user ordering while still allowing the topic to scale across many partitions  -  you get both order and parallelism. A single-partition topic achieves global ordering but kills horizontal scale.',
    interviewTip: 'Key = partitioning unit = ordering boundary; choose keys that match your ordering requirement.',
  },
  {
    id: 'kafka-partition-count-001',
    category: 'kafka',
    subcategory: 'partitions',
    difficulty: 'foundation',
    question: 'Why can you not reduce the number of partitions in a Kafka topic after it has been created?',
    options: [
      'It is a Kafka license restriction',
      'Reducing partitions would require merging existing data and could violate key-based ordering guarantees',
      'The Zookeeper metadata does not support it',
      'Consumers would need to be restarted to pick up the change',
    ],
    correctIndex: 1,
    explanation:
      'Kafka allows adding partitions but not removing them because removal would require redistributing existing records and could break the deterministic key-to-partition mapping that consumers rely on for ordering. If you genuinely need fewer partitions you must create a new topic, migrate producers, and drain the old one. This makes partition count an important up-front capacity decision  -  over-provisioning partitions is generally safer than under-provisioning.',
    interviewTip: 'Plan partition count carefully; adding is fine, removing is impossible without migration.',
  },
  {
    id: 'kafka-consumer-lag-001',
    category: 'kafka',
    subcategory: 'consumer-groups',
    difficulty: 'core',
    question: 'What does consumer lag measure in Kafka?',
    options: [
      'Network latency between the producer and broker',
      'The difference between the latest produced offset and the consumer\'s committed offset for a partition',
      'The number of messages waiting in the DLQ',
      'The replication lag between leader and follower replicas',
    ],
    correctIndex: 1,
    explanation:
      'Consumer lag is the count of messages that have been produced but not yet consumed and committed by a consumer group for a given partition. A growing lag means consumers cannot keep up with the produce rate and is one of the primary alerting metrics for Kafka-based systems. You reduce lag by adding consumers (up to the partition count), optimizing processing logic, or increasing partition count to allow more parallel consumers.',
    interviewTip: 'Alert on consumer lag growth rate, not just absolute value  -  a stable lag may be acceptable.',
  },
  {
    id: 'kafka-acks-zero-001',
    category: 'kafka',
    subcategory: 'producer-acks',
    difficulty: 'foundation',
    question: 'When should you use acks=0 on a Kafka producer?',
    options: [
      'For financial transactions where no data loss is acceptable',
      'For high-volume metrics or telemetry where occasional loss is acceptable and maximum throughput is required',
      'When you need exactly-once delivery',
      'When writing to a compacted topic',
    ],
    correctIndex: 1,
    explanation:
      'acks=0 means the producer does not wait for any acknowledgement  -  it is true fire-and-forget. Messages can be lost if the broker is unavailable or crashes. This is acceptable for high-frequency, low-value data like click-stream telemetry or sensor metrics where losing a small fraction of events is tolerable and the throughput benefit (no round-trip wait) is significant. Never use acks=0 for data where loss would cause business problems.',
    interviewTip: 'acks=0 for best-effort telemetry, acks=1 for balanced, acks=all for critical data.',
  },
  {
    id: 'kafka-topic-partition-001',
    category: 'kafka',
    subcategory: 'partitions',
    difficulty: 'foundation',
    question: 'What is the relationship between a Kafka topic and its partitions?',
    options: [
      'A topic is a partition; they are the same concept',
      'A topic is a logical grouping of one or more partitions that hold the actual ordered log of messages',
      'Partitions are optional sub-topics created on demand',
      'Each partition is a separate Kafka broker',
    ],
    correctIndex: 1,
    explanation:
      'A topic is just a named category; the physical storage unit is the partition. Each partition is an append-only, ordered log stored on one broker (with replicas on others). Increasing partitions spreads load across brokers and allows more consumers in a group to operate in parallel. Producers and consumers interact with the topic name, but Kafka internally routes writes and reads to specific partitions.',
    interviewTip: 'Topic = logical name; partition = physical unit of parallelism, ordering, and replication.',
  },
  {
    id: 'kafka-isr-001',
    category: 'kafka',
    subcategory: 'replication',
    difficulty: 'expert',
    question: 'What is the in-sync replica (ISR) set, and why does min.insync.replicas matter with acks=all?',
    options: [
      'ISR is the list of consumers that have committed their offsets; min.insync.replicas is ignored',
      'ISR is the set of replicas that are caught up with the leader; min.insync.replicas sets the minimum ISR size that must be present for a write to succeed with acks=all',
      'ISR tracks the number of active producers writing to a topic',
      'min.insync.replicas controls the number of partitions that must be available',
    ],
    correctIndex: 1,
    explanation:
      'The ISR set contains only the replicas that are fully caught up with the leader (within replica.lag.time.max.ms). With acks=all, the leader waits for all ISR members to acknowledge. min.insync.replicas (typically set to 2 for a replication factor of 3) ensures that if ISR shrinks to just the leader due to failures, the write is rejected rather than silently becoming unprotected. Without this setting, acks=all could be satisfied by a lone leader, providing false durability guarantees.',
    interviewTip: 'Safe defaults: replication.factor=3, min.insync.replicas=2, acks=all.',
  },
  {
    id: 'kafka-schema-registry-001',
    category: 'kafka',
    subcategory: 'schema',
    difficulty: 'core',
    question: 'Why is a Schema Registry important in a Kafka-based system?',
    options: [
      'It stores consumer group offsets',
      'It enforces schema compatibility so producers and consumers can evolve message formats without breaking each other',
      'It compresses messages at the broker level',
      'It manages Kafka broker configuration centrally',
    ],
    correctIndex: 1,
    explanation:
      'In a distributed event-driven system, producers and consumers of the same topic may be deployed independently. A Schema Registry (e.g. Confluent Schema Registry) stores versioned schemas (Avro, Protobuf, or JSON Schema) and enforces compatibility rules (backward, forward, full) when a new schema version is registered. This prevents a producer from publishing a schema change that silently breaks deserializing consumers  -  especially important with many teams sharing a topic.',
    interviewTip: 'Schema Registry is the contract layer between producers and consumers; backward compatibility is the safest default.',
  },
];
