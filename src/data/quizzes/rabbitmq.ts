import type { QuizQuestion } from '../../types';

export const rabbitmq: QuizQuestion[] = [
  {
    id: 'rmq-exchange-001',
    category: 'rabbitmq',
    subcategory: 'exchanges',
    difficulty: 'core',
    question: 'Which exchange type routes a message to every bound queue, ignoring the routing key?',
    options: ['direct', 'topic', 'fanout', 'headers'],
    correctIndex: 2,
    explanation:
      'A fanout exchange broadcasts each message to all bound queues regardless of routing key, which is ideal for publish/subscribe style events that many independent consumers care about. direct matches an exact routing key, and topic matches wildcard patterns like order.*. Choosing the exchange type is really choosing your routing semantics.',
    interviewTip: 'fanout = broadcast, direct = exact key, topic = wildcard patterns.',
  },
  {
    id: 'rmq-dlq-001',
    category: 'rabbitmq',
    subcategory: 'dead-letter-queues',
    difficulty: 'core',
    question: 'What is the primary purpose of a dead-letter queue (DLQ)?',
    options: [
      'To speed up message delivery',
      'To capture messages that were rejected, expired, or exceeded retry limits for later inspection',
      'To store a backup copy of every successful message',
      'To replace the need for acknowledgements',
    ],
    correctIndex: 1,
    explanation:
      'A DLQ is where messages go when they cannot be processed — they were nacked without requeue, hit a TTL, or blew past a retry limit. This keeps poison messages from blocking the main queue while preserving them so you can debug, fix, and replay. Pair it with a retry/back-off strategy so transient failures retry but permanent failures land in the DLQ.',
    interviewTip: 'DLQ isolates poison messages so one bad payload cannot stall the queue.',
  },
  {
    id: 'rmq-saga-001',
    category: 'rabbitmq',
    subcategory: 'saga',
    difficulty: 'expert',
    question: 'Why choose the Saga pattern over a two-phase commit (2PC) for a distributed transaction across microservices?',
    options: [
      'Saga guarantees stronger ACID isolation than 2PC',
      'Saga avoids a blocking global lock and coordinator, trading strict atomicity for availability via compensating actions',
      'Saga is simpler because it needs no error handling',
      '2PC is impossible over a message broker',
    ],
    correctIndex: 1,
    explanation:
      '2PC holds locks and depends on a coordinator across all participants, so a slow or failed node blocks everyone — poor availability at scale. A Saga instead runs a sequence of local transactions and, if a later step fails, executes compensating transactions to undo the earlier ones. You give up strict isolation and accept eventual consistency in exchange for resilience and loose coupling.',
    interviewTip: 'Say Saga = local transactions + compensations; 2PC = blocking locks + coordinator.',
  },
  {
    id: 'rmq-idempotency-001',
    category: 'rabbitmq',
    subcategory: 'idempotency',
    difficulty: 'expert',
    question: 'Your consumer uses at-least-once delivery. How do you stop duplicate messages from double-charging a wallet?',
    options: [
      'Switch to fire-and-forget with no acks',
      'Make processing idempotent, e.g. dedupe on a unique message/operation id stored transactionally',
      'Increase the prefetch count',
      'Use a fanout exchange',
    ],
    correctIndex: 1,
    explanation:
      'At-least-once delivery means a message can be redelivered (e.g. after a consumer crash before the ack), so consumers must be idempotent. The standard technique is to record a unique idempotency key (message id or business operation id) in the same database transaction as the side effect, and skip the work if the key was already seen. That way a redelivery is a safe no-op instead of a second charge.',
    interviewTip: 'At-least-once + idempotency key = effectively-once processing.',
  },
  {
    id: 'rmq-exchange-002',
    category: 'rabbitmq',
    subcategory: 'exchanges',
    difficulty: 'foundation',
    question: 'Which exchange type routes messages based on an exact match between the routing key and the queue binding key?',
    options: ['fanout', 'headers', 'topic', 'direct'],
    correctIndex: 3,
    explanation:
      'A direct exchange compares the message routing key to each binding key and forwards only to the queue whose binding key matches exactly. This is the simplest routing model and is perfect for command queues where a specific service should handle a specific work type. If no binding matches, the message is either discarded or sent to an alternate exchange.',
    interviewTip: 'direct is RabbitMQ\'s default exchange type and powers the default nameless exchange.',
  },
  {
    id: 'rmq-exchange-003',
    category: 'rabbitmq',
    subcategory: 'exchanges',
    difficulty: 'core',
    question: 'A topic exchange binding uses the pattern "order.#". Which routing keys does it match?',
    options: [
      'Only "order" exactly',
      'Only "order.created" and "order.updated"',
      '"order." followed by any number of dot-separated words, including zero',
      'Any key starting with "order" regardless of dots',
    ],
    correctIndex: 2,
    explanation:
      'In a topic exchange, # matches zero or more dot-delimited words, so "order.#" matches "order", "order.created", "order.payment.failed", and so on. The single-word wildcard is *, so "order.*" matches "order.created" but not "order.payment.failed". These two wildcards give topic exchanges powerful, readable routing semantics that sit between direct and fanout.',
    interviewTip: '* = one word, # = zero or more words in topic exchange patterns.',
  },
  {
    id: 'rmq-exchange-004',
    category: 'rabbitmq',
    subcategory: 'exchanges',
    difficulty: 'expert',
    question: 'What distinguishes a headers exchange from a topic exchange?',
    options: [
      'Headers exchanges use the routing key; topic exchanges ignore it',
      'Headers exchanges route based on message header attributes using x-match (any/all), ignoring the routing key entirely',
      'Headers exchanges only support fanout-style routing',
      'Headers exchanges are deprecated and should not be used',
    ],
    correctIndex: 1,
    explanation:
      'A headers exchange inspects AMQP message headers rather than the routing key, matching queues whose binding arguments satisfy the x-match rule — "all" requires every header to match, "any" requires at least one. This gives you rich, attribute-based routing without encoding metadata into the routing key string. The practical cost is heavier per-message inspection and a steeper mental model.',
    interviewTip: 'headers exchange = attribute routing without string-encoding in the key.',
  },
  {
    id: 'rmq-ack-001',
    category: 'rabbitmq',
    subcategory: 'acknowledgements',
    difficulty: 'foundation',
    question: 'What happens when a consumer sends a basic.nack with requeue=false?',
    options: [
      'The message is redelivered immediately to the same consumer',
      'The message is discarded or routed to the dead-letter exchange if one is configured',
      'The message returns to the front of the queue',
      'The consumer is disconnected from the broker',
    ],
    correctIndex: 1,
    explanation:
      'nack with requeue=false tells the broker the message was not processed and should not come back. The broker discards it unless the queue has a dead-letter exchange (DLX) configured, in which case it is republished there. This is the correct signal for permanent failures or poison messages that you want to move to a DLQ for inspection, not silently drop.',
    interviewTip: 'nack requeue=false + DLX = safe poison-message handling without silent loss.',
  },
  {
    id: 'rmq-ack-002',
    category: 'rabbitmq',
    subcategory: 'acknowledgements',
    difficulty: 'core',
    question: 'What is the risk of using auto-ack mode in RabbitMQ?',
    options: [
      'It slows throughput because every message needs a round-trip confirmation',
      'Messages are acknowledged the moment they are delivered, so a consumer crash loses them before processing completes',
      'It prevents the use of dead-letter queues',
      'It forces serial message processing',
    ],
    correctIndex: 1,
    explanation:
      'With auto-ack the broker marks a message delivered as soon as it is pushed to the consumer\'s network buffer. If the consumer crashes mid-processing, the message is gone — there is no way to redeliver it. Manual ack lets you send the acknowledgement only after the work is durably committed, giving you at-least-once semantics at the cost of slightly more complexity.',
    interviewTip: 'auto-ack = at-most-once; manual ack = at-least-once.',
  },
  {
    id: 'rmq-prefetch-001',
    category: 'rabbitmq',
    subcategory: 'prefetch',
    difficulty: 'core',
    question: 'What does setting channel.basicQos(1) achieve in RabbitMQ?',
    options: [
      'It limits the queue to holding only one message at a time',
      'It tells the broker to send no more than one unacknowledged message to the consumer at a time, enabling fair dispatch',
      'It enables batch acknowledgements',
      'It sets message priority to 1',
    ],
    correctIndex: 1,
    explanation:
      'basicQos sets the prefetch count — the maximum number of unacknowledged messages a consumer may hold before it must ack some. With prefetch=1, the broker waits for an ack before pushing the next message, which prevents a slow or overloaded consumer from accumulating a backlog while fast consumers sit idle. For most workloads a higher value (e.g. 10–50) gives better throughput while still load-balancing fairly.',
    interviewTip: 'Prefetch = 1 ensures round-robin fair dispatch; higher values increase throughput.',
  },
  {
    id: 'rmq-prefetch-002',
    category: 'rabbitmq',
    subcategory: 'prefetch',
    difficulty: 'expert',
    question: 'You have 4 consumer instances each with prefetch=100 but messages are piling up unevenly — one consumer is backlogged while others are idle. What is the most likely cause?',
    options: [
      'The exchange type is wrong for this use case',
      'All messages share the same routing key so only one consumer receives them all',
      'The prefetch is too high, causing the broker to fill one consumer\'s buffer entirely before dispatching to others',
      'Dead-letter queues are not configured',
    ],
    correctIndex: 2,
    explanation:
      'When prefetch is very high, the broker dispatches up to that many messages to one consumer before moving to the next, which can starve the others until the first consumer has worked through its buffer. Lowering prefetch to a smaller value (or even 1 for strict fair dispatch) ensures the broker round-robins more aggressively. The right prefetch value balances throughput with fair load distribution.',
    interviewTip: 'Tune prefetch: too low hurts throughput, too high causes uneven load.',
  },
  {
    id: 'rmq-publisher-confirms-001',
    category: 'rabbitmq',
    subcategory: 'publisher-confirms',
    difficulty: 'core',
    question: 'What do publisher confirms give you that a basic publish call alone does not?',
    options: [
      'They guarantee the consumer has processed the message',
      'They notify the publisher whether the broker accepted and persisted the message, enabling at-least-once publish',
      'They make the exchange route messages faster',
      'They provide message encryption in transit',
    ],
    correctIndex: 1,
    explanation:
      'A basic publish call is fire-and-forget — you get no feedback if the broker dropped the message or the connection broke. With publisher confirms enabled, the broker sends a basic.ack (or basic.nack) back for each message, so the producer knows it must retry on nack or connection failure. This is the foundation of reliable publishing and is essential when paired with persistent messages and durable queues.',
    interviewTip: 'Publisher confirms + persistent messages + durable queues = reliable delivery.',
  },
  {
    id: 'rmq-quorum-queues-001',
    category: 'rabbitmq',
    subcategory: 'quorum-queues',
    difficulty: 'expert',
    question: 'What is the main advantage of a quorum queue over a classic mirrored queue in RabbitMQ?',
    options: [
      'Quorum queues use less memory because they do not replicate',
      'Quorum queues use the Raft consensus algorithm for stronger data safety and predictable failover without split-brain risk',
      'Quorum queues support more exchange types',
      'Quorum queues process messages faster because they skip acknowledgements',
    ],
    correctIndex: 1,
    explanation:
      'Classic mirrored queues replicate by broadcasting to mirrors but have no strong consistency protocol, making them vulnerable to data loss during failures and complex promotion rules. Quorum queues use Raft consensus, where a write is only confirmed when a majority of replicas agree, preventing split-brain and data loss. RabbitMQ 3.8+ recommends quorum queues as the default durable, replicated queue type.',
    interviewTip: 'Quorum queues use Raft majority writes; classic mirrors are soft-deprecated.',
  },
  {
    id: 'rmq-saga-002',
    category: 'rabbitmq',
    subcategory: 'saga',
    difficulty: 'expert',
    question: 'In the choreography-based Saga pattern, how do services know when to execute their step?',
    options: [
      'A central orchestrator sends a command to each service in sequence',
      'Each service listens for domain events published by the previous step and reacts autonomously',
      'Services poll a shared database table for their turn',
      'The API gateway fans out to all services simultaneously',
    ],
    correctIndex: 1,
    explanation:
      'In choreography-based Sagas there is no central coordinator — each service subscribes to domain events (e.g. OrderCreated) and, when it handles one, performs its local transaction and emits the next event (e.g. PaymentReserved). This keeps services decoupled but makes the overall saga flow implicit and harder to visualize. Orchestration-based Sagas use a dedicated coordinator that explicitly commands each step, giving better observability at the cost of tighter coupling to the orchestrator.',
    interviewTip: 'Choreography = event-driven, implicit flow; Orchestration = explicit coordinator.',
  },
  {
    id: 'rmq-compensating-001',
    category: 'rabbitmq',
    subcategory: 'saga',
    difficulty: 'expert',
    question: 'An order Saga has reserved inventory and charged payment, then the shipping step fails. What should happen next?',
    options: [
      'Retry the shipping step indefinitely until it succeeds',
      'Execute compensating transactions to refund payment and release inventory in reverse order',
      'Leave the system in a partial state and alert an operator',
      'Roll back by restarting the entire Saga from step one',
    ],
    correctIndex: 1,
    explanation:
      'When a Saga step fails after earlier steps have committed, you cannot issue a single rollback because each step was a separate local transaction. Instead you execute compensating transactions — a refund for the payment service and an inventory release for the warehouse service — in reverse order. Compensating transactions must themselves be idempotent in case they fail and need retrying, and they represent undone business intent, not a silent technical rollback.',
    interviewTip: 'Design compensating transactions to be idempotent and explicitly model them as business events.',
  },
  {
    id: 'rmq-commands-vs-events-001',
    category: 'rabbitmq',
    subcategory: 'messaging-patterns',
    difficulty: 'core',
    question: 'What is the key semantic difference between a command message and an event message?',
    options: [
      'Commands use fanout exchanges; events use direct exchanges',
      'A command tells a specific service what to do; an event announces that something has already happened',
      'Commands are synchronous; events are asynchronous',
      'Commands carry no payload; events carry full state',
    ],
    correctIndex: 1,
    explanation:
      'A command (e.g. PlaceOrder) is an instruction aimed at a specific handler — if it is rejected the sender cares and may need to handle the failure. An event (e.g. OrderPlaced) is a fact broadcast to anyone interested — the publisher does not know or care who reacts. This distinction drives topology choices: commands typically go to a single named queue (direct), while events fan out to multiple subscribers (fanout/topic).',
    interviewTip: 'Commands are imperative and addressed; events are declarative and broadcast.',
  },
  {
    id: 'rmq-ttl-001',
    category: 'rabbitmq',
    subcategory: 'dead-letter-queues',
    difficulty: 'foundation',
    question: 'You set a per-queue message TTL of 5000 ms. What happens to a message that is not consumed within that time?',
    options: [
      'The message is redelivered to the same queue',
      'The message expires and is either discarded or forwarded to the dead-letter exchange',
      'The consumer receives it with a warning header',
      'The broker extends the TTL automatically',
    ],
    correctIndex: 1,
    explanation:
      'When a message exceeds its TTL it is classified as dead. If the queue has a dead-letter exchange (x-dead-letter-exchange) configured, the expired message is republished there — allowing it to land in a DLQ for inspection or delayed retry. If no DLX is configured the message is simply dropped. TTL combined with DLX is a common pattern for implementing delayed retry queues.',
    interviewTip: 'TTL + DLX = delayed retry: expire into DLQ, then re-publish after delay.',
  },
  {
    id: 'rmq-persistent-001',
    category: 'rabbitmq',
    subcategory: 'durability',
    difficulty: 'foundation',
    question: 'Which combination ensures a message survives a RabbitMQ broker restart?',
    options: [
      'Transient message on a durable queue',
      'Persistent message on a durable queue',
      'Transient message on a transient queue',
      'Persistent message on a transient queue',
    ],
    correctIndex: 1,
    explanation:
      'For a message to survive a restart both the queue and the message itself must be durable/persistent. A durable queue survives restart but if the message delivery mode is not set to persistent (mode=2), the message is still stored only in memory and lost on restart. Conversely, even a persistent message is lost if the queue is transient because the queue itself disappears. Both properties are required.',
    interviewTip: 'Durable queue + persistent message delivery mode = survives broker restart.',
  },
  {
    id: 'rmq-dlq-002',
    category: 'rabbitmq',
    subcategory: 'dead-letter-queues',
    difficulty: 'core',
    question: 'What header does RabbitMQ add to a dead-lettered message to explain why it was dead-lettered?',
    options: [
      'x-death',
      'x-retry-count',
      'x-failed-reason',
      'x-dlq-origin',
    ],
    correctIndex: 0,
    explanation:
      'RabbitMQ appends an x-death header array to dead-lettered messages. Each entry records the queue name, the reason (rejected, expired, or maxlen), the exchange, routing keys, and a timestamp. This metadata is invaluable for debugging — you can see exactly how many times a message was cycled through retry queues and why it ultimately ended up in the DLQ without needing separate logging.',
    interviewTip: 'Read x-death to find out how many retries occurred and why the message died.',
  },
  {
    id: 'rmq-routing-key-001',
    category: 'rabbitmq',
    subcategory: 'exchanges',
    difficulty: 'foundation',
    question: 'A direct exchange has two queues bound with keys "email" and "sms". A message is published with routing key "push". Where does it go?',
    options: [
      'Both queues receive it',
      'The queue bound first receives it',
      'The message is dropped or returned to the publisher',
      'The default queue receives it',
    ],
    correctIndex: 2,
    explanation:
      'If no binding on a direct exchange matches the routing key, the broker has nowhere to send the message. By default it is silently dropped. If the producer set the mandatory flag, the broker sends a basic.return back to the producer instead of silently discarding it. This is why monitoring unroutable messages is important — silent drops are a common source of mysterious message loss.',
    interviewTip: 'Set mandatory=true or configure an alternate-exchange to catch unroutable messages.',
  },
];
