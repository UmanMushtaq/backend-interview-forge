import type { QuizQuestion } from '../../types';

export const nestjs: QuizQuestion[] = [
  {
    id: 'nestjs-di-001',
    category: 'nestjs',
    subcategory: 'dependency-injection',
    difficulty: 'core',
    question: 'How does NestJS resolve a provider that another provider depends on via constructor injection?',
    options: [
      'It scans the constructor parameter metadata emitted by TypeScript and resolves each token from the module\'s injector',
      'It uses runtime reflection on parameter names and matches them to exported classes',
      'It requires every dependency to be passed manually when the class is instantiated',
      'It looks up dependencies lazily the first time a method is called',
    ],
    correctIndex: 0,
    explanation:
      'Nest relies on emitDecoratorMetadata: TypeScript emits the constructor parameter types, and the IoC container resolves each type token from the module injector. Providers are singletons by default and cached per module scope. This is why a class must be decorated (e.g. @Injectable) and registered in a module providers array for the metadata to be usable.',
    interviewTip: 'Mention that DI is token-based and the default scope is a single shared instance.',
  },
  {
    id: 'nestjs-lifecycle-001',
    category: 'nestjs',
    subcategory: 'request-lifecycle',
    difficulty: 'core',
    question: 'What is the correct order of the NestJS request pipeline?',
    options: [
      'Guard -> Middleware -> Controller -> Interceptor -> Pipe',
      'Middleware -> Guard -> Interceptor -> Pipe -> Controller',
      'Pipe -> Guard -> Middleware -> Interceptor -> Controller',
      'Middleware -> Interceptor -> Guard -> Controller -> Pipe',
    ],
    correctIndex: 1,
    explanation:
      'Requests flow through middleware first, then guards (authorization), then interceptors (which wrap the handler), then pipes (validation/transformation of arguments), and finally the controller handler. The response then travels back out through the interceptors. Knowing this order explains why a guard cannot read a body transformed by a pipe.',
    interviewTip: 'Remember: middleware -> guard -> interceptor -> pipe -> handler.',
  },
  {
    id: 'nestjs-scope-001',
    category: 'nestjs',
    subcategory: 'provider-scopes',
    difficulty: 'expert',
    question: 'What is the main cost of marking a provider with REQUEST scope?',
    options: [
      'It is instantiated once but shared across requests, causing data leaks',
      'A new instance is created per request, and that scope bubbles up to every provider that injects it',
      'It can only be used inside controllers, never in services',
      'It disables dependency injection for that provider',
    ],
    correctIndex: 1,
    explanation:
      'REQUEST-scoped providers are created fresh for each incoming request, which lets you inject the request object but adds per-request instantiation overhead. Crucially, scope is contagious: any provider that depends on a request-scoped provider also becomes request-scoped, which can ripple performance costs up the graph. Prefer DEFAULT (singleton) scope unless you genuinely need per-request state.',
    interviewTip: 'Call out that request scope is contagious up the dependency chain.',
  },
  {
    id: 'nestjs-circular-001',
    category: 'nestjs',
    subcategory: 'circular-dependencies',
    difficulty: 'expert',
    question: 'Two services depend on each other. What is the idiomatic NestJS fix?',
    options: [
      'Merge them into one giant service',
      'Use forwardRef(() => OtherService) on both sides of the injection',
      'Make both providers REQUEST scoped',
      'Remove the @Injectable decorator from one of them',
    ],
    correctIndex: 1,
    explanation:
      'When a true circular dependency is unavoidable, Nest provides forwardRef so the container can defer resolving one side until both classes are defined. You wrap the token on both the @Inject site and the module reference. That said, a circular dependency is often a design smell, so the better long-term fix is to extract the shared logic into a third provider or emit an event instead.',
  },
  {
    id: 'nestjs-modules-001',
    category: 'nestjs',
    subcategory: 'modules',
    difficulty: 'foundation',
    question: 'What is the purpose of the exports array in a NestJS @Module decorator?',
    options: [
      'It registers providers so they can be injected within the same module',
      'It lists providers that other modules can use after importing this module',
      'It tells the CLI which files to compile',
      'It sets the HTTP path prefix for all controllers in the module',
    ],
    correctIndex: 1,
    explanation:
      'The providers array registers providers for use inside that module, but they are private by default. Anything listed in exports becomes available to any module that imports this one. Forgetting to export a provider is a common source of "Nest cannot find provider" errors when you try to inject it from another module.',
    interviewTip: 'Think of exports as the module\'s public API for the DI container.',
  },
  {
    id: 'nestjs-modules-002',
    category: 'nestjs',
    subcategory: 'modules',
    difficulty: 'core',
    question: 'What is the difference between forRoot and forRootAsync on a dynamic module?',
    options: [
      'forRoot is for lazy-loaded modules; forRootAsync loads eagerly',
      'forRoot accepts a static options object; forRootAsync lets you inject services to build the options asynchronously',
      'forRootAsync is deprecated in favour of forRoot',
      'They are identical; the naming is only a convention',
    ],
    correctIndex: 1,
    explanation:
      'forRoot takes a plain configuration object at module registration time, which is fine when values are known statically (e.g. hard-coded in a config file). forRootAsync accepts a factory function wrapped in useFactory, useClass, or useExisting, allowing you to inject a ConfigService or other provider to resolve options at runtime. This is how database modules accept environment-variable-driven credentials.',
    interviewTip: 'forRootAsync with useFactory and inject: [ConfigService] is the standard pattern for env-driven config.',
  },
  {
    id: 'nestjs-di-002',
    category: 'nestjs',
    subcategory: 'dependency-injection',
    difficulty: 'core',
    question: 'You want to provide a plain value (e.g. a configuration object) as an injectable token. Which provider syntax is correct?',
    options: [
      '{ provide: CONFIG_TOKEN, useClass: ConfigObject }',
      '{ provide: CONFIG_TOKEN, useValue: { host: "localhost" } }',
      '{ provide: CONFIG_TOKEN, useExisting: ConfigService }',
      'You cannot inject plain values; all providers must be classes',
    ],
    correctIndex: 1,
    explanation:
      'useValue lets you register any JavaScript value  -  object, string, array, or even a function  -  under an injection token. useClass instantiates a class, useExisting aliases one token to another, and useFactory calls a function (optionally with injected deps). For static config objects, useValue is the cleanest option.',
    interviewTip: 'String or Symbol tokens combined with useValue are great for environment configs that don\'t need a class.',
  },
  {
    id: 'nestjs-di-003',
    category: 'nestjs',
    subcategory: 'dependency-injection',
    difficulty: 'expert',
    question: 'When should you use useFactory instead of useClass for a provider?',
    options: [
      'Only when the provider is a third-party class you cannot decorate',
      'When provider creation requires async setup or depends on other injected services',
      'useFactory is identical to useClass but faster',
      'When you want the provider to be REQUEST-scoped automatically',
    ],
    correctIndex: 1,
    explanation:
      'useFactory receives an inject array of tokens and calls your function with their instances, enabling conditional logic, async initialization, or combining multiple services to build one output. A database connection pool that needs a ConfigService and then calls connect() asynchronously is the canonical example. useClass just instantiates the class with normal DI and cannot easily do async setup at module load time.',
  },
  {
    id: 'nestjs-exception-001',
    category: 'nestjs',
    subcategory: 'exception-filters',
    difficulty: 'core',
    question: 'Where should you bind an exception filter to catch exceptions from every route in the application?',
    options: [
      'Pass it to @UseFilters on every individual controller',
      'Call app.useGlobalFilters(new MyFilter()) in main.ts',
      'Throw it from a guard',
      'Add it to the module\'s providers array without any token',
    ],
    correctIndex: 1,
    explanation:
      'Global filters are registered on the application instance in main.ts via app.useGlobalFilters(), which means they cover every route without annotating individual controllers. You can also register a global filter through the module system using the APP_FILTER token if you need DI inside the filter itself. Per-controller or per-route filters are applied with @UseFilters.',
    interviewTip: 'Use APP_FILTER (DI-aware) over app.useGlobalFilters when the filter needs injected services.',
  },
  {
    id: 'nestjs-interceptor-001',
    category: 'nestjs',
    subcategory: 'interceptors',
    difficulty: 'core',
    question: 'Which interface must a NestJS interceptor implement?',
    options: [
      'CanActivate',
      'NestInterceptor',
      'PipeTransform',
      'ExceptionFilter',
    ],
    correctIndex: 1,
    explanation:
      'NestInterceptor requires an intercept(context, next) method that returns an Observable. The ExecutionContext gives access to request/response objects, and the CallHandler\'s handle() method triggers the next step. This wrapping model lets interceptors transform the response stream, log timing, or cache the result using RxJS operators like map, tap, or catchError.',
    interviewTip: 'Interceptors sit on both the inbound and outbound path, making them ideal for logging request duration.',
  },
  {
    id: 'nestjs-interceptor-002',
    category: 'nestjs',
    subcategory: 'interceptors',
    difficulty: 'expert',
    question: 'You want every response wrapped in a { data: ... } envelope. Where is the best place to implement this?',
    options: [
      'In every controller method manually',
      'In a global interceptor that maps the observable with the RxJS map operator',
      'In a middleware that serializes the response',
      'In a guard that modifies the response object',
    ],
    correctIndex: 1,
    explanation:
      'A global response-transform interceptor calls next.handle().pipe(map(value => ({ data: value }))) to wrap every outgoing value uniformly. This approach is DRY: you define the envelope once and it applies everywhere without touching each controller. Middleware runs before the handler so it cannot easily transform the response body; guards decide whether to proceed and cannot modify the response payload.',
  },
  {
    id: 'nestjs-guard-001',
    category: 'nestjs',
    subcategory: 'guards',
    difficulty: 'core',
    question: 'A guard returns false. What happens to the request?',
    options: [
      'It continues to the next guard',
      'Nest automatically throws a 403 ForbiddenException',
      'It is passed to the error handler with a 500 status',
      'The controller method is called with req.user set to null',
    ],
    correctIndex: 1,
    explanation:
      'When canActivate returns false, NestJS throws a ForbiddenException (403) by default. If you need a different status (e.g. 401 Unauthorized for unauthenticated users), throw the exception yourself inside the guard rather than returning false. Returning false is shorthand only for the generic 403 case.',
    interviewTip: 'Throw UnauthorizedException (401) for "who are you?" and ForbiddenException (403) for "I know who you are but you can\'t do this".',
  },
  {
    id: 'nestjs-pipe-001',
    category: 'nestjs',
    subcategory: 'pipes',
    difficulty: 'foundation',
    question: 'What are the two roles a NestJS pipe can play?',
    options: [
      'Authentication and authorization',
      'Transformation (coercing input type) and validation (throwing on invalid data)',
      'Logging requests and caching responses',
      'Compressing responses and encrypting payloads',
    ],
    correctIndex: 1,
    explanation:
      'Pipes receive the argument before it reaches the handler and can either transform it (e.g. convert a string "42" to the number 42) or validate it (e.g. throw a BadRequestException if the value fails class-validator rules). The built-in ValidationPipe does both: it uses class-transformer to instantiate a DTO and class-validator to check constraints.',
    interviewTip: 'Always enable ValidationPipe globally in main.ts to avoid writing validation logic in every controller.',
  },
  {
    id: 'nestjs-custom-decorator-001',
    category: 'nestjs',
    subcategory: 'custom-decorators',
    difficulty: 'core',
    question: 'You need to extract the authenticated user from the request in every controller. What is the cleanest approach?',
    options: [
      'Inject the Request object into every handler with @Req()',
      'Create a custom parameter decorator using createParamDecorator that pulls user from req.user',
      'Use a global guard to add user to a shared singleton service',
      'Use a middleware to attach user to a static global variable',
    ],
    correctIndex: 1,
    explanation:
      'createParamDecorator receives a factory (data, ctx) => value and returns a decorator you can use like @CurrentUser() in any handler. It cleanly encapsulates the logic of reading req.user, gives you a typed return value, and is easy to test. Injecting the raw Request object via @Req() works but couples your handler to the HTTP layer and makes unit testing harder.',
    interviewTip: 'Custom param decorators are a first-class Nest feature  -  interviewers like to see you know they exist.',
  },
  {
    id: 'nestjs-testing-001',
    category: 'nestjs',
    subcategory: 'testing',
    difficulty: 'core',
    question: 'How do you create an isolated unit test for a NestJS service without spinning up the full HTTP server?',
    options: [
      'Use Test.createTestingModule with providers listed and call module.get() to retrieve the service',
      'Call NestFactory.create() and send HTTP requests with supertest',
      'Import the service class directly and instantiate it with new',
      'Use the @TestModule decorator on the test file',
    ],
    correctIndex: 0,
    explanation:
      'Test.createTestingModule({ providers: [...] }) builds a lightweight DI container without HTTP. You list only the service under test and any mocked dependencies, then call module.get(MyService) to get the instance. This keeps tests fast and isolated. End-to-end tests that need HTTP use the full app with supertest, but unit tests should not.',
    interviewTip: 'Mock dependencies with { provide: DepService, useValue: { method: jest.fn() } } to keep tests isolated.',
  },
  {
    id: 'nestjs-testing-002',
    category: 'nestjs',
    subcategory: 'testing',
    difficulty: 'expert',
    question: 'When writing an e2e test for a NestJS application, which utility is commonly paired with the test module to send real HTTP requests?',
    options: [
      'axios directly against a running server',
      'supertest, using app.getHttpServer() as its target',
      'fetch with a hard-coded localhost URL',
      'The Nest TestingModule has built-in HTTP call methods',
    ],
    correctIndex: 1,
    explanation:
      'Nest\'s e2e convention is to call app.getHttpServer() to get the underlying Node HTTP server and pass it to supertest\'s request() function. This avoids binding to a real port and keeps tests deterministic. You still call app.init() to boot the module and app.close() in afterAll to release resources.',
    interviewTip: 'Use beforeAll/afterAll, not beforeEach, when creating the app so you pay the startup cost only once per test suite.',
  },
  {
    id: 'nestjs-scope-002',
    category: 'nestjs',
    subcategory: 'provider-scopes',
    difficulty: 'core',
    question: 'What does TRANSIENT scope mean for a NestJS provider?',
    options: [
      'The provider is shared across the entire application like a singleton',
      'A fresh instance is created each time the provider is injected into a consumer',
      'The provider is destroyed after the first request',
      'The provider is only available in non-production environments',
    ],
    correctIndex: 1,
    explanation:
      'TRANSIENT scope creates a new instance for every consumer that injects it, unlike DEFAULT (singleton) which shares one instance application-wide, or REQUEST which creates one per HTTP request. Use TRANSIENT when you need truly isolated state per consumer, such as a stateful builder object. Be aware that TRANSIENT scope does not bubble up the way REQUEST scope does.',
  },
  {
    id: 'nestjs-lifecycle-002',
    category: 'nestjs',
    subcategory: 'request-lifecycle',
    difficulty: 'expert',
    question: 'Which lifecycle hook is called after a NestJS module\'s dependencies are initialized, making it safe to open a database connection?',
    options: [
      'constructor()',
      'onModuleDestroy()',
      'onModuleInit()',
      'onApplicationBootstrap()',
    ],
    correctIndex: 2,
    explanation:
      'onModuleInit() is called once all providers in the module have been instantiated and all dependencies resolved, so it is safe to perform async startup work such as connecting to a database. onApplicationBootstrap() is called after all modules have initialized (good for inter-module communication), while onModuleDestroy() is used for cleanup on shutdown.',
    interviewTip: 'Implement OnModuleInit and make onModuleInit async to await connection setup before accepting traffic.',
  },
  {
    id: 'nestjs-middleware-001',
    category: 'nestjs',
    subcategory: 'middleware',
    difficulty: 'foundation',
    question: 'NestJS middleware is functionally equivalent to which well-known Node.js concept?',
    options: [
      'A TypeScript decorator',
      'An Express/Fastify middleware function with access to req, res, and next',
      'A RxJS observable operator',
      'A class-validator constraint',
    ],
    correctIndex: 1,
    explanation:
      'NestJS middleware maps directly onto the underlying HTTP framework\'s (Express or Fastify) middleware concept: a function that receives the request, response, and a next callback. It runs before guards and is used for tasks like logging, CORS, body parsing, or reading cookies. Because it runs before the Nest pipeline, it cannot use DI-resolved guards or interceptors.',
    interviewTip: 'Apply middleware in the module\'s configure() method via MiddlewareConsumer, not in the providers array.',
  },
];
