import type { LearnModule } from '../../types';

export const nestjs: LearnModule = {
  id: 'nestjs',
  title: 'NestJS',
  blurb: 'Modules, dependency injection, and the request lifecycle.',
  quizCategory: 'nestjs',
  lessons: [
    {
      id: 'nest-di-modules',
      title: 'Modules and dependency injection',
      content:
        'NestJS organises code into modules, each declaring providers (injectables), controllers, imports, and exports. The IoC container builds a dependency graph from constructor parameter types and injects shared singletons by default. Provider scopes (default singleton, request, transient) control lifetime — request scope is contagious up the graph, so use it sparingly. Use forRoot/forRootAsync to configure dynamic modules like database or config at startup. In interviews, be ready to explain what happens when a module exports a provider: any module that imports that module gains access to the provider without re-declaring it. The AppModule is the root that stitches everything together, and lazy-loaded modules can reduce startup time for large applications.',
    },
    {
      id: 'nest-lifecycle',
      title: 'The request lifecycle',
      content:
        'A request flows through middleware, then guards (authorization), then interceptors (which wrap the handler before and after), then pipes (validation/transformation), then the controller; the response travels back out through the interceptors. Exception filters wrap the whole chain to turn thrown errors into responses. Knowing this order explains, for example, why a guard cannot see a value a pipe will later transform. Middleware is Express/Fastify-level and has no access to Nest metadata, while guards receive an ExecutionContext and can inspect decorators. Interceptors use RxJS observables, so you can tap, map, or timeout the response stream — a common pattern is adding a logging interceptor that measures handler duration by noting the time before calling next.handle() and subscribing to the result.',
    },
    {
      id: 'nest-custom-providers',
      title: 'Custom providers: useClass, useValue, and useFactory',
      content:
        'When the default "provide a class, inject an instance" pattern is not enough, NestJS offers three custom provider forms. useValue injects a plain value or mock object — perfect for injecting a config object or a Jest mock during tests. useClass lets you swap one class for another based on environment, for example providing a MockMailService in development and the real one in production. useFactory is the most powerful: it calls an async function with injected dependencies and uses the return value as the provider — ideal for constructing a database connection pool that needs config values resolved first. All three forms accept a string or Symbol token for the provide key, and you inject them with the Inject decorator rather than relying on type inference. In interviews, explain the difference between a provider token (the key) and the provider value (what gets injected) to show deep understanding of the DI system.',
    },
    {
      id: 'nest-guards-interceptors-pipes-filters',
      title: 'Guards, interceptors, pipes, and exception filters',
      content:
        'Guards implement CanActivate and return a boolean or observable; they are the canonical place for authentication and authorization checks because they run after middleware but before the handler. Interceptors implement NestInterceptor and receive a CallHandler whose handle() method returns an observable of the response — this lets you transform, cache, or timeout responses using RxJS operators. Pipes implement PipeTransform and serve two roles: validation (throw a BadRequestException if the input is invalid) and transformation (parse "42" into the number 42); the built-in ValidationPipe combined with class-validator decorators handles most REST validation needs. Exception filters implement ExceptionFilter and catch specific error types, converting them to HTTP responses; a global HttpExceptionFilter is a clean place to log errors and normalise response shapes across the entire API. All four can be applied at the global level (app.useGlobalGuards etc.), at the controller level with decorator syntax, or at the individual handler level — scoping them narrowly keeps your code easier to reason about.',
    },
    {
      id: 'nest-custom-decorators-dynamic-modules',
      title: 'Custom decorators and dynamic modules',
      content:
        'Custom parameter decorators let you extract request data once and reuse the logic everywhere, for example a CurrentUser decorator that reads req.user (populated by a guard) so your handlers never touch the raw request. You create them with createParamDecorator, which receives the data argument (the value passed in the decorator call) and the ExecutionContext. Custom method or class decorators are composed with applyDecorators, letting you bundle a guard and a Swagger annotation into a single ApiAuth decorator. Dynamic modules extend this further: a module\'s static forRoot method accepts options and returns a DynamicModule object, allowing callers to configure the module at import time. The forRootAsync variant accepts a useFactory function so the options themselves can be resolved from injected providers, which is essential when your database URL comes from a ConfigService that is itself async. In interviews, be prepared to draw the flow from AppModule importing DatabaseModule.forRootAsync to the factory receiving ConfigService and returning a connection object.',
    },
    {
      id: 'nest-microservices-testing',
      title: 'Microservices, configuration, and testing',
      content:
        'NestJS microservices swap the HTTP layer for a transport: TCP, Redis pub/sub, RabbitMQ, Kafka, and NATS are all supported via a common ClientProxy interface so application code stays transport-agnostic. Patterns are matched with the MessagePattern and EventPattern decorators; the difference is that MessagePattern expects a reply while EventPattern is fire-and-forget. The ConfigModule (from @nestjs/config) loads .env files and exposes a ConfigService with typed get calls; using ConfigModule.forRoot with isGlobal: true means you do not have to import it in every feature module. For testing, Test.createTestingModule mirrors the real module system but lets you override providers with mocks using overrideProvider().useValue() or useClass(). The testing module compiles, injects, and wires providers the same way the runtime does, so unit tests for guards or interceptors run against real Nest lifecycle logic rather than hand-rolled fakes. A common interview question asks how to test a service that depends on a repository: override the repository token with a plain object that has jest.fn() methods, compile the module, and call service methods directly.',
    },
    {
      id: 'nest-interceptors-rxjs',
      title: 'Interceptors with RxJS — timeout, logging, and response mapping',
      content:
        'Interceptors sit on both sides of a route handler. Before the handler runs they can enrich the request context. After the handler responds they receive an Observable of the result and can transform it using any RxJS operator.\n\nThe most common production use-case is a logging interceptor that measures how long each handler takes:\n\n```typescript\n@Injectable()\nexport class LoggingInterceptor implements NestInterceptor {\n  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {\n    const start = Date.now();\n    const { method, url } = context.switchToHttp().getRequest();\n    return next.handle().pipe(\n      tap(() => console.log(`${method} ${url} — ${Date.now() - start}ms`)),\n    );\n  }\n}\n```\n\nThe `tap` operator runs a side effect without changing the emitted value. `map` transforms it — useful for wrapping every response in a standard envelope: `map(data => ({ data, timestamp: new Date().toISOString() }))`.\n\nA timeout interceptor prevents slow handlers from holding resources indefinitely:\n\n```typescript\nreturn next.handle().pipe(\n  timeout(5000),\n  catchError(err => {\n    if (err instanceof TimeoutError) throw new RequestTimeoutException();\n    throw err;\n  }),\n);\n```\n\nThe `timeout` operator from `rxjs/operators` throws a `TimeoutError` if the Observable does not emit within the given milliseconds. The `catchError` operator converts it to a NestJS HTTP exception so the client gets a clean 408 response.\n\nGlobal interceptors are registered with `app.useGlobalInterceptors(new LoggingInterceptor())` or, better, as a provider with `APP_INTERCEPTOR` so they participate in NestJS dependency injection and can receive services.\n\nIn an interview, be ready to explain why interceptors use Observables rather than plain async/await. The Observable model lets you compose operators declaratively, handle streaming responses, and cancel in-flight work — capabilities that Promises do not have.',
    },
    {
      id: 'nest-param-decorators',
      title: 'Custom param decorators and applyDecorators',
      content:
        'Every handler parameter in NestJS is extracted by a decorator — `@Body()`, `@Param()`, `@Query()`. You can create your own with `createParamDecorator`.\n\nA `@CurrentUser()` decorator reads the user that an auth guard placed on the request:\n\n```typescript\nexport const CurrentUser = createParamDecorator(\n  (data: keyof User | undefined, ctx: ExecutionContext) => {\n    const request = ctx.switchToHttp().getRequest();\n    const user: User = request.user;\n    return data ? user?.[data] : user;\n  },\n);\n```\n\nThe `data` argument is whatever you pass inside the decorator call — `@CurrentUser(\'id\')` returns only the user\'s id. `@CurrentUser()` with no argument returns the whole object. This pattern removes raw `req.user` access from every controller handler, making your handlers cleaner and your intention clearer.\n\n`applyDecorators` lets you bundle multiple decorators into a single named composite:\n\n```typescript\nexport function Auth(...roles: Role[]) {\n  return applyDecorators(\n    SetMetadata(ROLES_KEY, roles),\n    UseGuards(JwtAuthGuard, RolesGuard),\n    ApiBearerAuth(),\n    ApiUnauthorizedResponse({ description: \'Unauthorised\' }),\n  );\n}\n```\n\nNow `@Auth(Role.ADMIN)` on a route applies the guard, attaches roles metadata, and annotates Swagger — all in one decorator. If you ever need to add a new security concern, you add it in one place instead of updating dozens of controllers.\n\nIn an interview, explain that `createParamDecorator` uses the `ExecutionContext` (the same object guards and interceptors receive), so it works across HTTP, WebSocket, and microservice contexts — you switch context with `ctx.switchToHttp()`, `ctx.switchToWs()`, or `ctx.switchToRpc()`.',
    },
    {
      id: 'nest-dynamic-modules',
      title: 'Dynamic modules — forRoot and forRootAsync in depth',
      content:
        'A static NestJS module is imported once and its providers are fixed. A **dynamic module** is configured at import time, letting the caller pass options that control how the module behaves — essential for database connections, HTTP clients, and feature flags.\n\nThe `forRoot` pattern accepts synchronous options:\n\n```typescript\n@Module({})\nexport class DatabaseModule {\n  static forRoot(options: DatabaseOptions): DynamicModule {\n    return {\n      module: DatabaseModule,\n      providers: [\n        { provide: DATABASE_OPTIONS, useValue: options },\n        DatabaseService,\n      ],\n      exports: [DatabaseService],\n    };\n  }\n}\n```\n\nThe caller does `DatabaseModule.forRoot({ host: \'localhost\', port: 5432 })` in its imports array.\n\nBut what if the options themselves come from a `ConfigService` that is resolved asynchronously? That is exactly what `forRootAsync` solves:\n\n```typescript\nstatic forRootAsync(asyncOptions: DatabaseAsyncOptions): DynamicModule {\n  return {\n    module: DatabaseModule,\n    imports: asyncOptions.imports ?? [],\n    providers: [\n      {\n        provide: DATABASE_OPTIONS,\n        useFactory: asyncOptions.useFactory,\n        inject: asyncOptions.inject ?? [],\n      },\n      DatabaseService,\n    ],\n    exports: [DatabaseService],\n  };\n}\n```\n\nThe caller then does:\n\n```typescript\nDatabaseModule.forRootAsync({\n  imports: [ConfigModule],\n  inject: [ConfigService],\n  useFactory: (config: ConfigService) => ({\n    host: config.get(\'DB_HOST\'),\n    port: config.get<number>(\'DB_PORT\'),\n  }),\n})\n```\n\nThe factory is called by the IoC container during module compilation, with `ConfigService` injected automatically.\n\nIn an interview, the key insight to articulate is that `forRootAsync` delegates provider construction to the DI container itself — the factory runs inside the dependency resolution phase, which is why it can receive injected services. Without this pattern, your module would have to access environment variables directly, bypassing the config service abstraction.',
    },
    {
      id: 'nest-microservices-transport',
      title: 'Microservices transports — TCP, Redis, RabbitMQ, and Kafka',
      content:
        'NestJS microservices work with multiple transports through a unified interface. The transport you pick changes the wire protocol and broker, but your handler code stays identical because NestJS abstracts the details behind `ClientProxy` and the pattern-matching decorators.\n\n**TCP** is the default. It is a direct socket connection with no broker — suitable for internal, low-latency service-to-service calls in a trusted network. There is no message persistence: if the consumer is down, the call fails immediately.\n\n**Redis** uses pub/sub channels as the messaging layer. The broker is a Redis instance. Messages are published to a channel named after the pattern, and subscribers listen on that channel. Redis pub/sub is at-most-once — if the consumer is offline, the message is lost.\n\n**RabbitMQ** adds durable queues, acknowledgements, and dead-letter routing. Configure it with:\n\n```typescript\nNestFactory.createMicroservice<MicroserviceOptions>(AppModule, {\n  transport: Transport.RMQ,\n  options: {\n    urls: [process.env.RABBITMQ_URL],\n    queue: \'orders-queue\',\n    queueOptions: { durable: true },\n    prefetchCount: 10,\n  },\n});\n```\n\n**Kafka** is configured similarly but adds consumer group semantics and offset management:\n\n```typescript\n{\n  transport: Transport.KAFKA,\n  options: {\n    client: { brokers: [\'kafka:9092\'] },\n    consumer: { groupId: \'orders-consumer-group\' },\n  },\n}\n```\n\nOn the producer (client) side, register a `ClientsModule` in the module imports and inject `ClientProxy` by token. Use `client.send(pattern, payload)` for request/reply (`MessagePattern` on the server side) and `client.emit(pattern, payload)` for fire-and-forget (`EventPattern` on the server side).\n\nIn an interview, be ready to explain why `send()` returns an Observable rather than a Promise. NestJS uses Observables because the RxJS ecosystem allows timeout, retry, and cancellation operators to be composed directly on the result — you call `.toPromise()` or `firstValueFrom()` when you just need the single reply.',
    },
    {
      id: 'nest-websockets',
      title: 'WebSocket gateways — rooms, broadcasting, and authentication',
      content:
        'NestJS WebSocket support is built on the `@nestjs/websockets` package, which abstracts over Socket.IO or the native `ws` library. You choose the adapter by calling `app.useWebSocketAdapter(new IoAdapter(app))` or `new WsAdapter(app)` in `main.ts`.\n\nA gateway is a class decorated with `@WebSocketGateway()`. It can declare a namespace, a port, and CORS options:\n\n```typescript\n@WebSocketGateway({ namespace: \'/chat\', cors: { origin: \'*\' } })\nexport class ChatGateway implements OnGatewayInit, OnGatewayConnection, OnGatewayDisconnect {\n  @WebSocketServer() server: Server;\n\n  handleConnection(client: Socket) {\n    console.log(`Client connected: ${client.id}`);\n  }\n\n  @SubscribeMessage(\'joinRoom\')\n  handleJoinRoom(client: Socket, room: string) {\n    client.join(room);\n    this.server.to(room).emit(\'userJoined\', { id: client.id });\n  }\n\n  @SubscribeMessage(\'sendMessage\')\n  handleMessage(client: Socket, payload: { room: string; text: string }) {\n    this.server.to(payload.room).emit(\'message\', payload);\n  }\n}\n```\n\n`client.join(room)` adds the socket to a Socket.IO room. `this.server.to(room).emit(...)` broadcasts to all sockets in that room — this is how chat, collaborative editing, and live dashboard updates are built.\n\nAuthentication happens in a guard. WebSocket guards implement `CanActivate` and receive an `ExecutionContext` — call `context.switchToWs().getClient()` to get the raw socket and read its `handshake.auth.token`. Validate the JWT there and attach the user to the socket data:\n\n```typescript\n@Injectable()\nexport class WsJwtGuard implements CanActivate {\n  canActivate(context: ExecutionContext): boolean {\n    const client = context.switchToWs().getClient<Socket>();\n    const token = client.handshake.auth?.token;\n    const user = this.jwtService.verify(token);\n    client.data.user = user;\n    return true;\n  }\n}\n```\n\nIn an interview, explain the difference between broadcasting to a room (`server.to(room).emit`) and broadcasting to everyone except the sender (`client.broadcast.emit`). Also be ready to discuss scaling WebSockets across multiple server instances — Socket.IO requires a Redis adapter (`@socket.io/redis-adapter`) so that events emitted on one instance are delivered to sockets connected to other instances.',
    },
    {
      id: 'nest-graphql',
      title: 'GraphQL code-first — resolvers, DataLoader, and subscriptions',
      content:
        'NestJS supports GraphQL in two modes: schema-first (you write SDL and NestJS generates TypeScript types) and code-first (you write TypeScript classes and NestJS generates the SDL). Code-first is more common in NestJS projects because it keeps the schema and the types in sync automatically.\n\nInstall `@nestjs/graphql`, `@nestjs/apollo`, and `graphql`. Configure the module:\n\n```typescript\nGraphQLModule.forRoot<ApolloDriverConfig>({\n  driver: ApolloDriver,\n  autoSchemaFile: join(process.cwd(), \'src/schema.gql\'),\n  sortSchema: true,\n})\n```\n\nDefine types with `@ObjectType()` and `@Field()`, then create a resolver:\n\n```typescript\n@ObjectType()\nexport class Post {\n  @Field() id: string;\n  @Field() title: string;\n  @Field(() => User) author: User;\n}\n\n@Resolver(() => Post)\nexport class PostsResolver {\n  @Query(() => [Post]) posts() { return this.postsService.findAll(); }\n  @ResolveField(() => User) author(@Parent() post: Post) {\n    return this.usersService.findOne(post.authorId);\n  }\n}\n```\n\nThe `@ResolveField` on `author` creates the N+1 problem: for 100 posts, it calls `usersService.findOne` 100 times. The fix is **DataLoader** — a batching and caching library that collects all `findOne(id)` calls within a single event loop tick and issues one `findMany([id1, id2, ...])` instead:\n\n```typescript\n@Injectable({ scope: Scope.REQUEST })\nexport class UsersLoader {\n  readonly loader = new DataLoader<string, User>(async (ids) =>\n    this.usersService.findManyByIds(ids as string[]),\n  );\n}\n```\n\nDataLoaders must be request-scoped so each GraphQL request gets its own batch context — a singleton DataLoader would mix data across concurrent requests.\n\nSubscriptions push real-time events to connected clients over WebSockets. Mark a resolver method with `@Subscription(() => Post)` and use `PubSub` from `graphql-subscriptions` to publish events from mutations:\n\n```typescript\n@Subscription(() => Post)\npostCreated() { return this.pubSub.asyncIterator(\'postCreated\'); }\n```\n\nIn an interview, the DataLoader N+1 problem is the most common GraphQL question. Explain the problem, explain why naively fixing it with a join in the resolver breaks resolver composability, and explain why DataLoader is the correct solution.',
    },
    {
      id: 'nest-file-uploads',
      title: 'File uploads with Multer',
      content:
        'File uploads in NestJS are handled by Multer, the same middleware used in plain Express. NestJS wraps it with `@UseInterceptors(FileInterceptor(...))` and exposes the uploaded file via `@UploadedFile()`.\n\nA single file upload endpoint looks like this:\n\n```typescript\n@Post(\'avatar\')\n@UseInterceptors(FileInterceptor(\'file\', {\n  storage: diskStorage({\n    destination: \'./uploads\',\n    filename: (req, file, cb) => cb(null, `${uuid()}-${file.originalname}`),\n  }),\n  fileFilter: (req, file, cb) => {\n    if (!file.mimetype.startsWith(\'image/\')) return cb(new BadRequestException(\'Images only\'), false);\n    cb(null, true);\n  },\n  limits: { fileSize: 5 * 1024 * 1024 }, // 5 MB\n}))\nasync uploadAvatar(@UploadedFile() file: Express.Multer.File) {\n  return { path: file.path };\n}\n```\n\nFor multiple files, swap `FileInterceptor` for `FilesInterceptor(\'files\', 10)` and `@UploadedFiles()` for the parameter.\n\nIn production, you rarely write to disk on the API server itself — the server is stateless and may have multiple replicas. Instead, stream directly to object storage. The `multer-s3` storage engine uploads directly to S3 without buffering to disk:\n\n```typescript\nstorage: multerS3({\n  s3, bucket: process.env.S3_BUCKET,\n  key: (req, file, cb) => cb(null, `avatars/${uuid()}`),\n})\n```\n\nAlways validate file type using the MIME type from Multer\'s `file.mimetype` and also by inspecting the file\'s magic bytes — the `file-type` npm package reads the first few bytes to detect the true format. Client-supplied MIME types can be forged.\n\nIn an interview, the key concern to raise is security. Never use the original filename from `file.originalname` as the storage path — it is user-supplied input and can contain path traversal characters. Always generate a UUID for the storage key. Also mention that serving uploaded files through the same server that runs business logic is a security risk; serve them from a CDN or a separate static file server instead.',
    },
    {
      id: 'nest-throttler-terminus',
      title: 'Rate limiting with ThrottlerModule and health checks with TerminusModule',
      content:
        'Two features every production NestJS API needs are rate limiting and health checks — and NestJS provides first-class modules for both.\n\n**Rate limiting** is handled by `@nestjs/throttler`. Install it, register it globally, and it automatically applies a request counter per IP address:\n\n```typescript\nThrottlerModule.forRoot([{ ttl: 60000, limit: 100 }])\n```\n\nThis allows 100 requests per IP per 60 seconds. Apply `@Throttle({ default: { ttl: 10000, limit: 3 } })` on a specific route to override the global limit — useful for sensitive endpoints like login or password reset. Apply `@SkipThrottle()` to routes like `/health` that should never be throttled.\n\nThe default storage is in-memory, which does not work across multiple replicas. For distributed rate limiting, use `ThrottlerStorageRedisService` from `nestjs-throttler-storage-redis`, which stores counters in Redis:\n\n```typescript\nThrottlerModule.forRootAsync({\n  useFactory: (redis: Redis) => ({\n    throttlers: [{ ttl: 60000, limit: 100 }],\n    storage: new ThrottlerStorageRedisService(redis),\n  }),\n  inject: [REDIS_CLIENT],\n})\n```\n\n**Health checks** are handled by `@nestjs/terminus`. Create a `/health` endpoint that checks your database, Redis, and any external dependencies:\n\n```typescript\n@Controller(\'health\')\nexport class HealthController {\n  constructor(\n    private readonly health: HealthCheckService,\n    private readonly db: TypeOrmHealthIndicator,\n    private readonly redis: MicroserviceHealthIndicator,\n  ) {}\n\n  @Get()\n  @HealthCheck()\n  check() {\n    return this.health.check([\n      () => this.db.pingCheck(\'database\'),\n      () => this.redis.pingCheck(\'redis\', { transport: Transport.REDIS }),\n    ]);\n  }\n}\n```\n\nKubernetes uses liveness probes (`/health/live`) to restart crashed pods and readiness probes (`/health/ready`) to stop routing traffic to pods that are starting up or overloaded. The Terminus module returns structured JSON with the status of each indicator, making it easy to configure precise probe paths.\n\nIn an interview, emphasise that a good health endpoint has two levels: liveness (is the process alive?) which should only check the process itself, and readiness (is it ready to serve traffic?) which checks downstream dependencies. Failing both levels for the same reasons causes avoidable pod restarts.',
    },
    {
      id: 'nest-cqrs',
      title: 'CQRS module — CommandBus, QueryBus, and EventBus',
      content:
        'The `@nestjs/cqrs` module implements the Command Query Responsibility Segregation pattern inside a NestJS application, giving you a structured way to separate writes (commands), reads (queries), and side effects (events).\n\nInstall it and add `CqrsModule` to your feature module\'s imports. Then define a command, its handler, and wire them together:\n\n```typescript\n// create-order.command.ts\nexport class CreateOrderCommand {\n  constructor(public readonly userId: string, public readonly items: OrderItem[]) {}\n}\n\n// create-order.handler.ts\n@CommandHandler(CreateOrderCommand)\nexport class CreateOrderHandler implements ICommandHandler<CreateOrderCommand> {\n  constructor(\n    private readonly orderRepo: OrderRepository,\n    private readonly eventBus: EventBus,\n  ) {}\n\n  async execute(command: CreateOrderCommand) {\n    const order = await this.orderRepo.create(command);\n    this.eventBus.publish(new OrderCreatedEvent(order.id));\n    return order;\n  }\n}\n```\n\nIn your service or controller, inject `CommandBus` and dispatch:\n\n```typescript\nconst order = await this.commandBus.execute(new CreateOrderCommand(userId, items));\n```\n\nQueries follow the same pattern but use `QueryBus` and `@QueryHandler`. The query handler reads from a denormalised read model — a different table or a Redis cache — so queries never touch the write-side database.\n\nEvents published to `EventBus` are handled by classes decorated with `@EventsHandler(OrderCreatedEvent)`. Multiple handlers can listen to the same event independently, which is how you send a confirmation email, update an analytics projection, and notify a WebSocket gateway from a single domain event without coupling those concerns.\n\nThe practical benefit of this pattern is that your domain logic lives in focused, single-purpose handlers that are trivial to unit test in isolation. You can test `CreateOrderHandler` by providing a mock repository and asserting that the right domain event is published — no HTTP, no database, no NestJS test module needed.\n\nIn an interview, the question "how do you keep your NestJS controllers thin?" is best answered by describing this pattern: controllers translate HTTP into commands and queries, the buses dispatch them, and handlers do the work.',
    },
    {
      id: 'nest-swagger',
      title: 'OpenAPI and Swagger decorators',
      content:
        'Documentation that drifts from the actual API is worse than no documentation at all. NestJS solves this by generating OpenAPI documentation directly from your code using `@nestjs/swagger`.\n\nSet up the Swagger module in `main.ts`:\n\n```typescript\nconst config = new DocumentBuilder()\n  .setTitle(\'Orders API\')\n  .setVersion(\'1.0\')\n  .addBearerAuth()\n  .build();\nconst document = SwaggerModule.createDocument(app, config);\nSwaggerModule.setup(\'api\', app, document);\n```\n\nDecorate your DTOs and controllers to enrich the generated spec:\n\n```typescript\n@ApiTags(\'orders\')\n@ApiBearerAuth()\n@Controller(\'orders\')\nexport class OrdersController {\n  @Post()\n  @ApiOperation({ summary: \'Create a new order\' })\n  @ApiCreatedResponse({ type: OrderResponseDto })\n  @ApiBadRequestResponse({ description: \'Invalid payload\' })\n  create(@Body() dto: CreateOrderDto) { ... }\n}\n```\n\nOn DTOs, use `@ApiProperty()` to describe each field:\n\n```typescript\nexport class CreateOrderDto {\n  @ApiProperty({ example: [\'item-1\', \'item-2\'], description: \'Array of item IDs\' })\n  @IsArray()\n  @IsString({ each: true })\n  itemIds: string[];\n}\n```\n\n`@nestjs/swagger` reads TypeScript metadata and `class-validator` decorators to infer types automatically. Use `PartialType`, `PickType`, and `IntersectionType` from `@nestjs/swagger` (not `@nestjs/mapped-types`) so the generated DTO classes carry Swagger metadata through inheritance.\n\nFor responses, always annotate the exact return type with `@ApiOkResponse({ type: [UserDto] })` for arrays. This lets consumer teams generate type-safe API clients from the OpenAPI spec using tools like `openapi-typescript` or Swagger Codegen.\n\nIn an interview, the value proposition is clear: the spec is generated from the same decorators that drive your validation — it is always in sync with the code, unlike hand-written OpenAPI YAML files that grow stale.',
    },
    {
      id: 'nest-lazy-loading',
      title: 'Lazy loading modules for faster startup',
      content:
        'By default, NestJS eagerly loads every module at startup. In a large monorepo application with dozens of feature modules, this adds seconds to cold start time — a problem in serverless environments where every invocation may start a fresh instance.\n\nLazy loading defers module initialisation until the module is first needed. NestJS supports this via the `LazyModuleLoader` service:\n\n```typescript\n@Injectable()\nexport class ReportService {\n  constructor(private readonly lazyModuleLoader: LazyModuleLoader) {}\n\n  async generateReport() {\n    const { ReportModule } = await import(\'./report/report.module\');\n    const moduleRef = await this.lazyModuleLoader.load(() => ReportModule);\n    const service = moduleRef.get(ReportGeneratorService);\n    return service.generate();\n  }\n}\n```\n\nThe `import()` is a dynamic ES module import — the module file is not evaluated until this line runs. `lazyModuleLoader.load()` then builds and wires the NestJS module on first call, and caches it for subsequent calls.\n\nLazy loading is most valuable for:\n- Modules that are rarely used (admin-only features, one-off report generators)\n- Heavy modules with large dependency trees (PDF generation, image processing)\n- Serverless functions where startup latency directly impacts cost\n\nThe trade-off is that lazy-loaded modules do not participate in the main application\'s DI scope. They create a child module context, which means providers from lazy modules cannot be injected into eagerly-loaded modules. Keep lazy modules self-contained.\n\nIn an interview, mention that in NestJS Nx monorepos this problem is often addressed differently — each domain is its own deployable app rather than a lazy module, so startup time per function is naturally small. Lazy loading is the right tool when you are working within a monolithic NestJS application and need to trim startup time without splitting into separate deployments.',
    },
    {
      id: 'nest-nx-monorepo',
      title: 'NestJS with Nx monorepo',
      content:
        'An Nx monorepo houses multiple applications and libraries in a single repository, sharing code through a dependency graph managed by Nx. For backend teams building several NestJS services, this eliminates duplicated configuration and makes cross-service code reuse straightforward.\n\nScaffold a new Nx workspace with NestJS support:\n\n```bash\nnpx create-nx-workspace@latest my-org --preset=nest\nnx g @nx/nest:app orders-service\nnx g @nx/nest:lib shared-dto\n```\n\nLibraries (like `shared-dto`) contain shared types, DTOs, validators, and utility functions. Applications (like `orders-service`) import those libraries with TypeScript path aliases configured by Nx: `import { CreateOrderDto } from \'@my-org/shared-dto\'`.\n\nNx\'s key feature is its computation cache and affected-build graph. When you run `nx affected --target=build`, Nx only builds and tests the applications and libraries that were changed or depend on something that changed. In a large monorepo, this reduces CI time from 30 minutes to 3 minutes on a typical PR.\n\nThe project graph (`nx graph`) visualises dependencies between apps and libs. Enforce architectural boundaries with module boundary rules in `.eslintrc.json` — for example, prevent `orders-service` from importing directly from `payments-service`, requiring all cross-service communication to go through shared library types or published events.\n\nGlobal configuration (ESLint, Jest, TypeScript) lives at the root and is inherited by all projects, so you configure linting rules once and they apply everywhere. Nx plugins for Docker, Terraform, and Kubernetes deployments let you manage the full infrastructure lifecycle from the same repository.\n\nIn an interview, Nx is the answer to "how do you manage code sharing across multiple NestJS services without publishing npm packages for every change?" The monorepo approach gives you atomic commits across services and shared libraries without the overhead of package versioning.',
    },
  ],
};
