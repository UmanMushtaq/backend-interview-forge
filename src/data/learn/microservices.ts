import type { LearnModule } from '../../types';

export const microservices: LearnModule = {
  id: 'microservices',
  title: 'Microservices Architecture',
  blurb: 'Patterns, trade-offs, and production techniques for distributed systems.',
  lessons: [
    {
      id: 'ms-monolith-vs-micro',
      title: 'Microservices vs monolith — when to split',
      content:
        'The single most important question in microservices is whether to split at all. A monolith is simpler to develop, test, deploy, and debug — all code is in one place, function calls replace network calls, and transactions are trivial. The penalty only appears at scale: a large team making changes to the same codebase creates merge conflicts and slow CI; a single deployment artifact makes it hard to scale only the hot components; a bug in one feature can crash the whole application.\n\nMicroservices address these problems by splitting the system into independently deployable services, each owning its own process and data store. The cost is significant: you replace function calls with network calls (adding latency, serialisation overhead, and failure modes that do not exist in-process), you lose ACID transactions across service boundaries, and you need infrastructure for service discovery, distributed tracing, and inter-service authentication.\n\nConway\'s Law states that the architecture of a system mirrors the communication structure of the organisation that builds it. If your engineering org has a single team, a monolith is usually correct — there is no team boundary to enforce with a service boundary. When teams reach 8-10 engineers and begin stepping on each other\'s code, splitting by team boundary becomes valuable. The key heuristic: the pain of organisational coupling (two teams blocking each other) must exceed the operational overhead of running separate services before splitting is justified.\n\nA modular monolith is a useful intermediate step: well-defined module boundaries enforced within a single deployment. When you do need to split, extract the module that has the most independent deployment needs first — typically the component with the highest traffic or the one that needs different scaling behaviour than the rest of the system. Never split for the sake of using microservices; split to solve a concrete problem of team independence, scaling, or technology diversity.',
    },
    {
      id: 'ms-communication',
      title: 'Service communication — sync vs async, REST vs gRPC vs events',
      content:
        'How services communicate is one of the most consequential design decisions in a microservices system, with direct implications for latency, coupling, and resilience.\n\nSynchronous communication (REST, gRPC) has the caller block until it receives a response. REST over HTTP is universal, easy to debug with curl, and well-understood by every team. gRPC uses Protocol Buffers for schema-defined, binary-serialised messages and HTTP/2 for multiplexed connections — it is 5-10x more compact than JSON and generates type-safe client stubs in any language. Use gRPC for high-throughput internal service-to-service calls where performance matters; use REST for public APIs and when clients may be in environments where gRPC is not supported (browsers without gRPC-web).\n\nAsynchronous communication (events via RabbitMQ, Kafka, etc.) decouples services in time: the producer publishes an event and does not wait for the consumer to process it. This improves resilience (the producer is unaffected if the consumer is temporarily down) and throughput (producers are not blocked by consumer processing latency). The cost is complexity: you lose the immediate feedback of a return value, errors surface later and must be tracked, and eventual consistency means data is briefly out of sync across services.\n\nThe heuristic: use synchronous calls when the caller needs an immediate result to proceed (e.g. "does this user exist?", "is this payment authorised?") and asynchronous events when the caller does not need to wait for the side effect to complete (e.g. "send a welcome email", "update the search index"). A common pattern is synchronous for queries and commands that need acknowledgement, asynchronous for domain events that notify other services of state changes.\n\nIn NestJS, `ClientProxy.send()` handles synchronous request/reply over a message broker, while `ClientProxy.emit()` fires events without waiting. For REST calls between services, use `HttpService` (wrapping Axios) with a circuit breaker.\n\nAvoid mixing synchronous chains that span many services — a chain of five synchronous calls multiplies latency and means a single service being slow impacts the entire chain. If a flow can be decomposed into a trigger plus asynchronous steps, the asynchronous version is almost always more resilient.',
    },
    {
      id: 'ms-saga',
      title: 'The Saga pattern — choreography, orchestration, and failure handling',
      content:
        'Distributed transactions — operations that must atomically update data across multiple services — cannot use traditional two-phase commit (2PC) in a microservices system because 2PC requires all participants to hold locks until a coordinator decides to commit or rollback. In a system with network failures and independent service deployments, a coordinator crash or network partition leaves all participants stuck holding locks indefinitely.\n\nThe Saga pattern replaces a distributed transaction with a sequence of local transactions, each within a single service\'s database boundary. After each local transaction, the service publishes an event or sends a command to trigger the next step. If any step fails, the saga runs compensating transactions in reverse order to undo the already-committed steps.\n\nChoreography-based sagas have no central coordinator: each service listens for events from the previous step and emits events after its own step. For example: OrderService publishes `order.created`, PaymentService listens and charges the card then publishes `payment.completed`, InventoryService listens and reserves stock then publishes `stock.reserved`. Each service independently handles failures by publishing failure events that trigger compensations upstream. Choreography is simple and decentralised, but the overall flow is implicit and hard to track — you must correlate events across multiple services to understand what is happening.\n\nOrchestration-based sagas have a central orchestrator service that explicitly commands each participant and waits for replies. The orchestrator knows the entire flow and decides what to do at each step and on failure. The flow is explicit and testable, but the orchestrator becomes a hub of coupling and a potential single point of failure — mitigate by making the orchestrator stateless and storing saga state in a durable store.\n\nCompensating transactions must be idempotent because they may be retried. They do not reverse time — if an email was sent, you cannot "un-send" it; instead, you send a cancellation email. Design sagas with idempotency keys stored in the database so duplicate events (from at-least-once message delivery) are detected and ignored.',
    },
    {
      id: 'ms-cqrs',
      title: 'CQRS — command/query split and eventual consistency',
      content:
        'Command Query Responsibility Segregation (CQRS) separates the write model (commands that change state) from the read model (queries that return data). In a traditional architecture, the same data model serves both reads and writes, which means the schema is a compromise: normalised enough for write integrity, but joins and aggregations make complex queries slow.\n\nWith CQRS, the write side handles commands and stores data in a normalised form (or as events in an event store). After a command succeeds, a handler publishes an event. Read-model projections (event handlers that maintain denormalised, query-optimised views) subscribe to these events and update separate read stores — these could be the same database in a different table, a Redis cache, an Elasticsearch index, or a purpose-built read replica.\n\nIn NestJS, a common CQRS implementation uses the `@nestjs/cqrs` package:\n\n```typescript\n// Command\nexport class CreateOrderCommand { constructor(public readonly dto: CreateOrderDto) {} }\n\n// CommandHandler\n@CommandHandler(CreateOrderCommand)\nexport class CreateOrderHandler implements ICommandHandler<CreateOrderCommand> {\n  async execute(command: CreateOrderCommand) {\n    const order = this.orderRepo.create(command.dto);\n    await this.orderRepo.save(order);\n    this.eventBus.publish(new OrderCreatedEvent(order.id));\n  }\n}\n\n// Query\nexport class GetOrderQuery { constructor(public readonly id: string) {} }\n\n// QueryHandler reads from a denormalised read store\n@QueryHandler(GetOrderQuery)\nexport class GetOrderHandler implements IQueryHandler<GetOrderQuery> {\n  async execute(query: GetOrderQuery) {\n    return this.orderReadRepo.findOne(query.id); // pre-joined, flat projection\n  }\n}\n```\n\nThe trade-off is eventual consistency: there is a window between when a command is committed and when the read projections are updated during which a query may return stale data. For most UI interactions, 50-500ms of staleness is acceptable. Design your UI to account for this — after submitting a command, show the expected outcome optimistically rather than immediately re-querying. CQRS adds significant complexity and is only justified when read and write workloads have meaningfully different scaling requirements or when the write schema is incompatible with efficient querying.',
    },
    {
      id: 'ms-event-sourcing',
      title: 'Event sourcing — append-only log, replay, and snapshots',
      content:
        'Event sourcing stores state as an ordered, immutable sequence of domain events rather than storing the current state directly. Instead of a `wallets` table with a `balance` column, you have a `wallet_events` table with rows like `{ type: "deposited", amount: 100, at: timestamp }` and `{ type: "withdrawn", amount: 30, at: timestamp }`. The current balance is derived by replaying all events for a wallet from the beginning.\n\nThe advantages are significant for audit-heavy domains like financial systems: you have a complete, immutable audit log of every state change for free; you can reconstruct the state at any point in time by replaying events up to that timestamp; you can introduce new projections (read models) retroactively by replaying all historical events through the new projection logic — this is extremely powerful when requirements change.\n\nThe challenges are equally significant. Replaying thousands of events per entity on every read is slow — mitigate with snapshots: periodically capture the current aggregate state as a snapshot, store it alongside the event log, and on read load the most recent snapshot and replay only events after the snapshot timestamp. As your domain evolves, old events may need structural changes — this is called upcasting: when loading an event, check its version and transform it to the current schema before applying it to the aggregate.\n\nIn NestJS, the `@nestjs/cqrs` package has an `EventStore` concept. In practice, PostgreSQL JSONB columns for events work well at small to medium scale — store `{ type, payload, aggregateId, version, createdAt }` with a unique constraint on `(aggregateId, version)` to prevent concurrent writes creating the same version twice (optimistic concurrency control).\n\nEvent sourcing is not appropriate for every aggregate — use it where auditability, temporal queries, or retroactive projections provide concrete value. A user profile with no audit requirements benefits little from event sourcing; a payment ledger benefits enormously.',
    },
    {
      id: 'ms-service-discovery',
      title: 'Service discovery and the API gateway',
      content:
        'In a microservices system, services need to find each other without hardcoding IP addresses that change on every deployment. Service discovery solves this by maintaining a registry of healthy service instances.\n\nClient-side discovery has the caller query a service registry (like HashiCorp Consul or a DNS-based registry) and select an instance, then call it directly. The client must implement load balancing logic. Server-side discovery has the caller route all requests through a load balancer, which queries the registry and forwards to an appropriate instance. In Kubernetes, server-side discovery is the default: a Service resource provides a stable DNS name and the kube-proxy routes to healthy pods, handling both discovery and load balancing transparently.\n\nThe API gateway is the single entry point for external traffic into your microservices cluster. It handles cross-cutting concerns: TLS termination, authentication and authorisation (validating JWT tokens and adding user context headers before forwarding to internal services, so individual services do not need to verify tokens), rate limiting, request logging, and routing rules. Popular API gateways include AWS API Gateway, Kong, and Traefik. In NestJS projects, a lightweight custom gateway can be implemented as a NestJS application that aggregates calls to downstream services.\n\nThe Backend for Frontend (BFF) pattern takes this further: instead of one generic gateway, create one gateway per type of client (web BFF, mobile BFF). Each BFF aggregates and shapes the response for its specific client\'s needs — the mobile BFF might return a smaller payload with only the fields the mobile app uses, reducing bandwidth. BFFs are owned by the frontend teams, giving them control over their API contract without modifying the underlying services.\n\nA common antipattern is a chatty gateway that makes many sequential service calls on each request, multiplying latency. Solve with parallel calls (Promise.all), caching at the gateway level, and moving aggregation logic into a dedicated aggregation service or into a CQRS read model that pre-joins the data.',
    },
    {
      id: 'ms-tracing',
      title: 'Distributed tracing — correlation IDs, OpenTelemetry, and Jaeger',
      content:
        'In a monolith, a slow request is debugged by examining a single process\'s logs and stack trace. In microservices, a slow request may involve ten services, and understanding the flow requires correlating logs and timings across all of them. Distributed tracing provides this visibility.\n\nThe core concepts: a trace represents a single request\'s journey through the system, composed of spans. Each span represents a unit of work (an HTTP call, a database query, a message send) with a start time, end time, and metadata (service name, operation name, tags, logs). Spans form a tree: the root span is created when the request enters the system, and each downstream call creates a child span. The trace context (a trace ID and the current span ID) is propagated through HTTP headers (the W3C `traceparent` header is the standard) and message queue headers.\n\nOpenTelemetry (OTel) is the vendor-neutral standard for instrumenting applications. Install `@opentelemetry/sdk-node` and configure exporters to send telemetry to Jaeger (open-source, self-hosted), Tempo (Grafana Labs), or a commercial vendor:\n\n```typescript\nimport { NodeSDK } from \'@opentelemetry/sdk-node\';\nimport { JaegerExporter } from \'@opentelemetry/exporter-jaeger\';\nimport { HttpInstrumentation } from \'@opentelemetry/instrumentation-http\';\n\nconst sdk = new NodeSDK({\n  traceExporter: new JaegerExporter({ endpoint: \'http://jaeger:14268/api/traces\' }),\n  instrumentations: [new HttpInstrumentation()],\n});\nsdk.start();\n```\n\nAuto-instrumentation patches `http`, `pg`, `redis`, and other libraries to create spans automatically. Add custom spans for business logic with `tracer.startActiveSpan("processPayment", (span) => { ... span.end(); })`. Add correlation IDs as span attributes so you can query by order ID, user ID, or transaction ID in Jaeger\'s search UI. The investment in tracing pays dividends in production: what took hours of log grep-ing becomes a 30-second Jaeger query showing exactly which service added latency on a specific failing request.',
    },
    {
      id: 'ms-resilience',
      title: 'Circuit breaker and bulkhead patterns',
      content:
        'When a downstream service becomes slow or unavailable, callers that keep waiting for it exhaust their own thread pools and connection pools, causing cascading failures that can take down the entire system. The circuit breaker and bulkhead patterns prevent this.\n\nThe circuit breaker pattern wraps calls to a downstream service and tracks the failure rate. When failures exceed a threshold (e.g. 50% over the last 10 calls), the circuit trips to the "open" state and all calls fail immediately with a fallback response, without even attempting the downstream call. After a configured timeout (e.g. 30 seconds), the circuit enters "half-open" state and allows one probe request through. If it succeeds, the circuit closes and normal traffic resumes; if it fails, the circuit reopens for another timeout period. This prevents the caller from wasting resources waiting for a service that is clearly down, and gives the downstream service time to recover without being hammered with traffic.\n\nIn Node.js, the `opossum` library implements circuit breakers:\n\n```typescript\nimport CircuitBreaker from \'opossum\';\n\nconst breaker = new CircuitBreaker(callPaymentService, {\n  timeout: 3000,         // fail if call takes longer than 3s\n  errorThresholdPercentage: 50,\n  resetTimeout: 30000,   // try again after 30s\n  volumeThreshold: 5,    // need at least 5 calls before tripping\n});\n\nbreaker.fallback(() => ({ status: \'unavailable\', cached: true }));\nconst result = await breaker.fire(paymentData);\n```\n\nThe bulkhead pattern limits the resources allocated to calls to a specific downstream service, preventing one slow downstream from consuming all available resources. In Node.js, implement bulkheads with a concurrency limit: use a semaphore or the `p-limit` library to cap simultaneous calls to any one service. If 50 concurrent calls to the payment service are all slow, the 51st call fails fast rather than queuing and holding resources indefinitely. Combine circuit breakers and bulkheads with retry logic (with exponential backoff and jitter) and you have a resilient service mesh without a service mesh infrastructure.',
    },
    {
      id: 'ms-database-per-service',
      title: 'Database-per-service and data consistency patterns',
      content:
        'The database-per-service principle states that each microservice should own its data exclusively — no other service should query or write its database directly. This enforces encapsulation and allows services to evolve their schema independently. If two services share a database, deploying one service\'s schema migration can break the other, negating the deployment independence that microservices are supposed to provide.\n\nIn practice, database-per-service creates the challenge of data consistency and querying across service boundaries. There are several patterns for handling this.\n\nAPI composition assembles data for a query by calling multiple services and joining the results in memory. An OrderDetailsQuery might call the OrderService for order data, the UserService for customer details, and the ShippingService for tracking — then merge the results. The downside is latency (multiple network hops) and the impossibility of server-side filtering across services (you cannot WHERE on a field owned by a different service).\n\nThe shared read model (also a CQRS projection) has each service publish its data changes as events, and a dedicated query service consumes these events to maintain a denormalised read database that contains data from multiple services. This enables efficient joins and server-side filtering, at the cost of eventual consistency in the read model.\n\nShared-nothing data also means you cannot use foreign key constraints across services. If the OrderService stores a `userId`, it cannot have a FK to the UserService\'s `users` table. You must accept that the UserService could delete a user while the OrderService still holds references, and handle this at the application level (soft deletes with event propagation, or accepting orphaned records as valid state).\n\nThe anti-patterns to avoid: calling another service\'s database directly (creates hidden coupling), sharing a single "platform database" across services (shared schema = shared deployment constraint), and distributed transactions spanning multiple service databases (use Saga instead). The operational overhead of running many separate databases is real — Kubernetes with separate StatefulSets per database, plus a tool like Zalando Postgres Operator or CockroachDB for multi-region scenarios, is the modern approach.',
    },
    {
      id: 'ms-deployment',
      title: 'Deploying microservices — Docker, Kubernetes, health checks, and rolling deploys',
      content:
        'Microservices require robust deployment infrastructure because you are managing many more independent deployable units than a monolith. Understanding the deployment stack is expected of senior engineers working in distributed systems.\n\nEach service is packaged as a Docker image. A minimal Dockerfile for a NestJS service uses multi-stage builds to keep the final image small:\n\n```dockerfile\nFROM node:20-alpine AS build\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --only=production\nCOPY . .\nRUN npm run build\n\nFROM node:20-alpine\nWORKDIR /app\nCOPY --from=build /app/dist ./dist\nCOPY --from=build /app/node_modules ./node_modules\nUSER node\nCMD ["node", "dist/main.js"]\n```\n\nIn Kubernetes, each service is a Deployment (which manages replicas of a pod), paired with a Service (which provides stable DNS and load balancing to the pods). A rolling deploy updates pods one at a time: Kubernetes starts a new pod with the updated image, waits for it to pass its readiness probe, then terminates an old pod. This ensures zero downtime as long as the new version is backwards-compatible (API contracts are not broken, migrations are additive-only). Configure `maxUnavailable: 0` and `maxSurge: 1` in the Deployment strategy to never drop below the desired replica count during a deploy.\n\nHealth checks are mandatory: the liveness probe (`/health/live`, returns 200 if the process is running) triggers a pod restart if it fails; the readiness probe (`/health/ready`, returns 200 when all dependencies are healthy) removes a pod from the Service\'s load balancer without killing it. Use `@nestjs/terminus` for structured `/health` endpoints.\n\nThe sidecar pattern runs a companion container in the same pod as the main service, sharing the pod\'s network and volume. Common sidecars: an Envoy proxy for service mesh (mTLS, circuit breaking, retries at the infrastructure layer without changing application code), a Fluent Bit log forwarder, or a secrets injector (Vault Agent Sidecar). The sidecar pattern is the foundation of service meshes like Istio, which remove resilience concerns from application code and handle them transparently at the infrastructure layer.',
    },
  ],
};
