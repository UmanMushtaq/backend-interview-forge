import type { LearnModule } from '../../types';

export const dsa: LearnModule = {
  id: 'dsa',
  title: 'DSA Patterns',
  blurb: 'The algorithmic patterns that appear in backend interviews at EU tech companies.',
  lessons: [
    {
      id: 'dsa-two-pointers',
      title: 'Two pointers — sliding window vs converging pointers',
      content:
        'The two-pointer technique uses two indices to traverse a data structure, eliminating the need for a nested loop and reducing time complexity from O(n²) to O(n). It appears in two main flavours: converging pointers (both ends moving toward the middle) and sliding window (a fixed or variable-width window moving across the input).\n\nConverging pointers are used on sorted arrays. The classic example is Two Sum on a sorted array: start with `left = 0` and `right = arr.length - 1`. If `arr[left] + arr[right] === target`, you found it. If the sum is too small, move `left` right; if too large, move `right` left. This works because sorting allows you to reason about whether increasing or decreasing the window sum moves you toward the target:\n\n```typescript\nfunction twoSum(arr: number[], target: number): [number, number] | null {\n  let left = 0, right = arr.length - 1;\n  while (left < right) {\n    const sum = arr[left] + arr[right];\n    if (sum === target) return [left, right];\n    if (sum < target) left++;\n    else right--;\n  }\n  return null;\n}\n```\n\nSliding window is used for subarray or substring problems where you want to find the maximum, minimum, or valid range of a window of elements. The window is defined by `left` and `right` pointers that both move forward (never backward), giving amortised O(n) time. For a fixed-size window, advance both together. For a variable-size window, expand `right` on each iteration and shrink `left` when a constraint is violated:\n\n```typescript\n// Longest substring without repeating characters\nfunction lengthOfLongestSubstring(s: string): number {\n  const seen = new Map<string, number>();\n  let left = 0, best = 0;\n  for (let right = 0; right < s.length; right++) {\n    const ch = s[right];\n    if (seen.has(ch) && seen.get(ch)! >= left) {\n      left = seen.get(ch)! + 1;\n    }\n    seen.set(ch, right);\n    best = Math.max(best, right - left + 1);\n  }\n  return best;\n}\n```\n\nThe key interview signal: recognise the pattern from "subarray" or "substring" in the problem statement. If the problem asks for a maximum/minimum/longest/shortest with a constraint on contiguous elements, sliding window is almost always the right approach. Common problems: maximum sum subarray of size K, minimum window substring, longest subarray with sum ≤ K.',
    },
    {
      id: 'dsa-binary-search',
      title: 'Binary search — sorted arrays and answer space',
      content:
        'Binary search halves the search space on each iteration, achieving O(log n) time. It is only applicable when the search space is sorted (or monotonic), but this includes more situations than the classic "search a sorted array" example.\n\nThe canonical sorted-array implementation has one critical pitfall — off-by-one errors in the loop condition and the mid-point calculation:\n\n```typescript\nfunction binarySearch(arr: number[], target: number): number {\n  let left = 0, right = arr.length - 1;\n  while (left <= right) {              // <= not <, so single-element arrays work\n    const mid = left + Math.floor((right - left) / 2); // avoids integer overflow\n    if (arr[mid] === target) return mid;\n    if (arr[mid] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return -1;\n}\n```\n\nUsing `left + Math.floor((right - left) / 2)` instead of `Math.floor((left + right) / 2)` avoids integer overflow in languages with fixed-size integers (important in Java/C++; less so in JavaScript but good practice).\n\nBinary search on the answer space is more powerful and less obvious. When a problem asks "what is the minimum X such that some condition holds?", and the condition is monotonic (false for all small X, true for all large X), you can binary search on the answer. Example: "Given workers with capacities and a deadline, what is the minimum number of batches to ship all packages?" Binary search on the number of batches; for each candidate, greedily check if it is feasible in O(n) time.\n\n```typescript\n// Binary search on answer space template\nfunction minFeasible(lo: number, hi: number, isFeasible: (mid: number) => boolean): number {\n  while (lo < hi) {\n    const mid = lo + Math.floor((hi - lo) / 2);\n    if (isFeasible(mid)) hi = mid;   // mid might be the answer; don\'t skip it\n    else lo = mid + 1;               // mid is too small\n  }\n  return lo;\n}\n```\n\nFinding the leftmost or rightmost occurrence of a target in an array with duplicates requires adjusting how you move the pointers when `arr[mid] === target` — for leftmost, set `right = mid` instead of returning; for rightmost, set `left = mid + 1`. This is a common interview question that trips up candidates who only know the basic variant.',
    },
    {
      id: 'dsa-hashmaps',
      title: 'Hash maps and sets — frequency counting and grouping',
      content:
        'Hash maps and sets are the most frequently useful data structure in algorithm interviews because they provide O(1) average-case lookup, insert, and delete, turning many O(n²) brute-force solutions into O(n) ones.\n\nFrequency counting is the core pattern: iterate the input once to count occurrences in a map, then use the counts to answer queries in O(1). The classic problem is finding the first non-repeating character in a string:\n\n```typescript\nfunction firstUnique(s: string): string | null {\n  const count = new Map<string, number>();\n  for (const ch of s) count.set(ch, (count.get(ch) ?? 0) + 1);\n  for (const ch of s) if (count.get(ch) === 1) return ch;\n  return null;\n}\n```\n\nThe two-sum pattern stores complements as you iterate, enabling a single-pass O(n) solution:\n\n```typescript\nfunction twoSumUnsorted(nums: number[], target: number): [number, number] | null {\n  const seen = new Map<number, number>(); // value -> index\n  for (let i = 0; i < nums.length; i++) {\n    const complement = target - nums[i];\n    if (seen.has(complement)) return [seen.get(complement)!, i];\n    seen.set(nums[i], i);\n  }\n  return null;\n}\n```\n\nGrouping with a canonical key: group anagrams by sorting each word\'s characters to form a canonical key, then map from canonical key to list of words:\n\n```typescript\nfunction groupAnagrams(words: string[]): string[][] {\n  const map = new Map<string, string[]>();\n  for (const word of words) {\n    const key = word.split(\'\').sort().join(\'\');\n    if (!map.has(key)) map.set(key, []);\n    map.get(key)!.push(word);\n  }\n  return [...map.values()];\n}\n```\n\nSets are the right tool for O(1) membership tests. Use a Set when you need to detect duplicates, find elements that appear in both collections (intersection), or find elements that appear in only one collection (symmetric difference). In backend contexts, Sets and Maps appear when implementing caches, deduplication logic, in-memory indexes, and graph adjacency lists. In interviews, reach for a Map or Set whenever you see O(n²) comparisons — storing one pass\'s data in a Map to answer the second pass in O(1) is the most common optimisation.',
    },
    {
      id: 'dsa-stacks-queues',
      title: 'Stacks and queues — monotonic stack and O(1) max queue',
      content:
        'Stacks (LIFO) and queues (FIFO) are the building blocks for a surprising number of interview problems, especially when combined into the monotonic stack and sliding window maximum patterns.\n\nThe monotonic stack maintains elements in strictly increasing or decreasing order by popping elements that violate the invariant when a new element is pushed. This solves "next greater element" in O(n):\n\n```typescript\nfunction nextGreaterElement(nums: number[]): number[] {\n  const result = new Array(nums.length).fill(-1);\n  const stack: number[] = []; // stores indices\n  for (let i = 0; i < nums.length; i++) {\n    while (stack.length > 0 && nums[stack.at(-1)!] < nums[i]) {\n      result[stack.pop()!] = nums[i]; // i is the next greater for the popped index\n    }\n    stack.push(i);\n  }\n  return result;\n}\n```\n\nThe same pattern solves "largest rectangle in a histogram" — a frequently-asked hard problem — by using a monotonic increasing stack to track the left boundary of each bar.\n\nBracket matching uses a stack: push opening brackets; on a closing bracket, pop and check if it matches:\n\n```typescript\nfunction isValid(s: string): boolean {\n  const stack: string[] = [];\n  const pairs: Record<string, string> = { \')\': \'(\', \']\': \'[\', \'}\': \'{\' };\n  for (const ch of s) {\n    if (\'([{\'.includes(ch)) { stack.push(ch); }\n    else if (stack.pop() !== pairs[ch]) return false;\n  }\n  return stack.length === 0;\n}\n```\n\nThe sliding window maximum problem (find the maximum in every window of size K) requires a deque (double-ended queue) to achieve O(n) time instead of O(nK). Maintain a monotonic decreasing deque of indices: when sliding the window, remove the front if it is out of the window, then remove from the back any indices smaller than the current element (they can never be the maximum while the current element is in the window). The front of the deque is always the index of the current window\'s maximum. This pattern appears in backend systems when computing rolling metrics — the maximum request latency in the last 60 seconds, for example.',
    },
    {
      id: 'dsa-linked-lists',
      title: 'Linked lists — reversal, cycle detection, and merge',
      content:
        'Linked list problems test your ability to manipulate pointers carefully without losing track of nodes. They rarely appear in backend system work but are extremely common in algorithm interviews.\n\nIn-place reversal is the foundational operation. The key is maintaining three pointers: previous, current, and next:\n\n```typescript\ninterface ListNode { val: number; next: ListNode | null; }\n\nfunction reverseList(head: ListNode | null): ListNode | null {\n  let prev: ListNode | null = null;\n  let curr = head;\n  while (curr !== null) {\n    const next = curr.next;\n    curr.next = prev;\n    prev = curr;\n    curr = next;\n  }\n  return prev;\n}\n```\n\nCycle detection uses Floyd\'s tortoise-and-hare algorithm: two pointers, slow advancing one step and fast advancing two. If there is a cycle, fast will eventually lap slow and they will meet:\n\n```typescript\nfunction hasCycle(head: ListNode | null): boolean {\n  let slow = head, fast = head;\n  while (fast !== null && fast.next !== null) {\n    slow = slow!.next;\n    fast = fast.next.next;\n    if (slow === fast) return true;\n  }\n  return false;\n}\n```\n\nTo find the start of the cycle: after slow and fast meet, reset one pointer to head and advance both one step at a time — they meet at the cycle\'s entry point. This is a mathematical result based on the distances in the cycle.\n\nMerging two sorted linked lists in-place (no extra array) uses a dummy head to simplify the edge cases:\n\n```typescript\nfunction mergeTwoLists(l1: ListNode | null, l2: ListNode | null): ListNode | null {\n  const dummy: ListNode = { val: 0, next: null };\n  let cur = dummy;\n  while (l1 !== null && l2 !== null) {\n    if (l1.val <= l2.val) { cur.next = l1; l1 = l1.next; }\n    else { cur.next = l2; l2 = l2.next; }\n    cur = cur.next!;\n  }\n  cur.next = l1 ?? l2;\n  return dummy.next;\n}\n```\n\nThe dummy head technique eliminates the special case for an empty result list and appears in many linked list problems. Finding the Kth node from the end uses two pointers K apart: advance the far pointer K steps, then advance both until far reaches the end — near is at position K from the end.',
    },
    {
      id: 'dsa-trees',
      title: 'Trees — DFS traversals, BFS, and lowest common ancestor',
      content:
        'Binary tree problems test recursive thinking and the ability to combine top-down and bottom-up information flow. Mastering the three DFS traversals and BFS level-order traversal covers the vast majority of tree interview questions.\n\nDFS traversals differ only in when you process the current node relative to its children:\n\n```typescript\ninterface TreeNode { val: number; left: TreeNode | null; right: TreeNode | null; }\n\n// Pre-order: root -> left -> right (used for tree copy, serialisation)\nfunction preorder(node: TreeNode | null, result: number[] = []): number[] {\n  if (!node) return result;\n  result.push(node.val);          // process NOW\n  preorder(node.left, result);\n  preorder(node.right, result);\n  return result;\n}\n\n// In-order: left -> root -> right (BST gives sorted output)\nfunction inorder(node: TreeNode | null, result: number[] = []): number[] {\n  if (!node) return result;\n  inorder(node.left, result);\n  result.push(node.val);          // process BETWEEN children\n  inorder(node.right, result);\n  return result;\n}\n\n// Post-order: left -> right -> root (used for tree deletion, height calculation)\nfunction postorder(node: TreeNode | null, result: number[] = []): number[] {\n  if (!node) return result;\n  postorder(node.left, result);\n  postorder(node.right, result);\n  result.push(node.val);          // process AFTER children\n  return result;\n}\n```\n\nBFS level-order traversal uses a queue and is the natural approach for level-by-level processing or finding the shortest path in a tree:\n\n```typescript\nfunction levelOrder(root: TreeNode | null): number[][] {\n  if (!root) return [];\n  const result: number[][] = [];\n  const queue: TreeNode[] = [root];\n  while (queue.length > 0) {\n    const levelSize = queue.length;\n    const level: number[] = [];\n    for (let i = 0; i < levelSize; i++) {\n      const node = queue.shift()!;\n      level.push(node.val);\n      if (node.left) queue.push(node.left);\n      if (node.right) queue.push(node.right);\n    }\n    result.push(level);\n  }\n  return result;\n}\n```\n\nLowest Common Ancestor (LCA) is found with a post-order DFS: for each node, check if the left subtree contains one target and the right subtree contains the other — if so, the current node is the LCA. If both targets are in one subtree, recurse further. This runs in O(n) with a single tree traversal.',
    },
    {
      id: 'dsa-graphs',
      title: 'Graphs — BFS shortest path, DFS cycle detection, topological sort',
      content:
        'Graphs generalise trees by allowing arbitrary connections and cycles. Most graph problems use an adjacency list representation (a Map from node to its neighbours) and fall into three categories: shortest path, cycle detection, and ordering.\n\n```typescript\ntype Graph = Map<number, number[]>;\n\nfunction buildGraph(edges: [number, number][], directed = false): Graph {\n  const g: Graph = new Map();\n  for (const [u, v] of edges) {\n    if (!g.has(u)) g.set(u, []);\n    if (!g.has(v)) g.set(v, []);\n    g.get(u)!.push(v);\n    if (!directed) g.get(v)!.push(u);\n  }\n  return g;\n}\n```\n\nBFS finds the shortest path in an unweighted graph because it explores nodes level by level — the first time you reach a node is via the shortest path:\n\n```typescript\nfunction bfsShortestPath(graph: Graph, start: number, end: number): number {\n  const visited = new Set([start]);\n  const queue: [number, number][] = [[start, 0]]; // [node, distance]\n  for (const [node, dist] of queue) {\n    if (node === end) return dist;\n    for (const neighbour of graph.get(node) ?? []) {\n      if (!visited.has(neighbour)) {\n        visited.add(neighbour);\n        queue.push([neighbour, dist + 1]);\n      }\n    }\n  }\n  return -1;\n}\n```\n\nDFS cycle detection on a directed graph uses three colours — white (unvisited), grey (in current DFS path), black (fully processed). A back edge (grey node encountered again) means a cycle:\n\n```typescript\nfunction hasCycleDirected(graph: Graph): boolean {\n  const state = new Map<number, \'white\' | \'grey\' | \'black\'>();\n  function dfs(node: number): boolean {\n    state.set(node, \'grey\');\n    for (const nb of graph.get(node) ?? []) {\n      if (state.get(nb) === \'grey\') return true;\n      if (!state.get(nb) && dfs(nb)) return true;\n    }\n    state.set(node, \'black\');\n    return false;\n  }\n  for (const node of graph.keys()) if (!state.get(node) && dfs(node)) return true;\n  return false;\n}\n```\n\nTopological sort orders nodes in a directed acyclic graph (DAG) such that all edges point forward. It is used for build systems, dependency resolution, and scheduling. Kahn\'s algorithm uses in-degree counts: enqueue all nodes with in-degree 0, process each and reduce neighbour in-degrees, enqueue newly zero-degree neighbours. If the output length equals the number of nodes, the graph is a DAG; otherwise it has a cycle.',
    },
    {
      id: 'dsa-dp',
      title: 'Dynamic programming — memoization vs tabulation and 1D patterns',
      content:
        'Dynamic programming (DP) solves problems by breaking them into overlapping subproblems and storing results to avoid recomputation. It applies when a problem has optimal substructure (the optimal solution uses optimal solutions to subproblems) and overlapping subproblems (the same subproblems are solved multiple times in a naive recursive solution).\n\nTop-down (memoization) starts with the recursive solution and caches results. Bottom-up (tabulation) fills a table from the base cases up. Both have the same time complexity; tabulation is usually faster in practice (no call stack overhead) and has bounded space, while memoization is easier to derive from the recursive formulation.\n\nClimbing stairs — the foundational 1D DP pattern:\n\n```typescript\n// Memoization (top-down)\nfunction climbMemo(n: number, memo = new Map<number, number>()): number {\n  if (n <= 1) return 1;\n  if (memo.has(n)) return memo.get(n)!;\n  const result = climbMemo(n - 1, memo) + climbMemo(n - 2, memo);\n  memo.set(n, result);\n  return result;\n}\n\n// Tabulation (bottom-up), O(n) time, O(1) space with rolling variables\nfunction climbTab(n: number): number {\n  if (n <= 1) return 1;\n  let prev2 = 1, prev1 = 1;\n  for (let i = 2; i <= n; i++) {\n    const curr = prev1 + prev2;\n    prev2 = prev1;\n    prev1 = curr;\n  }\n  return prev1;\n}\n```\n\nHouse robber — choosing non-adjacent elements for maximum sum:\n\n```typescript\nfunction rob(nums: number[]): number {\n  let prev2 = 0, prev1 = 0;\n  for (const num of nums) {\n    const curr = Math.max(prev1, prev2 + num);\n    prev2 = prev1;\n    prev1 = curr;\n  }\n  return prev1;\n}\n```\n\nThe pattern: `dp[i] = max/min of some combination of dp[i-1], dp[i-2], ...`. Always identify the state definition first ("dp[i] is the maximum profit using houses 0..i"), then the recurrence, then the base cases. For 2D DP (grid problems, subsequences), the state is two indices and the table is an (n×m) matrix. The coin change problem, longest common subsequence, and 0/1 knapsack are the canonical 2D DP problems to practice.',
    },
    {
      id: 'dsa-heaps',
      title: 'Heaps and priority queues — top-K elements and merge K sorted lists',
      content:
        'A heap is a complete binary tree that satisfies the heap property: every parent is smaller than its children (min-heap) or larger (max-heap). This gives O(log n) insert and O(log n) extract-min/max, and O(1) peek at the minimum/maximum. JavaScript does not have a built-in heap, so interviews often allow a simplified implementation or assume one is available.\n\nA minimal min-heap implementation:\n\n```typescript\nclass MinHeap {\n  private heap: number[] = [];\n  push(val: number) {\n    this.heap.push(val);\n    this.bubbleUp(this.heap.length - 1);\n  }\n  pop(): number | undefined {\n    const min = this.heap[0];\n    const last = this.heap.pop()!;\n    if (this.heap.length > 0) { this.heap[0] = last; this.sinkDown(0); }\n    return min;\n  }\n  peek() { return this.heap[0]; }\n  size() { return this.heap.length; }\n  private bubbleUp(i: number) {\n    while (i > 0) {\n      const parent = Math.floor((i - 1) / 2);\n      if (this.heap[parent] <= this.heap[i]) break;\n      [this.heap[parent], this.heap[i]] = [this.heap[i], this.heap[parent]];\n      i = parent;\n    }\n  }\n  private sinkDown(i: number) {\n    const n = this.heap.length;\n    while (true) {\n      let smallest = i;\n      const l = 2 * i + 1, r = 2 * i + 2;\n      if (l < n && this.heap[l] < this.heap[smallest]) smallest = l;\n      if (r < n && this.heap[r] < this.heap[smallest]) smallest = r;\n      if (smallest === i) break;\n      [this.heap[smallest], this.heap[i]] = [this.heap[i], this.heap[smallest]];\n      i = smallest;\n    }\n  }\n}\n```\n\nTop-K largest elements: maintain a min-heap of size K. For each element, if it is larger than the heap\'s minimum, push it and pop the minimum. Final heap contains the K largest. This is O(n log K) — far better than sorting the entire array O(n log n) when K is small.\n\nMerging K sorted lists: push the first element of each list onto a min-heap with its list index. Each extraction gives the global minimum; then push the next element from that same list. This is O(n log K) where n is total elements — the heap always has at most K elements, one per list. This pattern appears in backend systems when merging paginated results from multiple shards.',
    },
    {
      id: 'dsa-sorting',
      title: 'Sorting and searching — merge sort, quicksort, and when to use each',
      content:
        'While JavaScript\'s `Array.prototype.sort` handles most needs, understanding sorting algorithm internals is tested in interviews and relevant when implementing custom comparison logic at scale.\n\nMerge sort is stable (preserves relative order of equal elements), O(n log n) worst case, and O(n) extra space. It works by dividing the array in half recursively until single elements, then merging sorted halves:\n\n```typescript\nfunction mergeSort(arr: number[]): number[] {\n  if (arr.length <= 1) return arr;\n  const mid = Math.floor(arr.length / 2);\n  const left = mergeSort(arr.slice(0, mid));\n  const right = mergeSort(arr.slice(mid));\n  return merge(left, right);\n}\n\nfunction merge(left: number[], right: number[]): number[] {\n  const result: number[] = [];\n  let i = 0, j = 0;\n  while (i < left.length && j < right.length) {\n    result.push(left[i] <= right[j] ? left[i++] : right[j++]);\n  }\n  return result.concat(left.slice(i)).concat(right.slice(j));\n}\n```\n\nQuicksort partitions the array around a pivot, placing all smaller elements to the left and larger to the right, then recursively sorts both sides. Average O(n log n) but O(n²) worst case (sorted input with a naive pivot choice). In-place with O(log n) stack space on average:\n\n```typescript\nfunction partition(arr: number[], lo: number, hi: number): number {\n  const pivot = arr[hi];\n  let i = lo - 1;\n  for (let j = lo; j < hi; j++) {\n    if (arr[j] <= pivot) { i++; [arr[i], arr[j]] = [arr[j], arr[i]]; }\n  }\n  [arr[i + 1], arr[hi]] = [arr[hi], arr[i + 1]];\n  return i + 1;\n}\n```\n\nCounting sort achieves O(n + k) where k is the range of values, beating comparison sorts when k is small (e.g. sorting scores 0-100, ages, or enum values). It counts occurrences in an array indexed by value, then reconstructs the sorted output from the counts. It is the foundation of radix sort, which sorts multi-digit numbers digit by digit using counting sort as a stable subroutine.\n\nThe practical interview question: "Which sorting algorithm would you use for a list of objects by a date field?" Answer: a stable sort (merge sort or TimSort, which is what V8 uses). Stability matters whenever you sort by multiple fields sequentially — sort by name first, then by date — so equal dates preserve the name ordering.',
    },
    {
      id: 'dsa-backtracking',
      title: 'Backtracking — permutations, combinations, and pruning',
      content:
        'Backtracking is a systematic way to explore all possible solutions to a problem by building candidates incrementally and abandoning ("backtracking") a candidate as soon as you determine it cannot lead to a valid solution. It is the right tool for problems that ask you to enumerate all valid arrangements, paths, or selections.\n\nThe template is always the same:\n\n```typescript\nfunction backtrack(state: State, result: State[], choices: Choice[]) {\n  if (isSolution(state)) {\n    result.push([...state]); // copy, not reference\n    return;\n  }\n  for (const choice of choices) {\n    if (!isValid(state, choice)) continue; // prune\n    makeChoice(state, choice);\n    backtrack(state, result, remaining(choices, choice));\n    undoChoice(state, choice); // backtrack\n  }\n}\n```\n\n**Permutations** (all orderings of n elements) — every element can appear in every position:\n\n```typescript\nfunction permutations(nums: number[]): number[][] {\n  const result: number[][] = [];\n  function bt(current: number[], remaining: number[]) {\n    if (remaining.length === 0) { result.push([...current]); return; }\n    for (let i = 0; i < remaining.length; i++) {\n      current.push(remaining[i]);\n      bt(current, [...remaining.slice(0, i), ...remaining.slice(i + 1)]);\n      current.pop();\n    }\n  }\n  bt([], nums);\n  return result;\n}\n// O(n * n!) time — there are n! permutations, each takes O(n) to copy\n```\n\n**Combinations** (choose k from n, order does not matter) — use a `start` index to avoid revisiting earlier elements:\n\n```typescript\nfunction combinations(n: number, k: number): number[][] {\n  const result: number[][] = [];\n  function bt(start: number, current: number[]) {\n    if (current.length === k) { result.push([...current]); return; }\n    for (let i = start; i <= n; i++) {\n      current.push(i);\n      bt(i + 1, current);\n      current.pop();\n    }\n  }\n  bt(1, []);\n  return result;\n}\n```\n\n**Pruning** is the key optimisation. In the combinations example above, you can prune early: if `n - i + 1 < k - current.length`, there are not enough remaining numbers to complete a valid combination, so stop the loop:\n\n```typescript\nfor (let i = start; i <= n - (k - current.length) + 1; i++) {\n```\n\nThis single change can cut the search space dramatically for large n and small k.\n\nCommon backtracking problems: subsets, combinations summing to a target (coin change variants), N-Queens, Sudoku solver, and word search on a grid. The grid search variant replaces the explicit `remaining` array with a visited matrix — mark a cell visited before recursing and unmark it after.\n\nIn an interview, identify a backtracking problem by the phrase "find all" or "enumerate all." State the time complexity in terms of the output size — backtracking is rarely better than O(output * work per solution), and that is expected for enumeration problems.',
    },
    {
      id: 'dsa-tries',
      title: 'Tries — insert, search, and autocomplete',
      content:
        'A trie (also called a prefix tree) is a tree data structure where each node represents a character and paths from root to leaves spell out stored strings. It is the natural data structure for prefix-based operations: autocomplete, spell-checking, and IP routing tables.\n\nWhere a hash map answers "does this exact string exist?", a trie answers "give me all strings that start with \'pre\'" — efficiently, without scanning every stored string.\n\n```typescript\nclass TrieNode {\n  children: Map<string, TrieNode> = new Map();\n  isEnd = false;\n}\n\nclass Trie {\n  private root = new TrieNode();\n\n  insert(word: string): void {\n    let node = this.root;\n    for (const ch of word) {\n      if (!node.children.has(ch)) node.children.set(ch, new TrieNode());\n      node = node.children.get(ch)!;\n    }\n    node.isEnd = true;\n  }\n\n  search(word: string): boolean {\n    let node = this.root;\n    for (const ch of word) {\n      if (!node.children.has(ch)) return false;\n      node = node.children.get(ch)!;\n    }\n    return node.isEnd;\n  }\n\n  startsWith(prefix: string): boolean {\n    let node = this.root;\n    for (const ch of prefix) {\n      if (!node.children.has(ch)) return false;\n      node = node.children.get(ch)!;\n    }\n    return true;\n  }\n}\n```\n\nInsert, search, and startsWith all run in O(L) where L is the length of the string — independent of the number of stored words.\n\n**Autocomplete** collects all words under a prefix node:\n\n```typescript\ngetWordsWithPrefix(prefix: string): string[] {\n  let node = this.root;\n  for (const ch of prefix) {\n    if (!node.children.has(ch)) return [];\n    node = node.children.get(ch)!;\n  }\n  const results: string[] = [];\n  const dfs = (n: TrieNode, current: string) => {\n    if (n.isEnd) results.push(current);\n    for (const [ch, child] of n.children) dfs(child, current + ch);\n  };\n  dfs(node, prefix);\n  return results;\n}\n```\n\nFor ranked autocomplete (return the top-K most frequent suggestions), store a frequency count at each end node and use a max-heap during collection.\n\nThe memory trade-off: a trie storing 100,000 words uses more memory than a hash set of the same words — each character is a separate node with a map of children. Compressed tries (Patricia trees) merge single-child nodes into edge labels to reduce memory, which is how IP routing tables are stored in practice.\n\nIn an interview, reach for a trie whenever the problem involves prefixes or character-by-character processing of multiple strings. Common problems: longest common prefix, word search II, replace words with shortest root in dictionary.',
    },
    {
      id: 'dsa-bit-manipulation',
      title: 'Bit manipulation — XOR tricks, counting set bits, and finding the unique number',
      content:
        'Bit manipulation is a class of O(1) or O(n) solutions that operate directly on the binary representation of integers. It appears in backend interview questions around space-efficient algorithms, cryptography, and network programming.\n\n**XOR properties** are the foundation of the most elegant bit tricks:\n- `a ^ a = 0` — XOR of a number with itself is zero\n- `a ^ 0 = a` — XOR with zero is identity\n- XOR is commutative and associative\n\n**Single non-duplicate**: given an array where every element appears twice except one, find the unique element. XOR all elements — the pairs cancel to zero, leaving the single unique element:\n\n```typescript\nfunction singleNumber(nums: number[]): number {\n  return nums.reduce((acc, n) => acc ^ n, 0);\n}\n// O(n) time, O(1) space — no hash map needed\n```\n\n**Two non-duplicates**: if two numbers appear once and all others appear twice, XOR all elements to get `a ^ b`. Find any set bit in `a ^ b` (use `diff & -diff` to isolate the lowest set bit). Partition all numbers into two groups by that bit — each group XORs to one of the unique numbers.\n\n**Counting set bits (Hamming weight)**: count the number of 1 bits in an integer:\n\n```typescript\nfunction hammingWeight(n: number): number {\n  let count = 0;\n  while (n !== 0) {\n    n &= n - 1; // clears the lowest set bit\n    count++;\n  }\n  return count;\n}\n// n & (n-1) removes the rightmost set bit in one operation\n// Loop runs exactly k times where k is the number of set bits\n```\n\n**Common bit operations**:\n```typescript\nconst getBit = (n: number, i: number) => (n >> i) & 1;    // get bit i\nconst setBit = (n: number, i: number) => n | (1 << i);     // set bit i to 1\nconst clearBit = (n: number, i: number) => n & ~(1 << i);  // set bit i to 0\nconst toggleBit = (n: number, i: number) => n ^ (1 << i);  // flip bit i\nconst isPowerOfTwo = (n: number) => n > 0 && (n & (n - 1)) === 0;\n```\n\n**Checking power of two** is a classic example: a power of two has exactly one set bit. `n & (n-1)` clears the lowest set bit — if the result is zero, only one bit was set.\n\n**Bit masking** in backend systems: permission systems store role flags as bit fields. `user.permissions & PERMISSION.WRITE` checks if the write bit is set. This is how Unix file permissions (`rwxr-xr--`) and many REST API permission models work internally.\n\nIn an interview, bit manipulation problems are recognisable by the phrase "O(1) extra space" combined with duplicate-finding or integer-manipulation. The XOR trick for single non-duplicate is worth knowing by heart — it appears in about 20% of bit manipulation questions.',
    },
  ],
};
