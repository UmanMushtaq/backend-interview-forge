import type { LearnModule } from '../../types';

export const systemDesign: LearnModule = {
  id: 'system-design',
  title: 'System Design & Architecture',
  blurb: 'Building blocks, scaling, and the patterns interviewers probe.',
  quizCategory: 'system-design',
  lessons: [
    {
      id: 'sd-building-blocks',
      title: 'Building blocks and scaling',
      content:
        'Most systems combine a handful of blocks: a load balancer spreading traffic across stateless app instances, a cache (Redis) for hot reads, a database for the source of truth, a message broker for async work, and a CDN for static assets. Scale stateless tiers horizontally by adding instances; scale data with read replicas, partitioning/sharding, and caching. Always name the bottleneck first (usually the database) and design around it. CAP reminds you that during a network partition you trade consistency for availability per operation. In an interview, establish requirements and estimate the order-of-magnitude load (requests per second, data volume) before jumping to solutions — this narrows the design space quickly. A system handling 1k RPS with a single-digit GB dataset needs a very different architecture than one handling 100k RPS with petabyte-scale storage, and naming that difference early shows senior thinking.',
    },
    {
      id: 'sd-cap-consistency',
      title: 'CAP theorem and consistency models',
      content:
        'The CAP theorem states that a distributed system can guarantee at most two of Consistency, Availability, and Partition tolerance; since network partitions are unavoidable in practice, you are really choosing between CP (reject requests to stay consistent) and AP (serve stale data to stay available). In reality the choice is per-operation: a checkout flow might be CP (never double-charge) while a product catalogue view is AP (briefly stale is acceptable). PACELC extends CAP by acknowledging that even without a partition you trade latency for consistency: quorum writes (waiting for multiple replicas) reduce latency variance but increase average write latency. Strong consistency (linearizability) means every read sees the most recent write; eventual consistency means replicas converge given no new writes; read-your-own-writes consistency is a useful middle ground where a user always sees their own changes. Choose the consistency level per use-case and document it explicitly, because mismatched expectations between teams are a common source of production bugs.',
    },
    {
      id: 'sd-ddd-hexagonal',
      title: 'DDD layers and hexagonal architecture',
      content:
        'Domain-Driven Design organises code into four layers: the domain layer holds entities, value objects, aggregates, and domain services with zero framework dependencies; the application layer orchestrates use cases using domain objects and calls ports; the infrastructure layer provides adapters that implement those ports (HTTP controllers, database repositories, message publishers); and the interfaces layer handles serialisation and transport concerns. Hexagonal architecture (ports and adapters) formalises this by defining the domain as a hexagon with named ports on its boundary, each port implemented by a swappable adapter. This means you can test the entire domain and application layer with in-memory adapters, then plug in real database and broker adapters for integration tests. In interviews, this pattern answers "how do you keep your business logic testable as the system grows?" — the answer is to make all I/O cross a boundary defined by the domain, never call infrastructure directly from domain objects.',
    },
    {
      id: 'sd-microservices-monolith',
      title: 'Microservices vs monolith and database-per-service',
      content:
        'A modular monolith has a single deployable unit with well-defined internal module boundaries; it is easier to develop, test, debug, and operate than microservices, and should be the default for a new product or small team. Extract a service only when a specific module has a scaling requirement, release cadence, or technology choice that genuinely differs from the rest. Microservices buy independent deployability and fault isolation at the cost of network latency, distributed tracing overhead, and the loss of ACID transactions across services. The database-per-service pattern enforces this boundary: each service owns its schema, preventing the tight coupling that develops when services share tables. The downside is that cross-service queries require API composition (gateway aggregates multiple service calls) or a read-side projection (one service subscribes to events from another and maintains a local denormalised view). In interviews, always acknowledge that "microservices" is not a free upgrade — articulate the operational maturity and team size that justifies the cost.',
    },
    {
      id: 'sd-cqrs-event-sourcing',
      title: 'CQRS and event sourcing',
      content:
        'CQRS (Command Query Responsibility Segregation) splits the write model (commands that mutate state) from the read model (queries that return data). The write side can be a normalised relational store optimised for integrity; the read side can be a denormalised projection in Redis, Elasticsearch, or a read replica, optimised for specific query patterns. Updates flow from write to read asynchronously, so read models are eventually consistent. Event sourcing takes this further: instead of storing current state, the system stores an immutable append-only log of domain events ("OrderPlaced", "PaymentConfirmed", "OrderShipped"). Current state is derived by replaying events, and any read projection can be rebuilt from the log at any time. This gives a complete audit trail, time-travel debugging, and the ability to add new projections retroactively. The trade-off is query complexity (you cannot simply SELECT * from an events table to get current state) and the need to handle event schema evolution carefully using techniques like upcasting.',
    },
    {
      id: 'sd-resilience-caching-outbox',
      title: 'Circuit breakers, caching strategies, idempotency, and the outbox',
      content:
        'A circuit breaker wraps calls to a downstream dependency and trips to an open state after a threshold of failures, returning an error immediately instead of letting threads queue up waiting for a slow service. After a timeout it enters half-open state and allows one probe request; on success it resets to closed. This prevents cascading failures: if the payment service is down, the order service fails fast instead of exhausting its thread pool. Cache-aside (lazy loading) reads from cache and populates on miss; write-through writes to cache and DB together on every write, keeping them in sync at the cost of higher write latency. Write-behind (write-back) writes to cache immediately and flushes to DB asynchronously, giving lower write latency but risking data loss on cache failure. Idempotency keys on mutating API endpoints let clients safely retry: the server stores the key with the result and returns the cached result on duplicate requests. The outbox pattern solves dual-write between a database and a message broker: write the event to an outbox table in the same transaction as the business change, then a relay (or CDC tool like Debezium) publishes committed outbox rows to the broker, guaranteeing exactly-once publish relative to the database transaction.',
    },
    {
      id: 'sd-load-balancing',
      title: 'Load balancing algorithms — round robin, least connections, and consistent hashing',
      content:
        'A load balancer distributes incoming requests across multiple backend servers. The algorithm it uses determines which server receives each request — and the wrong choice can leave some servers idle while others are overloaded.\n\n**Round robin** is the simplest: request 1 goes to server A, request 2 to server B, request 3 to server C, request 4 back to server A. It works well when every request costs roughly the same amount of work. But a single slow database query can cause one request to hold a server connection for 10 seconds while round robin keeps sending it more requests.\n\n**Weighted round robin** assigns each server a weight proportional to its capacity. A server with 8 CPUs gets twice the traffic of a server with 4 CPUs. This handles heterogeneous hardware but still does not account for current server load.\n\n**Least connections** routes each new request to the server with the fewest active connections. This naturally handles variable-cost requests: a server holding a long-running query gets fewer new requests until it finishes. It is the default choice for HTTP/2 or WebSocket workloads where connections stay open.\n\n**IP hash** routes requests from the same client IP to the same server. This provides sticky sessions without a shared session store. The downside is poor distribution if clients cluster behind a NAT gateway.\n\n**Consistent hashing** solves a different problem: distributing keys (cache keys, user IDs, tenant IDs) across a ring of nodes such that when a node is added or removed, only ~1/N of keys need to be remapped. Standard hashing would remap all keys on a node change, causing a thundering herd of cache misses. Consistent hashing places nodes at random points on a 0-2³²−1 ring, and each key maps to the next node clockwise from its hash. Virtual nodes (vnodes) give each physical node multiple ring positions, improving the evenness of distribution. Consistent hashing is used in Redis Cluster, Cassandra, DynamoDB, and CDN edge routing.\n\nIn an interview, the question "how does Redis Cluster route requests without a broker?" is answered with consistent hashing over 16384 hash slots. The question "how do you handle sticky sessions?" is answered with either IP hash or a shared session store (Redis) — and you should explain why the shared store is more robust.',
    },
    {
      id: 'sd-sharding',
      title: 'Database read replicas and sharding strategies',
      content:
        'A single PostgreSQL primary handles both reads and writes. As traffic grows, reads become the first bottleneck — writes are serialised but reads can be parallelised. The first scaling move is almost always to add **read replicas**.\n\nA read replica is a standby server that streams WAL from the primary and applies it continuously. Your application sends all writes to the primary and routes read queries to one or more replicas. TypeORM and Prisma both support this with a replication configuration that splits reads and writes automatically.\n\nRead replicas have a small lag — typically under 100ms in the same data centre. For most read queries this is acceptable. But for reads that must see their own writes (user just created an account, immediately redirect to their dashboard), route to the primary or wait for replication to catch up using `pg_last_wal_replay_lsn()`.\n\nWhen the write volume itself outgrows one server, you need **sharding** — horizontally partitioning data across multiple primary databases. Each shard owns a range of keys.\n\nThe shard key determines the distribution. Common choices:\n- **User ID**: all data for a user lives on the same shard, making per-user queries local\n- **Tenant ID**: all data for a tenant lives on one shard (natural for multi-tenant SaaS)\n- **Geographic region**: EU data stays in EU, US data in US (regulatory compliance)\n\nThe challenge with sharding is cross-shard queries. A report that aggregates across all users must scatter the query to every shard and gather the results in a fan-out pattern. This is expensive and slow. For this reason, most teams build a separate analytics database (Redshift, BigQuery, Snowflake) that aggregates data from all shards for reporting, rather than trying to run analytical queries on the sharded OLTP database.\n\nManaging shards requires a shard map — a central lookup that says "user 1-10000 is on shard 1, user 10001-20000 is on shard 2". Shard rebalancing (when a shard grows too large) is painful without built-in support. Citus (a PostgreSQL extension) and PlanetScale handle this automatically. CockroachDB and Spanner provide automatic sharding with distributed ACID transactions.\n\nIn an interview, the expected answer for "how would you scale your database past what one machine can handle?" is: add read replicas first, then evaluate whether sharding or a different data model (denormalisation, caching) solves the write bottleneck before committing to the operational complexity of sharding.',
    },
    {
      id: 'sd-cache-stampede',
      title: 'Cache stampede and multi-layer caching',
      content:
        'A **cache stampede** (also called a thundering herd) happens when a popular cache key expires and many concurrent requests all find a miss at the same instant. Every request falls through to the database simultaneously, flooding it with identical queries — the exact scenario the cache was meant to prevent.\n\nThere are three ways to prevent it.\n\n**Locking**: when a miss occurs, only one worker acquires a per-key lock and refreshes the cache. Other workers wait behind the lock and read the refreshed value. In Redis, implement this with `SET lock:key 1 NX EX 5` — only the request that succeeds in setting the lock does the database query. The rest wait with a short sleep and retry.\n\n**Probabilistic early expiry** (also called XFetch): before the TTL expires, each request has a small probability of refreshing the cache early. As the key approaches expiry, the probability increases, so the cache is refreshed by a single lucky request before the expiry wave hits. No coordination required.\n\n**Background refresh**: the cache key never expires. A background job periodically re-fetches the value and updates the cache. Requests always get a cache hit, even if slightly stale. This works well for data that is expensive to compute and acceptable to be seconds or minutes stale (e.g. homepage hero data, trending product lists).\n\n**Multi-layer caching** stacks caches at different levels. A typical stack:\n1. **In-process cache** (a JavaScript `Map` or `lru-cache`): nanosecond latency, no network hop, but not shared across instances\n2. **Shared cache** (Redis): microsecond latency, shared across all instances, survives restarts\n3. **CDN** (Cloudflare, Fastly): millisecond latency globally, caches at the edge near the user\n4. **Database read replica**: the last resort before hitting the primary\n\nHot keys that every instance reads in every request are good candidates for in-process caching with a short TTL (a few seconds). This eliminates Redis RTT for the hottest data while still invalidating quickly enough that stale data does not persist long.\n\nIn an interview, cache stampede is a classic senior-level question. Show you know at least two mitigation strategies and can articulate the trade-offs between them.',
    },
    {
      id: 'sd-message-queues-backpressure',
      title: 'Message queues and backpressure',
      content:
        'A message queue decouples a producer from a consumer in time. The producer writes tasks to the queue at its own pace. The consumer reads and processes them at its own pace. When the producer is faster than the consumer, messages accumulate in the queue — this is normal and expected up to a point.\n\nBut what happens when the queue grows unboundedly? The broker runs out of memory or disk. The consumer falls further and further behind. When it finally catches up, it is processing events that are hours old — notifications about events that the user has forgotten.\n\n**Backpressure** is the mechanism by which a system signals overload upstream and slows the inflow. Without backpressure, a fast producer drowns a slow consumer.\n\nThere are several ways to implement backpressure.\n\n**Queue depth limits**: configure a maximum queue depth (e.g. `x-max-length: 10000` in RabbitMQ). When the limit is reached, the broker rejects new publishes or drops the oldest messages (depending on the overflow strategy). The producer receives an error and must slow down or buffer locally.\n\n**Consumer-driven pull**: instead of the broker pushing messages to consumers as fast as possible, consumers pull messages only when they are ready for more. Kafka works this way — consumers request messages by polling, so a slow consumer simply polls less frequently without any messages being lost.\n\n**Prefetch count** in RabbitMQ limits how many unacknowledged messages a consumer can hold. A `prefetchCount: 5` means a consumer only receives 5 messages at once. Once it acknowledges some, the broker sends more. This prevents a fast broker from flooding a slow consumer.\n\n**Rate limiting at the producer**: if backpressure signals reach the API layer (queue is full, broker returned an error), the API should return a 503 or 429 to the caller and surface the congestion. The caller must implement retry with exponential backoff.\n\nIn an interview, any system design involving async processing should acknowledge the possibility of consumer lag. Mention that you would monitor queue depth with alerts and have a plan for catching up — either scaling consumers horizontally, throttling producers, or shedding low-priority work.',
    },
    {
      id: 'sd-rate-limiting-distributed',
      title: 'Distributed rate limiting — token bucket vs sliding window',
      content:
        'Rate limiting controls how many requests a client can make in a time window. On a single server this is trivial — keep a counter in memory. But on a distributed system with multiple API gateway instances, each instance sees only a fraction of the traffic. A client making 100 requests per second can slip past per-instance limits if the limit is 20 RPS per instance with 10 instances.\n\nDistributed rate limiting requires all instances to share state — typically Redis.\n\nThe **token bucket** algorithm works like a bucket that fills with tokens at a constant rate (say, 10 tokens per second). Each request consumes one token. If the bucket is empty, the request is rejected. The bucket has a maximum capacity, which allows short bursts up to the cap. This is the algorithm used by most commercial APIs — it allows bursting while enforcing an average rate.\n\nImplementation in Redis using a Lua script (atomic read-modify-write):\n\n```lua\nlocal key = KEYS[1]\nlocal capacity = tonumber(ARGV[1])\nlocal refill_rate = tonumber(ARGV[2])  -- tokens per second\nlocal now = tonumber(ARGV[3])\nlocal data = redis.call(\'HMGET\', key, \'tokens\', \'last_refill\')\nlocal tokens = tonumber(data[1]) or capacity\nlocal last_refill = tonumber(data[2]) or now\nlocal elapsed = now - last_refill\ntokens = math.min(capacity, tokens + elapsed * refill_rate)\nif tokens >= 1 then\n  tokens = tokens - 1\n  redis.call(\'HMSET\', key, \'tokens\', tokens, \'last_refill\', now)\n  redis.call(\'EXPIRE\', key, 3600)\n  return 1  -- allowed\nend\nreturn 0  -- rejected\n```\n\nThe **sliding window counter** uses two fixed-window buckets and a weighted average to approximate a true sliding window with O(1) memory. It is accurate to within 0.1% and is the algorithm used by Cloudflare.\n\nFor fine-grained distributed limiting (per-user, per-tenant, per-endpoint), combine the Redis counter with the `X-RateLimit-Remaining` and `Retry-After` response headers so clients can back off gracefully before being rejected.\n\nIn an interview, explain the difference between the fixed window (boundary burst problem), the sliding window log (accurate but O(requests) memory), and the token bucket or sliding window counter (memory-efficient, practical). Name the algorithm used by a real system you know — this signals hands-on experience.',
    },
    {
      id: 'sd-rest-graphql-grpc',
      title: 'REST vs GraphQL vs gRPC — trade-offs and when to choose',
      content:
        'Choosing an API protocol is one of the most consequential early decisions in a new service. It determines developer experience, performance characteristics, and tooling.\n\n**REST** is the safe default. It uses HTTP verbs (GET, POST, PUT, DELETE) and URLs to represent resources. It is universally understood, works in any browser, and has excellent tooling (Swagger, Postman, curl). The weakness is over-fetching (the endpoint returns more fields than needed) and under-fetching (a mobile app needs data from three REST endpoints, so it makes three requests).\n\n**GraphQL** addresses over- and under-fetching by letting the client specify exactly which fields it needs in a single query. One request can fetch an order with its items, the user\'s name, and the shipping address — all joined on the server. This is powerful for frontend teams that own their queries. The weakness is complexity: query parsing and validation add overhead on every request, the N+1 problem requires DataLoader, schema design requires careful thought about graph relationships, and caching is harder because every query can be unique.\n\n**gRPC** uses Protocol Buffers (protobuf) for schema definition and binary serialisation. It is 5-10x more compact than JSON, uses HTTP/2 for multiplexed streaming, and generates type-safe client and server stubs in any language from the same `.proto` file. It is the right choice for high-throughput internal service-to-service communication where latency and bandwidth matter. The weakness is that browsers cannot use gRPC directly without a gRPC-web proxy, and debugging binary messages is harder than inspecting JSON with curl.\n\nA common pattern in mature systems: gRPC between internal microservices for performance, REST for public APIs for compatibility, and GraphQL for the API consumed by the web and mobile frontends.\n\nIn an interview, the question "what API protocol would you use?" is answered by first asking about the client. "Who is the consumer?" If it is a browser, REST or GraphQL. If it is another microservice, gRPC. If it is a third-party developer, REST with an OpenAPI spec. Demonstrating that the answer depends on context shows architectural judgement.',
    },
    {
      id: 'sd-pagination',
      title: 'Cursor-based vs offset pagination',
      content:
        'Every API that returns lists needs pagination. There are two main approaches, and choosing the wrong one causes subtle bugs in production.\n\n**Offset pagination** is the intuitive approach: `GET /orders?page=3&limit=20`. The server runs `SELECT ... LIMIT 20 OFFSET 60`. It is simple to implement and easy for clients to understand. The page number maps directly to an offset.\n\nBut it has two serious problems.\n\nFirst, as the offset grows, PostgreSQL must scan and discard all preceding rows before returning the 20 you want. `OFFSET 100000 LIMIT 20` is slow — the database does 100,000 units of work to return 20 rows.\n\nSecond, if a new row is inserted while a user is paginating, rows shift. The user sees a duplicate on the next page (if a row was inserted before the current page boundary) or skips a row (if a row was deleted). This is the "moving page" bug.\n\n**Cursor-based pagination** solves both problems. Instead of an offset, the client sends a cursor — an opaque token encoding the position in the result set. Typically the cursor is the ID (or a base64-encoded composite of ID + timestamp) of the last item on the current page.\n\n```sql\n-- First page\nSELECT * FROM orders WHERE created_at < NOW() ORDER BY created_at DESC LIMIT 20;\n\n-- Next page (cursor is the created_at of the last item returned)\nSELECT * FROM orders WHERE created_at < \'2024-01-15T10:00:00Z\' ORDER BY created_at DESC LIMIT 20;\n```\n\nThis is a **range scan** on an indexed column — fast regardless of how deep into the result set you are. And because the cursor is a fixed point in time, new inserts and deletes do not shift pages.\n\nThe trade-off: you can only go forward (or backward if you store both forward and backward cursors). You cannot jump to "page 50". Cursor pagination is the right choice for infinite scroll UIs and any API consumed by a backend service. Offset pagination is still acceptable for admin dashboards where users genuinely need to jump to a specific page.\n\nIn an interview, cursor pagination is expected knowledge for any senior backend role. Always mention the index requirement — the cursor column must be indexed and the ORDER BY must match it.',
    },
    {
      id: 'sd-retries-backoff',
      title: 'Retries with exponential backoff and jitter',
      content:
        'Network failures are inevitable. A well-designed system retries failed requests — but naive retries make failure worse, not better.\n\nImagine a downstream service goes down for 30 seconds. 1,000 clients are waiting. The service comes back up. All 1,000 clients retry simultaneously, overwhelming the recovering service and taking it down again. This is the **retry storm**.\n\n**Exponential backoff** spreads retries over time. After the first failure, wait 1 second. After the second failure, wait 2 seconds. After the third, 4 seconds. The formula is `wait = base * 2^attempt`. This gives the downstream service time to recover and reduces the load each retry generates.\n\nBut exponential backoff alone does not solve the retry storm. If all 1,000 clients failed at the same time, they all wait exactly 1 second, then retry simultaneously. The timing is still synchronised.\n\n**Jitter** fixes this by adding randomness to the wait time. The "full jitter" formula is `wait = random(0, base * 2^attempt)`. Each client picks a random wait time within the exponential window, spreading retries across the full interval.\n\nAWS recommends the "decorrelated jitter" algorithm:\n\n```typescript\nfunction retryDelay(attempt: number, base = 100): number {\n  const cap = 30_000; // 30 seconds max\n  const sleep = Math.min(cap, base * Math.pow(2, attempt));\n  return Math.random() * sleep; // full jitter\n}\n```\n\nCombine this with a **retry budget**: a maximum number of retries (typically 3) and a maximum total elapsed time. After the budget is exhausted, fail permanently and surface the error to the caller.\n\nNot all errors should be retried. Retry on transient errors (network timeout, 503, 429 with Retry-After). Do not retry on permanent errors (400, 401, 404) — retrying them wastes resources and changes nothing.\n\nIn an interview, always mention idempotency alongside retries. Retrying a non-idempotent request (e.g. POST /payments) can cause duplicate charges. Ensure that retried requests include an idempotency key so the server deduplicates them.',
    },
    {
      id: 'sd-observability',
      title: 'The three pillars of observability — logs, metrics, and traces',
      content:
        'A system you cannot observe is a system you cannot operate safely. The three pillars of observability — logs, metrics, and traces — each answer a different question about your running system.\n\n**Logs** answer "what happened?" They are timestamped records of discrete events: a request received, a payment charged, an error thrown. Use structured logging (JSON) so logs are machine-parseable:\n\n```typescript\nlogger.info({ userId, orderId, amount }, \'Payment processed\');\n```\n\nLog aggregators (Datadog, Loki, Splunk, ELK) collect logs from all instances and let you search and filter. Always include a correlation ID or request ID in every log line so you can filter to all events from a single request.\n\n**Metrics** answer "how is the system performing right now?" They are numeric measurements sampled over time: request rate (RPS), error rate, p50/p95/p99 latency, database connection pool usage, queue depth. Metrics are cheap to store and fast to query because they are just numbers. Prometheus scrapes metrics exposed by your service at `/metrics`, and Grafana visualises them as dashboards and sends alerts when thresholds are breached.\n\nThe four golden signals (from Google\'s SRE book) are the minimum set to monitor:\n1. **Latency** — how long requests take (distinguish successful vs failed)\n2. **Traffic** — how many requests per second\n3. **Errors** — what fraction of requests are failing\n4. **Saturation** — how full the system is (CPU, memory, queue depth)\n\n**Traces** answer "why is this specific request slow?" A trace captures a single request\'s journey through every service, database call, and cache lookup, with precise start/end times for each span. OpenTelemetry is the vendor-neutral standard for instrumenting services. Jaeger or Tempo stores and queries traces.\n\nLogs and metrics alone leave a gap: you can see that p99 latency spiked to 5 seconds, but you cannot tell whether the slow path was the database query, the Redis call, or an HTTP call to a downstream service. Traces close that gap.\n\nIn an interview, describe all three pillars and give concrete examples of the question each one answers. Then mention that the three are converging — modern observability platforms like Datadog and Grafana Cloud correlate them: click on a spike in a latency metric, see the relevant log lines, click through to the trace.',
    },
    {
      id: 'sd-slo-sla',
      title: 'SLI, SLO, SLA, and error budgets',
      content:
        'Reliability is not binary — a system is not either "up" or "down". It is available some percentage of the time, with some distribution of latencies. Service Level Indicators, Objectives, and Agreements give teams a precise language for reasoning about reliability.\n\nA **Service Level Indicator (SLI)** is a specific metric that measures reliability. For a web API, the most important SLI is the ratio of successful requests (HTTP 2xx or 3xx) to total requests:\n\n```\nSLI = (successful requests / total requests) * 100\n```\n\nOther common SLIs: p99 latency below 200ms, availability of a checkout endpoint, or freshness of a data pipeline.\n\nA **Service Level Objective (SLO)** is the target value for an SLI over a rolling window: "99.9% of requests over the past 28 days should succeed." SLOs are internal targets that engineering teams commit to.\n\nA **Service Level Agreement (SLA)** is the contractual commitment made to customers, typically with financial penalties for breaches. SLAs are set conservatively below SLOs — if your SLO is 99.9%, your SLA might be 99.5%, so you have a buffer.\n\nAn **error budget** is the inverse of the SLO: if your SLO is 99.9%, you have a 0.1% budget for failures per month. 0.1% of 28 days is about 43 minutes of downtime. The error budget is a shared resource between product teams (who want to ship faster, introducing risk) and reliability teams (who want to keep things stable). When the budget is flush, shipping is prioritised. When the budget is nearly exhausted, new deployments are frozen until it refills.\n\nThis framework, from Google\'s Site Reliability Engineering book, aligns business goals (reliability as a feature customers pay for) with engineering goals (the budget makes risk quantitative and negotiable).\n\nIn an interview, mention the 9s: 99% (1.7 days/year downtime), 99.9% (8.7 hours), 99.99% (52 minutes), 99.999% (5 minutes). Each additional 9 is exponentially more expensive to achieve. Demonstrate that you know 99.99% availability for a payment API may be justified, while 99.9% for an internal admin dashboard is perfectly acceptable.',
    },
    {
      id: 'sd-url-shortener',
      title: 'System design walkthrough — URL shortener',
      content:
        'A URL shortener (like bit.ly) is one of the most common system design interview questions because it touches caching, database design, hashing, and scalability in a compact problem.\n\n**Functional requirements**: given a long URL, generate a short code. Given a short code, redirect to the original URL. Links may expire.\n\n**Non-functional requirements**: the read-to-write ratio is roughly 100:1 (every link is created once but clicked many times). At 1,000 new links per second and 100,000 redirects per second, durability and low redirect latency are the key constraints.\n\n**Short code generation**: a 7-character base-62 code (a-z, A-Z, 0-9) gives 62^7 ≈ 3.5 trillion unique codes — enough for any foreseeable scale. Three strategies:\n\n1. **Hash and truncate**: MD5 or SHA-256 the long URL, take the first 7 characters. Collisions are rare but must be handled (retry with different characters).\n2. **Counter + base-62 encode**: an atomic counter in the database (or Redis INCR) gives a unique integer, encoded to base-62. Predictable, collision-free, but reveals insertion order.\n3. **Pre-generated codes**: a worker pre-generates millions of random codes and stores them in a pool table. Shortening picks one from the pool. No collision risk at write time.\n\n**Storage**: a simple schema — `short_code VARCHAR(7) PK, long_url TEXT, created_at TIMESTAMP, expires_at TIMESTAMP`. A unique index on `long_url` lets you return the existing code if the same URL is shortened twice.\n\n**Redirect flow**: client requests `GET /abc1234` → look up in Redis cache (first) → if miss, query PostgreSQL → store in Redis with a TTL matching the link expiry → return 301 (permanent, browser caches) or 302 (temporary, server decides on every click).\n\nUse 302 if you want to count clicks accurately — a 301 is cached by the browser and subsequent clicks never reach your server. Use 301 to reduce server load if analytics are not needed.\n\n**Scaling to 100k RPS**: Redis handles redirects with sub-millisecond latency. A single Redis instance handles ~100k GET/s. The database only sees cache misses — perhaps 1% of traffic = 1k RPS, well within a single PostgreSQL primary. The API layer is stateless and scales horizontally behind a load balancer.\n\nIn an interview, work through requirements → estimation → data model → read path → write path → scaling → failure modes. State your assumptions explicitly and invite the interviewer to adjust them.',
    },
    {
      id: 'sd-notification-system',
      title: 'System design walkthrough — notification system',
      content:
        'A notification system sends messages across multiple channels (push, email, SMS, in-app) to millions of users, with different priority levels and delivery guarantees.\n\n**Functional requirements**: trigger notifications from events in other services. Support push (iOS/Android), email, and SMS. Users can set preferences (opt out of marketing emails). High-priority notifications (security alerts, order confirmations) must not be dropped.\n\n**Core components**:\n\n**Notification service**: receives notification requests from other services (via a REST API or a message broker). Accepts a structured request: `{ userId, type, priority, payload }`.\n\n**User preference service**: stores per-user, per-channel, per-type settings. Before dispatching, query this service to filter to channels the user has enabled for this notification type.\n\n**Message queue**: decouple the notification service from the delivery workers. Use a priority queue — Kafka topics with separate high-priority and low-priority partitions, or RabbitMQ with separate queues. High-priority notifications (security alerts) use a queue with more consumers; low-priority (marketing) share a smaller pool.\n\n**Channel workers**: separate worker processes per channel — push worker, email worker, SMS worker. Each worker reads from its queue and calls the channel\'s third-party API: APNs/FCM for push, SendGrid/SES for email, Twilio for SMS. Workers retry with exponential backoff on transient failures. Failed notifications after max retries go to a dead-letter queue for alerting.\n\n**Rate limiting**: third-party APIs impose rate limits (SES limits per-second sends). Each channel worker enforces a per-channel rate limit using Redis token buckets.\n\n**Deduplication**: at-least-once delivery means a notification can be dispatched twice (network failure after delivery but before acknowledgement). Store the notification ID in a Redis set with a 24-hour TTL. Before sending, check if the ID is already in the set.\n\n**Scaling**: the stateless workers scale horizontally. At 10 million notifications per day, a single email worker at 100 emails/s handles that in ~27 hours — scale to 10 workers for near-real-time delivery. Push is faster (APNs/FCM accept batch sends of up to 1,000 tokens per request).\n\nIn an interview, the key decisions to defend are: why a queue between the API and workers (decoupling, retry, backpressure), why separate workers per channel (different rate limits, different failure modes), and why deduplication at the worker rather than the API (the API does not know about delivery outcomes).',
    },
    {
      id: 'sd-job-queue',
      title: 'System design walkthrough — distributed job queue',
      content:
        'A job queue runs background work asynchronously — sending emails, resizing images, generating reports, processing payments. Designing one from scratch covers reliability, exactly-once semantics, scheduling, and monitoring.\n\n**Functional requirements**: producers enqueue jobs with a type and payload. Workers pull and execute jobs. Jobs should be retried on failure with backoff. Support delayed execution (run at a future time). Jobs should not be lost on worker crash.\n\n**Storage layer options**:\n- **Redis sorted sets**: the score is the Unix timestamp when the job should run. Workers call `ZPOPMIN` (or `ZRANGEBYSCORE max=now + ZREM` atomically in Lua) to claim the next due job. BullMQ is a mature Node.js library built on this approach. The limitation is that Redis is in-memory — for very large job volumes or long retention, you need a larger Redis or a different backend.\n- **PostgreSQL**: store jobs in a `jobs` table with a `status` column (pending, running, completed, failed) and a `run_at` timestamp. Workers do `SELECT ... FOR UPDATE SKIP LOCKED LIMIT 1` — the `SKIP LOCKED` clause is the key: it skips rows already locked by another worker without blocking, enabling multiple workers to pull concurrently without contention.\n\n**Claim and heartbeat**: when a worker picks a job, it marks it `running` and records a `claimed_at` timestamp. A watchdog job scans for jobs that have been `running` for longer than the timeout (e.g. 5 minutes) — these workers likely crashed mid-job. The watchdog resets them to `pending` for retry. This is the at-least-once delivery guarantee.\n\n**Exactly-once semantics**: true exactly-once requires the job handler to be idempotent. Before performing the side effect, check an `executed_jobs` table for the job ID. If found, return success immediately. Otherwise perform the work and write the ID in the same transaction.\n\n**Delayed jobs**: with Redis sorted sets, set the score to the future Unix timestamp. With PostgreSQL, set `run_at` to the future time. Workers only pull jobs where `run_at <= NOW()`.\n\n**Dead-letter queue**: after N retries, move the job to a `failed_jobs` table with the error and stack trace. Alert on growing dead-letter queues — they signal a code bug or a degraded downstream.\n\n**Scaling**: workers are stateless and scale horizontally. With `SKIP LOCKED` in PostgreSQL, 100 workers can all pull concurrently without a lock contention bottleneck. Monitor queue depth (jobs waiting) and processing latency (time from enqueue to execution start) as the primary SLIs.\n\nIn an interview, the `SELECT FOR UPDATE SKIP LOCKED` pattern is the insight that separates candidates who have built real job queues from those who have not. Mention it by name.',
    },
  ],
};
