import type { LearnModule } from '../../types';

export const oop: LearnModule = {
  id: 'oop',
  title: 'Object-Oriented Programming',
  blurb: 'The four pillars, SOLID principles, and design patterns applied to real TypeScript and NestJS code.',
  lessons: [
    {
      id: 'oop-why',
      title: 'Why OOP - the problem it solves',
      content:
        'Picture a fintech backend where wallet balances are handled by a handful of standalone functions: `deductBalance(walletsById, id, amount)`, `addBalance(walletsById, id, amount)`, `getBalance(walletsById, id)`. Every function takes the same `walletsById` map as its first argument and mutates it directly. Nothing stops a controller from doing `walletsById[id].balance = 999999` instead of going through one of those functions. Nothing groups the balance, the currency, and the operations allowed on them into one place. As the file grows, every new feature - freezing a wallet, adding a transaction history, converting currencies - means touching the same scattered set of functions and hoping you did not forget one of the call sites.\n\nObject-Oriented Programming (OOP) gives you a practical way to structure software around real-world things like Users, Orders, Payments, and Notifications.\n\nInstead of scattering data across variables and wiring behavior through unrelated functions, you bundle state and behavior into self-contained units. That makes code easier to reason about, extend, test, and maintain as the project grows.\n\nHere is the same wallet modeled as a **class** in TypeScript:\n\n```typescript\nclass Wallet {\n  private balance: number;\n\n  constructor(private readonly currency: "EUR" | "USD", initialBalance = 0) {\n    this.balance = initialBalance;\n  }\n\n  deposit(amount: number): void {\n    if (amount <= 0) throw new Error("Deposit amount must be positive");\n    this.balance += amount;\n  }\n\n  withdraw(amount: number): void {\n    if (amount > this.balance) throw new Error("Insufficient funds");\n    this.balance -= amount;\n  }\n\n  getBalance(): number {\n    return this.balance;\n  }\n}\n\nconst wallet = new Wallet("EUR", 100);\nwallet.deposit(50);\nwallet.withdraw(30);\nconsole.log(wallet.getBalance()); // 120\n```\n\nThe balance now lives inside the `Wallet` itself, next to the only operations allowed to change it. There is no `walletsById` map to pass around, no risk of a controller reaching in and setting an invalid balance, and no ambiguity about where the deposit and withdrawal rules live. But OOP is not just about writing classes. It is about understanding a small set of core ideas that help you model complexity, control change, and avoid turning your codebase into a fragile mess. The rest of this course walks through those ideas one at a time, each grounded in TypeScript and NestJS code you will actually write.\n\nIn an interview, if asked why use OOP at all, do not recite "four pillars" from memory. Say that OOP groups related state and behavior so that invariants (like "balance can never go negative") are enforced in one place instead of hoped for at every call site, and that this becomes more valuable as a codebase grows past a handful of files.',
    },
    {
      id: 'oop-classes',
      title: 'Classes and objects in TypeScript',
      content:
        'A **class** is a blueprint. An object (or instance) is one concrete thing built from that blueprint. Every `new Wallet("EUR")` call produces a distinct object with its own balance, while the `Wallet` class defines the shape and behavior every wallet shares.\n\nA TypeScript class constructor runs once, when the object is created, and is the natural place to set up initial state:\n\n```typescript\nclass Invoice {\n  readonly id: string;\n  private status: "draft" | "sent" | "paid" = "draft";\n\n  constructor(id: string, public readonly amount: number) {\n    this.id = id;\n  }\n\n  send(): void {\n    if (this.status !== "draft") throw new Error("Invoice already sent");\n    this.status = "sent";\n  }\n\n  markPaid(): void {\n    this.status = "paid";\n  }\n\n  isPaid(): boolean {\n    return this.status === "paid";\n  }\n}\n```\n\nTypeScript\'s constructor parameter shorthand (`public readonly amount: number`) declares and assigns a property in one line, which is why NestJS constructors are usually short even though they take several dependencies.\n\nAccess modifiers control who can reach a property or method. Public is the default and is visible everywhere. Private is visible only inside the class itself - `invoice.status` from outside the class is a compile error. Protected is like private but also visible to subclasses. Readonly allows a property to be set once, typically in the constructor, and never reassigned after that. Static members (`static count = 0`) belong to the class itself rather than to any one instance, useful for shared counters or factory methods that do not need instance state.\n\nHere is a real NestJS service:\n\n```typescript\n@Injectable()\nexport class InvoicesService {\n  constructor(\n    private readonly invoicesRepository: InvoicesRepository,\n    private readonly notificationsService: NotificationsService,\n  ) {}\n\n  async issue(dto: CreateInvoiceDto): Promise<Invoice> {\n    const invoice = new Invoice(randomUUID(), dto.amount);\n    await this.invoicesRepository.save(invoice);\n    await this.notificationsService.notifyInvoiceIssued(invoice);\n    return invoice;\n  }\n}\n```\n\n`InvoicesService` is a class, exactly like `Invoice` and `Wallet` above. The `@Injectable()` decorator does not change that; it just tells NestJS this class can be constructed and handed to other classes automatically. The `private readonly` parameters are ordinary constructor property shorthand: `invoicesRepository` and `notificationsService` become private fields on the instance the moment the service is constructed. Every NestJS controller, service, guard, and pipe you write is a plain TypeScript class underneath the decorators.\n\nIn an interview, if asked to explain access modifiers, give the one-line version for each: public is open to everyone, private is locked to the declaring class, protected extends that lock to subclasses, and readonly stops reassignment after construction. Then mention that TypeScript\'s access modifiers are compile-time only - they disappear at runtime, unlike genuinely private fields (`#balance`) which are enforced by the JavaScript engine itself.',
    },
    {
      id: 'oop-interfaces',
      title: 'Interfaces - defining contracts',
      content:
        'Think about payment processing in an e-commerce app. You need to charge customers, but you do not want to be locked into a single payment provider. So you define a contract that says "any payment gateway must support charging and refunding," and then Stripe, PayPal, or any future provider can plug in.\n\nIn TypeScript, that contract is an **interface**:\n\n```typescript\ninterface PaymentGateway {\n  charge(amountCents: number, source: string): Promise<{ transactionId: string }>;\n  refund(transactionId: string, amountCents: number): Promise<void>;\n}\n```\n\n`StripeGateway` and `PaypalGateway` both implement it, each translating the same contract into their own SDK calls:\n\n```typescript\nclass StripeGateway implements PaymentGateway {\n  async charge(amountCents: number, source: string) {\n    const charge = await stripe.charges.create({ amount: amountCents, source, currency: "eur" });\n    return { transactionId: charge.id };\n  }\n\n  async refund(transactionId: string, amountCents: number) {\n    await stripe.refunds.create({ charge: transactionId, amount: amountCents });\n  }\n}\n\nclass PaypalGateway implements PaymentGateway {\n  async charge(amountCents: number, source: string) {\n    const order = await paypalClient.orders.create({ amount: amountCents, payerId: source });\n    return { transactionId: order.id };\n  }\n\n  async refund(transactionId: string, amountCents: number) {\n    await paypalClient.payments.refund(transactionId, amountCents);\n  }\n}\n```\n\nThe beauty of interfaces is that your checkout service can work with `PaymentGateway` without knowing whether it is talking to Stripe or PayPal. Swapping providers means changing one line of configuration, not rewriting your business logic:\n\n```typescript\n@Injectable()\nclass CheckoutService {\n  constructor(@Inject("PAYMENT_GATEWAY") private readonly gateway: PaymentGateway) {}\n\n  async checkout(amountCents: number, source: string) {\n    return this.gateway.charge(amountCents, source);\n  }\n}\n```\n\nThis is exactly how NestJS dependency injection works day to day. `@Injectable()` services almost never depend directly on a concrete class like `StripeGateway`; they depend on an interface (or an abstract class, since TypeScript interfaces do not exist at runtime and cannot be used as DI tokens on their own) and NestJS\'s module system decides which concrete implementation to hand over, based on the providers array:\n\n```typescript\n{ provide: "PAYMENT_GATEWAY", useClass: StripeGateway }\n```\n\nChange that one line to `useClass: PaypalGateway` and every service that depends on `PAYMENT_GATEWAY` starts talking to PayPal instead, with zero changes to their own code.\n\nIn an interview, be precise about what makes this valuable: interfaces let you program against a contract instead of a concrete implementation, so the caller and the implementation can change independently. Mention that TypeScript interfaces are erased at compile time, which is why NestJS DI uses string or symbol tokens (or abstract classes) to identify which implementation to inject, since you cannot ask for "give me something that implements PaymentGateway" at runtime.',
    },
    {
      id: 'oop-encapsulation',
      title: 'Encapsulation - hiding what should be hidden',
      content:
        'Consider a rate limiter. Other parts of your system only need to ask "can this user make another request?" They should not be able to directly mess with the internal counters or reset the time window.\n\nHere is what happens without **encapsulation**:\n\n```typescript\nclass RateLimiter {\n  requestCount = 0;\n  windowStart = Date.now();\n}\n\nconst limiter = new RateLimiter();\nlimiter.requestCount = -100; // nothing stops this\nlimiter.windowStart = 0; // or this\n```\n\nAnyone can set `requestCount = -100` or reset the window manually, putting the object into an invalid state. There is no single place that enforces "requestCount can never go negative" or "the window resets exactly every 60 seconds", because every field is exposed and every caller is free to touch it directly.\n\nWith encapsulation, the fields become private and the only way in is through methods that protect the invariants:\n\n```typescript\nclass RateLimiter {\n  private requestCount = 0;\n  private windowStart = Date.now();\n\n  constructor(private readonly limit: number, private readonly windowMs: number) {}\n\n  allowRequest(): boolean {\n    this.resetWindowIfExpired();\n    if (this.requestCount >= this.limit) return false;\n    this.requestCount++;\n    return true;\n  }\n\n  getRemainingRequests(): number {\n    this.resetWindowIfExpired();\n    return Math.max(0, this.limit - this.requestCount);\n  }\n\n  private resetWindowIfExpired(): void {\n    if (Date.now() - this.windowStart >= this.windowMs) {\n      this.requestCount = 0;\n      this.windowStart = Date.now();\n    }\n  }\n}\n```\n\nWith encapsulation, nobody can corrupt the internal state. The only way to interact with the limiter is through `allowRequest()` and `getRemainingRequests()`. If you later switch from a fixed window to a sliding window algorithm, none of the calling code needs to change - `requestCount` and `windowStart` can be replaced entirely with a different internal representation, and every controller that calls `allowRequest()` keeps working exactly as before.\n\nThis is also why NestJS services keep their repository fields private:\n\n```typescript\n@Injectable()\nexport class OrdersService {\n  constructor(private readonly ordersRepository: OrdersRepository) {}\n\n  async findById(id: string): Promise<Order> {\n    return this.ordersRepository.findOneOrFail({ where: { id } });\n  }\n}\n```\n\n`ordersRepository` is private so no controller or other service can reach into `OrdersService` and call the repository directly, skipping whatever validation or business rules `OrdersService` is supposed to enforce around order lookups. Every consumer of orders goes through `OrdersService`\'s public methods, which is the whole point.\n\nIn an interview, describe encapsulation as protecting invariants, not just "hiding fields". The private keyword by itself does nothing useful; what matters is that the public methods you expose are the only doors in, and each of those doors enforces a rule that keeps the object in a valid state.',
    },
    {
      id: 'oop-abstraction',
      title: 'Abstraction - hiding complexity',
      content:
        'Say your app needs to upload files, but the actual mechanics differ wildly between providers: S3 needs a bucket name, a region, and a signed request; Google Cloud Storage needs a project id and a different auth flow entirely. Callers should not need to know any of that. They should just call `upload(file)` and get a URL back.\n\n**Abstraction** means exposing only what the caller needs and hiding everything else behind it. TypeScript\'s abstract class is built for exactly this: it can hold shared logic while still forcing subclasses to fill in the provider-specific parts.\n\n```typescript\nabstract class CloudStorage {\n  abstract uploadRaw(file: Buffer, key: string): Promise<string>;\n\n  async upload(file: Buffer, filename: string): Promise<string> {\n    const key = `${Date.now()}-${filename}`;\n    this.validateSize(file);\n    return this.uploadRaw(file, key);\n  }\n\n  private validateSize(file: Buffer): void {\n    if (file.byteLength > 50 * 1024 * 1024) throw new Error("File exceeds 50MB limit");\n  }\n}\n\nclass S3Storage extends CloudStorage {\n  constructor(private readonly bucket: string) {\n    super();\n  }\n\n  async uploadRaw(file: Buffer, key: string): Promise<string> {\n    await s3Client.putObject({ Bucket: this.bucket, Key: key, Body: file });\n    return `https://${this.bucket}.s3.amazonaws.com/${key}`;\n  }\n}\n\nclass GcsStorage extends CloudStorage {\n  constructor(private readonly bucketName: string) {\n    super();\n  }\n\n  async uploadRaw(file: Buffer, key: string): Promise<string> {\n    const bucket = gcsClient.bucket(this.bucketName);\n    await bucket.file(key).save(file);\n    return `https://storage.googleapis.com/${this.bucketName}/${key}`;\n  }\n}\n```\n\n`upload()` is defined once, on the abstract base, and contains the validation logic every provider needs. `uploadRaw()` is declared but not implemented on the base class - abstract means TypeScript will refuse to compile `new CloudStorage()` directly, and every subclass is forced to provide its own `uploadRaw()`. A controller that receives a `CloudStorage` (whichever concrete subclass was injected) only ever calls `upload(file, filename)` and never has to know that S3 needs a bucket and GCS needs a different client entirely.\n\nThis is exactly the shape of an abstract base controller or repository in NestJS. A `BaseCrudController<T>` can implement `findAll()`, `findOne()`, and shared pagination and error handling once, while leaving an abstract `getRepository()` for each concrete controller (`UsersController`, `OrdersController`) to supply:\n\n```typescript\nabstract class BaseCrudController<T> {\n  protected abstract getRepository(): Repository<T>;\n\n  @Get()\n  findAll(): Promise<T[]> {\n    return this.getRepository().find();\n  }\n}\n```\n\nThe difference between abstraction and encapsulation is easy to blur: encapsulation hides data so it cannot be corrupted, while abstraction hides complexity so the caller does not need to think about it. A private field is encapsulation. An abstract method that hides three different upload SDKs behind one `upload()` call is abstraction.\n\nIn an interview, if asked to distinguish abstraction from encapsulation, use exactly that line: encapsulation protects state, abstraction simplifies what the caller has to know. Then point to abstract class and interface as TypeScript\'s two main abstraction tools, and mention that an abstract class can share implementation across subclasses while an interface cannot.',
    },
    {
      id: 'oop-inheritance',
      title: 'Inheritance - sharing behaviour',
      content:
        'Every domain event in your system needs the same handful of things: an id, a timestamp, and the name of the event. Writing that boilerplate into `UserRegisteredEvent`, `OrderPlacedEvent`, `PaymentFailedEvent`, and every other event class separately means fixing a bug in the timestamp logic in a dozen places at once.\n\n**Inheritance** lets a class reuse another class\'s implementation instead of duplicating it. A `DomainEvent` base class carries the shared fields, and each concrete event extends it:\n\n```typescript\nabstract class DomainEvent {\n  readonly occurredAt: Date = new Date();\n  abstract readonly eventName: string;\n\n  constructor(readonly aggregateId: string) {}\n}\n\nclass UserRegisteredEvent extends DomainEvent {\n  readonly eventName = "user.registered";\n\n  constructor(aggregateId: string, readonly email: string) {\n    super(aggregateId);\n  }\n}\n\nclass OrderPlacedEvent extends DomainEvent {\n  readonly eventName = "order.placed";\n\n  constructor(aggregateId: string, readonly totalCents: number) {\n    super(aggregateId);\n  }\n}\n```\n\n`UserRegisteredEvent extends DomainEvent` is an is-a relationship: a `UserRegisteredEvent` is a `DomainEvent`, so anywhere your code expects a `DomainEvent` (an event bus, a logger, an outbox table), a `UserRegisteredEvent` fits without any special casing. `super(aggregateId)` calls the parent constructor to set up the shared fields before the subclass adds its own.\n\nInheritance is powerful, but it comes with a real cost known as the **fragile base class problem**: because subclasses depend on the internal behavior of their parent, a change to `DomainEvent` can silently break every event that extends it. If someone adds a new required constructor parameter to `DomainEvent`, every single subclass constructor needs updating. If someone changes what `occurredAt` means (say, from "when the object was created" to "when the event was published"), every subclass that assumed the old meaning is now subtly wrong, often without a compile error to catch it.\n\nThis is exactly why "favor composition over inheritance" is common interview advice: a deep inheritance hierarchy (`DomainEvent` -> `FinancialEvent` -> `PaymentEvent` -> `RefundEvent`) means understanding any single leaf class requires reading four files, and changing a base class requires understanding every leaf that depends on it. A single level of inheritance, like the `DomainEvent` example above, is usually safe. Multiple levels deep is where the fragile base class problem starts to bite.\n\nIn an interview, explain inheritance as code reuse through an is-a relationship, then immediately show you know its limits: mention the fragile base class problem by name, and be ready to say that composition (covered next) is usually preferred once a hierarchy would need more than one level.',
    },
    {
      id: 'oop-polymorphism',
      title: 'Polymorphism - one interface, many implementations',
      content:
        'A user signs up, and three different things need to happen: an email confirmation, a Slack alert to the ops channel, and maybe an SMS if they opted in. Each channel sends completely differently, but from the calling code\'s point of view, they should all just "send a notification".\n\n**Polymorphism** means one interface, many implementations, chosen at runtime based on the actual object rather than its declared type. Define the shared contract:\n\n```typescript\ninterface NotificationChannel {\n  send(message: string, recipient: string): Promise<void>;\n}\n\nclass EmailChannel implements NotificationChannel {\n  async send(message: string, recipient: string) {\n    await mailer.send({ to: recipient, body: message });\n  }\n}\n\nclass SlackChannel implements NotificationChannel {\n  async send(message: string, recipient: string) {\n    await slackClient.postMessage({ channel: recipient, text: message });\n  }\n}\n\nclass SmsChannel implements NotificationChannel {\n  async send(message: string, recipient: string) {\n    await twilioClient.messages.create({ to: recipient, body: message });\n  }\n}\n```\n\nNow a list of channels can be looped over without the caller knowing or caring which concrete class each one is:\n\n```typescript\nconst channels: NotificationChannel[] = [new EmailChannel(), new SlackChannel(), new SmsChannel()];\n\nasync function notifyAll(channels: NotificationChannel[], message: string, recipient: string) {\n  for (const channel of channels) {\n    await channel.send(message, recipient); // resolves to the right implementation at runtime\n  }\n}\n```\n\nThis is runtime polymorphism: the same line, `channel.send(message, recipient)`, calls `EmailChannel.send`, `SlackChannel.send`, or `SmsChannel.send` depending on which object actually sits in that slot of the array at the moment the loop runs. `notifyAll` never uses a type check or an `if (channel instanceof SlackChannel)` branch; it just trusts the interface.\n\nNestJS wires exactly this pattern through module providers. A `NOTIFICATION_CHANNELS` token can resolve to an array of channel implementations, injected as a group:\n\n```typescript\n@Module({\n  providers: [\n    { provide: "NOTIFICATION_CHANNELS", useValue: [new EmailChannel(), new SlackChannel(), new SmsChannel()] },\n    NotificationsService,\n  ],\n})\nexport class NotificationsModule {}\n\n@Injectable()\nclass NotificationsService {\n  constructor(@Inject("NOTIFICATION_CHANNELS") private readonly channels: NotificationChannel[]) {}\n\n  async notifyAll(message: string, recipient: string) {\n    await Promise.all(this.channels.map((c) => c.send(message, recipient)));\n  }\n}\n```\n\nAdding a new channel, like a push notification service, means writing one new class that implements `NotificationChannel` and adding it to the providers array. `NotificationsService` does not change at all, because it was never written against a specific channel type in the first place.\n\nIn an interview, be precise about the distinction between polymorphism and just having an interface: polymorphism is specifically about a single call site behaving differently depending on the runtime type of the object behind it, without any conditional logic at the call site. If you find yourself writing `if (channel instanceof EmailChannel)`, that is a sign polymorphism is not being used properly - the whole point is that the caller should not need to know which subclass it has.',
    },
    {
      id: 'oop-relationships',
      title: 'Association, Aggregation, Composition - how objects relate',
      content:
        'Not every relationship between two classes is the same kind of relationship, and mixing them up leads to modeling objects with a lifecycle that does not match reality. TypeScript will happily let you write any of the three; understanding which one you actually mean is what makes a domain model correct.\n\n**Association** is the loosest relationship: two objects know about each other and interact, but neither owns the other and neither controls the other\'s lifecycle. Think of a developer and a repository on GitHub: a `Developer` can contribute to many `Repository` objects, and a `Repository` has many contributing `Developer` objects, but deleting one developer\'s account does not delete the repository, and archiving a repository does not delete the developer.\n\n```typescript\nclass Repository {\n  constructor(readonly name: string) {}\n}\n\nclass Developer {\n  contribute(repo: Repository): void {\n    console.log(`Pushed a commit to ${repo.name}`);\n  }\n}\n```\n\n`Developer` simply uses a `Repository` it is handed; there is no ownership in either direction.\n\n**Aggregation** is a whole-part relationship where the parts can exist independently of the whole, and outlive it. A `Team` has `Microservice` objects assigned to it, but if the team is disbanded, the microservices keep running, owned by someone else:\n\n```typescript\nclass Microservice {\n  constructor(readonly name: string) {}\n}\n\nclass Team {\n  private services: Microservice[] = [];\n\n  assign(service: Microservice): void {\n    this.services.push(service);\n  }\n}\n\nconst paymentsService = new Microservice("payments-service");\nconst team = new Team();\nteam.assign(paymentsService);\n// paymentsService still exists and can be reassigned even if `team` is deleted\n```\n\n**Composition** is the strictest relationship: the parts are created by the whole, belong exclusively to it, and are destroyed along with it. An `Order` owns its `LineItem` objects - a line item has no meaning and no independent existence outside the order it belongs to:\n\n```typescript\nclass LineItem {\n  constructor(readonly productId: string, readonly quantity: number, readonly priceCents: number) {}\n}\n\nclass Order {\n  private readonly items: LineItem[] = [];\n\n  addItem(productId: string, quantity: number, priceCents: number): void {\n    this.items.push(new LineItem(productId, quantity, priceCents));\n  }\n\n  get total(): number {\n    return this.items.reduce((sum, item) => sum + item.quantity * item.priceCents, 0);\n  }\n}\n```\n\nNotice `LineItem` instances are created inside `Order.addItem()` and never handed out or created independently - if the `Order` is deleted, its line items go with it. There is no scenario where a `LineItem` floats around detached from an `Order`, the way a `Microservice` can float around detached from a `Team`.\n\nThe distinction that matters in practice: association is "uses", aggregation is "has-a, but can live apart", and composition is "has-a, and cannot live apart". Getting this right affects real decisions, like whether deleting a parent row should cascade-delete its children in the database - composition usually means yes (`ON DELETE CASCADE`), aggregation usually means no.\n\nIn an interview, use the ownership and lifecycle test to tell them apart on the spot: if deleting the whole should delete the part, it is composition; if the part survives and can be reassigned, it is aggregation; if there is no ownership in either direction, it is association.',
    },
    {
      id: 'oop-dependency-realization',
      title: 'Dependency and Realization',
      content:
        'Two more relationships round out how classes connect to each other, and both come up constantly in real code even though they rarely get named explicitly.\n\n**Dependency** is the most temporary relationship of all: one class uses another only for the duration of a single method call, usually by receiving it as a parameter, without holding onto it as a field. A `DeploymentService` that needs to make one HTTP call to trigger a deploy depends on an `HttpClient` for that call, but does not own or store a reference to it long-term:\n\n```typescript\nclass DeploymentService {\n  async triggerDeploy(httpClient: HttpClient, serviceUrl: string): Promise<void> {\n    await httpClient.post(`${serviceUrl}/deploy`, {});\n  }\n}\n```\n\nCompare this to composition or association, where the relationship is a field that lives as long as the object does. Dependency is scoped to a single method call: as soon as `triggerDeploy` returns, there is no lingering reference to `httpClient` anywhere. This is weaker (and safer to change) than a constructor-injected dependency, because nothing about `DeploymentService`\'s long-term shape depends on `HttpClient` at all.\n\n**Realization** (sometimes called "implementation") is the relationship between a class and the interface it implements. `RedisCache implements CacheStore` is a realization relationship - `RedisCache` commits to fulfilling every method `CacheStore` promises, without inheriting any actual code from it, because an interface has no implementation to inherit in the first place:\n\n```typescript\ninterface CacheStore {\n  get(key: string): Promise<string | null>;\n  set(key: string, value: string, ttlSeconds?: number): Promise<void>;\n}\n\nclass RedisCache implements CacheStore {\n  constructor(private readonly client: RedisClient) {}\n\n  async get(key: string): Promise<string | null> {\n    return this.client.get(key);\n  }\n\n  async set(key: string, value: string, ttlSeconds?: number): Promise<void> {\n    if (ttlSeconds) await this.client.set(key, value, "EX", ttlSeconds);\n    else await this.client.set(key, value);\n  }\n}\n```\n\nRealization is different from inheritance in a way that matters for design: inheritance (`extends`) shares implementation and creates the fragile base class risk from earlier in this course; realization (`implements`) shares only the contract, with zero shared code and zero risk of a base class change breaking subclasses, because there is no base class code to change.\n\nIn an interview, if the relationship names themselves come up, keep the distinctions short: association is a persistent "uses", aggregation and composition are persistent "has-a" relationships that differ in lifecycle ownership, dependency is a temporary "uses-a" scoped to one method call, and realization is "implements the contract of" with no shared code. Most real interview questions do not test the UML vocabulary directly, but understanding dependency is what makes "depend on abstractions, not concretions" (covered in the SOLID section next) click.',
    },
    {
      id: 'oop-srp',
      title: 'SOLID - Single Responsibility Principle',
      content:
        'A `UserService` starts small: register a user, hash their password, save them to the database. Then the product grows, and so does the service:\n\n```typescript\n@Injectable()\nclass UserService {\n  async register(dto: RegisterDto): Promise<User> { /* ... */ }\n  async verifyKycDocuments(userId: string, documents: Buffer[]): Promise<void> { /* ... */ }\n  async sendWelcomeEmail(user: User): Promise<void> { /* ... */ }\n  async generateAccountStatementPdf(userId: string): Promise<Buffer> { /* ... */ }\n}\n```\n\nEach method looks harmless on its own, but the class as a whole now has four separate reasons to change: a new password hashing algorithm, a new KYC provider, a new email template engine, a new PDF layout. Any one of those changes means editing `UserService`, redeploying every consumer of `UserService`, and re-testing registration even though registration itself did not change. Worse, a bug introduced while fixing the PDF generator can now break registration, because everything lives in the same class and shares the same test suite, the same imports, and the same blast radius.\n\nThe **Single Responsibility Principle** says a class should have exactly one reason to change. Splitting the class along those reasons gives each piece its own boundary:\n\n```typescript\n@Injectable()\nclass UserAuthService {\n  async register(dto: RegisterDto): Promise<User> { /* ... */ }\n}\n\n@Injectable()\nclass KycService {\n  async verifyDocuments(userId: string, documents: Buffer[]): Promise<void> { /* ... */ }\n}\n\n@Injectable()\nclass EmailService {\n  async sendWelcomeEmail(user: User): Promise<void> { /* ... */ }\n}\n```\n\nNow a change to the KYC provider only touches `KycService`, only requires re-testing `KycService`, and only redeploys the module `KycService` lives in if your deployment is granular enough to do that. `UserAuthService` never has to know KYC exists.\n\nThis maps directly onto how NestJS modules should be drawn. A `UsersModule`, a `KycModule`, and a `NotificationsModule`, each exporting one focused service and importing only what it needs, keep the module boundaries matching the actual reasons the code changes. A single giant `UserModule` that exports a service doing all four jobs is a sign the module boundary was drawn around a noun ("user-related stuff") instead of around a responsibility.\n\nIn an interview, do not define SRP as "one class, one method" - that is wrong and will get pushback from a senior interviewer. Define it as "one class, one reason to change", then justify it with the actual cost: unrelated changes stop being coupled, so a KYC provider swap cannot accidentally break registration, and the blast radius of any single change shrinks.',
    },
    {
      id: 'oop-ocp',
      title: 'SOLID - Open/Closed Principle',
      content:
        'A `PaymentProcessor` starts with two payment methods and a switch statement feels like the fastest way to write it:\n\n```typescript\nclass PaymentProcessor {\n  process(method: "stripe" | "paypal", amountCents: number): Promise<string> {\n    switch (method) {\n      case "stripe":\n        return stripeSdk.charge(amountCents);\n      case "paypal":\n        return paypalSdk.charge(amountCents);\n      default:\n        throw new Error("Unknown payment method");\n    }\n  }\n}\n```\n\nThen the product adds Apple Pay, then a bank transfer option, then a crypto payment provider for one specific market. Every single addition means opening `PaymentProcessor`, adding a new case, and touching a class that every existing payment method already depends on and already has tests for. There is a real risk that adding the fifth case accidentally changes behavior for the first four, because they are all crammed into one method and one switch.\n\nThe **Open/Closed Principle** says a class should be open for extension but closed for modification: you should be able to add new behavior without editing existing, already-tested code. Reshape the same processor around an interface instead of a switch:\n\n```typescript\ninterface PaymentProvider {\n  charge(amountCents: number): Promise<string>;\n}\n\nclass StripeProvider implements PaymentProvider {\n  charge(amountCents: number): Promise<string> {\n    return stripeSdk.charge(amountCents);\n  }\n}\n\nclass PaypalProvider implements PaymentProvider {\n  charge(amountCents: number): Promise<string> {\n    return paypalSdk.charge(amountCents);\n  }\n}\n\nclass PaymentProcessor {\n  constructor(private readonly providers: Record<string, PaymentProvider>) {}\n\n  process(method: string, amountCents: number): Promise<string> {\n    const provider = this.providers[method];\n    if (!provider) throw new Error(`Unknown payment method: ${method}`);\n    return provider.charge(amountCents);\n  }\n}\n\nconst processor = new PaymentProcessor({\n  stripe: new StripeProvider(),\n  paypal: new PaypalProvider(),\n});\n```\n\nAdding Apple Pay now means writing one new `ApplePayProvider implements PaymentProvider` class and registering it in the map handed to `PaymentProcessor`. `PaymentProcessor` itself is never touched again, so its existing tests keep passing without being re-run against a new code path, and there is no switch statement anywhere to accidentally get the case order wrong on.\n\nIn an interview, the sharp answer is that OCP is really about isolating the parts of your system likely to change (payment methods, notification channels, storage backends) behind an interface, so that adding a new variant is additive - a new file - rather than a diff to a class every other variant already depends on.',
    },
    {
      id: 'oop-lsp',
      title: 'SOLID - Liskov Substitution Principle',
      content:
        'A `ReadOnlyList` sounds like a reasonable thing to model as `class ReadOnlyList<T> extends Array<T>`, reusing all of Array\'s existing methods like `map`, `filter`, and `slice`. The only change needed is blocking mutation:\n\n```typescript\nclass ReadOnlyList<T> extends Array<T> {\n  push(...items: T[]): number {\n    throw new Error("Cannot push to a read-only list");\n  }\n}\n\nfunction addItem(list: Array<number>): void {\n  list.push(99);\n}\n\nconst readOnly = new ReadOnlyList<number>(1, 2, 3);\naddItem(readOnly); // compiles fine, throws at runtime\n```\n\n`addItem` is written against `Array<number>`, and `ReadOnlyList<number>` is a subtype of `Array<number>` as far as TypeScript\'s structural type system is concerned, so this compiles without a single error. It then throws at runtime the moment `addItem` runs, even though nothing in `addItem`\'s signature suggested that could happen. Any code that legitimately expects an `Array` and happens to receive a `ReadOnlyList` breaks in a way the type system never warned about.\n\nThe **Liskov Substitution Principle** says a subclass must be usable anywhere its parent class is expected, without breaking the caller\'s assumptions. `ReadOnlyList` violates it: `Array` promises that `push()` succeeds, and `ReadOnlyList` silently breaks that promise while still claiming, through `extends Array`, to be one.\n\nThe fix is to stop pretending read-only-ness is a subtype of a mutable array, and model it as its own type that only exposes the operations it actually supports:\n\n```typescript\nclass ReadOnlyList<T> {\n  constructor(private readonly items: readonly T[]) {}\n\n  get(index: number): T {\n    return this.items[index];\n  }\n\n  map<U>(fn: (item: T) => U): U[] {\n    return this.items.map(fn);\n  }\n\n  get length(): number {\n    return this.items.length;\n  }\n}\n```\n\n`ReadOnlyList` no longer claims to be an `Array`, so `addItem(list: Array<number>)` simply will not accept one - the type system now catches the mismatch at compile time, instead of the caller finding out at runtime that a promised method secretly throws.\n\nIn an interview, this is a strong example when asked about LSP because it shows a real TypeScript-specific trap: TypeScript\'s structural typing means `extends` can create a subtype that technically compiles everywhere the parent is expected, while still violating LSP at runtime by throwing on inherited methods it cannot honestly support. The lesson generalizes beyond `Array`: any time a subclass overrides a method to throw, return nothing meaningful, or silently do less than the parent promised, that is an LSP violation waiting to break a caller that trusted the parent type.',
    },
    {
      id: 'oop-isp',
      title: 'SOLID - Interface Segregation Principle',
      content:
        'A generic `IRepository<T>` interface tends to grow every time a new query pattern is needed somewhere in the codebase, until it looks like this:\n\n```typescript\ninterface IRepository<T> {\n  findById(id: string): Promise<T | null>;\n  findAll(): Promise<T[]>;\n  findByEmail(email: string): Promise<T | null>;\n  findPaginated(page: number, size: number): Promise<T[]>;\n  create(entity: T): Promise<T>;\n  update(id: string, patch: Partial<T>): Promise<T>;\n  delete(id: string): Promise<void>;\n  softDelete(id: string): Promise<void>;\n  restore(id: string): Promise<void>;\n  bulkCreate(entities: T[]): Promise<T[]>;\n  count(): Promise<number>;\n  exists(id: string): Promise<boolean>;\n  // ...eight more methods added over two years\n}\n```\n\nA `ReportingService` that only ever needs `findById` to look up a user for a report is now forced to depend on the entire twenty-method surface. It has to accept a mock or a fake implementing all twenty methods in its tests, even though it calls exactly one of them. Worse, if `IRepository<T>` gains a twenty-first method, every implementation, real or fake, has to be updated to satisfy the interface, even implementations that will never use that method.\n\nThe **Interface Segregation Principle** says clients should not be forced to depend on methods they do not use. Split the fat interface along how it is actually consumed:\n\n```typescript\ninterface IReadRepository<T> {\n  findById(id: string): Promise<T | null>;\n  findAll(): Promise<T[]>;\n}\n\ninterface IWriteRepository<T> {\n  create(entity: T): Promise<T>;\n  update(id: string, patch: Partial<T>): Promise<T>;\n  delete(id: string): Promise<void>;\n}\n\nclass UserRepository implements IReadRepository<User>, IWriteRepository<User> {\n  async findById(id: string): Promise<User | null> { /* ... */ }\n  async findAll(): Promise<User[]> { /* ... */ }\n  async create(entity: User): Promise<User> { /* ... */ }\n  async update(id: string, patch: Partial<User>): Promise<User> { /* ... */ }\n  async delete(id: string): Promise<void> { /* ... */ }\n}\n```\n\n`ReportingService` now depends on `IReadRepository<User>` alone, a two-method contract that matches exactly what it uses. Its unit tests only need to fake two methods, not twenty. A service that only ever writes, like an import job, can depend on `IWriteRepository<User>` alone and never see the read methods at all. `UserRepository` still implements both, because the concrete repository genuinely does everything, but nothing forces every consumer of that repository to know about capabilities they never touch.\n\nIn an interview, tie ISP back to testability specifically: a narrow interface means a narrow, honest mock in your tests, and a caller\'s dependency list becomes documentation of exactly what it needs, rather than an accident of which shared interface happened to exist first.',
    },
    {
      id: 'oop-dip',
      title: 'SOLID - Dependency Inversion Principle',
      content:
        '`UserService` needs to save users somewhere. The direct way is to construct the concrete database implementation right inside the service:\n\n```typescript\nclass UserService {\n  private repository = new TypeOrmUserRepository(dataSource);\n\n  async register(dto: RegisterDto): Promise<User> {\n    return this.repository.save(new User(dto.email));\n  }\n}\n```\n\nThis works, until you need a different repository in tests (an in-memory fake, so tests do not need a real database), or the team decides to migrate from TypeORM to Prisma. Both changes now require editing `UserService` directly, even though registering a user has nothing to do with which ORM is underneath. `UserService`, the high-level business logic, has become tightly coupled to `TypeOrmUserRepository`, a low-level implementation detail.\n\nThe **Dependency Inversion Principle** says high-level modules should not depend on low-level modules; both should depend on an abstraction. Introduce an interface that both sides depend on instead of depending on each other:\n\n```typescript\ninterface IUserRepository {\n  save(user: User): Promise<User>;\n  findByEmail(email: string): Promise<User | null>;\n}\n\nclass TypeOrmUserRepository implements IUserRepository {\n  constructor(private readonly dataSource: DataSource) {}\n  async save(user: User): Promise<User> { /* ... */ }\n  async findByEmail(email: string): Promise<User | null> { /* ... */ }\n}\n\n@Injectable()\nclass UserService {\n  constructor(@Inject("USER_REPOSITORY") private readonly repository: IUserRepository) {}\n\n  async register(dto: RegisterDto): Promise<User> {\n    return this.repository.save(new User(dto.email));\n  }\n}\n```\n\n`UserService` now depends only on `IUserRepository`, never on `TypeOrmUserRepository` directly. Wiring the concrete implementation happens once, in the module:\n\n```typescript\n@Module({\n  providers: [\n    UserService,\n    { provide: "USER_REPOSITORY", useClass: TypeOrmUserRepository },\n  ],\n})\nexport class UsersModule {}\n```\n\nSwapping to Prisma means writing `PrismaUserRepository implements IUserRepository` and changing one `useClass` line. Testing `UserService` in isolation means providing `{ provide: "USER_REPOSITORY", useClass: InMemoryUserRepository }` in a test module, with zero changes to `UserService` itself.\n\nThis is the entire foundation of NestJS\'s dependency injection system. Every `@Injectable()` class declares what it needs through constructor parameters, and NestJS\'s module system - not the class itself - decides which concrete implementation to construct and hand over. NestJS reads the constructor\'s parameter types (or the `@Inject()` token) at startup, using reflect-metadata, and resolves them from whatever the current module\'s providers array says. The class author never writes `new TypeOrmUserRepository()` anywhere; NestJS does that wiring for you, which is exactly the inversion the principle is named for - control over which implementation gets used is inverted, from the class itself to the module configuration.\n\nIn an interview, when asked what dependency injection actually gives you beyond fewer new keywords, answer with DIP directly: it decouples high-level business logic from low-level implementation details, so either side can change (swap ORMs, swap payment providers, swap a real service for a test double) without touching the other. NestJS\'s DI container is a mechanical tool for applying DIP automatically to every service in your application.',
    },
    {
      id: 'oop-patterns',
      title: 'Design patterns in TypeScript - the ones that actually come up in interviews',
      content:
        'Design patterns are named solutions to problems that show up over and over. A handful come up constantly in TypeScript and NestJS interviews, usually because you have already been using them without the name attached.\n\n**Singleton** ensures a class has exactly one instance shared across the whole application. NestJS providers are singletons by default: when a module declares a provider, NestJS constructs it once at startup and hands the same instance to every class that injects it, unless you explicitly opt a provider into `Scope.REQUEST` or `Scope.TRANSIENT`. This is why a service that keeps an in-memory cache or counter behaves the way you expect - every controller injecting `CacheService` is sharing the exact same object, not getting a fresh one per request.\n\n```typescript\n@Injectable() // singleton by default\nexport class CacheService {\n  private store = new Map<string, unknown>();\n}\n```\n\n**Factory** delegates object creation to a dedicated function or class instead of calling `new` directly, useful when construction needs logic (reading config, picking an implementation) rather than just assigning fields. NestJS\'s `useFactory` provider is this pattern built into the DI system:\n\n```typescript\n{\n  provide: "PAYMENT_GATEWAY",\n  useFactory: (config: ConfigService) => {\n    return config.get("PAYMENT_PROVIDER") === "stripe" ? new StripeGateway() : new PaypalGateway();\n  },\n  inject: [ConfigService],\n}\n```\n\n**Strategy** is exactly the swappable-algorithm shape from the Open/Closed Principle lesson: define an interface for an algorithm, and let different implementations be swapped in without changing the code that uses them. `PaymentProvider`, `NotificationChannel`, and `CacheStore` from earlier in this course are all strategies - the calling code holds a reference to the interface and never branches on which concrete strategy it received.\n\n**Observer** lets one object (the subject) notify a list of interested objects (observers) when something happens, without the subject needing to know who is listening. Node\'s built-in EventEmitter, and NestJS\'s `@nestjs/event-emitter` module built on it, are this pattern directly:\n\n```typescript\n@Injectable()\nclass OrdersService {\n  constructor(private readonly eventEmitter: EventEmitter2) {}\n\n  async placeOrder(dto: CreateOrderDto) {\n    const order = await this.save(dto);\n    this.eventEmitter.emit("order.placed", new OrderPlacedEvent(order.id, order.totalCents));\n    return order;\n  }\n}\n\n@Injectable()\nclass SendReceiptListener {\n  @OnEvent("order.placed")\n  async handle(event: OrderPlacedEvent) {\n    await this.emailService.sendReceipt(event.aggregateId);\n  }\n}\n```\n\n`OrdersService` has no idea `SendReceiptListener` exists. It emits an event and moves on; any number of listeners can react, and new listeners can be added later without touching `OrdersService` at all.\n\n**Decorator** attaches extra behavior to an object or function without changing its underlying code. NestJS decorators are not just inspired by the decorator pattern, they are TypeScript\'s actual implementation of it: `@Controller()`, `@Injectable()`, `@Get()`, and `@UseGuards()` all wrap the class or method they are attached to with additional behavior (route registration, dependency injection metadata, guard execution) while leaving the original method body untouched.\n\n```typescript\n@Controller("orders")\n@UseGuards(AuthGuard) // wraps every route handler with auth logic\nexport class OrdersController {\n  @Get(":id")\n  findOne(@Param("id") id: string) {\n    return this.ordersService.findById(id);\n  }\n}\n```\n\nIn an interview, the strongest answer is not reciting pattern names but pointing at code you already use: NestJS providers are singletons, `useFactory` is the factory pattern, `@nestjs/event-emitter` is the observer pattern, and the decorators themselves are the decorator pattern - showing you understand these are not academic exercises, they are the actual mechanics of the framework you build with every day.',
    },
  ],
};
