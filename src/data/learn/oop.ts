import type { LearnModule } from '../../types';

export const oop: LearnModule = {
  id: 'oop',
  title: 'Object-Oriented Programming',
  blurb: 'Start from zero and understand every class, interface, extends, and implements you see in a real TypeScript codebase.',
  lessons: [
    {
      id: 'oop-what-is-a-class',
      title: 'What is a class and why does it exist',
      content:
        'You are building a banking app. User data lives in plain objects: `{ name: "Alice", balance: 500 }`. The functions that operate on that data live somewhere else entirely, often in a completely different file:\n\n```typescript\nfunction deposit(account: { balance: number }, amount: number) {\n  account.balance += amount;\n}\n\nfunction withdraw(account: { balance: number }, amount: number) {\n  account.balance -= amount;\n}\n\nconst account = { name: "Alice", balance: 500 };\ndeposit(account, 100);\nwithdraw(account, 50);\n```\n\nNothing connects `account` to `deposit` and `withdraw` except that you remembered to pass the right object in. Any other function in the codebase can reach into `account` and change `balance` directly, with no rule enforced anywhere: `account.balance = -999999` is perfectly legal TypeScript. As the app grows, the functions that belong to accounts get scattered across files with no clear home, and nothing stops someone from writing a new function that forgets an important check the others have.\n\nA **class** fixes this by putting the data and the behaviour that belongs to it in the same place:\n\n```typescript\nclass BankAccount {\n  name: string;\n  balance: number;\n\n  constructor(name: string, initialBalance: number) {\n    this.name = name;\n    this.balance = initialBalance;\n  }\n\n  deposit(amount: number): void {\n    this.balance += amount;\n  }\n\n  withdraw(amount: number): void {\n    this.balance -= amount;\n  }\n}\n\nconst account = new BankAccount("Alice", 500);\naccount.deposit(100);\naccount.withdraw(50);\nconsole.log(account.balance); // 550\n```\n\nThis is a class: a blueprint that bundles data (`balance`, `name`) together with the functions that operate on that data (`deposit`, `withdraw`), so the two can never drift apart. The `class` keyword declares it, `constructor` runs once when an account is created and sets up its starting values, the properties (`balance`, `name`) hold its state, and the methods (`deposit`, `withdraw`) are the operations that state supports. `deposit` and `withdraw` no longer need `account` passed in as an argument. They already know which account they belong to, through `this`.\n\nIn an interview, if asked why classes exist at all, do not start with syntax. Say that a class groups the data an entity owns together with the operations allowed on that data, so anyone reading the class can see, in one place, everything that entity can do and everything it knows.',
    },
    {
      id: 'oop-objects',
      title: 'Creating objects from classes',
      content:
        '`BankAccount` describes what every account looks like: a name, a balance, a `deposit` method, a `withdraw` method. It does not describe any specific account. Alice\'s account and Bob\'s account are both built from the exact same `BankAccount` class, using the `new` keyword:\n\n```typescript\nconst alice = new BankAccount("Alice", 500);\nconst bob = new BankAccount("Bob", 200);\n\nalice.deposit(100);\n\nconsole.log(alice.balance); // 600\nconsole.log(bob.balance); // 200, untouched\n```\n\nDepositing into `alice` has no effect on `bob`, even though both were created from the same class. Each call to `new BankAccount(...)` builds a completely independent **object**, with its own copy of `name` and `balance` in memory. The class is the blueprint. The object is the actual thing built from it, sitting in memory with real values. This process of building an object from a class is called **instantiation** - `alice` and `bob` are two separate instantiations of `BankAccount`.\n\nTwo operators let you check this relationship at runtime. `typeof alice` returns `"object"`, telling you `alice` is a JavaScript object, but nothing about which class built it. `alice instanceof BankAccount` returns `true`, telling you specifically that `alice` was built from the `BankAccount` class (or one of its subclasses, which the next few lessons get to):\n\n```typescript\nconsole.log(typeof alice); // "object"\nconsole.log(alice instanceof BankAccount); // true\nconsole.log(alice instanceof Array); // false\n```\n\nIn an interview, a clean way to state the difference: a class is a blueprint that exists once, in your source code. An object is a specific instance built from that blueprint, existing at runtime, with its own independent state. You can create as many objects from one class as you need, and changing one never affects the others.',
    },
    {
      id: 'oop-access-modifiers',
      title: 'Access modifiers - public, private, protected',
      content:
        'Nothing in the `BankAccount` class from the last two lessons stops this:\n\n```typescript\nconst alice = new BankAccount("Alice", 500);\nalice.balance = -999999;\n```\n\n`balance` is a plain public property, so any code anywhere in the codebase can set it directly, completely bypassing `deposit` and `withdraw` and whatever validation you might add to them later. There is no way to enforce a rule like "balance can never go negative" if anyone can reach in and overwrite it directly.\n\nTypeScript gives you **access modifiers** to control exactly this. `public` is the default: a public property or method can be read and written from anywhere. `private` restricts a property or method so it can only be touched from inside the class itself:\n\n```typescript\nclass BankAccount {\n  private balance: number;\n\n  constructor(public name: string, initialBalance: number) {\n    this.balance = initialBalance;\n  }\n\n  deposit(amount: number): void {\n    if (amount <= 0) throw new Error("Deposit amount must be positive");\n    this.balance += amount;\n  }\n\n  withdraw(amount: number): void {\n    if (amount > this.balance) throw new Error("Insufficient funds");\n    this.balance -= amount;\n  }\n\n  getBalance(): number {\n    return this.balance;\n  }\n}\n\nconst alice = new BankAccount("Alice", 500);\nalice.balance = -999999; // compile error: balance is private\nalice.deposit(-50); // throws at runtime: Deposit amount must be positive\n```\n\nNow the only way to change `balance` is through `deposit` and `withdraw`, and both enforce a rule before touching it. `protected` sits between the two: a protected member is visible inside the declaring class and inside any class that extends it (covered in lesson 7), but still invisible to outside code. `readonly` is different from the other three because it is not about who can see a property, but about whether it can change after being set: a readonly property can only be assigned once, inside the constructor, and never reassigned after that:\n\n```typescript\nclass BankAccount {\n  readonly accountNumber: string;\n  private balance: number;\n\n  constructor(accountNumber: string, initialBalance: number) {\n    this.accountNumber = accountNumber;\n    this.balance = initialBalance;\n  }\n}\n\nconst alice = new BankAccount("ACC-001", 500);\nalice.accountNumber = "ACC-002"; // compile error: accountNumber is readonly\n```\n\nIn an interview, do not describe private fields as just hiding data. Say that private fields protect data integrity: they force every change to go through a method that can validate the change first, which is the only way to guarantee an invariant like "balance never goes negative" actually holds.',
    },
    {
      id: 'oop-constructor',
      title: 'The constructor - setting up an object',
      content:
        'Every `BankAccount` so far has repeated the same three lines: declare the property, accept it as a constructor parameter, assign the parameter to the property.\n\n```typescript\nclass BankAccount {\n  name: string;\n  balance: number;\n\n  constructor(name: string, balance: number) {\n    this.name = name;\n    this.balance = balance;\n  }\n}\n```\n\nTypeScript has a shorthand that collapses all three steps into the constructor\'s parameter list itself. Adding an access modifier (`public`, `private`, `protected`, or `readonly`) directly in front of a constructor parameter declares the property and assigns it in one move:\n\n```typescript\nclass BankAccount {\n  constructor(public name: string, private balance: number) {}\n}\n\nconst alice = new BankAccount("Alice", 500);\nconsole.log(alice.name); // "Alice"\n```\n\n`public name: string` inside the constructor\'s parentheses does exactly the same thing as declaring `name: string` above the constructor and writing `this.name = name` inside it. This is why real NestJS constructors, which often take four or five injected dependencies, stay short: every one of those dependencies is declared and assigned in the same line it is received.\n\nConstructor parameters can have default values, exactly like regular function parameters:\n\n```typescript\nclass BankAccount {\n  constructor(public name: string, private balance: number = 0) {}\n}\n\nconst newAccount = new BankAccount("Bob"); // balance defaults to 0\n```\n\nIf you do not write a constructor at all, TypeScript gives the class an implicit empty one that takes no arguments and does nothing. This only works if the class has no properties that require a value, since there is no constructor left to set them.\n\nTypeScript does not support true constructor overloading the way Java or C# does; you cannot write two separate constructor implementations with different parameter lists. The common workaround is a single constructor with optional or default parameters, or a static factory method (covered in the next lesson) that builds the object in a different way and calls the real constructor internally:\n\n```typescript\nclass BankAccount {\n  constructor(\n    public name: string,\n    private balance: number = 0,\n    private currency: "EUR" | "USD" = "EUR",\n  ) {}\n}\n\nnew BankAccount("Alice"); // balance 0, currency EUR\nnew BankAccount("Bob", 500, "USD"); // both overridden\n```\n\nIn an interview, the short answer is that a constructor is a special method that runs exactly once, at the moment `new` is called, and its job is to put the object into a valid starting state before any other code can touch it. Everything it sets up is guaranteed to exist by the time the object is handed back to whoever called `new`.',
    },
    {
      id: 'oop-methods',
      title: 'Methods - the behaviour of a class',
      content:
        'Every method written so far, `deposit`, `withdraw`, `getBalance`, works on one specific account through `this.balance`. These are instance methods: they only make sense in the context of one particular object, and calling them requires an object to call them on.\n\nSome behaviour has nothing to do with any specific instance. `Math.random()` does not belong to any particular `Math` object, because there is no such thing as a `Math` object at all. It belongs to the `Math` class itself. TypeScript calls this a **static method**, declared with the `static` keyword:\n\n```typescript\nclass BankAccount {\n  constructor(public name: string, private balance: number) {}\n\n  static fromCsvRow(row: string): BankAccount {\n    const [name, balanceStr] = row.split(",");\n    return new BankAccount(name, Number(balanceStr));\n  }\n\n  deposit(amount: number): void {\n    this.balance += amount;\n  }\n}\n\nconst account = BankAccount.fromCsvRow("Alice,500");\nconsole.log(account.name); // "Alice"\n```\n\n`fromCsvRow` is called on the class itself, `BankAccount.fromCsvRow(...)`, not on an object, because at the point it runs there is no object yet. Its entire job is to build one. This pattern, a static method whose only purpose is to construct and return an instance, is called a static factory method, and it is a common way to work around TypeScript\'s lack of constructor overloading from the previous lesson: you can have as many differently named static factory methods as you need, each building a `BankAccount` in its own way, while the real constructor stays simple.\n\nInstance methods can also return `this`, which lets calls be chained one after another:\n\n```typescript\nclass BankAccount {\n  constructor(public name: string, private balance: number) {}\n\n  deposit(amount: number): this {\n    this.balance += amount;\n    return this;\n  }\n\n  withdraw(amount: number): this {\n    this.balance -= amount;\n    return this;\n  }\n}\n\nconst alice = new BankAccount("Alice", 500);\nalice.deposit(100).withdraw(50).deposit(20);\n```\n\nEach call returns the same object it was called on, so the next method in the chain has something to call on immediately.\n\nIn an interview, the deciding question for static versus instance is: does this operation need a specific object\'s state to run? `deposit` needs a specific balance to add to, so it is an instance method. `fromCsvRow` needs no existing account, since its entire job is to create one, so it is static.',
    },
    {
      id: 'oop-interfaces',
      title: 'Interfaces - making a promise',
      content:
        'Your checkout flow needs to support three payment providers: Stripe, PayPal, and a plain bank transfer. Each one has completely different internal code:\n\n```typescript\nclass StripePayment {\n  chargeViaStripe(amountCents: number): string {\n    return `stripe-txn-${amountCents}`;\n  }\n}\n\nclass PaypalPayment {\n  sendPaypalCharge(amountCents: number): string {\n    return `paypal-txn-${amountCents}`;\n  }\n}\n```\n\nNotice the method names do not even match: `chargeViaStripe` and `sendPaypalCharge` do the same job but are called something different in each class. Any part of your checkout flow that wants to charge a customer now needs to know exactly which provider it is talking to and call the right method name for that specific provider. Adding a fourth provider means updating every single place that branches on provider type.\n\nAn **interface** fixes this by defining a contract: a list of method signatures that any class can promise to fulfil, with no implementation of its own.\n\n```typescript\ninterface Payable {\n  charge(amountCents: number): string;\n  refund(transactionId: string): void;\n}\n```\n\n`Payable` does not say how charging works. It only says that anything claiming to be `Payable` must have a `charge` method that takes an amount and returns a string, and a `refund` method that takes a transaction id. A class commits to that contract with the `implements` keyword:\n\n```typescript\nclass StripePayment implements Payable {\n  charge(amountCents: number): string {\n    return `stripe-txn-${amountCents}`;\n  }\n\n  refund(transactionId: string): void {\n    console.log(`Refunding ${transactionId} via Stripe`);\n  }\n}\n\nclass PaypalPayment implements Payable {\n  charge(amountCents: number): string {\n    return `paypal-txn-${amountCents}`;\n  }\n\n  refund(transactionId: string): void {\n    console.log(`Refunding ${transactionId} via PayPal`);\n  }\n}\n\nclass BankTransferPayment implements Payable {\n  charge(amountCents: number): string {\n    return `wire-txn-${amountCents}`;\n  }\n\n  refund(transactionId: string): void {\n    console.log(`Refunding ${transactionId} via bank transfer`);\n  }\n}\n```\n\nAll three classes now expose the exact same two method names, even though what happens inside `charge` and `refund` is completely different for each one. If you delete `refund` from `BankTransferPayment`, TypeScript refuses to compile: "Class BankTransferPayment incorrectly implements interface Payable. Property refund is missing." The contract is enforced by the compiler, not by convention or documentation that someone might forget to read.\n\nThis is what makes interfaces valuable. Code that processes a payment can be written entirely against `Payable`, without knowing or caring which of the three classes it actually received:\n\n```typescript\nfunction processPayment(provider: Payable, amountCents: number): string {\n  return provider.charge(amountCents);\n}\n\nprocessPayment(new StripePayment(), 2000);\nprocessPayment(new PaypalPayment(), 2000);\nprocessPayment(new BankTransferPayment(), 2000);\n```\n\n`processPayment` never mentions `StripePayment`, `PaypalPayment`, or `BankTransferPayment` by name. Adding a fourth provider, like Apple Pay, means writing one new class that implements `Payable`. `processPayment` does not change at all, because it was never written against a specific provider in the first place.\n\nIn an interview, be exact about what `implements` means: a class that implements an interface promises to have every method the interface lists, with matching parameter and return types, but the interface itself contains zero implementation and disappears completely once TypeScript compiles to JavaScript. The difference between an interface and a class is that a class can hold real, running code and can be instantiated with `new`. An interface can never be instantiated on its own. It only describes a shape that something else must satisfy.',
    },
    {
      id: 'oop-extends',
      title: 'Extends - inheriting from a parent class',
      content:
        'Interfaces solve the problem of shared behaviour without shared code. But sometimes you genuinely want to share the code itself, not just the contract. That is where `extends` comes in.\n\nEvery table in your database needs the same three columns: `id`, `createdAt`, `updatedAt`. Without any way to share code, every entity class in your app repeats them:\n\n```typescript\nclass User {\n  id: string;\n  createdAt: Date;\n  updatedAt: Date;\n  email: string;\n}\n\nclass Transaction {\n  id: string;\n  createdAt: Date;\n  updatedAt: Date;\n  amountCents: number;\n}\n```\n\n`id`, `createdAt`, and `updatedAt` are copied into every single entity class, and if you ever need to change how any of them work, like switching `id` from a plain string to a branded type, you have to find and update every class that copied them.\n\n**Inheritance**, using the `extends` keyword, lets one class build directly on another, receiving all of its properties and methods without retyping them:\n\n```typescript\nclass BaseEntity {\n  id: string;\n  createdAt: Date;\n  updatedAt: Date;\n\n  constructor(id: string) {\n    this.id = id;\n    this.createdAt = new Date();\n    this.updatedAt = new Date();\n  }\n}\n\nclass User extends BaseEntity {\n  email: string;\n\n  constructor(id: string, email: string) {\n    super(id);\n    this.email = email;\n  }\n}\n\nclass Transaction extends BaseEntity {\n  amountCents: number;\n\n  constructor(id: string, amountCents: number) {\n    super(id);\n    this.amountCents = amountCents;\n  }\n}\n\nconst user = new User("u1", "alice@example.com");\nconsole.log(user.id, user.createdAt); // both exist, inherited from BaseEntity\n```\n\n`User extends BaseEntity` means every `User` automatically has `id`, `createdAt`, and `updatedAt`, without `User` ever declaring them itself. The `super(id)` call inside `User`\'s constructor runs `BaseEntity`\'s constructor first, which is mandatory: TypeScript will not let a subclass use `this` anywhere in its constructor until `super()` has been called, because until then the parent portion of the object has not been set up yet.\n\nA subclass can also replace a method it inherits with its own version, a process called **overriding**. If `BaseEntity` has a `describe()` method and `User` wants to add extra detail, it can define its own `describe()` with the same name:\n\n```typescript\nclass BaseEntity {\n  constructor(public id: string) {}\n\n  describe(): string {\n    return `Entity ${this.id}`;\n  }\n}\n\nclass User extends BaseEntity {\n  constructor(id: string, public email: string) {\n    super(id);\n  }\n\n  describe(): string {\n    return `${super.describe()} (${this.email})`;\n  }\n}\n\nconst user = new User("u1", "alice@example.com");\nconsole.log(user.describe()); // "Entity u1 (alice@example.com)"\n```\n\n`super.describe()` calls `BaseEntity`\'s original version of the method before `User` adds its own piece on top, so the parent\'s behaviour is not thrown away, just extended.\n\nIn an interview, `extends` should be described as sharing both a type relationship and real, running code: a `User` is a `BaseEntity`, and it inherits `BaseEntity`\'s actual implementation, not just a list of method names it promises to have.',
    },
    {
      id: 'oop-extends-vs-implements',
      title: 'Extends vs implements - the key difference',
      content:
        'This is one of the most commonly confused topics in TypeScript interviews, and the confusion usually comes from the fact that both keywords look like they do the same thing: they both make one type relate to another.\n\n`extends` means "I am a type of this thing, and I inherit everything it has." A `User` that extends `BaseEntity` genuinely is a `BaseEntity`, sharing its real implementation, exactly as the previous lesson showed. `implements` means something much weaker: "I promise to have these methods, but I inherit no code at all." A class that implements `Payable` is not a type of `Payable` in any meaningful sense. It is only committing to a shape.\n\nThe mechanical difference matters too. A class can only `extends` one other class: TypeScript does not support extending two classes at once. A class can `implements` as many interfaces as it needs, since a contract does not carry any implementation that could conflict:\n\n```typescript\nclass PremiumStripePayment implements Payable, Loggable, Auditable {\n  charge(amountCents: number): string { return ""; }\n  refund(transactionId: string): void {}\n  log(message: string): void {}\n  audit(): void {}\n}\n```\n\nChoosing between them comes down to one question: is this genuinely an is-a relationship where you also want to reuse code, or do you only need to guarantee a shape? Use `extends` when a `Transaction` really is a `BaseEntity` and you want `createdAt` and `updatedAt` for free. Use `implements` when you want to force a class to have certain methods without tying it to any shared implementation at all.\n\nNestJS gives a clean real example of `implements` used exactly this way:\n\n```typescript\nimport { Injectable, OnModuleInit } from "@nestjs/common";\n\n@Injectable()\nclass UserService implements OnModuleInit {\n  onModuleInit(): void {\n    console.log("UserService is ready");\n  }\n}\n```\n\n`UserService` is not a type of `OnModuleInit` in any real sense. `OnModuleInit` is an interface with a single method signature, `onModuleInit(): void`. By implementing it, `UserService` is promising NestJS that this method exists, so when NestJS finishes wiring up the module, it can call `onModuleInit()` on every provider that promised to have it. There is zero shared code between `UserService` and `OnModuleInit`, only a guarantee about a method name and signature.\n\nIn an interview, if asked directly what the difference between `extends` and `implements` is, give exactly this answer: `extends` is a real is-a relationship with inherited implementation, and a class can only extend one class. `implements` is a promise to satisfy a shape with zero inherited code, and a class can implement as many interfaces as it needs.',
    },
    {
      id: 'oop-abstract-classes',
      title: 'Abstract classes - halfway between a class and an interface',
      content:
        '`extends` shares all of a parent\'s code. `implements` shares none of it. Sometimes you want something in between: share some code, but force each subclass to fill in the rest itself.\n\nEvery notification channel needs to build a payload (the subject, the body, the timestamp) in largely the same way, but each one sends that payload completely differently: email goes through an SMTP client, SMS goes through a telecom API. Neither a plain base class nor an interface fits perfectly. A base class with a real `send()` implementation would force every channel to share sending logic that does not apply to it. An interface would force every channel to reimplement the payload-building logic from scratch, even though it is identical everywhere.\n\nAn **abstract class** is built for exactly this middle ground: it can hold real, shared implementation, while marking certain methods as `abstract`, meaning it declares their signature but leaves the body for each subclass to fill in.\n\n```typescript\nabstract class NotificationSender {\n  buildPayload(subject: string, body: string): { subject: string; body: string; sentAt: Date } {\n    return { subject, body, sentAt: new Date() };\n  }\n\n  abstract send(payload: { subject: string; body: string; sentAt: Date }): void;\n}\n\nclass EmailSender extends NotificationSender {\n  send(payload: { subject: string; body: string; sentAt: Date }): void {\n    console.log(`Emailing: ${payload.subject}`);\n  }\n}\n\nclass SmsSender extends NotificationSender {\n  send(payload: { subject: string; body: string; sentAt: Date }): void {\n    console.log(`Texting: ${payload.body}`);\n  }\n}\n\nconst email = new EmailSender();\nconst payload = email.buildPayload("Welcome", "Thanks for signing up");\nemail.send(payload);\n```\n\n`buildPayload()` is written once, on `NotificationSender`, and both `EmailSender` and `SmsSender` get it for free through `extends`, exactly like the `BaseEntity` example from two lessons ago. `send()` has no body at all on `NotificationSender`. It only declares what `send` must accept and return, and TypeScript refuses to compile any subclass that forgets to implement it, exactly the way it refused to compile a class missing an interface method.\n\nThe one thing you can never do is create a `NotificationSender` directly:\n\n```typescript\nconst sender = new NotificationSender(); // compile error: cannot create an instance of an abstract class\n```\n\nAn abstract class exists only to be extended. It is deliberately incomplete, missing the one piece (`send`, in this case) that only makes sense once you know exactly which kind of sender you are building.\n\nIn an interview, the clean distinction is that an interface has zero implementation and can be implemented by any class, while an abstract class can hold real shared code and can only be extended, never instantiated on its own, by classes willing to fill in whatever pieces it left abstract.',
    },
    {
      id: 'oop-encapsulation',
      title: 'Encapsulation - hiding what should not be touched',
      content:
        'Access modifiers from lesson 3 already touched on hiding data. Encapsulation is the name for that idea taken all the way, and a rate limiter shows exactly why it matters.\n\nConsider a rate limiter. Other parts of your system only need to ask: can this user make another request? They should not be able to directly mess with the internal counters or reset the time window.\n\n```typescript\nclass RateLimiter {\n  requestCount = 0;\n  windowStart = Date.now();\n}\n\nconst limiter = new RateLimiter();\nlimiter.requestCount = -100; // nothing stops this\nlimiter.windowStart = 0; // or this\n```\n\nHere is what happens without encapsulation: anyone can set `requestCount` to `-100` or reset the window manually, putting the object into a state that should never be possible. There is no code anywhere that enforces "requestCount can never go negative" or "the window resets on a fixed schedule", because every field sits wide open for any caller to touch.\n\n```typescript\nclass RateLimiter {\n  private requestCount = 0;\n  private windowStart = Date.now();\n\n  constructor(private readonly limit: number, private readonly windowMs: number) {}\n\n  allowRequest(): boolean {\n    this.resetWindowIfExpired();\n    if (this.requestCount >= this.limit) return false;\n    this.requestCount++;\n    return true;\n  }\n\n  getRemainingRequests(): number {\n    this.resetWindowIfExpired();\n    return Math.max(0, this.limit - this.requestCount);\n  }\n\n  private resetWindowIfExpired(): void {\n    if (Date.now() - this.windowStart >= this.windowMs) {\n      this.requestCount = 0;\n      this.windowStart = Date.now();\n    }\n  }\n}\n```\n\nWith encapsulation, nobody can corrupt the internal state. The only way to interact with the limiter is through `allowRequest()` and `getRemainingRequests()`. If you later switch from a fixed window to a sliding window algorithm, none of the calling code needs to change, because `requestCount` and `windowStart` can be replaced entirely with a different internal representation and every caller keeps working exactly as before.\n\nThis is called encapsulation - the practice of hiding an object\'s internal state behind a small set of methods that are the only legal way to change it. Encapsulation is not really about making fields private for its own sake. It is about protecting **invariants**, rules that must always hold true about an object\'s state, like "requestCount never goes negative" or "balance never drops below zero" from lesson 3. A private field with no validation logic behind it protects nothing. A private field guarded by a method that checks a rule before changing it is what actually keeps the object valid.\n\nIn an interview, if asked to define encapsulation, do not stop at "hiding data". Say that encapsulation protects invariants by making the public methods the only doors into an object\'s state, and each of those doors enforces a rule that guarantees the object never ends up in a state it should not be able to reach.',
    },
    {
      id: 'oop-inheritance-deep-dive',
      title: 'Inheritance deep dive - what you actually inherit',
      content:
        'Lesson 7 introduced `extends`. This lesson goes deeper into exactly what gets inherited, and where inheritance can start to hurt.\n\nWhen `User extends BaseEntity`, JavaScript wires the two classes together through something called the **prototype chain**. Every method on `BaseEntity` lives on `BaseEntity.prototype`, and `User.prototype` is linked to point at `BaseEntity.prototype`. When you call `user.describe()`, JavaScript looks for `describe` on `user` itself first, then walks up to `User.prototype`, then up to `BaseEntity.prototype`, until it finds a match. This is what `extends` actually sets up behind the scenes.\n\nNot everything makes the trip up that chain. Public and protected members are inherited normally, but private members are not visible to a subclass at all, even though they still technically exist on the parent portion of the object:\n\n```typescript\nclass BaseEntity {\n  private internalId = Math.random();\n  protected id: string;\n\n  constructor(id: string) {\n    this.id = id;\n  }\n}\n\nclass User extends BaseEntity {\n  showId(): void {\n    console.log(this.id); // fine, protected is visible to subclasses\n    console.log(this.internalId); // compile error, private is not visible here\n  }\n}\n```\n\nInheritance can go more than one level deep. `Dog extends Animal`, and `Animal extends BaseEntity`, means a `Dog` inherits from both of its ancestors:\n\n```typescript\nclass Animal extends BaseEntity {\n  constructor(id: string, public species: string) {\n    super(id);\n  }\n}\n\nclass Dog extends Animal {\n  constructor(id: string, public breed: string) {\n    super(id, "Canine");\n  }\n}\n\nconst rex = new Dog("d1", "Labrador");\nconsole.log(rex instanceof Dog); // true\nconsole.log(rex instanceof Animal); // true\nconsole.log(rex instanceof BaseEntity); // true\n```\n\n`instanceof` checks the entire chain, not just the immediate parent, which is why `rex` is reported as an instance of all three classes at once.\n\nThe deeper a hierarchy gets, the more fragile it becomes. This is known as the **fragile base class problem**: a change to `BaseEntity`, like renaming `id` or changing what `createdAt` means, can silently break `Animal`, `Dog`, and every other subclass down the chain, because they all depend on assumptions about how the base class behaves internally. Someone maintaining `Dog` might have no idea that `BaseEntity`, two levels up, changed at all, until something breaks in production. This is exactly why "favour composition over inheritance", the subject of the next lesson, is repeated so often in software engineering: the deeper the chain, the more classes silently depend on internal behaviour they never directly wrote or reviewed.\n\nIn an interview, describe the prototype chain as the mechanism `extends` sets up to look up inherited methods, and describe the fragile base class problem as the real cost of inheritance: the more levels deep a hierarchy goes, the more classes are silently coupled to a base class\'s internal behaviour, and the higher the risk that changing it breaks something far away that nobody was looking at.',
    },
    {
      id: 'oop-polymorphism',
      title: 'Polymorphism - one type, many shapes',
      content:
        'The prototype chain lets an object be treated as its base type. Polymorphism is what makes that actually useful: one piece of code that works correctly no matter which specific subtype it is handed.\n\nThree completely different notification channels, email, Slack, and SMS, all need to be triggered from the same place when an order ships:\n\n```typescript\ninterface NotificationChannel {\n  send(message: string): void;\n}\n\nclass EmailChannel implements NotificationChannel {\n  send(message: string): void {\n    console.log(`Email: ${message}`);\n  }\n}\n\nclass SlackChannel implements NotificationChannel {\n  send(message: string): void {\n    console.log(`Slack: ${message}`);\n  }\n}\n\nclass SmsChannel implements NotificationChannel {\n  send(message: string): void {\n    console.log(`SMS: ${message}`);\n  }\n}\n\nconst channels: NotificationChannel[] = [new EmailChannel(), new SlackChannel(), new SmsChannel()];\n\nfunction notifyAll(channels: NotificationChannel[], message: string): void {\n  for (const channel of channels) {\n    channel.send(message);\n  }\n}\n\nnotifyAll(channels, "Your order has shipped");\n```\n\n`notifyAll` calls `channel.send(message)` once, in one place, but that single line behaves differently on every loop iteration: `EmailChannel.send`, then `SlackChannel.send`, then `SmsChannel.send`, depending entirely on which object actually sits in that slot of the array at that moment. `notifyAll` itself contains no branching logic at all, no `if (channel instanceof SlackChannel)` anywhere. It just trusts that every object in the array satisfies `NotificationChannel`.\n\nThis is called **polymorphism**: writing code against a general type, where the actual behaviour at runtime depends on the specific type of the object handed to it. It only works because every channel commits to the same `NotificationChannel` contract. Without that shared interface, `notifyAll` would have no way to know that every object in the array has a `send` method at all.\n\nDiscriminated unions are a different flavour of the same idea. Instead of many classes sharing one interface, one type carries a tag that tells you exactly which shape you are looking at:\n\n```typescript\ntype PaymentEvent =\n  | { kind: "charge"; amountCents: number }\n  | { kind: "refund"; transactionId: string };\n\nfunction handleEvent(event: PaymentEvent): void {\n  switch (event.kind) {\n    case "charge":\n      console.log(`Charged ${event.amountCents} cents`);\n      break;\n    case "refund":\n      console.log(`Refunded ${event.transactionId}`);\n      break;\n  }\n}\n```\n\n`handleEvent` behaves differently depending on the actual shape of `event` it receives, exactly like `notifyAll` behaved differently depending on the actual channel it received. The mechanism is different, a tag and a switch instead of an interface and a method call, but the underlying idea, one function handling many shapes correctly, is the same.\n\nIn an interview, give a concrete example of why this matters: adding a fourth notification channel, like push notifications, means writing one new class that implements `NotificationChannel`. `notifyAll` never changes, because it was written against the interface, not against any specific channel. That is what makes polymorphism valuable in practice: new variants can be added without touching the code that already works.',
    },
    {
      id: 'oop-composition-vs-inheritance',
      title: 'Composition vs inheritance',
      content:
        '`Dog extends Animal`, and `Animal extends LivingThing`, feels natural at first. Every `Dog` really is an `Animal`, and every `Animal` really is a `LivingThing`. Then the product needs a `Robot` that can also move around a warehouse floor, using the exact same `move()` method every `Animal` has. A `Robot` is not a `LivingThing` in any real sense, so it cannot extend `Animal` without the class hierarchy lying about what a robot actually is.\n\n```typescript\nclass LivingThing {\n  breathe(): void {}\n}\n\nclass Animal extends LivingThing {\n  move(): void {\n    console.log("Walking");\n  }\n}\n\nclass Dog extends Animal {}\n\n// A Robot is not a LivingThing, so it cannot fit into this hierarchy honestly\nclass Robot extends Animal {\n  // now Robot.breathe() exists, even though robots do not breathe\n}\n```\n\nThis is the trap rigid inheritance hierarchies fall into: as soon as a new type needs some, but not all, of the behaviour further up the chain, the hierarchy either grows incorrect (a `Robot` that breathes) or has to be redesigned from scratch.\n\n**Composition** solves this by describing what something can do through small, focused interfaces, rather than what something is through a deep chain of classes:\n\n```typescript\ninterface Movable {\n  move(): void;\n}\n\nclass Dog implements Movable {\n  move(): void {\n    console.log("Running");\n  }\n}\n\nclass Robot implements Movable {\n  move(): void {\n    console.log("Rolling forward");\n  }\n}\n\nfunction moveAll(movables: Movable[]): void {\n  for (const m of movables) m.move();\n}\n\nmoveAll([new Dog(), new Robot()]);\n```\n\n`Dog` and `Robot` share the ability to move without either one pretending to be a type of the other. Neither is forced into a hierarchy that does not honestly describe what it is. Each one composes the `Movable` behaviour into itself through `implements`, rather than inheriting it from a shared ancestor that may not apply to both.\n\nNestJS services follow exactly this pattern, and it is easy to miss because it looks so ordinary. A service does not extend the services it depends on. It receives them through the constructor, as dependencies, and composes their behaviour into its own:\n\n```typescript\n@Injectable()\nclass OrdersService {\n  constructor(\n    private readonly paymentsService: PaymentsService,\n    private readonly notificationsService: NotificationsService,\n  ) {}\n\n  async placeOrder(dto: CreateOrderDto) {\n    await this.paymentsService.charge(dto.amountCents);\n    await this.notificationsService.notifyAll("Order placed");\n  }\n}\n```\n\n`OrdersService` is not a type of `PaymentsService`, and it does not extend it. It simply holds a reference to one and calls its methods, composing behaviour from several independent services rather than inheriting it from a single rigid ancestor.\n\nIn an interview, "favour composition over inheritance" comes up constantly, and the sharp answer is: choose inheritance when there is a genuine, stable is-a relationship you want to share real code through, and choose composition when you only need a piece of behaviour without committing to a rigid hierarchy that might not fit every future case, like a `Robot` that needs to move but is not, and never will be, a `LivingThing`.',
    },
    {
      id: 'oop-solid',
      title: 'SOLID in 5 minutes - the interview version',
      content:
        'SOLID is five separate principles, but in an interview you rarely get five minutes each. Here is enough on every one to answer confidently, in the order the letters spell out.\n\n**Single Responsibility**: a class should have one reason to change. A `UserService` that registers users and also sends welcome emails has two reasons to change: a new password rule, or a new email template. Split it:\n\n```typescript\nclass UserService {\n  register(dto: RegisterDto): void { /* ... */ }\n}\n\nclass EmailService {\n  sendWelcomeEmail(email: string): void { /* ... */ }\n}\n```\n\n**Open/Closed**: a class should be open for extension but closed for modification. Add new payment methods by adding new classes that implement `Payable` from lesson 6, not by adding another branch to an existing switch statement inside a class every payment method already depends on:\n\n```typescript\nclass ApplePayPayment implements Payable {\n  charge(amountCents: number): string {\n    return `apple-pay-${amountCents}`;\n  }\n  refund(transactionId: string): void { /* ... */ }\n}\n```\n\n**Liskov Substitution**: a subclass should work correctly anywhere its parent is expected. If `ReadOnlyList` extends `Array` but throws on `push()`, any code written against `Array` that tries to push into a `ReadOnlyList` breaks at runtime, even though the type system said it was fine. A subclass that cannot honestly support everything the parent promises should not extend it.\n\n**Interface Segregation**: do not force a class to implement methods it does not need. A twenty-method `IRepository` interface forces a service that only ever reads data to depend on fifteen write methods it will never call. Split it into `IReadRepository` and `IWriteRepository` instead, so each consumer only depends on what it actually uses.\n\n**Dependency Inversion**: depend on interfaces, not concrete implementations. A `UserService` that constructs `new TypeOrmUserRepository()` directly is locked to that one database library. A `UserService` that depends on an `IUserRepository` interface instead can be handed any implementation, a real database, an in-memory fake for tests, without changing a single line of `UserService` itself:\n\n```typescript\ninterface IUserRepository {\n  save(user: User): Promise<User>;\n}\n\n@Injectable()\nclass UserService {\n  constructor(@Inject("USER_REPOSITORY") private readonly repository: IUserRepository) {}\n}\n```\n\nThis is exactly what NestJS\'s dependency injection system does automatically for every `@Injectable()` class, which the final lesson of this course walks through directly.\n\nIn an interview, if asked to explain SOLID, give the one-line version of each in order, then pick one, usually Dependency Inversion, and go one level deeper, since it connects most directly to how real frameworks like NestJS are built.',
    },
    {
      id: 'oop-nestjs',
      title: 'OOP in NestJS - how it all connects',
      content:
        'Every idea in this course shows up in a single, ordinary NestJS file. Here is a realistic one, explained piece by piece:\n\n```typescript\nimport { Injectable, Inject, OnModuleInit } from "@nestjs/common";\nimport { Entity, PrimaryGeneratedColumn, CreateDateColumn, BaseEntity } from "typeorm";\n\n@Entity()\nclass User extends BaseEntity {\n  @PrimaryGeneratedColumn("uuid")\n  id: string;\n\n  @CreateDateColumn()\n  createdAt: Date;\n\n  email: string;\n}\n\ninterface IUserRepository {\n  findByEmail(email: string): Promise<User | null>;\n  save(user: User): Promise<User>;\n}\n\n@Injectable()\nclass UserService implements OnModuleInit {\n  constructor(@Inject("USER_REPOSITORY") private readonly repository: IUserRepository) {}\n\n  async register(email: string): Promise<User> {\n    const user = new User();\n    user.email = email;\n    return this.repository.save(user);\n  }\n\n  onModuleInit(): void {\n    console.log("UserService is ready");\n  }\n}\n```\n\n`class UserService` and `class User` are ordinary classes, exactly like `BankAccount` from lesson 1: they bundle data and behaviour together. `constructor(@Inject("USER_REPOSITORY") private readonly repository: IUserRepository)` is the shorthand from lesson 4, declaring and assigning `repository` as a private field in one line. `@Injectable()` is a decorator, not a keyword from this course, but the effect it has connects directly to lesson 14: it tells NestJS this class can be constructed automatically and handed to whatever else depends on it, exactly the way Dependency Inversion asks for.\n\n`User extends BaseEntity` is the real inheritance from lessons 7 and 11: `BaseEntity` here comes from TypeORM, and it provides the save, update, and delete methods every entity needs, so `User` gets database persistence behaviour for free instead of rewriting it. `UserService implements OnModuleInit` is the interface promise from lessons 6 and 8: `UserService` is not a type of `OnModuleInit`, it only promises to have an `onModuleInit()` method, which NestJS calls automatically the moment the module has finished wiring every provider together.\n\n`IUserRepository` ties directly back to Dependency Inversion from lesson 14: `UserService` never constructs a database repository directly. It depends on the interface, and NestJS\'s dependency injection container decides which concrete class, a real TypeORM repository or an in-memory fake for tests, actually gets handed to it at runtime, based entirely on what the module\'s providers array says. This is the whole reason NestJS applications are built out of classes, interfaces, and decorators in the first place: every class declares what it needs through its constructor, every interface defines a contract that a concrete implementation must satisfy, and the framework wires the two together automatically, using exactly the mechanisms this course walked through one lesson at a time.\n\nIn an interview, if you are asked to explain a piece of unfamiliar NestJS code, work through it exactly this way: find the classes and ask what data and behaviour each one owns, find every `extends` and ask what shared implementation it is reusing, find every `implements` and ask what contract is being promised, and find every constructor parameter and ask which interface it depends on rather than which concrete class. That is the whole of object-oriented programming, applied to one real file.',
    },
  ],
};
