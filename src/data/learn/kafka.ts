import type { LearnModule } from '../../types';

export const kafka: LearnModule = {
  id: 'kafka',
  title: 'Kafka',
  blurb: 'The log, partitions, consumer groups, and delivery semantics.',
  quizCategory: 'kafka',
  lessons: [
    {
      id: 'kafka-log',
      title: 'The log, partitions, and ordering',
      content:
        'Kafka is a distributed append-only log. A topic is split into partitions, and ordering is guaranteed only within a partition — messages with the same key hash to the same partition, so key by entity id when per-entity order matters. Messages are retained by time or size and consumers track their own offset, which is what makes replay and fan-out to many independent consumer groups possible without copying data. This log model is fundamentally different from a traditional message queue: the broker does not discard a message after delivery, so any consumer group can independently re-read history from any offset. Choosing partition count is an important capacity decision — you cannot reduce it later, and each partition adds leader-election overhead. A rule of thumb is to target partitions that give each consumer in the group roughly equal throughput, then multiply by two for headroom.',
    },
    {
      id: 'kafka-consumer-groups',
      title: 'Consumer groups and rebalancing',
      content:
        'Within a consumer group each partition is owned by exactly one consumer at a time, so the maximum useful parallelism equals the partition count. When a consumer joins or leaves the group, Kafka triggers a rebalance: the group coordinator pauses all consumers, reassigns partitions, and resumes. Eager rebalancing (the classic strategy) revokes all partition assignments during the pause, causing a stop-the-world gap; cooperative (incremental) rebalancing introduced in Kafka 2.4 only moves partitions that need to change, keeping the rest processing continuously. During a rebalance any in-flight work can cause duplicate processing because uncommitted offsets get re-read by the new owner, so consumers should commit offsets frequently and process idempotently. Different consumer groups each receive the full stream independently, which makes Kafka ideal for fan-out to analytics, search indexing, audit logging, and operational consumers simultaneously without any coordination between them.',
    },
    {
      id: 'kafka-offsets-retention',
      title: 'Offsets, commits, retention, and log compaction',
      content:
        'Consumer offsets are stored in the internal __consumer_offsets topic. Auto-commit (enable.auto.commit=true) commits periodically in a background thread, but this can commit an offset before the application has finished processing the corresponding message, losing work on crash. Manual commit after processing (commitSync or commitAsync) gives at-least-once semantics: if the consumer crashes after processing but before committing, it re-reads and re-processes the same message. Retention can be time-based (log.retention.hours, default 168 hours) or size-based (log.retention.bytes). Log compaction is an alternative retention policy that keeps only the latest message per key, making a topic behave like a compacted changelog or materialised view — useful for event-sourced state snapshots that new consumers can bootstrap from. Deleted keys are represented by tombstone messages (null value) which are themselves eventually removed after the tombstone retention period.',
    },
    {
      id: 'kafka-replication',
      title: 'Replication, ISR, and producer acks',
      content:
        'Each partition has one leader and zero or more follower replicas. The in-sync replicas (ISR) set contains the leader plus any followers that are caught up within replica.lag.time.max.ms. The producer acks setting controls durability: acks=0 fires and forgets; acks=1 waits for the leader to write (loses data if the leader crashes before followers replicate); acks=all (or -1) waits for all ISR members to write, which is the only setting that prevents data loss during a clean leader failover. min.insync.replicas (typically 2 for a 3-node cluster) sets the minimum ISR size that must acknowledge before the broker accepts the write — if fewer replicas are in sync the produce call is rejected with NotEnoughReplicasException, trading availability for durability. Unclean leader election (unclean.leader.election.enable=false by default since 0.11) prevents an out-of-date replica from becoming leader and introducing data loss or divergence.',
    },
    {
      id: 'kafka-exactly-once',
      title: 'Idempotent producer, transactions, and exactly-once',
      content:
        'The idempotent producer (enable.idempotence=true) assigns each message a producer id and sequence number; the broker deduplicates retries within a single producer session, eliminating duplicate writes caused by network timeouts. Kafka transactions extend this to atomic multi-partition writes: the producer opens a transaction, writes to multiple partitions and/or the offsets topic, then calls commitTransaction — consumers with isolation.level=read_committed see only committed data. Combining the idempotent producer, transactions, and read_committed consumers gives exactly-once stream processing semantics (EOS), which Kafka Streams uses internally. The cost is higher latency from the two-phase commit protocol between the producer and the transaction coordinator. In practice, many applications accept at-least-once with idempotent consumers rather than paying the EOS overhead, especially when side effects are naturally idempotent (upserts, set operations) or when duplicate event rates are very low.',
    },
    {
      id: 'kafka-vs-rabbitmq-outbox',
      title: 'Kafka vs RabbitMQ and the outbox pattern',
      content:
        'Kafka and RabbitMQ solve overlapping but different problems. RabbitMQ is a traditional message broker: it excels at task queues, complex routing (exchange types), per-message TTL, and request/reply patterns, and it is operationally simpler for small-scale deployments. Kafka is optimised for high-throughput event streaming, long-term retention, replay, and fan-out to many independent consumers; it is harder to operate but offers horizontal scale and a durable ordered log that RabbitMQ does not. Choose RabbitMQ for command dispatch and workflow coordination; choose Kafka for event streaming, audit logs, and systems where consumers need to replay history. The outbox pattern solves the dual-write problem in either broker: instead of writing to the database and publishing to the broker in two separate calls (which can fail independently), you write an outbox record in the same database transaction as the business change, then a relay process reads committed outbox rows and publishes them, guaranteeing that a message is published if and only if the database transaction committed. Kafka Connect with Debezium implements this via CDC (change-data capture) on the database transaction log, removing the need for a custom relay.',
    },
    {
      id: 'kafka-streams',
      title: 'Kafka Streams and stateful stream processing',
      content:
        'Kafka Streams is a client library (not a separate cluster) that turns a JVM application into a stream processor. It reads from input topics, applies transformations, and writes results to output topics — all with the delivery guarantees of Kafka itself.\n\nThe core abstraction is the **KStream** — an unbounded sequence of records. A KStream represents event-by-event data: each record is independent, like a log of user clicks.\n\nA **KTable** represents the latest value per key — it is the materialised view of a changelog topic. When a new record arrives for a key, it replaces the old value. A KTable models state: the current balance of each account, the current status of each order.\n\nStateful operations like aggregations and joins require **state stores** — local RocksDB databases embedded in the Streams application. State stores are fault-tolerant: every state change is written to a Kafka changelog topic, so if the application restarts, it replays the changelog to rebuild state without reprocessing the entire input stream.\n\nA simple word count example:\n\n```java\nStreamsBuilder builder = new StreamsBuilder();\nKStream<String, String> textLines = builder.stream("text-input");\ntextLines\n  .flatMapValues(line -> Arrays.asList(line.toLowerCase().split("\\\\W+")))\n  .groupBy((key, word) -> word)\n  .count(Materialized.as("word-counts-store"))\n  .toStream()\n  .to("word-counts-output");\n```\n\n**Windowing** groups records by time to compute aggregates over rolling or tumbling windows. A tumbling window is a fixed, non-overlapping interval — sales per hour, events per 5-minute bucket. A hopping window slides by a step smaller than the window size — a 1-hour window that advances every 10 minutes. A session window groups records with a maximum gap between them — a user session where activity within 30 minutes is grouped together.\n\nKafka Streams applications scale by running multiple instances with the same `application.id`. Partitions are distributed across instances, and each instance owns a subset of partitions and their associated state stores — no coordination server required.\n\nIn an interview, Kafka Streams is the answer to "how would you compute real-time aggregations on a Kafka topic?" It is a library, not an infrastructure component, which means it deploys as part of your application and inherits Kafka\'s operational tooling.',
    },
    {
      id: 'kafka-connect-debezium',
      title: 'Kafka Connect and Debezium for change data capture',
      content:
        'Getting data into and out of Kafka reliably — without writing custom producers and consumers — is the job of **Kafka Connect**. It is a distributed framework that runs **connectors**: plugins that read from a source system and write to Kafka (source connectors) or read from Kafka and write to a sink system (sink connectors).\n\nThe Connect cluster is separate from the Kafka brokers and manages connectors via a REST API:\n\n```bash\ncurl -X POST http://kafka-connect:8083/connectors \\\n  -H \'Content-Type: application/json\' \\\n  -d \'{\n    "name": "postgres-source",\n    "config": {\n      "connector.class": "io.debezium.connector.postgresql.PostgresConnector",\n      "database.hostname": "postgres",\n      "database.port": "5432",\n      "database.user": "replication_user",\n      "database.password": "secret",\n      "database.dbname": "mydb",\n      "table.include.list": "public.orders",\n      "topic.prefix": "mydb"\n    }\n  }\'\n```\n\n**Debezium** is a Kafka Connect source connector that implements **change data capture (CDC)**. Instead of polling the database with periodic SELECT queries, Debezium reads the database\'s transaction log (PostgreSQL\'s WAL, MySQL\'s binlog) and streams every insert, update, and delete as a Kafka message — in real time, with no added load on the database.\n\nEach CDC message contains both the before-image and after-image of the changed row, the transaction ID, and the timestamp:\n\n```json\n{\n  "before": { "id": 1, "status": "pending" },\n  "after":  { "id": 1, "status": "shipped" },\n  "op": "u",\n  "ts_ms": 1700000000000\n}\n```\n\nThis is the outbox pattern at the infrastructure level: Debezium reads committed database transactions and publishes them to Kafka, eliminating the dual-write problem without any application-level outbox table.\n\n**Sink connectors** write from Kafka to external systems: a JDBC sink connector writes Kafka messages back to a database, an Elasticsearch sink indexes events for search, an S3 sink archives events for long-term storage. Combining source and sink connectors with Kafka as the hub creates an event-driven data pipeline with no custom code.\n\nIn an interview, CDC with Debezium is the answer to "how do you keep a search index or read model in sync with your database without dual writes?" It is also the correct answer to "how do you implement the outbox pattern without an outbox table?"',
    },
    {
      id: 'kafka-schema-registry',
      title: 'Schema Registry and Avro — evolving message schemas safely',
      content:
        'A Kafka topic is a byte stream. Without a schema, producers and consumers must agree out-of-band about what those bytes mean. When schemas change — a new field added, an old field renamed — consumers break silently.\n\nThe **Confluent Schema Registry** solves this by maintaining a versioned registry of schemas. Producers register a schema before publishing, and consumers fetch the schema to deserialise messages. Every message includes only a small schema ID (4 bytes) rather than the full schema, keeping messages compact.\n\n**Apache Avro** is the most common serialisation format used with Schema Registry. Avro schemas are defined in JSON:\n\n```json\n{\n  "type": "record",\n  "name": "OrderCreated",\n  "namespace": "com.example.events",\n  "fields": [\n    { "name": "orderId", "type": "string" },\n    { "name": "userId", "type": "string" },\n    { "name": "amount", "type": "double" },\n    { "name": "status", "type": { "type": "enum", "name": "Status",\n      "symbols": ["PENDING", "CONFIRMED", "SHIPPED"] } }\n  ]\n}\n```\n\nSchema Registry enforces **compatibility rules** when a new schema version is registered. The most important rules:\n- **BACKWARD**: new schema can read data written with the old schema. Consumers can be upgraded first.\n- **FORWARD**: old schema can read data written with the new schema. Producers can be upgraded first.\n- **FULL**: both backward and forward. The safe default for most teams.\n\nUnder FULL compatibility, you can safely add a new optional field with a default value. You cannot remove a required field or change a field\'s type — Schema Registry rejects the registration.\n\nThis enforces a contract between teams: the producer team cannot ship a breaking schema change without Schema Registry rejecting it, giving consumer teams time to adapt.\n\nIn NestJS, use the `@confluentinc/schemaregistry` client alongside the Kafka transport. Produce and consume messages with the registered Avro schema, letting the library handle serialisation, schema lookup, and deserialisaton automatically.\n\nIn an interview, Schema Registry is the answer to "how do you manage schema evolution across many Kafka consumers?" Show that you understand the three compatibility modes and can explain which one to choose for a given deployment strategy (consumer-first vs producer-first upgrade).',
    },
    {
      id: 'kafka-nestjs',
      title: 'Kafka with NestJS microservices transport',
      content:
        'NestJS has first-class Kafka support through `@nestjs/microservices`. Understanding its wiring helps you build reliable event-driven services and debug problems in production.\n\nOn the **consumer side**, create a microservice with the Kafka transport:\n\n```typescript\nconst app = await NestFactory.createMicroservice<MicroserviceOptions>(AppModule, {\n  transport: Transport.KAFKA,\n  options: {\n    client: {\n      clientId: \'orders-consumer\',\n      brokers: [process.env.KAFKA_BROKER],\n    },\n    consumer: {\n      groupId: \'orders-consumer-group\',\n    },\n  },\n});\nawait app.listen();\n```\n\nHandle messages with `@MessagePattern` (request/reply) or `@EventPattern` (fire-and-forget):\n\n```typescript\n@Controller()\nexport class OrdersController {\n  @EventPattern(\'order.created\')\n  async handleOrderCreated(@Payload() data: OrderCreatedDto, @Ctx() context: KafkaContext) {\n    const { offset, partition, topic } = context.getMessage();\n    await this.ordersService.process(data);\n  }\n}\n```\n\nNestJS commits the offset after the handler returns successfully. If the handler throws, the offset is not committed and the message is reprocessed on the next poll — giving at-least-once delivery. Make your handlers idempotent.\n\nOn the **producer side**, register a client and inject it:\n\n```typescript\n// In module imports:\nClientsModule.register([{\n  name: \'KAFKA_SERVICE\',\n  transport: Transport.KAFKA,\n  options: {\n    client: { clientId: \'orders-producer\', brokers: [process.env.KAFKA_BROKER] },\n    producer: { allowAutoTopicCreation: false },\n  },\n}])\n\n// In service:\nconstructor(@Inject(\'KAFKA_SERVICE\') private readonly kafka: ClientKafka) {}\n\nasync onModuleInit() {\n  await this.kafka.connect();\n}\n\nasync publishOrderCreated(order: Order) {\n  this.kafka.emit(\'order.created\', { key: order.id, value: order });\n}\n```\n\nKey production considerations: set `allowAutoTopicCreation: false` in the producer — auto-created topics get default settings (one partition, no replication) which are wrong for production. Create topics with the correct partition count and replication factor ahead of time using the Kafka admin API or Terraform.\n\nIn an interview, be ready to explain the difference between `send()` and `emit()` in NestJS Kafka: `send()` uses `MessagePattern` and expects a reply (unusual for Kafka, more natural for RPC transports like RabbitMQ), while `emit()` uses `EventPattern` and is fire-and-forget — the correct choice for Kafka event streaming.',
    },
    {
      id: 'kafka-metrics-consumer-lag',
      title: 'Key Kafka metrics and consumer lag monitoring',
      content:
        'Running Kafka in production without monitoring is like flying blind. The metrics that matter most are consumer lag, broker disk usage, and under-replicated partition counts.\n\n**Consumer lag** is the most critical operational metric. It measures how many messages a consumer group is behind the latest offset on each partition. Zero lag means the consumer is keeping up in real time. Growing lag means the consumer is slower than the producer — work is piling up.\n\nMonitor lag with the Kafka consumer groups command:\n\n```bash\nkafka-consumer-groups.sh \\\n  --bootstrap-server kafka:9092 \\\n  --describe \\\n  --group orders-consumer-group\n```\n\nThis outputs each partition\'s current offset, the log-end offset, and the lag (difference). In production, export this to Prometheus using `kafka-lag-exporter` or `Burrow`, then alert when lag exceeds a threshold (e.g. > 10,000 messages for a high-priority consumer group).\n\n**Under-replicated partitions (URP)** count the number of partition replicas that are not in sync with the leader. A non-zero URP is an immediate alert — it means some partitions have reduced redundancy and a broker failure could cause data loss. The metric is available at the broker level via JMX: `kafka.server:type=ReplicaManager,name=UnderReplicatedPartitions`.\n\n**Broker disk usage**: Kafka retains messages on disk until the retention policy expires. Monitor disk usage per broker and alert before it fills — a full disk causes the broker to shut down. Size disks for at least 110% of `(retention_hours * throughput_bytes_per_second * replication_factor)`.\n\n**Producer request rate** and **consumer fetch rate** show the throughput flowing through the cluster. A sudden drop in producer rate without a corresponding consumer lag drop means producers are down, not consumers catching up.\n\n**Request latency** (p99 produce latency and p99 fetch latency) should stay below 10ms for healthy clusters. Spikes correlate with leader elections, disk I/O pressure, or network congestion.\n\nSet up Kafka with Prometheus JMX exporter and a Grafana dashboard (the Confluent community dashboard is a good starting point) before the first production deployment, not after the first outage.\n\nIn an interview, consumer lag is the answer to "how do you know if your Kafka-based system is healthy?" Show that you understand lag is per-consumer-group-per-partition, not a single global number, and that the right alert threshold depends on the expected processing latency and the maximum acceptable staleness for that consumer\'s downstream system.',
    },
  ],
};
