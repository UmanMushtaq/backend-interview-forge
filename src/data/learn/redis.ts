import type { LearnModule } from '../../types';

export const redis: LearnModule = {
  id: 'redis',
  title: 'Redis',
  blurb: 'Caching patterns, distributed locks, eviction, and persistence.',
  quizCategory: 'redis',
  lessons: [
    {
      id: 'redis-caching',
      title: 'Caching patterns and pitfalls',
      content:
        'The cache-aside pattern has the application read from the cache, fall back to the database on a miss, and populate the cache with a TTL. The main hazards are stale data (mitigated by TTL and invalidation on write) and the cache stampede, where a hot key expires and many requests hit the database at once — solved with a per-key lock, probabilistic early expiry, or serving stale while one worker refreshes. Pick an eviction policy like allkeys-lru for a pure cache. Cache penetration is a different hazard: requests for keys that will never exist in the cache keep hitting the database; mitigate it by caching negative results (a sentinel value like an empty string) or using a Bloom filter at the edge to reject obviously invalid lookups. Write-through and write-behind caches keep the cache in sync with the database, trading write latency for read consistency — choose the pattern based on whether your workload is read-heavy or write-heavy.',
    },
    {
      id: 'redis-data-structures',
      title: 'Data structures: choosing the right type',
      content:
        'Redis is not a plain key-value store — each key holds a typed data structure, and choosing the right one is a core interview topic. Strings store bytes, integers, or serialised JSON; INCR and INCRBY are atomic, making strings ideal for counters and rate limiters. Hashes represent objects as field-value maps and are memory-efficient for small objects because Redis uses a ziplist encoding internally below a configurable threshold. Lists are ordered, doubly linked sequences supporting O(1) push and pop at both ends; BLPOP enables blocking queue consumers. Sets enforce uniqueness and support O(1) membership tests plus set operations (union, intersection, difference) in O(n) — useful for storing user tags or friend lists. Sorted sets (zsets) attach a float score to each member and keep members sorted; ZADD, ZRANGE, and ZRANGEBYSCORE power leaderboards and time-series windowed queries. Streams (added in Redis 5) provide a persistent, consumer-group-aware log with at-least-once delivery semantics, making them a lightweight alternative to Kafka for modest throughput requirements.',
    },
    {
      id: 'redis-locks',
      title: 'Distributed locks and the single thread',
      content:
        'Redis executes commands on a single thread, so each command is atomic and you rarely need locks for one key. A simple distributed lock is SET key token NX EX 30: NX gives mutual exclusion, EX auto-expires if the holder crashes, and a unique token lets the owner release safely with a compare-and-delete Lua script. For multi-node safety the Redlock algorithm requires acquiring the lock on a majority of N independent Redis nodes within a validity window; if the total elapsed time exceeds the lock TTL, the acquisition is abandoned. Redlock is debated — Martin Kleppmann argues that even Redlock is unsafe without a fencing token (a monotonically increasing number appended to writes so the storage backend can reject late writes). In practice, a single well-configured Redis instance with appropriate TTLs is sufficient for most distributed lock use cases, and the added complexity of Redlock is only justified when the Redis instance itself is a single point of failure.',
    },
    {
      id: 'redis-persistence-eviction',
      title: 'Persistence: RDB, AOF, and eviction policies',
      content:
        'Redis offers two persistence mechanisms. RDB (Redis Database) snapshots the entire dataset to disk at configurable intervals using a fork-and-write approach; snapshots are compact and fast to restore but you can lose up to the last snapshot interval of writes. AOF (Append-Only File) logs every write command and can be configured to fsync on every write (no data loss), every second (at most one second of loss), or never (OS decides); AOF files grow large and are periodically rewritten in the background with BGREWRITEAOF. For production, combining both (AOF for durability, RDB for fast restarts) is recommended. Eviction policies define what Redis does when maxmemory is reached: noeviction returns errors on writes (suitable for a primary database), allkeys-lru evicts the least-recently used key from the entire keyspace (suitable for a pure cache), and volatile-lru only evicts keys that have a TTL set (suitable for a mixed workload). In interviews, demonstrate that you understand the trade-off: allkeys-lru keeps the most popular data in memory but can evict data you wanted to keep; set maxmemory conservatively and monitor evicted_keys in INFO stats.',
    },
    {
      id: 'redis-pipelines-transactions-lua',
      title: 'Pipelines, transactions, and Lua scripts',
      content:
        'A Redis pipeline batches multiple commands into one network round trip, dramatically reducing latency for bulk operations; the client sends all commands at once and reads all replies together. Pipelines do not guarantee atomicity — other clients can interleave commands between pipeline commands. MULTI/EXEC transactions provide atomicity in the sense that no other client\'s commands execute between MULTI and EXEC, but Redis does not roll back on command errors (a wrong type error in the middle of a transaction still commits the successful commands). Lua scripts executed with EVAL are truly atomic: Redis runs the entire script without interruption, which makes them the right tool for compare-and-swap patterns like releasing a lock only if the token matches. WATCH implements optimistic transactions: it monitors keys and causes EXEC to fail if any watched key changed, letting the application retry. In interviews, the key point is to match the tool to the consistency need: pipeline for throughput, MULTI/EXEC for isolation without rollback, Lua for atomic read-modify-write.',
    },
    {
      id: 'redis-cluster-replication',
      title: 'Pub/Sub, cluster, and replication',
      content:
        'Redis Pub/Sub provides at-most-once messaging: publishers send to a channel and all subscribers receive the message, but if a subscriber is offline it misses the message entirely. Streams (XADD, XREAD, XREADGROUP) solve this with persistence and consumer groups — each group tracks a last-read ID, and unacknowledged messages can be reclaimed, giving at-least-once delivery. Redis Replication is asynchronous by default: a primary streams commands to one or more replicas, which can serve read traffic. Redis Sentinel monitors the primary and coordinates automatic failover if it becomes unreachable, promoting a replica in roughly 10-30 seconds. Redis Cluster shards data across up to 1000 nodes using consistent hashing over 16384 hash slots; each slot is assigned to a primary, which may have replicas. The CLUSTER KEYSLOT command reveals which slot a key maps to; keys in the same hash tag (the substring between curly braces) always land on the same slot, which is important when a Lua script or transaction needs to operate on multiple keys. In interviews, be clear that Redis Cluster is a horizontal scaling solution, not a high-availability solution by itself — you still need replicas per shard for HA.',
    },
    {
      id: 'redis-streams-deep',
      title: 'Redis Streams: persistent messaging with consumer groups',
      content:
        'Redis Streams, introduced in version 5.0, are an append-only log data structure that solves the fundamental limitation of Pub/Sub — message durability and delivery guarantees. Every entry added with XADD gets an auto-generated monotonic ID in the format `<milliseconds>-<sequence>`, for example `1700000000000-0`. This ID doubles as a timestamp cursor, enabling range queries with XRANGE and XREVRANGE. Unlike lists, entries are never removed from a stream unless explicitly trimmed with XLEN/XTRIM or a MAXLEN argument on XADD.\n\nConsumer groups are the key feature that enables Kafka-like at-least-once delivery. You create a group with `XGROUP CREATE stream-key group-name $ MKSTREAM`, where `$` means start from new messages. Multiple consumers in the same group share the message workload — each message is delivered to exactly one consumer in the group. Consumers read with XREADGROUP, and after processing they call XACK to acknowledge. Unacknowledged messages remain in the group\'s Pending Entries List (PEL) and can be reclaimed with XCLAIM after a timeout, enabling automatic failover when a consumer crashes mid-processing.\n\nIn production, Streams are a natural fit for audit logs, activity feeds, and event-driven architectures at moderate scale (millions of messages per day rather than billions). For a payment system, every wallet transaction gets XADD-ed to a `wallet:transactions` stream. A notification service consumer group reads and sends confirmations. A fraud detection group reads the same stream independently. Both groups maintain their own position cursors without interfering with each other. The key difference from Kafka: Streams are stored in Redis memory (with optional persistence), making them unsuitable for very large datasets but excellent when you need low-latency event processing without a separate Kafka cluster. Always set MAXLEN with the `~` approximate trimming flag (`XADD stream MAXLEN ~ 100000 * field value`) to keep memory bounded without the performance hit of exact trimming.',
    },
    {
      id: 'redis-hyperloglog-bitmaps',
      title: 'HyperLogLog, Bitmaps, and probabilistic data structures',
      content:
        'Redis ships several probabilistic and space-efficient data structures that solve specific problems no other type handles as efficiently. Understanding when to reach for them separates a Redis novice from a senior engineer.\n\nHyperLogLog is a probabilistic cardinality estimator: PFADD adds elements and PFCOUNT returns the approximate number of unique elements, using only 12 KB of memory regardless of the cardinality. The error margin is 0.81%. This is perfect for counting unique visitors per page without storing every user ID — a problem where a plain set would grow unboundedly. You can merge multiple HyperLogLogs with PFMERGE to compute union cardinalities across time windows.\n\nBitmaps are not a separate type — they are bit-level operations on String values. SETBIT, GETBIT, and BITCOUNT operate on individual bits, enabling dense boolean storage. The classic use case is tracking daily active users: key `active:2024-01-15` has bit N set if user N was active on that date. BITCOUNT returns the count of active users for that day. BITOP AND/OR/XOR between two bitmap keys computes users active on both days (AND) or either day (OR). Storing 10 million user flags in a bitmap uses only 1.25 MB — compared to a Set which would use ~300 MB for 10M string user IDs.\n\nGeo commands (GEOADD, GEODIST, GEORADIUS) store latitude/longitude pairs encoded as sorted set scores using the Geohash algorithm. GEORADIUS returns all points within a given radius, which powers location-aware features without a spatial database extension. The encoding is lossy at ~0.6cm accuracy, which is more than sufficient for "find nearby restaurants" queries.\n\nIn interviews, the question "how would you count unique visitors at scale with minimal memory?" is a direct HyperLogLog opportunity. Demonstrate you know the error bounds and when approximate counting is acceptable — for analytics dashboards, 0.81% error is fine; for billing, you need exact counts from a real counter.',
    },
    {
      id: 'redis-rate-limiting',
      title: 'Rate limiting patterns in Redis',
      content:
        'Rate limiting is one of the most common Redis use cases in production APIs, and interviewers frequently ask you to implement one from scratch. There are three main algorithms, each with different guarantees.\n\nThe fixed window counter is the simplest: `INCR rate:user123:2024-01-15T10:00` increments a per-minute key, and `EXPIRE` sets it to 60 seconds if it is newly created. The problem is the boundary burst — a user can make max requests at 10:00:59 and max requests again at 10:01:01, doubling the effective rate for a two-second window. In code: `const count = await redis.incr(key); if (count === 1) await redis.expire(key, 60); if (count > limit) throw new TooManyRequestsError();`\n\nThe sliding window log stores a sorted set of request timestamps per user. ZADD adds the current timestamp as a score, ZREMRANGEBYSCORE removes timestamps older than the window, and ZCARD returns the current count. This is perfectly accurate but uses O(requests) memory per user. For APIs with low-per-user limits this is fine; for high-volume public APIs the memory cost is prohibitive.\n\nThe sliding window counter approximates a sliding window using two fixed window buckets and a weighted average: `count = current_bucket_count + previous_bucket_count * (1 - elapsed_in_current_window / window_size)`. This uses O(1) memory, is accurate within 0.1% in practice, and is the algorithm used by Cloudflare and Nginx. Implementation requires two keys per user per window, swapped atomically with a Lua script to prevent race conditions between reading the previous bucket and writing the current one.\n\nFor distributed rate limiting across multiple API gateway instances, all instances must share the same Redis, and the Lua script must be atomic. A token bucket variant using INCRBY and a background job that replenishes credits on a schedule is another production-grade approach. Always return `X-RateLimit-Remaining` and `Retry-After` headers to help clients back off gracefully.',
    },
    {
      id: 'redis-production-ops',
      title: 'Redis in production: monitoring, tuning, and memory management',
      content:
        'Running Redis reliably in production requires understanding the operational knobs beyond basic commands. The INFO command is your observability entry point: `INFO memory` shows `used_memory`, `mem_fragmentation_ratio` (above 1.5 means fragmentation is wasting significant memory — restart or use MEMORY PURGE), and `maxmemory_human`. `INFO stats` shows `evicted_keys` (nonzero means maxmemory is being hit and data is being lost), `keyspace_hits` and `keyspace_misses` (use these to calculate hit rate; below 95% for a cache is concerning), and `total_commands_processed`.\n\nThe slowlog records commands that exceed a configurable threshold (default 10ms): `SLOWLOG GET 10` returns the last 10 slow commands with their execution time and arguments. This is invaluable for diagnosing latency spikes — a single `KEYS *` or `SMEMBERS` on a set with millions of members blocks all other commands because Redis is single-threaded. Replace KEYS with SCAN (cursor-based, non-blocking, safe for production) and large SMEMBERS with SSCAN.\n\nMemory optimization is critical because Redis stores everything in RAM. Use `OBJECT ENCODING key` to inspect the internal encoding — Redis automatically uses compact encodings (ziplist, intset) for small collections and switches to hashtable/skiplist/quicklist when they grow. Configure `hash-max-ziplist-entries 128` and `hash-max-ziplist-value 64` to keep hashes compact. The `MEMORY USAGE key` command returns the total bytes a key occupies including overhead.\n\nLazy freeing (`lazyfree-lazy-eviction yes`) is critical for production: without it, evicting a key that holds a large sorted set causes a synchronous O(n) deletion that blocks the main thread. With lazy freeing, the memory is reclaimed in a background thread. Similarly, `UNLINK` is the non-blocking alternative to `DEL` for large keys. In Kubernetes, set `maxmemory` to 80% of the container\'s memory limit so the Redis process does not get OOM-killed — Redis needs headroom for its own overhead and for fork operations during RDB snapshots.',
    },
    {
      id: 'redis-nestjs',
      title: 'Redis with NestJS — ioredis, caching interceptor, and session storage',
      content:
        'NestJS does not include Redis out of the box, but integrating it is straightforward with `ioredis` — the most feature-complete Redis client for Node.js.\n\nSet up a Redis provider using a custom factory:\n\n```typescript\n// redis.module.ts\n@Module({\n  providers: [{\n    provide: \'REDIS_CLIENT\',\n    useFactory: () => new Redis({\n      host: process.env.REDIS_HOST,\n      port: Number(process.env.REDIS_PORT),\n      maxRetriesPerRequest: 3,\n      lazyConnect: true,\n    }),\n  }],\n  exports: [\'REDIS_CLIENT\'],\n})\nexport class RedisModule {}\n```\n\nInject the client anywhere with `@Inject(\'REDIS_CLIENT\') private readonly redis: Redis`.\n\nFor **caching**, NestJS offers `CacheModule` from `@nestjs/cache-manager` with a Redis store adapter:\n\n```typescript\nCacheModule.registerAsync({\n  isGlobal: true,\n  useFactory: () => ({\n    store: redisStore,\n    host: process.env.REDIS_HOST,\n    port: Number(process.env.REDIS_PORT),\n    ttl: 300, // seconds\n  }),\n})\n```\n\nApply `@UseInterceptors(CacheInterceptor)` to a controller route to cache its response automatically. The cache key defaults to the route URL. Use `@CacheKey(\'my-key\')` and `@CacheTTL(60)` to override per route. For cache invalidation, inject `Cache` from `@nestjs/cache-manager` and call `cacheManager.del(key)` after a mutating operation.\n\nFor **session storage**, the `express-session` middleware with `connect-redis` stores sessions in Redis instead of server memory:\n\n```typescript\nconst RedisStore = connectRedis(session);\napp.use(session({\n  store: new RedisStore({ client: redis }),\n  secret: process.env.SESSION_SECRET,\n  resave: false,\n  saveUninitialized: false,\n  cookie: { maxAge: 86400000, httpOnly: true, secure: true },\n}));\n```\n\nThis lets session data survive server restarts and be shared across multiple API instances — a requirement for horizontal scaling. Each session is stored as a Redis hash keyed by the session ID.\n\nFor **queues**, `bullmq` is the production-grade job queue library built on Redis. It uses sorted sets, lists, and streams to implement delayed jobs, retries, and concurrency limits with at-least-once delivery semantics.\n\nIn an interview, be ready to explain why in-memory caching at the NestJS level (a plain JavaScript Map) is faster but not shareable across instances, whereas Redis-backed caching adds ~0.2ms per operation but works correctly with multiple replicas. The right choice depends on whether the cache key is instance-specific or global.',
    },
    {
      id: 'redis-jwt-blacklist',
      title: 'JWT blacklisting and refresh token rotation in Redis',
      content:
        'JSON Web Tokens are stateless — once issued, they are valid until they expire. But what if a user logs out, changes their password, or is banned? Without a stateful store, you cannot invalidate an active token before its TTL expires.\n\nThe solution is a **JWT blacklist in Redis**. When a user logs out or a token is revoked, write the token\'s `jti` (JWT ID — a unique UUID embedded in the token at creation time) to Redis with an expiry matching the token\'s remaining lifetime:\n\n```typescript\nasync revokeToken(jti: string, expiresAt: Date): Promise<void> {\n  const ttl = Math.floor((expiresAt.getTime() - Date.now()) / 1000);\n  if (ttl > 0) await this.redis.setex(`blacklist:${jti}`, ttl, \'1\');\n}\n```\n\nIn the JWT guard, check the blacklist before accepting a token:\n\n```typescript\nasync validate(payload: JwtPayload): Promise<User> {\n  const isBlacklisted = await this.redis.exists(`blacklist:${payload.jti}`);\n  if (isBlacklisted) throw new UnauthorizedException(\'Token revoked\');\n  return this.usersService.findOne(payload.sub);\n}\n```\n\nThe blacklist entry expires automatically when the token would have expired anyway — so the set never grows unboundedly.\n\n**Refresh token rotation** is the companion pattern for long-lived sessions. The flow:\n1. On login, issue a short-lived access token (15 minutes) and a long-lived refresh token (7 days). Store the refresh token hash in Redis: `SET refresh:userId tokenHash EX 604800`.\n2. When the access token expires, the client sends the refresh token to `POST /auth/refresh`.\n3. The server looks up the refresh token in Redis. If found, issues a new access token AND a new refresh token, and atomically replaces the old one in Redis.\n4. The old refresh token is immediately invalid — this is rotation. If an attacker steals an old refresh token and tries to use it, Redis returns a miss (the token was rotated out), signalling a potential token theft. The server can revoke all refresh tokens for that user as a precaution.\n\nStore the refresh token as a bcrypt hash in Redis, not plaintext — refresh tokens are secrets equivalent to passwords.\n\nIn an interview, the key point is that JWTs are not inherently "unrevokable." A blacklist adds ~0.2ms to every authenticated request (one Redis GET) to achieve instant revocation. The alternative — short TTLs plus refresh tokens — defers revocation to the access token TTL window, which is acceptable for most applications but not for security-sensitive actions like password changes.',
    },
  ],
};
