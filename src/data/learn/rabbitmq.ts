import type { LearnModule } from '../../types';

export const rabbitmq: LearnModule = {
  id: 'rabbitmq',
  title: 'RabbitMQ + Saga',
  blurb: 'Exchanges, acknowledgements, dead-letter queues, and the Saga pattern.',
  quizCategory: 'rabbitmq',
  lessons: [
    {
      id: 'rmq-routing',
      title: 'Exchanges and routing',
      content:
        'Producers publish to an exchange, which routes to queues by type: direct matches an exact routing key, topic matches wildcard patterns (star is one word, hash is zero or more), fanout broadcasts to all bound queues, and headers routes on message attributes. Model a command (do this, one handler) with direct routing and an event (this happened, many subscribers) with topic or fanout. Choosing the exchange is really choosing your coupling. In practice a direct exchange named after the target queue is the simplest starting point; switch to topic when you need multiple subscribers to filter by category, such as "orders.created" vs "orders.cancelled". Fanout is useful for cache-invalidation broadcasts where every running instance must react. Headers exchange is rarely needed but allows routing without encoding information in the routing key, which is useful when callers are in different languages with different key-formatting conventions.',
    },
    {
      id: 'rmq-queues-bindings',
      title: 'Queues, bindings, and QoS',
      content:
        'A queue is a durable FIFO buffer; a binding is the rule that ties an exchange to a queue with an optional routing key. Queues can be durable (survive broker restart), exclusive (tied to one connection), or auto-delete (removed when the last consumer disconnects). The prefetch count (basic.qos) controls how many unacknowledged messages a consumer may hold at once — setting it to 1 gives round-robin fair dispatch so a slow consumer does not starve while a fast one sits idle. In high-throughput scenarios a prefetch of 10-50 amortises the round-trip cost without letting one slow worker accumulate a huge backlog. Quorum queues, introduced in RabbitMQ 3.8, use the Raft consensus algorithm to replicate messages across a majority of nodes, replacing classic mirrored queues and offering much stronger durability guarantees without split-brain risk. Always enable durable queues and persistent message delivery mode together; a durable queue with transient messages still loses data on restart.',
    },
    {
      id: 'rmq-reliability',
      title: 'Acknowledgements, nack, and requeue',
      content:
        'With auto-ack enabled the broker removes a message as soon as it is delivered, giving at-most-once semantics — if the consumer crashes the message is gone. Manual acknowledgement (basic.ack) flips this to at-least-once: the broker redelivers any unacknowledged message when the channel closes. A consumer can call basic.nack with requeue=true to put the message back at the head of the queue for an immediate retry, or requeue=false to discard it (or route it to a dead-letter exchange if one is configured). Reject with requeue=true is safe only when the failure is transient; for a poison message or a bug, always nack with requeue=false so the same bad message does not loop forever. Because at-least-once delivery means you may process a message more than once, every consumer must be idempotent — check a processed-ids store or use a database unique constraint before applying the side effect.',
    },
    {
      id: 'rmq-dlq-retry',
      title: 'Dead-letter queues and retry patterns',
      content:
        'A dead-letter exchange (DLX) is a regular exchange that receives messages when they are rejected without requeue, expire (per-message or queue TTL), or exceed the queue max-length. Attach a DLX to a queue by setting the x-dead-letter-exchange argument at queue declaration time. A common retry pattern uses multiple queues with increasing TTLs: the DLX on the primary queue routes to a "wait-30s" queue whose DLX routes back to the primary, implementing exponential back-off without a scheduler. After a fixed number of retries (tracked via the x-death header, which records each dead-lettering event), route permanently failed messages to a "dead" queue for manual inspection or alerting. This isolates poison messages from live traffic and gives you a full audit trail. Always monitor dead-letter queue depth in production — a growing count signals that a downstream dependency is degraded or that a code bug was deployed.',
    },
    {
      id: 'rmq-publisher-confirms',
      title: 'Publisher confirms and idempotency',
      content:
        'Publisher confirms (also called broker acknowledgements) make the producer side durable: the broker sends a basic.ack back to the producer after it has written the message to disk on all replicas, or basic.nack if it could not. Without confirms a publish call returns before the message is persisted, so a broker crash in that window loses the message silently. In code, enable confirms on the channel and either wait synchronously (simple but slower) or collect confirms asynchronously in a callback and retry nacked messages. Combined with a unique message id field, this gives you idempotent publish: if the network drops after the broker commits but before the ack arrives, the producer retries with the same id, and the consumer deduplicates using that id stored alongside the side-effect record. This pattern — persist the message id with the business transaction in one DB write — is the lightweight version of the outbox pattern and is suitable when producer and consumer share a database.',
    },
    {
      id: 'rmq-saga',
      title: 'Saga pattern, compensation, and 2PC vs Saga',
      content:
        'A distributed transaction spanning multiple services cannot use two-phase commit (2PC) in practice: it requires all participants to hold locks until the coordinator says commit, creating availability problems and coupling the services to a single coordinator whose failure blocks everything. The Saga pattern replaces 2PC with a sequence of local transactions, each publishing an event or command that triggers the next step. If any step fails, the saga executes compensating transactions in reverse order — for example, if "charge card" succeeds but "reserve inventory" fails, a compensation action voids the charge. In the choreography style each service reacts to events autonomously; in the orchestration style a central saga orchestrator sends commands and awaits results, which makes the overall flow explicit and testable but re-introduces a coordinator. Key interview point: sagas give ACD without the I — they lack isolation, so an intermediate state is visible to concurrent requests. Design compensating actions to be idempotent because they may be retried. RabbitMQ fits saga orchestration well because the orchestrator can publish commands to specific queues and consume result events from reply queues.',
    },
    {
      id: 'rmq-request-reply',
      title: 'Request/reply pattern with correlation IDs',
      content:
        'The request/reply pattern turns a message queue into a synchronous RPC mechanism, useful when a caller needs a response from a downstream service without a direct HTTP connection between them. The caller publishes a request message to a well-known queue and includes two metadata fields: `reply_to` (the name of an exclusive, auto-delete callback queue the caller created just for this request) and `correlation_id` (a UUID that ties the reply to the original request). The handler processes the request, then publishes the result to the queue named in `reply_to`, copying the `correlation_id` into the reply headers. The caller blocks waiting on its callback queue and matches incoming replies by correlation_id.\n\nThis pattern decouples services at the network level — no service needs to know the other\'s hostname or port — while preserving the semantics of a request-response interaction. The RPC-over-MQ approach also benefits from the broker\'s routing and buffering: if the handler is temporarily down, the request waits in the queue rather than returning an immediate connection error.\n\nIn NestJS with `@nestjs/microservices`, this is implemented transparently using `ClientProxy.send()` which returns an Observable. Under the hood, NestJS creates a reply queue per connection, manages correlation IDs, and resolves the Observable when the matching reply arrives. When building the pattern manually, always set a timeout on the caller side — if the handler crashes after receiving the request but before replying, the caller would block indefinitely without one. A reasonable timeout is 5-30 seconds depending on expected handler latency, and on timeout you should nack the original request (if you hold it) and return an error to the upstream caller. Also set the reply queue to be exclusive and auto-delete so it is cleaned up automatically when the channel closes, preventing queue accumulation from callers that disconnect before receiving their replies.',
    },
    {
      id: 'rmq-vs-kafka',
      title: 'RabbitMQ vs Kafka: when to choose which',
      content:
        'RabbitMQ and Kafka are both message brokers but with fundamentally different designs, and picking the wrong one is a costly architectural mistake. Understanding the distinction is a favourite senior interview topic.\n\nRabbitMQ is a traditional message broker: it routes, transforms, and delivers messages, then removes them once acknowledged. It excels at complex routing logic (multiple exchange types, header-based routing, fanout), task queues where each job is processed once and discarded, and request/reply patterns. Its flow model is push-based — the broker pushes messages to consumers. RabbitMQ\'s strength is flexibility of routing and its rich protocol (AMQP 0-9-1); its weakness is that it is not designed for replay or long-term storage.\n\nKafka is a distributed commit log. Messages (events) are retained for a configurable period (days or forever) regardless of whether they have been consumed. Consumers pull messages at their own pace and track their position (offset) independently. Multiple consumer groups can read the same topic without interfering with each other. This makes Kafka ideal for event sourcing, audit logs, stream processing (Kafka Streams, Flink), and rebuilding read models. Kafka\'s weakness is its operational complexity (ZooKeeper or KRaft, partition rebalancing, offset management) and the fact that topic-level routing is its only primitive — fine-grained routing requires multiple topics or application-level filtering.\n\nThe heuristic: if you need tasks processed once and gone, use RabbitMQ; if you need an immutable event log that multiple consumers replay at different speeds, use Kafka. In a microservices payment system, command processing (charge card, reserve inventory) fits RabbitMQ while the event log of all transactions for analytics, fraud detection, and ledger replay fits Kafka. Many mature systems run both: RabbitMQ for synchronous-ish service communication and Kafka as the event backbone.',
    },
    {
      id: 'rmq-delayed',
      title: 'Delayed messages and the rabbitmq-delayed-message-exchange plugin',
      content:
        'Delayed message delivery — scheduling a message to be processed at a future time — is a common requirement for retry back-off, scheduled notifications, and deferred jobs. RabbitMQ does not support native delays in its core, but there are two practical approaches.\n\nThe TTL + dead-letter approach uses queue-level TTL and a dead-letter exchange to simulate delays. You create a queue with `x-message-ttl: 30000` (30 seconds) and `x-dead-letter-exchange: work-exchange`. Messages published to this waiting queue sit until their TTL expires, then are routed by the DLX to the actual work queue. You need one queue per delay interval (30s, 60s, 5min, 30min), which is inflexible but requires no plugins and works on all RabbitMQ versions. The x-death header accumulates each dead-lettering event, so you can implement exponential backoff by routing to progressively longer waiting queues based on the retry count.\n\nThe `rabbitmq-delayed-message-exchange` plugin adds a new exchange type `x-delayed-message` that accepts a per-message delay in milliseconds via the `x-delay` header. `XADD` your message with `headers: { "x-delay": 30000 }` and the exchange holds it in memory for 30 seconds before routing it to the bound queue. This is elegant and supports arbitrary per-message delays, but the plugin stores delayed messages in memory backed by Mnesia (RabbitMQ\'s internal database), which does not scale well beyond millions of delayed messages and can be lost on node restart if persistence is not carefully configured.\n\nFor production scheduling at scale, a dedicated job queue like Bull (backed by Redis sorted sets with timestamp scores) or a purpose-built scheduler is often more appropriate than either RabbitMQ approach. The sorted set pattern — ZADD with the future Unix timestamp as score, worker polls ZRANGEBYSCORE with max=now — is naturally durable and scales to billions of pending jobs within Redis\'s memory limits.',
    },
    {
      id: 'rmq-nestjs',
      title: 'RabbitMQ with NestJS microservices',
      content:
        'NestJS has first-class support for RabbitMQ through its `@nestjs/microservices` package, which provides a clean abstraction over the underlying AMQP protocol. Understanding how it works under the hood prepares you to debug production issues and architect correct message patterns.\n\nOn the server (consumer) side, you create a microservice with `NestFactory.createMicroservice()` passing the RMQ transport: `{ transport: Transport.RMQ, options: { urls: [process.env.RABBITMQ_URL], queue: \'orders-queue\', queueOptions: { durable: true }, prefetchCount: 10 } }`. Message handlers are decorated with `@MessagePattern(\'order.create\')` for request/reply or `@EventPattern(\'order.created\')` for fire-and-forget events. The pattern value is sent as the `pattern` field in the AMQP message headers.\n\nOn the client (producer) side, inject `ClientProxy` via `@Inject(\'ORDERS_SERVICE\')` where the token is registered with `ClientsModule.register([{ name: \'ORDERS_SERVICE\', transport: Transport.RMQ, options: {...} }])`. Use `client.send(\'order.create\', payload).toPromise()` for request/reply (blocks until the consumer replies using the correlation ID pattern) or `client.emit(\'order.created\', payload).subscribe()` for fire-and-forget.\n\nCommon production gotchas: the default prefetch count is 0 (unlimited), which allows one slow consumer to accumulate the entire queue backlog — always set it explicitly. NestJS auto-creates the queue on startup but does not set `x-dead-letter-exchange` by default; configure it via `queueOptions` to prevent message loss on consumer errors. When running multiple replicas, each replica connects to the same queue and messages are distributed round-robin, giving you horizontal scaling automatically. For saga orchestration, a common pattern is to use separate NestJS microservice applications for each bounded context (OrderService, PaymentService, InventoryService), each consuming from its own queue, with an OrchestrationService that publishes commands and listens for result events.',
    },
  ],
};
