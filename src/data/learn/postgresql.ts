import type { LearnModule } from '../../types';

export const postgresql: LearnModule = {
  id: 'postgresql',
  title: 'PostgreSQL',
  blurb: 'Indexes, transactions, isolation levels, and query performance.',
  quizCategory: 'postgresql',
  lessons: [
    {
      id: 'pg-indexes',
      title: 'Indexes and query planning',
      content:
        'Indexes turn full table scans into fast lookups, but each one costs write time and storage, so index your real access patterns. B-tree suits equality and range queries; GIN suits containment lookups inside JSONB and arrays; partial indexes cover a hot subset of rows (for example, WHERE deleted_at IS NULL) so they are smaller and faster to maintain. Composite indexes are powerful but column order matters: a composite index on (user_id, created_at) serves queries that filter on user_id alone or on both columns, but not on created_at alone. Use EXPLAIN ANALYZE to see the real plan and timings  -  a large gap between estimated and actual rows usually means stale statistics, fixed by ANALYZE. In interviews, explain that an index-only scan is the fastest path because PostgreSQL can answer the query entirely from the index without touching heap pages, which requires the visibility map to be up to date via VACUUM.',
    },
    {
      id: 'pg-transactions',
      title: 'Transactions, isolation, and locking',
      content:
        'ACID transactions group statements so they all commit or all roll back. The default isolation level READ COMMITTED prevents dirty reads but allows non-repeatable reads; REPEATABLE READ and SERIALIZABLE add stronger guarantees at a concurrency cost. For contended updates choose between pessimistic locking (SELECT FOR UPDATE, which acquires a row-level lock) and optimistic locking (a version column checked on UPDATE  -  if the row changed, the update affects zero rows and the application retries). PostgreSQL\'s SERIALIZABLE isolation uses Serializable Snapshot Isolation (SSI), detecting dangerous cycles without blocking reads, making it practical for correctness-critical workloads. Store money as numeric(precision, scale) or as integer cents, never float, because floating-point arithmetic is inexact and will silently produce wrong totals in financial reporting.',
    },
    {
      id: 'pg-mvcc-vacuum',
      title: 'MVCC, VACUUM, and bloat',
      content:
        'PostgreSQL implements multi-version concurrency control (MVCC) by keeping old row versions on the heap instead of overwriting them; each row version carries xmin and xmax transaction IDs that define its visibility window. This lets readers and writers never block each other, which is a key performance advantage over lock-based databases. The cost is that dead row versions (tuples) accumulate and must be cleaned up by VACUUM. Regular autovacuum runs in the background and also updates the visibility map used by index-only scans. Table bloat occurs when autovacuum cannot keep up with a high update or delete rate; VACUUM FULL rewrites the table to reclaim space but takes an exclusive lock, so it should only be used in scheduled maintenance windows. In interviews, be ready to explain transaction ID wraparound: PostgreSQL uses 32-bit transaction IDs, so a table not vacuumed for roughly two billion transactions becomes unreadable  -  autovacuum\'s anti-wraparound pass exists specifically to prevent this.',
    },
    {
      id: 'pg-jsonb-window',
      title: 'JSONB and window functions',
      content:
        'JSONB stores JSON in a decomposed binary format that supports GIN indexing and fast containment queries using the @> operator (for example, data @> \'{"role": "admin"}\' finds all rows where the role key equals admin). It is slower to insert than plain JSON because it parses and reorders keys at write time, but much faster to query. Avoid reaching for JSONB as a substitute for proper columns on frequently queried attributes  -  it should extend relational data, not replace it. Window functions (ROW_NUMBER, RANK, LAG, LEAD, SUM OVER) perform calculations across a set of rows related to the current row without collapsing them into groups. A common interview pattern is: rank users by revenue within each country using RANK() OVER (PARTITION BY country ORDER BY revenue DESC). Unlike GROUP BY, window functions preserve every row, making them ideal for running totals, moving averages, and gap-and-island problems.',
    },
    {
      id: 'pg-connection-pooling',
      title: 'Connection pooling and performance tuning',
      content:
        'PostgreSQL spawns a separate OS process for each client connection, so each connection costs roughly 5-10 MB of RAM and significant connection setup time. In high-concurrency applications this becomes a bottleneck, which is why PgBouncer (a lightweight connection pooler) sits between the application and the database. PgBouncer supports three pooling modes: session (a server connection follows the client for the whole session), transaction (returned to the pool after each transaction  -  the most common mode), and statement (returned after each statement, incompatible with multi-statement transactions). In interviews, the key trade-off to articulate is that transaction-mode pooling makes the database scale to thousands of application connections with dozens of server connections, but prepared statements and session-level settings (like SET LOCAL) become problematic because the server connection may differ on the next transaction. Application-side pools (like pg-pool in Node.js) are simpler but do not multiplex connections as aggressively.',
    },
    {
      id: 'pg-normalization-replication',
      title: 'Normalization, replication, and high availability',
      content:
        'Normalization eliminates redundancy through normal forms: first (1NF) removes repeating groups, second (2NF) removes partial dependencies on composite keys, third (3NF) removes transitive dependencies. In practice, denormalization is sometimes deliberate  -  read-heavy reporting tables may duplicate data to avoid expensive joins. PostgreSQL streaming replication sends WAL (write-ahead log) records to standby servers; logical replication selectively replicates individual tables and is used for zero-downtime migrations and cross-version upgrades. Write-ahead logging is also the foundation of crash recovery: every change is written to the WAL before it touches data pages, so the database can replay the WAL on restart after a crash. In interviews, explain the difference between synchronous and asynchronous replication: synchronous (synchronous_commit = on) guarantees no data loss on primary failure at the cost of added write latency, while asynchronous is faster but risks losing the last few transactions if the primary crashes before the standby applies them.',
    },
    {
      id: 'pg-ctes',
      title: 'CTEs and recursive CTEs for tree queries',
      content:
        'A **Common Table Expression (CTE)** is a named, temporary result set that you define with `WITH` and reference in the main query. CTEs make complex queries readable by giving each step a name.\n\nA simple CTE that calculates top customers and filters them:\n\n```sql\nWITH top_customers AS (\n  SELECT customer_id, SUM(amount) AS total\n  FROM orders\n  GROUP BY customer_id\n  HAVING SUM(amount) > 10000\n)\nSELECT c.name, tc.total\nFROM top_customers tc\nJOIN customers c ON c.id = tc.customer_id\nORDER BY tc.total DESC;\n```\n\nThis is equivalent to a subquery but far easier to read and debug. You can reference the same CTE multiple times in the main query  -  a subquery would require duplication.\n\nRecursive CTEs query hierarchical data  -  trees and graphs  -  that would otherwise require application-level iteration. The pattern has two parts: an **anchor member** (the base case) and a **recursive member** (the step that references the CTE itself):\n\n```sql\nWITH RECURSIVE category_tree AS (\n  -- anchor: start from the root\n  SELECT id, name, parent_id, 0 AS depth\n  FROM categories\n  WHERE parent_id IS NULL\n\n  UNION ALL\n\n  -- recursive step: find children of current rows\n  SELECT c.id, c.name, c.parent_id, ct.depth + 1\n  FROM categories c\n  JOIN category_tree ct ON c.parent_id = ct.id\n)\nSELECT * FROM category_tree ORDER BY depth, name;\n```\n\nThis walks the entire category hierarchy in one SQL query, regardless of depth. The `depth` column tracks how many levels down each node is. Add a `path` column (`ct.path || \'/\' || c.name`) to build breadcrumb strings.\n\nBe careful with cycles. A recursive CTE without a termination condition on a graph with cycles will loop forever. Add a `WHERE depth < 20` guard or use the `CYCLE` clause introduced in PostgreSQL 14.\n\nIn an interview, recursive CTEs come up whenever the schema has a self-referential foreign key  -  `categories.parent_id REFERENCES categories.id`, `employees.manager_id REFERENCES employees.id`, `comments.parent_comment_id`. The ability to answer "how do you fetch an entire comment thread?" with a recursive CTE demonstrates SQL maturity.',
    },
    {
      id: 'pg-partitioning',
      title: 'Table partitioning  -  range, list, and hash',
      content:
        'As a table grows into hundreds of millions of rows, even a well-indexed query starts slowing down because the planner must consider a large heap. **Table partitioning** splits one logical table into multiple physical child tables, each holding a subset of rows. The planner can then skip irrelevant partitions entirely  -  a technique called **partition pruning**.\n\nPostgreSQL supports three partitioning strategies:\n\n**Range partitioning** divides rows by a column range. It is the natural choice for time-series data:\n\n```sql\nCREATE TABLE events (\n  id BIGSERIAL,\n  user_id INT,\n  payload JSONB,\n  created_at TIMESTAMPTZ NOT NULL\n) PARTITION BY RANGE (created_at);\n\nCREATE TABLE events_2024_q1 PARTITION OF events\n  FOR VALUES FROM (\'2024-01-01\') TO (\'2024-04-01\');\nCREATE TABLE events_2024_q2 PARTITION OF events\n  FOR VALUES FROM (\'2024-04-01\') TO (\'2024-07-01\');\n```\n\nA query with `WHERE created_at >= \'2024-04-01\'` touches only `events_2024_q2` and later partitions  -  the planner ignores all earlier ones. Old partitions can be detached and archived cheaply: `ALTER TABLE events DETACH PARTITION events_2024_q1`.\n\n**List partitioning** divides by discrete values  -  useful for multi-tenancy or region-based data separation:\n\n```sql\nCREATE TABLE orders (\n  id BIGSERIAL, region TEXT NOT NULL, amount NUMERIC\n) PARTITION BY LIST (region);\n\nCREATE TABLE orders_eu PARTITION OF orders FOR VALUES IN (\'DE\', \'FR\', \'NL\');\nCREATE TABLE orders_us PARTITION OF orders FOR VALUES IN (\'US\', \'CA\');\n```\n\n**Hash partitioning** distributes rows evenly by the hash of a column. It has no natural data locality but is useful for even load distribution when no range or list grouping makes sense:\n\n```sql\nCREATE TABLE users (...) PARTITION BY HASH (id);\nCREATE TABLE users_0 PARTITION OF users FOR VALUES WITH (MODULUS 4, REMAINDER 0);\n-- creates 4 even partitions\n```\n\nEach partition can have its own indexes, tablespaces, and storage parameters. You can add new partitions without locking the parent table. But foreign keys from other tables pointing to a partitioned table are not supported in older PostgreSQL versions  -  check compatibility before adopting.\n\nIn an interview, the question "how would you handle a `logs` table growing to 10 billion rows?" is answered with range partitioning by date, plus automated partition creation and old-partition archival.',
    },
    {
      id: 'pg-full-text-search',
      title: 'Full-text search with tsvector and tsquery',
      content:
        'PostgreSQL has a built-in full-text search engine that handles stemming, stop words, and ranking  -  without a separate Elasticsearch cluster for many use cases.\n\nThe core data type is **tsvector**: a pre-processed, sorted list of lexemes (normalised word roots) extracted from a text document. The core query type is **tsquery**: a boolean expression of lexemes to match against a tsvector.\n\n```sql\n-- Convert text to tsvector\nSELECT to_tsvector(\'english\', \'The quick brown fox jumps over the lazy dog\');\n-- Result: \'brown\':3 \'dog\':9 \'fox\':4 \'jump\':5 \'lazi\':8 \'quick\':2\n-- Stop words (\'the\', \'over\') are removed. Words are stemmed (\'jumps\' -> \'jump\').\n\n-- Match against a query\nSELECT to_tsvector(\'english\', body) @@ to_tsquery(\'english\', \'jump & fox\')\nFROM documents;\n```\n\nFor production use, store the tsvector in a generated column so you do not recompute it on every search:\n\n```sql\nALTER TABLE articles ADD COLUMN search_vector tsvector\n  GENERATED ALWAYS AS (\n    to_tsvector(\'english\', coalesce(title, \'\') || \' \' || coalesce(body, \'\'))\n  ) STORED;\n\nCREATE INDEX articles_search_idx ON articles USING GIN (search_vector);\n```\n\nThe GIN index makes searches fast. Query the generated column directly:\n\n```sql\nSELECT title, ts_rank(search_vector, query) AS rank\nFROM articles, to_tsquery(\'english\', \'postgres & index\') query\nWHERE search_vector @@ query\nORDER BY rank DESC\nLIMIT 20;\n```\n\n`ts_rank` scores each document by how often and how prominently the query terms appear  -  a basic relevance ranking. `ts_headline` extracts a snippet with matched terms highlighted.\n\nFor phrase search, use `phraseto_tsquery(\'english\', \'full text search\')`, which requires words to appear in order. For prefix search (autocomplete), use `to_tsquery(\'english\', \'quick:*\')`  -  the `:*` suffix matches any word starting with "quick".\n\nIn an interview, full-text search in PostgreSQL is the right answer for applications that need decent search quality on moderate data volumes (under a few million documents). For more than a few million documents, faceted search, custom scoring, or multi-language support, mention Elasticsearch as the next step.',
    },
    {
      id: 'pg-rls',
      title: 'Row-level security policies',
      content:
        'Row-level security (RLS) is a PostgreSQL feature that lets the database enforce data access rules at the row level  -  so that a query on `orders` automatically filters to only the rows the current user is allowed to see, regardless of whether the application remembered to add a WHERE clause.\n\nEnable RLS on a table, then define policies:\n\n```sql\nALTER TABLE orders ENABLE ROW LEVEL SECURITY;\n\n-- Users can only see their own orders\nCREATE POLICY orders_isolation ON orders\n  USING (user_id = current_setting(\'app.current_user_id\')::INT);\n```\n\nThe `USING` expression is appended as an invisible WHERE clause to every SELECT, UPDATE, and DELETE on that table. A user can only ever see rows where `user_id` matches the value stored in the session-level setting `app.current_user_id`.\n\nSet the setting at the start of each transaction in your application:\n\n```sql\nSET LOCAL app.current_user_id = \'42\';\n```\n\nIn a NestJS application, do this in a TypeORM subscriber or a Prisma middleware that wraps every transaction.\n\nYou can have separate policies for different commands:\n\n```sql\nCREATE POLICY orders_select ON orders FOR SELECT\n  USING (user_id = current_setting(\'app.current_user_id\')::INT);\n\nCREATE POLICY orders_insert ON orders FOR INSERT\n  WITH CHECK (user_id = current_setting(\'app.current_user_id\')::INT);\n```\n\nThe `WITH CHECK` expression applies to INSERT and UPDATE and prevents a user from writing rows they would not be able to read back.\n\nTable owners and superusers bypass RLS by default. Use `ALTER TABLE orders FORCE ROW LEVEL SECURITY` to enforce it even for the table owner  -  important when your application connects as the table owner.\n\nIn an interview, RLS is the answer to "how do you prevent data leaks in a multi-tenant application at the database level?" It creates a defence-in-depth layer: even if your application code has a bug and omits a WHERE clause, the database enforces tenant isolation.',
    },
    {
      id: 'pg-triggers',
      title: 'PL/pgSQL triggers',
      content:
        'A trigger is a function that PostgreSQL calls automatically when a row is inserted, updated, or deleted. Triggers let you enforce complex business rules, maintain audit logs, and update derived data  -  all inside the database, close to the data.\n\nA trigger has two parts: the trigger function and the trigger definition that binds it to a table and event.\n\n```sql\n-- Trigger function: automatically set updated_at\nCREATE OR REPLACE FUNCTION set_updated_at()\nRETURNS TRIGGER AS $$\nBEGIN\n  NEW.updated_at = NOW();\n  RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\n-- Bind it to the orders table\nCREATE TRIGGER orders_set_updated_at\n  BEFORE UPDATE ON orders\n  FOR EACH ROW\n  EXECUTE FUNCTION set_updated_at();\n```\n\nInside a `BEFORE` trigger, `NEW` is the row about to be written. You can modify `NEW` before returning it, which is how the timestamp is injected. In an `AFTER` trigger, `NEW` is the committed row  -  use this for sending notifications or updating summary tables.\n\nAn audit log trigger records every change:\n\n```sql\nCREATE TABLE order_audit (\n  id BIGSERIAL PRIMARY KEY,\n  order_id INT,\n  operation TEXT,\n  old_data JSONB,\n  new_data JSONB,\n  changed_at TIMESTAMPTZ DEFAULT NOW()\n);\n\nCREATE OR REPLACE FUNCTION audit_orders()\nRETURNS TRIGGER AS $$\nBEGIN\n  INSERT INTO order_audit (order_id, operation, old_data, new_data)\n  VALUES (\n    COALESCE(NEW.id, OLD.id),\n    TG_OP,\n    CASE WHEN TG_OP = \'INSERT\' THEN NULL ELSE to_jsonb(OLD) END,\n    CASE WHEN TG_OP = \'DELETE\' THEN NULL ELSE to_jsonb(NEW) END\n  );\n  RETURN COALESCE(NEW, OLD);\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER orders_audit\n  AFTER INSERT OR UPDATE OR DELETE ON orders\n  FOR EACH ROW EXECUTE FUNCTION audit_orders();\n```\n\n`TG_OP` is a special variable that holds `\'INSERT\'`, `\'UPDATE\'`, or `\'DELETE\'`. `to_jsonb(OLD)` serialises the entire old row as JSONB, giving you a complete before-image.\n\nIn an interview, triggers are the answer to "how do you maintain an audit log without changing every application query?" But also acknowledge the trade-off: triggers add hidden complexity, run in the same transaction as the triggering statement (so a failing trigger rolls back the original write), and are easy to forget when debugging unexpected behaviour.',
    },
    {
      id: 'pg-stat-statements',
      title: 'Reading pg_stat_statements for query performance',
      content:
        'When a PostgreSQL database is slow, the first question is: which queries are consuming the most resources? `pg_stat_statements` answers that question. It is a PostgreSQL extension that tracks execution statistics for every distinct query shape.\n\nEnable it in `postgresql.conf`:\n\n```\nshared_preload_libraries = \'pg_stat_statements\'\npg_stat_statements.track = all\n```\n\nThen in your database: `CREATE EXTENSION pg_stat_statements;`\n\nNow query the view to find the worst offenders:\n\n```sql\nSELECT\n  query,\n  calls,\n  ROUND(total_exec_time::numeric, 2) AS total_ms,\n  ROUND(mean_exec_time::numeric, 2) AS mean_ms,\n  ROUND((100 * total_exec_time / SUM(total_exec_time) OVER ())::numeric, 2) AS pct_total,\n  rows\nFROM pg_stat_statements\nORDER BY total_exec_time DESC\nLIMIT 20;\n```\n\nThe key columns:\n- `calls`  -  how many times this query shape ran\n- `mean_exec_time`  -  average execution time in milliseconds\n- `total_exec_time`  -  total time spent across all calls (use this to find queries that are fast per-call but called thousands of times)\n- `rows`  -  average rows returned per call\n- `shared_blks_hit` / `shared_blks_read`  -  cache hit ratio; a low hit ratio means data is being read from disk\n\nA query with a high `total_exec_time` but low `mean_exec_time` is called too often  -  consider caching the result. A query with a high `mean_exec_time` needs index optimization  -  run `EXPLAIN (ANALYZE, BUFFERS)` on it to see the plan.\n\nReset statistics with `SELECT pg_stat_statements_reset()` before a load test so you get clean baseline numbers.\n\nIn an interview, mention `pg_stat_statements` as your first tool when diagnosing database performance. The workflow is: query the view to find the most expensive queries, then run `EXPLAIN ANALYZE` on each one to understand why it is slow, then add indexes, rewrite the query, or add caching based on what the plan reveals.',
    },
    {
      id: 'pg-typeorm',
      title: 'TypeORM entities and migrations',
      content:
        'TypeORM is the most common ORM in NestJS applications. It maps TypeScript classes to database tables and provides a query builder and migration system.\n\nDefine an entity with column decorators:\n\n```typescript\n@Entity(\'orders\')\nexport class Order {\n  @PrimaryGeneratedColumn(\'uuid\')\n  id: string;\n\n  @Column({ type: \'numeric\', precision: 10, scale: 2 })\n  amount: number;\n\n  @Column({ name: \'user_id\' })\n  userId: string;\n\n  @ManyToOne(() => User, user => user.orders)\n  @JoinColumn({ name: \'user_id\' })\n  user: User;\n\n  @CreateDateColumn()\n  createdAt: Date;\n\n  @UpdateDateColumn()\n  updatedAt: Date;\n}\n```\n\nRelationships are declared with `@ManyToOne`, `@OneToMany`, `@OneToOne`, and `@ManyToMany`. Always specify the foreign key column name explicitly with `@JoinColumn` to keep control over the schema.\n\nNever use `synchronize: true` in production. It automatically alters the database schema to match your entities  -  without a migration record, without review, and potentially with destructive column drops. Use it only in development.\n\nFor production, generate a migration:\n\n```bash\nnpx typeorm migration:generate src/migrations/AddOrderStatus -d src/data-source.ts\n```\n\nTypeORM compares the current entity definitions with the current database schema and generates an `up()` and `down()` SQL script. Review it before running:\n\n```typescript\nexport class AddOrderStatus1700000000000 implements MigrationInterface {\n  async up(queryRunner: QueryRunner): Promise<void> {\n    await queryRunner.addColumn(\'orders\', new TableColumn({\n      name: \'status\',\n      type: \'varchar\',\n      default: `\'pending\'`,\n    }));\n  }\n  async down(queryRunner: QueryRunner): Promise<void> {\n    await queryRunner.dropColumn(\'orders\', \'status\');\n  }\n}\n```\n\nFor querying without the full ORM overhead, use the `QueryBuilder`:\n\n```typescript\nreturn this.orderRepo.createQueryBuilder(\'o\')\n  .leftJoinAndSelect(\'o.user\', \'u\')\n  .where(\'o.status = :status\', { status: \'pending\' })\n  .orderBy(\'o.createdAt\', \'DESC\')\n  .take(20)\n  .getMany();\n```\n\nIn an interview, the two most important TypeORM topics are the N+1 problem (load relations eagerly with `relations: [\'user\']` or with `leftJoinAndSelect`, never with a lazy-loaded getter in a loop) and the migration workflow (generate, review, run in CI, never rely on synchronize).',
    },
    {
      id: 'pg-prisma',
      title: 'Prisma schema and migrations',
      content:
        'Prisma is the modern alternative to TypeORM. It uses a single schema file as the source of truth, generates a fully type-safe client, and has a migration system that is easier to reason about than TypeORM\'s.\n\nDefine the schema in `prisma/schema.prisma`:\n\n```prisma\ngenerator client {\n  provider = "prisma-client-js"\n}\n\ndatasource db {\n  provider = "postgresql"\n  url      = env("DATABASE_URL")\n}\n\nmodel Order {\n  id        String   @id @default(uuid())\n  amount    Decimal  @db.Decimal(10, 2)\n  status    String   @default("pending")\n  userId    String   @map("user_id")\n  user      User     @relation(fields: [userId], references: [id])\n  createdAt DateTime @default(now()) @map("created_at")\n\n  @@map("orders")\n}\n```\n\nRun `npx prisma migrate dev --name add-order-status` to generate a timestamped SQL migration file and apply it. Prisma stores applied migrations in a `_prisma_migrations` table. The generated migration is plain SQL  -  review it before committing.\n\nThe generated client is fully type-safe:\n\n```typescript\nconst orders = await prisma.order.findMany({\n  where: { status: \'pending\', user: { email: { endsWith: \'@example.com\' } } },\n  include: { user: { select: { name: true, email: true } } },\n  orderBy: { createdAt: \'desc\' },\n  take: 20,\n});\n```\n\nTypeScript knows the exact shape of `orders`  -  including the selected `user` fields. If you add a field to the `select` clause that does not exist on the model, the compiler errors at build time.\n\nUse `prisma.$transaction([...])` for sequential atomic operations or `prisma.$transaction(async (tx) => { ... })` for interactive transactions where you need to read within the same transaction.\n\nIn NestJS, inject the Prisma client as a singleton service. Extend `PrismaClient` and call `$connect()` in `onModuleInit` and `$disconnect()` in `onModuleDestroy`.\n\nIn an interview, the key Prisma advantage to articulate is developer experience: the schema file is the single source of truth for both the database schema and the TypeScript types, eliminating the class-of-entity-mismatch bugs that TypeORM\'s decorator-based approach is prone to.',
    },
    {
      id: 'pg-multitenancy',
      title: 'Multi-tenancy patterns in PostgreSQL',
      content:
        'A multi-tenant system serves multiple customers (tenants) from the same application instance. PostgreSQL supports three isolation strategies, each with different trade-offs between isolation, cost, and complexity.\n\n**Separate databases** give the strongest isolation. Each tenant has their own PostgreSQL database (or even their own cluster). Schema migrations, backups, and connection pools are managed per-tenant. This is the right model for enterprise SaaS with strict compliance requirements (HIPAA, financial services), but it scales to tens or hundreds of tenants  -  not thousands.\n\n**Separate schemas** use one database with one PostgreSQL schema per tenant (`tenant_abc.orders`, `tenant_xyz.orders`). Set `search_path` at connection time to route queries to the right schema. Migrations must run on every tenant schema  -  tools like `db-migrate` or custom scripts iterate the schema list. This balances isolation and operational overhead, supporting thousands of tenants.\n\n**Shared schema** (the most common approach for high-tenant-count SaaS) stores all tenants in the same tables with a `tenant_id` column:\n\n```sql\nALTER TABLE orders ADD COLUMN tenant_id UUID NOT NULL;\nCREATE INDEX orders_tenant_idx ON orders (tenant_id);\n```\n\nEvery query must include `WHERE tenant_id = $1`. Row-level security enforces this at the database level:\n\n```sql\nALTER TABLE orders ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_isolation ON orders\n  USING (tenant_id = current_setting(\'app.tenant_id\')::UUID);\n```\n\nSet `app.tenant_id` at the start of each request in your application middleware. Now even if a developer forgets the `WHERE tenant_id` clause, the database enforces isolation.\n\nPartitioning by `tenant_id` (hash partitioning) further improves query performance for large tenants by localising their data to specific partition files.\n\nIn an interview, describe all three models and explain how to choose: separate databases for < 100 enterprise tenants with strict compliance, shared schema with RLS for thousands of tenants, and separate schemas as a middle ground for regulated industries at moderate scale.',
    },
    {
      id: 'pg-backup-pitr',
      title: 'pg_dump and point-in-time recovery',
      content:
        'Backups are not optional. Understanding PostgreSQL\'s backup mechanisms is a signal of production readiness that many candidates lack.\n\n`pg_dump` takes a logical backup  -  a SQL script or custom-format file that reconstructs the schema and data:\n\n```bash\npg_dump \\\n  --format=custom \\\n  --compress=9 \\\n  --file=backup-$(date +%Y%m%d).dump \\\n  my_database\n```\n\nThe `custom` format is smaller than plain SQL and supports selective restore with `pg_restore --table=orders`. Pipe to S3 directly in production:\n\n```bash\npg_dump --format=custom my_database | aws s3 cp - s3://my-backups/db-$(date +%Y%m%d).dump\n```\n\n`pg_dump` takes a consistent snapshot without locking the database (it uses MVCC). But it only captures a single point in time  -  you lose all changes since the last dump if the primary fails.\n\n**Point-in-time recovery (PITR)** solves this by combining a base backup with a continuous WAL archive. Every change written to PostgreSQL goes through the WAL. If you archive WAL segments to S3 as they are produced, you can restore to any second in history.\n\nSet up continuous WAL archiving in `postgresql.conf`:\n\n```\narchive_mode = on\narchive_command = \'aws s3 cp %p s3://my-wal-archive/%f\'\n```\n\nTo recover to a point in time:\n1. Restore a base backup: `pg_basebackup -D /var/lib/postgresql/data ...`\n2. Create a `recovery.signal` file in the data directory\n3. Set `recovery_target_time = \'2024-01-15 14:30:00\'` in `postgresql.conf`\n4. Start PostgreSQL  -  it replays WAL until it reaches the target time\n\nManaged services (AWS RDS, Google Cloud SQL, Supabase) provide PITR with a retention window (typically 7-35 days) and a one-click restore UI. Understanding what they are doing under the hood helps you choose the right retention window and estimate recovery time objectives.\n\nIn an interview, the Recovery Time Objective (RTO) and Recovery Point Objective (RPO) are the key metrics. With `pg_dump`-only backups, your RPO is the dump interval (hours); with PITR, your RPO is seconds. Your RTO with PITR depends on how long WAL replay takes  -  for a multi-terabyte database this can be hours.',
    },
  ],
};
