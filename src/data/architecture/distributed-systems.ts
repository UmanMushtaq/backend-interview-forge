import type { ArchitectureModule } from '../../types';

export const distributedSystems: ArchitectureModule = {
  id: 'distributed-systems',
  title: 'Distributed Systems Concepts',
  blurb: 'CAP theorem, consistency models, distributed locks, idempotency, rate limiting, circuit breakers.',
  lessons: [
    {
      id: 'as-cap-theorem',
      title: 'CAP theorem in practice, not just the definition',
      content:
        'Every distributed systems interview eventually touches the **CAP theorem**, and most candidates can recite it: a distributed system can guarantee at most two of Consistency, Availability, and Partition tolerance. That definition alone is nearly useless in an interview, because it treats CAP as a one-time global choice, when in reality it is a decision you make differently for almost every individual operation in your system.\n\n**Partition tolerance** means the system keeps functioning even when a network failure splits it into pieces that cannot talk to each other, two data centers losing the link between them, for example. Here is the detail most memorized definitions skip: partitions are not optional to tolerate. Networks fail. Cables get cut. Data centers lose connectivity to each other. A real distributed system has no choice about whether to handle partitions, only about what it does while one is happening. This is why CAP is really a choice between just two options in practice: **CP**, consistency over availability, or **AP**, availability over consistency, during the partition.\n\nChoosing **CP** means that when a partition happens, the system refuses requests it cannot guarantee are correct, rejecting a write or read rather than risking two sides of the split disagreeing about the true state of the data. A bank transfer needs this: if two data centers cannot confirm to each other what the current balance is, it is better to reject the transfer entirely than to risk crediting or debiting the wrong amount because each side had a different, unsynchronized view of the account.\n\nChoosing **AP** means the system keeps answering requests on both sides of the partition, even though the two sides may temporarily disagree, and reconciles the difference once the partition heals. A product catalog page needs this far more than it needs perfect consistency: showing a product description that is a few seconds out of date is a minor inconvenience, but refusing to show the page at all because two data centers cannot currently confirm they agree is a genuine outage for something that never needed perfect agreement in the first place.\n\nThe real skill being tested is recognizing that a single system is rarely purely CP or purely AP everywhere at once. A payment platform is CP for balance updates, where two data centers disagreeing for even a moment is unacceptable, but can be AP for something like a user\'s notification preferences, where briefly serving a stale setting from a partitioned replica causes no real harm. Naming this split, operation by operation, rather than declaring the whole system CP or AP as one blanket label, is what shows you actually understand the trade-off instead of having memorized the acronym.\n\nIn an interview, do not just define CAP, apply it to the specific system in front of you. Say: "for the balance update, I would choose CP, rejecting the write during a partition rather than risking two data centers disagreeing about the true balance. For notification preferences, I would choose AP, since briefly serving a stale value from an unreachable replica is a far smaller cost than an outage for something this low-stakes." Naming the specific operation each choice applies to is what separates a memorized definition from real understanding.',
      designChallenge: {
        prompt:
          'A payment platform runs across two data centers. During a network partition between them, a balance update must never risk two data centers disagreeing about the true balance, even if that means rejecting the request. Design the write path for the balance update so it reflects a CP choice: correctness over availability during a partition.',
        gradingCriteria: [
          'The canvas has a service component connected to a database representing the balance-owning system',
          'A connection or component label references consistency, CP, or rejecting writes during a partition',
          'The design does not show the balance write going to multiple independent databases that could silently diverge without coordination',
          'The design reflects a single authoritative path for the balance write, consistent with choosing consistency over availability',
        ],
      },
    },
    {
      id: 'as-consistency-models',
      title: 'Consistency models: strong vs eventual',
      content:
        'CAP tells you that during a network partition, you have to pick between consistency and availability for a given operation. What CAP does not tell you is that consistency itself is not one single thing, it is a spectrum, and most real systems live somewhere in the middle rather than at either extreme. Understanding the specific points on that spectrum is what lets you actually design the trade-off instead of just naming it.\n\n**Strong consistency**, sometimes called linearizability, means every read sees the most recent successful write, immediately, no matter which replica answers it. This is the guarantee people intuitively expect until they learn it is expensive: enforcing it usually means a read has to check with, or wait on, enough replicas to be sure it is not returning stale data, which adds latency to every single read in exchange for a guarantee that many reads do not actually need. A bank balance check right before approving a large withdrawal needs this: reading a stale balance could approve a withdrawal the account cannot actually cover.\n\n**Eventual consistency** relaxes that guarantee: replicas are allowed to disagree temporarily, but given no new writes, they will all converge to the same value eventually. This is dramatically cheaper, since a read can be answered by any nearby replica without waiting to confirm it has the very latest write, at the cost of that read occasionally returning data that is a little behind. A social media post\'s like count is a textbook fit: seeing 4,382 likes instead of 4,384 for a few seconds causes no real harm, and the cost of guaranteeing perfect real-time accuracy on every single read of every single post would be enormous for a benefit nobody actually needs.\n\nBetween those two extremes sits **read-your-own-writes consistency**, a genuinely useful middle ground: a specific user is always guaranteed to see their own changes immediately, even if other users might briefly see a slightly stale view. A user who just updated their profile photo should never refresh the page and see their old photo, even though it is completely fine if a different user, viewing that same profile a moment later from a different replica, sees the old photo for another few hundred milliseconds until replication catches up. This is usually implemented by routing a user\'s own reads to the primary, or to whichever replica just accepted their write, right after they make a change, then falling back to any nearby replica afterward.\n\nChoosing the wrong consistency model in either direction has a real cost. Applying strong consistency everywhere, out of caution, adds latency and reduces availability for data that never needed the guarantee in the first place, a like count does not need to block on cross-replica confirmation. Applying eventual consistency to a bank balance, chasing the latency win, risks a user seeing stale numbers at exactly the moment they are making a financial decision based on them. The model has to match what the specific piece of data can actually tolerate, not a single default applied uniformly across the whole system.\n\nIn an interview, name the model per piece of data, not per system. Say: "I would use strong consistency for the account balance, since a stale read here has real financial consequences, but eventual consistency for the like count, since a few seconds of staleness costs nothing and lets us serve reads from any nearby replica without the latency of confirming the very latest write." Matching the model to what the data can tolerate, explicitly, is what shows real judgment.',
      designChallenge: {
        prompt:
          "A social app shows a post's like count, which can be a few seconds stale with no real harm, and separately shows a user's own profile updates, which must always reflect their own most recent change immediately. Design read paths for both: one that can be served from any nearby replica, and one that guarantees the user sees their own latest write.",
        gradingCriteria: [
          'The canvas has a primary database and at least one replica database',
          'The like-count read path is connected to a replica, representing eventual consistency',
          "The profile-update read path is connected to the primary, or otherwise clearly labeled to guarantee the user's latest write is seen",
          'The two read paths are visibly distinct connections, not merged into one generic database read',
        ],
      },
    },
    {
      id: 'as-distributed-locks',
      title: 'Distributed locks (the Redis wallet lock in NexusPay)',
      content:
        'Two requests arrive at nearly the same instant, both trying to debit the same wallet. Without something stopping them, both could read the same starting balance, both calculate a new balance based on that same stale read, and both write, with the second write silently overwriting the first, meaning one of the two debits is effectively lost. This is a **race condition**, and a **distributed lock** is how you stop two processes, potentially running on entirely different machines, from touching the same critical resource at the same time.\n\nNexusPay\'s wallet service solves exactly this problem with a Redis-based lock before processing any transfer: `SET NX EX 30` on a key like `lock:wallet:{walletId}`. The `NX` flag means "set this key only if it does not already exist," which is what gives you mutual exclusion, only the first request to arrive can successfully set the key, and every request after it fails to acquire the lock until the key is gone. The `EX 30` sets a 30-second expiration on that key automatically, which exists specifically so a crashed process that acquired the lock but never releases it does not hold that wallet locked forever, leaving every future transfer to that wallet permanently stuck.\n\nThat expiration is also where the hardest part of this pattern lives. What happens if the service crashes after acquiring the lock but before the `finally` block that would normally release it runs? Without the TTL, that wallet would be locked out of every future transfer indefinitely, since nothing would ever delete the key. With the TTL, the lock self-releases after 30 seconds even though nobody explicitly freed it, and the next transfer request can proceed once that window passes. The trade-off baked into that number is real: too short a TTL risks the lock expiring while the original request is still legitimately working, letting a second request in while the first is still mid-transfer; too long a TTL means a genuine crash leaves the wallet locked for longer than necessary.\n\nA subtler failure mode is worth naming directly, because interviewers specifically probe it: what if the original request is still running, genuinely still doing legitimate work, when the TTL expires and a second request acquires the lock, believing the first is done? Now two requests believe they each hold the lock at the same time, which is exactly the race condition the lock existed to prevent in the first place. Production-grade implementations address this with a unique token per lock acquisition, checked before release, so a request can only release a lock it actually still owns, and sometimes with lock renewal, extending the TTL periodically while genuinely still working, rather than trusting a single fixed timeout to always outlast the operation.\n\nLocks are not free even when everything works correctly. Every request that has to wait for a lock is added latency for that specific transfer, and a lock scoped too broadly, one lock for the entire wallet service rather than one lock per individual wallet, would force every transfer in the entire system to wait behind whichever one happens to be in progress, destroying throughput for no reason. Scoping the lock as narrowly as correctness actually requires, per wallet id here, not per service, is what keeps the lock from becoming its own bottleneck.\n\nIn an interview, if asked what happens when NexusPay\'s Redis lock holder crashes mid-transfer, walk through it precisely: "the `EX 30` TTL means the lock self-releases after 30 seconds even if the crashed process never reaches its cleanup code, so the wallet is not stuck forever. The risk is a second request acquiring the lock while the first crashed request\'s partial work is still being cleaned up, which is why a unique per-acquisition token checked on release is the safer production version of this pattern." Walking through the crash scenario specifically, not just naming `SET NX EX`, is what shows you understand why each piece exists.',
      designChallenge: {
        prompt:
          'Two requests arrive at nearly the same instant, both trying to debit the same wallet. Design the setup that prevents both from processing at once: a service that acquires a lock, backed by a fast in-memory store, before touching the wallet balance, and releases it after.',
        gradingCriteria: [
          'The canvas has a cache component representing the lock store, separate from the main database',
          'The service component connects to the cache to acquire the lock before connecting to the database to update the balance',
          'The connection to the cache is labeled to reference locking, mutual exclusion, or SET NX, not left as a generic label',
          'The database component is still present and holds the actual wallet balance, distinct from the lock itself',
        ],
      },
    },
    {
      id: 'as-idempotency',
      title: 'Idempotency and exactly-once processing',
      content:
        'A payment request times out on the client side. The user, unsure whether it went through, taps pay again. Or a message queue redelivers an event because a consumer crashed before acknowledging it, exactly as covered in an earlier lesson. In both cases, the same logical operation can arrive, or be attempted, more than once, and if your system is not built to handle that, a single payment can get charged twice. **Idempotency** is the property that prevents this: processing the same request twice produces the exact same result as processing it once.\n\nThe core mechanism is an **idempotency key**: a unique identifier, generated by the client or attached to the message, that travels with the request and lets the server recognize it has already handled this exact one. Before processing, the server checks whether it has seen that key before. If it has, it returns the same result it returned the first time without redoing the underlying work, a second charge, a second email, a second wallet credit. If it has not, it processes the request normally and records the key alongside the result, so any future retry with that same key is recognized immediately.\n\nNot every operation needs this treatment equally, because some operations are naturally idempotent already and some are naturally not. Setting a user\'s status to "active" is naturally idempotent: running it five times leaves the exact same end state as running it once, since it is an absolute assignment, not a relative change. Incrementing a wallet balance by ten is not naturally idempotent at all: running it five times adds fifty, not ten, because each execution changes the state relative to whatever it currently is rather than setting it to a fixed value. Anything that adds, increments, or appends rather than sets to an absolute value needs an explicit idempotency key, because the underlying operation itself offers no natural protection against being repeated.\n\n**Exactly-once processing** is the stronger, related guarantee that message queue systems often advertise, and it is worth being precise about what it actually means underneath the marketing term. True exactly-once delivery across a distributed network is famously difficult to guarantee in the general case, because the network itself can always fail in exactly the moment that would make either "delivered" or "not delivered" ambiguous to both sides. What most systems that claim exactly-once actually deliver in practice is **at-least-once delivery**, a message might be delivered more than once, **combined with idempotent processing** on the consumer side, which together produce an effect indistinguishable from true exactly-once, without requiring the network layer itself to solve an effectively unsolvable problem.\n\nThis is why the earlier lesson on message queues emphasized that consumers must be idempotent: it is not a nice-to-have alongside at-least-once delivery, it is the specific mechanism that makes at-least-once delivery plus idempotent processing behave exactly like exactly-once from the caller\'s perspective, even though the delivery guarantee underneath never actually became exactly-once at all.\n\nIn an interview, when discussing retries, timeouts, or message redelivery, always name idempotency as the actual solution, not simply "we would make sure it does not happen twice." Say: "the client generates an idempotency key per payment attempt, and the server stores it alongside the result. If the same key arrives again, whether from a client retry after a timeout or a redelivered queue message, I return the stored result instead of processing the payment a second time." Naming the key and where it is checked is what shows you understand the mechanism, not just the word.',
      designChallenge: {
        prompt:
          "A client's payment request times out and it retries with the same idempotency key. Design the service so a repeated request with a key it has already processed returns the stored result instead of charging the wallet a second time.",
        gradingCriteria: [
          'The canvas has a service component connected to a database or cache that stores processed idempotency keys and their results',
          'The canvas has a separate database component, or the same one clearly distinguished, holding the actual wallet balance',
          'The design shows the service checking the idempotency store before touching the wallet balance, not the other way around',
          'A connection label references idempotency keys or duplicate detection, not a generic label',
        ],
      },
    },
    {
      id: 'as-rate-limiting',
      title: 'Rate limiting strategies',
      content:
        'A single misbehaving client, a buggy retry loop, a scraper, or a deliberate attacker, can send far more requests than any reasonable user would, and without something stopping it, that one client can degrade the service for every other user sharing the same infrastructure. **Rate limiting** caps how many requests a given client can make in a given window, and the specific algorithm you choose changes how that cap actually behaves under real, bursty traffic.\n\n**Fixed window** counting is the simplest approach: allow 100 requests per client per one-minute window, reset the counter to zero the instant the clock ticks over into the next minute. It is trivial to implement and reason about, but it has a real edge-case flaw: a client can send 100 requests in the last second of one window and another 100 in the first second of the next, 200 requests in roughly two seconds even though the configured limit was 100 per minute, because the two windows are counted completely independently with no memory of each other.\n\n**Sliding window** fixes this by looking at a continuously moving one-minute period ending at the current instant, rather than a fixed clock-aligned window, so the double-burst-at-the-boundary problem from fixed windows cannot happen, since there is no hard boundary for a client to straddle. This is more accurate but costs more to implement, since the system now has to track individual request timestamps, or a close approximation of them, rather than a single integer counter that resets on a clock tick.\n\n**Token bucket** takes a different approach entirely, one built specifically to tolerate legitimate bursts rather than smooth them away entirely. Picture a bucket that holds up to, say, 100 tokens, refilling at a steady rate of a few tokens per second. Every request consumes one token, and a request is only rejected once the bucket is empty. A client that has been quiet for a while walks in with a full bucket and can legitimately burst, ten requests all at once, without being immediately rate-limited, since it had tokens saved up, while a client sending a sustained high rate quickly drains its bucket faster than it refills and gets throttled. This models real usage far better than a hard per-window cap, since genuine users are naturally bursty rather than perfectly evenly spaced.\n\nWhere the limit is enforced matters as much as which algorithm implements it. Rate limiting at the API Gateway, before a request ever reaches your actual services, protects your entire backend uniformly and in one place, rather than every individual service having to reimplement its own limiting logic and risk inconsistent behavior between them. The counter itself needs to live somewhere every gateway instance can read and update it, typically Redis, since a rate limit tracked only in one gateway instance\'s local memory would let a client trivially bypass it just by having requests happen to land on a different instance behind the load balancer each time.\n\nIn an interview, name the algorithm and where it is enforced: "I would use a token bucket at the API Gateway, backed by Redis so the count is shared across every gateway instance, since it tolerates a legitimate short burst better than a hard fixed-window cap, and enforcing it once at the gateway protects every service behind it uniformly." Naming both the algorithm and the enforcement point is what shows you have actually built one of these before.',
      designChallenge: {
        prompt:
          'Design a rate limiting setup at the API Gateway: a shared, fast store that tracks request counts across every gateway instance, so a client cannot bypass the limit just by having requests land on a different gateway instance.',
        gradingCriteria: [
          'The canvas has an api-gateway component that receives client requests',
          'The canvas has a cache component connected to the api-gateway, representing the shared rate-limit counter store',
          'The design implies more than one client or multiple gateway instances sharing the same cache, rather than a limit tracked only locally',
          'A connection label references rate limiting, token bucket, or request counting, not a generic label',
        ],
      },
    },
    {
      id: 'as-circuit-breakers',
      title: 'Circuit breakers, retries, and timeouts',
      content:
        'Your service calls a downstream dependency, another internal service, a third-party API, and that dependency starts failing or responding painfully slowly. Without protection, every request that depends on it queues up waiting, threads and connections pile up unreleased, and the failure of one struggling dependency spreads backward through your own system until it takes your service down too, even though your service\'s own code never had a bug in it. **Timeouts**, **retries**, and **circuit breakers** are the three mechanisms that stop a failing dependency from becoming a failing system.\n\nA **timeout** is the simplest and most fundamental of the three: never wait forever for a response. Without one, a single slow dependency can hold a request, and the thread or connection serving it, open indefinitely, and enough of those piling up simultaneously exhausts your service\'s own resources even though the actual failure belongs entirely to something else downstream. Every outbound call needs an explicit timeout, tuned to how long a legitimate, healthy response should realistically take, not left at whatever a library\'s default happens to be.\n\nA **retry** re-attempts a failed call, on the reasonable assumption that many failures are transient: a momentary network blip, a downstream instance that was mid-restart for a fraction of a second. But a naive retry, especially one fired immediately with no delay, can make a struggling dependency measurably worse: if a service is failing because it is overloaded, an immediate retry from every single caller adds even more load onto the exact thing that is already struggling to keep up, potentially the difference between it recovering and it falling over completely. **Exponential backoff**, waiting progressively longer between each retry attempt, and adding random jitter so many callers do not all retry at the exact same synchronized instant, is what makes retries actually help rather than compound the original problem.\n\nA **circuit breaker** goes a step further than retries by tracking failure rates over time and, once a dependency is failing enough, stopping calls to it entirely for a cooldown period, failing those calls immediately and locally instead of even attempting them. This mirrors an electrical circuit breaker tripping to prevent a bigger problem: better to fail fast and predictably than to keep hammering an already-struggling dependency with the exact traffic that is contributing to its failure. After the cooldown period passes, the circuit breaker allows a small number of test requests through; if those succeed, it closes again and resumes normal traffic, and if they fail, it stays open and waits longer before trying again.\n\nThese three mechanisms are meant to work together, layered, not as alternatives to pick just one of. A timeout caps how long any single call is allowed to take. A retry, with backoff, gives a genuinely transient failure a second chance without making things worse. A circuit breaker steps in once failures become sustained rather than occasional, stopping the retries themselves from continuing to hammer a dependency that clearly is not transiently blipping but is actually down. Using retries without a circuit breaker means a sustained outage gets retried forever, adding load to something that has no chance of recovering under that pressure. Using a circuit breaker without sensible timeouts means individual calls can still hang long enough to do real damage before the breaker even has a chance to notice the pattern.\n\nIn an interview, when a design calls another service, mention all three together, layered correctly: "I would set an explicit timeout on the call, retry a small number of times with exponential backoff and jitter for transient failures, and wrap the whole thing in a circuit breaker that trips after a sustained failure rate, so we stop hammering a dependency that is clearly down rather than continuing to retry into it forever." Naming all three, and how they layer, is what shows you have actually operated a system that depends on other systems, not just read the definitions once.',
      designChallenge: {
        prompt:
          'Your Order Service calls a downstream Payment Service that has started failing repeatedly. Design the setup so a sustained failure stops new calls to the Payment Service for a cooldown period, rather than every incoming order request hanging or retrying into a service that is clearly down.',
        gradingCriteria: [
          'The canvas has an Order Service component connected to a Payment Service component',
          'The connection between them is labeled to reference a circuit breaker, timeout, or retry with backoff, not left as a generic label',
          'The design does not show the Order Service calling the Payment Service through an unbounded number of parallel retrying paths',
          'The Payment Service is shown as a distinct, separately failing component rather than merged into the Order Service itself',
        ],
      },
    },
  ],
  plannedLessons: [],
};
