import type { ArchitectureModule } from '../../types';

export const foundations: ArchitectureModule = {
  id: 'foundations',
  title: 'Foundations',
  blurb: 'What interviews actually test, and the two levers you have when a single server stops being enough.',
  lessons: [
    {
      id: 'as-what-interviews-test',
      title: 'What system design interviews are actually testing',
      content:
        'You walk into a system design interview and the prompt is one sentence: "design a URL shortener." No function signature, no test cases, no obviously right answer. If your career so far has been solving well-defined coding problems, this format can feel like a trick question. It is not a trick. It is a completely different skill, and understanding what it actually measures is the first step to doing well at it.\n\nA system design interview is not testing whether you can draw boxes and arrows on a whiteboard. It is testing whether you can take an ambiguous, underspecified problem and turn it into a concrete plan, while explaining the trade-offs behind every decision out loud. The interviewer already knows how to design a URL shortener. What they do not know yet is how you think when nobody hands you the requirements up front.\n\nThis is why the strongest candidates spend the first few minutes asking questions instead of drawing anything. How many users, how many requests per second, is this read-heavy or write-heavy, does data need to be strongly consistent or is eventual consistency fine. A junior answer jumps straight to a diagram with a load balancer, five microservices, and a message queue because that is what "looks like" a real system. A senior answer starts with the smallest thing that could possibly work, and adds a piece of complexity only when a specific requirement demands it. Every extra box on the diagram should have a reason you can state in one sentence.\n\n**Trade-offs** are the actual currency of this interview. There is no configuration of a distributed system that is fast, perfectly consistent, and cheap to run all at the same time. Choosing a cache means accepting that some reads will occasionally be stale. Choosing strong consistency for a payment ledger means accepting that a write might have to wait on multiple replicas before it returns. An interviewer is listening for whether you notice you are making a trade-off and can defend it, not whether you avoided trade-offs altogether, because that is not possible.\n\nThe other thing being tested, quietly, is whether you can reason about a system you did not build from memory. In your day job you already know how your codebase works. In this interview you are handed a blank page and have to construct a mental model live, the same way you would need to on your first week at a new company reading an unfamiliar codebase for the first time. That is a real, transferable skill, and it is exactly why companies spend forty-five minutes on it.\n\nIn an interview, when you get an open-ended prompt, resist the urge to start drawing immediately. Say out loud: "before I start, let me confirm the scale: how many users, how many requests per second, and what happens if this data is a few seconds stale." That single habit, asking before designing, is the most reliable signal of senior-level thinking in the first two minutes of the interview, and it sets up everything you draw afterward to actually be justified by something real.',
      designChallenge: {
        prompt:
          'Before any specific pattern, warm up with the smallest system that could work. A user opens an app and checks their account balance. Design the simplest thing that could answer that request: something that receives it, and something that holds the real data.',
        gradingCriteria: [
          'The canvas has a client-like component that represents where the request starts',
          'The canvas has a service or server component that receives the request',
          'The canvas has a database or storage component holding the actual balance data',
          'The components are connected in order, showing the request flowing from client to service to database',
        ],
      },
    },
    {
      id: 'as-vertical-vs-horizontal-scaling',
      title: 'Vertical vs horizontal scaling',
      content:
        'Your single server handled the first thousand users fine. Now the product got popular and it is falling over under load, the CPU pegged at 100%, requests timing out. You have exactly two ways to add more capacity: make that one server bigger, or add more servers next to it. These are **vertical scaling** and **horizontal scaling**, and the difference between them shapes almost every other decision you will make about a system\'s architecture.\n\nVertical scaling means upgrading the machine itself: more CPU cores, more RAM, a faster disk. It is the simplest possible fix, because nothing about your application code has to change, you just move it to bigger hardware. For a while this works remarkably well; cloud providers rent machines with hundreds of gigabytes of RAM and dozens of cores, and a lot of real production systems never outgrow a single well-provisioned server. But vertical scaling has a hard ceiling: there is a biggest machine you can rent, and once you hit it, you are stuck. It also leaves you with a single point of failure. If that one machine crashes, every user is affected at once, with nothing else standing behind it.\n\nHorizontal scaling means adding more servers instead of making the existing one bigger, and routing traffic between them with a load balancer. In theory this scales without a practical ceiling, since you can keep adding servers as traffic grows. It also gives you fault tolerance for free: if one server dies, the others keep serving traffic while it is replaced. The catch is that horizontal scaling only works cleanly if your servers are stateless. If a user\'s session data lives only in the memory of the specific server that first handled their login, and their next request lands on a different server behind the load balancer, that server has no idea who they are. The fix is to move shared state, sessions, uploaded files, anything a user\'s next request might depend on, into a shared store like Redis or a database, so any server can serve any request at any time.\n\nIn practice, almost every real system does both. You scale vertically first because it avoids any added complexity, upgrading a single server as far as it reasonably goes. You reach for horizontal scaling once you hit that ceiling, or once you need fault tolerance that a single machine can never give you no matter how large it is. The database tier usually stays vertical, or scales through replicas and sharding rather than naive horizontal duplication, because a database inherently holds state that cannot simply be split across identical copies without careful design.\n\nThe database is actually the hardest part of this story, which is why "just add more servers" is a half answer in an interview. Stateless application servers scale horizontally with almost no thought once statelessness is guaranteed. The data layer behind them is a different problem entirely, one that needs replication and partitioning strategies of its own, which is why interviewers who ask about scaling usually want to hear you separate the two.\n\nIn an interview, when asked how you would handle 10x more traffic, do not just say "add more servers." Say: "First I would confirm the application servers are stateless, moving anything like sessions into Redis. Then I would scale horizontally behind a load balancer, since that has no practical ceiling once statelessness is guaranteed, while treating the database as a separate scaling problem that needs replicas or sharding rather than simple duplication." Naming the database as a distinct concern is what separates a senior answer from someone reciting "just add more servers."',
      designChallenge: {
        prompt:
          'Your single server is falling over under load. Redesign it so traffic is spread across multiple servers, and make sure a logged-in user does not lose their session just because a later request lands on a different server than their first one.',
        gradingCriteria: [
          'The canvas has a load balancer positioned in front of more than one service component',
          'The canvas has at least two separate service components, showing horizontal scaling rather than one bigger box',
          'The canvas has a shared store, cache or database, that every service instance can reach for session data instead of each holding its own',
          'Connections show the load balancer reaching each service instance, and each instance reaching the shared store',
        ],
      },
    },
    {
      id: 'as-load-balancers',
      title: 'Load balancers and load balancing algorithms',
      content:
        'You already know a load balancer sits in front of your servers and spreads requests across them. What is easy to skip past is the actual decision it makes on every single request: which of your ten, or hundred, servers should handle this one. That decision is made by a **load balancing algorithm**, and the algorithm you choose has real consequences for both performance and correctness.\n\nThe simplest algorithm is **round robin**: request one goes to server A, request two to server B, request three to server C, then back to A. It requires no state and is trivial to implement, and it works well when every request costs roughly the same amount of work and every server has roughly the same capacity. It breaks down the moment either of those assumptions is false. If server A happens to be handling five slow, expensive requests while server B sits idle, round robin will still send the next request to whichever server is "next in line," even though B is clearly the better choice.\n\n**Least connections** fixes this by tracking how many active requests each server currently has in flight, and routing the next request to whichever server has the fewest. This adapts naturally to servers that are momentarily overloaded, or to requests of wildly different cost, like a slow report-generation endpoint sitting next to a fast health check. The trade-off is that the load balancer now has to track state, each server\'s current connection count, rather than being a completely stateless router, a small amount of extra complexity for a meaningfully better distribution of load.\n\n**IP hash** routes based on a hash of the client\'s IP address, which means the same client reliably lands on the same server every time. This matters when a server holds any state in memory that a client\'s next request depends on, session data being the classic example, though the better fix, as covered in the previous lesson, is usually to make servers stateless in the first place rather than relying on IP hashing to paper over it. IP hash has its own failure mode too: if many users sit behind the same corporate NAT or VPN, they all hash to the same IP and get piled onto a single server, defeating the whole point of load balancing for that group.\n\nNone of these algorithms matter if the load balancer keeps sending traffic to a server that has crashed. **Health checks** are what make load balancing self-healing: the load balancer pings each server every few seconds, and the moment one stops responding correctly, it is removed from rotation automatically, with no human needing to notice or intervene. This is arguably the single most valuable thing a load balancer does, more than the specific algorithm chosen, because it turns a single server crash from a full outage for some fraction of users into a non-event that the system recovers from on its own.\n\nIn an interview, if you mention a load balancer, be ready to say which algorithm and why. "I would use least connections here rather than round robin, because this endpoint\'s cost varies a lot per request, and I would make sure health checks are configured so a crashed instance gets pulled out of rotation automatically rather than continuing to receive traffic it cannot answer." Naming the specific algorithm, not just "a load balancer," is what shows you understand what is actually happening on every request rather than just knowing the vocabulary.',
      designChallenge: {
        prompt:
          'Design a small setup with a load balancer in front of three separate service instances, where a crashed instance stops receiving new traffic automatically. Label at least one connection so it is clear the load balancer is actively health-checking and routing, not just blindly forwarding to all of them.',
        gradingCriteria: [
          'The canvas has exactly one load-balancer component',
          'The canvas has at least two separate service components behind it, not merged into a single box',
          'The load balancer is connected individually to each service instance',
          'At least one connection label references health checks, routing, or load distribution, showing more than a generic arrow',
        ],
      },
    },
    {
      id: 'as-client-server-vs-p2p',
      title: 'Client-server vs peer-to-peer',
      content:
        'Almost everything you have designed so far assumes one shape: a **client** asks, a **server** answers. Your phone opens an app, the app calls an API, the API returns data. This is the **client-server model**, and it is so dominant that it is easy to forget it is a choice at all, one with a real alternative: **peer-to-peer**, where instead of a central server, every participant, or "peer," can talk directly to every other peer, with no single machine acting as the authority everyone depends on.\n\nThe client-server model wins on almost every practical dimension for typical products. There is one place to enforce business rules, one place to check permissions, one database that is the unambiguous source of truth. When a streaming service decides what content you can watch, that decision is made once, on their servers, and every device just asks. This centralization is also what makes the model easy to reason about, monitor, and secure: you know exactly where your data lives and exactly which machine is responsible when something goes wrong.\n\nPeer-to-peer flips this entirely. BitTorrent is the clearest example: when you download a large file, you are not pulling it from one central server, you are pulling different pieces of it from dozens of other people\'s computers who already have those pieces, while simultaneously uploading pieces you have to others. There is no single machine that could be overwhelmed by popularity, in fact popularity helps, because more peers means more available sources for each piece of the file. Peer-to-peer systems are naturally resilient to any single point of failure, since no one node\'s disappearance takes the whole system down.\n\nThe trade-off is control and consistency. Nobody in a peer-to-peer network can unilaterally decide "this rule applies to everyone" the way a central server can, because there is no server, only equal participants. This is precisely why peer-to-peer struggles for use cases like a bank balance or a payment system: there needs to be one agreed-upon, authoritative answer to "how much money does this account have," and a network of equal peers without a central authority has no clean way to guarantee that agreement, without adding an entirely separate and complex consensus mechanism on top, the kind blockchain systems build specifically to solve this problem.\n\nMost real products you will be asked to design in an interview are client-server, and for good reason: they need a central authority over data, permissions, and consistency. Peer-to-peer shows up when the product\'s core value is specifically about not needing a central authority, direct file sharing, or resilience to any single company or server disappearing. Recognizing which category your interview prompt falls into early tells you whether "add a server" is even the right direction at all.\n\nIn an interview, if the prompt is anything like "design a payment system," "design a chat app with read receipts," or "design a notification service," default straight to client-server without hesitation, since these all need one authoritative source of truth. Only reach for peer-to-peer if the prompt specifically resembles file distribution at scale or explicitly names decentralization as a requirement, and say out loud why: "I am choosing client-server because we need one authoritative balance for this account, and peer-to-peer has no clean way to guarantee that without a lot of extra complexity this problem does not need."',
      designChallenge: {
        prompt:
          'A ride-sharing app needs to guarantee that only one driver can accept a given ride request, never two at once. Design the client-server shape that gives this system one single authoritative place to decide who won the ride, with a couple of client devices connected to it, so it is clear why a peer-to-peer model would not give you that same guarantee.',
        gradingCriteria: [
          'The canvas has at least two client components representing separate devices or users',
          'The canvas has exactly one central service or api-gateway component that both clients connect to, rather than clients connecting to several different services',
          'The canvas has a database or storage component owned by the central service, representing the single source of truth',
          'No connection is drawn directly between the two client components, showing there is no peer-to-peer link between them',
        ],
      },
    },
    {
      id: 'as-latency-vs-throughput',
      title: "Latency vs throughput, and why you can't maximize both",
      content:
        'Two systems can both be "fast" in completely different ways, and confusing the two is one of the most common mistakes candidates make in a design interview. **Latency** is how long a single request takes, from the moment it is sent to the moment the response arrives, measured in milliseconds. **Throughput** is how many requests the system can process per second, in total, across every user combined. A system can have excellent latency and terrible throughput, or the reverse, and optimizing for one often makes the other worse.\n\nThink of a single-lane toll booth versus a ten-lane highway. The toll booth might process each individual car in two seconds flat, every time, with beautifully consistent low latency. But it can only get through thirty cars a minute, no matter how fast each individual transaction is, because there is only one lane. A ten-lane highway might let each car take five seconds to get through, slightly higher latency per car, but the total number of cars passing through per minute is far higher, because ten cars are moving at once instead of one. Neither design is "faster" in an absolute sense; they are optimized for different things.\n\n**Batching** is the clearest example of this trade-off in backend systems. If you write database records one at a time, each individual write returns almost instantly, excellent latency. But the database has to do the overhead of a full transaction for every single record, so throughput, the total records written per second, is low. If you batch a thousand records into one write, that one write takes noticeably longer, worse latency for that specific operation, but the total records-per-second the system can sustain goes up dramatically, because you paid the transaction overhead once for a thousand records instead of a thousand times.\n\nThis is also why "just add more servers" does not always fix a slow endpoint. If ten servers behind a load balancer each individually take three seconds to answer a request because that request does genuinely expensive work, adding an eleventh server increases throughput, more total requests handled per second, but it does nothing at all for latency, since every individual request still takes three seconds on whichever single server handles it. Fixing latency requires making the actual work faster: a better index, a cache, doing less work per request, not simply running more copies of the same slow work in parallel.\n\nDifferent parts of the same system often have different priorities here. A checkout button needs low latency, because a user is sitting there staring at a loading spinner, waiting. A nightly batch job that reconciles a million transactions cares only about throughput, total records processed by morning, and could not care less if any individual record takes an extra hundred milliseconds. Naming which one a given endpoint actually needs, out loud, is what tells an interviewer you are not applying one generic "make it fast" instinct to every part of the system uniformly.\n\nIn an interview, when asked to improve a slow endpoint, ask yourself first: is the complaint about latency, one request feels slow, or throughput, the system falls over under many simultaneous requests? The fix for each is different. Say: "if this is a latency problem, I would look at caching or a better index to make each individual request faster. If it is a throughput problem, I would look at horizontal scaling or batching to handle more total volume, even if any single request stays the same speed." Separating the two explicitly is what shows real understanding rather than a reflexive "add a cache" answer to every performance question.',
      designChallenge: {
        prompt:
          'A dashboard endpoint recomputes an expensive report from the database on every single request, and users complain it feels slow to load. This is a latency problem on one specific read path, not a throughput problem. Add the component that fixes latency for this repeated, expensive read, without touching how writes work.',
        gradingCriteria: [
          'The canvas has a cache component sitting between the service and the database',
          'The cache is connected to the service, not directly to the client',
          'The database is still present and connected to the service, showing writes and cache misses still reach the real data',
          'The fix is not just a load balancer with duplicate service instances, since that addresses throughput rather than the latency problem described',
        ],
      },
    },
  ],
  plannedLessons: [],
};
