import type { ArchitectureModule } from '../../types';

export const fullSystemDesign: ArchitectureModule = {
  id: 'full-system-design',
  title: 'Full System Design Challenges',
  blurb: 'Bigger canvases, end to end: a URL shortener, a rate limiter, a payment system, and more.',
  lessons: [
    {
      id: 'as-url-shortener',
      title: 'Design a URL shortener',
      content:
        'A URL shortener takes a long link and returns a short one that redirects back to the original when visited. It sounds almost trivially simple, and the core operation genuinely is, but it is one of the most commonly asked system design interview questions precisely because doing it at scale forces you to apply nearly everything covered in this course to one small, easy-to-state problem: request patterns, storage choice, caching, and even a little bit of encoding theory.\n\nStart with the two core operations, since everything else in the design flows from correctly identifying their shape. **Creating** a short URL is comparatively rare and write-light: a user submits a long URL, the system generates a short code, and stores the mapping. **Redirecting** from a short URL to the original is extremely frequent and read-heavy: every single click on a shared short link is a lookup that needs to return the original URL as fast as possible, ideally in low tens of milliseconds, since a slow redirect is a bad experience on every single click across the entire product. This asymmetry, reads dramatically outnumbering writes, should shape almost every decision that follows.\n\nGenerating the short code itself has a genuinely interesting design choice buried in it. A naive auto-incrementing integer counter is simple and guarantees uniqueness with no coordination needed between separate application servers if you reserve blocks of ids in advance, but it makes short codes sequentially guessable, letting anyone enumerate every short URL ever created just by incrementing a number. Encoding that same counter value into **base62**, using the 62 characters `0-9a-zA-Z`, produces a short, dense code from the same underlying unique integer without needing any separate randomness, while a purely random code needs a uniqueness check against existing codes on every single creation to avoid an unlikely but real collision.\n\nGiven the read-heavy shape identified earlier, caching is not optional here, it is close to the entire performance story. A cache-aside Redis layer in front of the database, holding the hottest short-code-to-URL mappings, can serve the overwhelming majority of redirects without ever touching the database at all, since a small number of URLs, the ones that actually went viral or get shared often, account for a disproportionate share of all traffic, a pattern sometimes called a power-law distribution. The database itself still needs to be the source of truth and handle cache misses, but with caching in front of it, it only ever sees a small fraction of total redirect traffic.\n\nAt real scale, the mapping table itself, potentially billions of rows for a long-running popular service, is a natural candidate for sharding by short code, spreading both storage and write load for new URL creation across multiple database instances rather than one ever-growing table. A CDN sitting in front of the redirect endpoint can push read latency down even further for a globally distributed user base, serving cached redirects from an edge node near each user rather than routing every single click back to one origin region.\n\nIn an interview, walk through this in the order it was covered here: identify the read-heavy, write-light shape first, choose an encoding strategy for the short code and defend it, add caching since the access pattern is so skewed toward a small number of hot URLs, and only reach for sharding once you have explicitly stated the scale that justifies it. Say: "reads dominate writes here by orders of magnitude, so I would cache aggressively and shard the mapping table by short code only once a single database can no longer hold the volume, rather than sharding from the very first design." Building the design in that order is what shows a structured process, not a memorized diagram.',
      designChallenge: {
        prompt:
          'Design a URL shortener end to end: the write path for creating a short URL, and the read-heavy redirect path, with caching in front of the database to absorb the disproportionate traffic that a small number of popular links receive.',
        gradingCriteria: [
          'The canvas has at least four distinct components: a client, a service or api-gateway, a cache, and a database',
          'The redirect (read) path shows the service checking the cache before the database',
          'The create (write) path shows the service writing to the database, and optionally populating the cache',
          'The design reflects that reads (redirects) and writes (creates) are handled differently, not identical for both operations',
        ],
      },
    },
    {
      id: 'as-standalone-rate-limiter',
      title: 'Design a rate limiter as a standalone service',
      content:
        'An earlier lesson covered rate limiting as a feature living inside an API Gateway. This lesson asks a related but different question: what if the rate limiter itself needs to be a standalone service, callable by many different other services across an entire company, rather than logic embedded in one specific gateway. This shift, from a feature to a service, surfaces new design questions that only appear once multiple independent callers need to share the same rate-limiting infrastructure.\n\nThe core operation a standalone rate limiter needs to expose is deceptively simple to state: given a key, an api key, a user id, an ip address, and a limit, has this key exceeded its allowed request count in the current window. Every calling service sends this check before doing its own work, and the rate limiter answers allow or deny, extremely fast, since it now sits on the critical path of every single request across every service that calls it, exactly the kind of dependency the circuit breaker lesson warned needs a tight timeout and a fallback plan if it is ever slow or unavailable.\n\nBecause multiple, independent services all call this one rate limiter, its own storage has to be centralized and shared, typically Redis, holding a counter per key using one of the algorithms covered earlier, token bucket being a common choice since it tolerates legitimate bursts well. This centralization is exactly what makes a standalone rate limiter different from the gateway-embedded version: a gateway\'s rate limiter only ever needs its own instances to agree with each other, while a standalone service needs every calling service across the company to agree on the exact same counts for the exact same keys, which is only possible if there is one shared source of truth they are all checking against.\n\nThe rate limiter becoming a shared dependency for potentially every other service in the company raises the stakes on its own availability significantly. If the rate limiter itself goes down, do all of its callers now also become unavailable, since they cannot check whether to allow a request, or should they fail open, allowing requests through unchecked, and accept a temporary loss of rate limiting protection rather than a full outage cascading from one shared dependency. This decision, fail open versus fail closed, needs to be made explicitly and deliberately per calling service, not left as an accidental default, since the right answer plausibly differs for a login endpoint versus an internal analytics ingestion endpoint.\n\nLatency matters enormously here in a way it might not for other internal services, because this check now sits on literally every protected request across the company, adding to the total latency of every single one of them. A rate limiter response needs to come back in single-digit milliseconds to avoid becoming the new bottleneck for every service that depends on it, which is part of why the storage layer is almost always Redis specifically, an in-memory store built for exactly this kind of extremely low-latency, high-throughput counter workload.\n\nIn an interview, treat making the rate limiter a service as its own design problem rather than a restatement of the earlier gateway lesson. Say: "since multiple services now share this rate limiter, I would centralize the counters in Redis so every caller agrees on the same count for the same key, and I would decide explicitly, per calling service, whether to fail open or fail closed if the rate limiter itself becomes unavailable, rather than letting one shared dependency\'s outage silently become an outage for everything that depends on it." Naming the fail-open-versus-fail-closed decision explicitly is what shows you understand the new stakes of centralizing this.',
      designChallenge: {
        prompt:
          'Design a standalone Rate Limiter service that multiple other services, for example an Order Service and a Login Service, both call before processing their own requests, sharing one centralized counter store so every caller agrees on the same limits for the same keys.',
        gradingCriteria: [
          'The canvas has a Rate Limiter service component with a cache component as its shared counter store',
          'At least two separate calling services, for example Order Service and Login Service, are connected to the Rate Limiter',
          "The calling services connect to the Rate Limiter before their own downstream work, not after",
          "The Rate Limiter's cache is a single shared component, not duplicated per calling service",
        ],
      },
    },
    {
      id: 'as-payment-wallet-system',
      title: "Design a payment and wallet system (NexusPay's real architecture)",
      content:
        'Every concept covered across this course shows up at once in a real payment and wallet system, which is exactly why NexusPay, the flagship project referenced throughout, is worth designing end to end as a capstone rather than piece by piece. This lesson walks through the real shape of that system, and the answer key here is not hypothetical, it is the actual architecture NexusPay runs in production.\n\nThe system splits into seven independently deployable services, each owning its own PostgreSQL database, following the database-per-service pattern from an earlier lesson: a User Service, Wallet Service, Transaction Service, Notification Service, Analytics Service, Payment Gateway Service, and an API Gateway sitting in front of all of them, exactly the API Gateway pattern covered earlier, handling authentication and routing once for every client request rather than each service reimplementing it. Every client, web or mobile, only ever talks to the gateway directly, never to an internal service.\n\nTwo entirely different messaging technologies coexist deliberately, each matched to the shape of problem it solves, exactly the distinction drawn in the messaging lessons earlier. RabbitMQ handles directed commands and the Saga orchestration for a transfer: a message meant for one specific consumer, like crediting one specific wallet. Kafka handles event streaming to a `transactions.stream` topic, since fraud detection, analytics, and any future consumer each need their own full, independent view of every transaction that has ever happened, with the ability to replay history if a new consumer is added later, exactly the Kafka access pattern covered earlier and exactly wrong for RabbitMQ\'s shape.\n\nRedis shows up in four genuinely distinct roles here, and naming all four separately, rather than lumping them together as "we use Redis," is what a strong answer does. It caches user profiles with a five-minute TTL, cache-aside, absorbing read load for data that changes rarely. It backs rate limiting, a hundred requests per minute per IP, at the gateway, the exact pattern from the standalone rate limiter lesson. It implements the distributed lock, `SET NX EX 30`, that prevents two simultaneous requests from double-spending the same wallet, the exact mechanism from the distributed locks lesson. And it backs JWT blacklisting, letting a token be immediately revoked before its natural expiration, which none of the other three roles would cover on their own.\n\nThe transfer flow itself is the saga from that earlier lesson, running for real: acquire the Redis lock on the source wallet, save the transaction as PENDING in the Transaction Service\'s own database, request the debit via RabbitMQ, debit the source wallet, request the credit, credit the destination wallet, then publish `transaction.completed` to Kafka for every downstream consumer that cares. If the credit step fails after the debit succeeded, the saga\'s compensating transaction credits the source wallet back, exactly the failure path walked through in the saga lesson, now anchored to the specific service and specific database where it actually runs.\n\nThe KYC flow demonstrates event-driven decoupling working exactly as intended, end to end: a user submits documents, an admin approves them, `user.kyc.approved` publishes to RabbitMQ, the Wallet Service reacts by auto-creating a wallet, and the Notification Service reacts independently by sending a confirmation, with the admin\'s approval action never needing to know or care that a wallet will be created or that a notification will be sent, exactly the producer/consumer decoupling from the messaging lessons, now doing real, consequential work rather than an illustrative example.\n\nIn an interview, if you are ever asked to design a payment platform, this is the shape to reach for: services split along bounded contexts, database-per-service, a saga for any multi-step financial flow with named compensating transactions, RabbitMQ for directed commands and Kafka for event streams kept deliberately separate, and Redis doing several distinct, individually justified jobs rather than one vague catch-all. Naming each piece and the specific lesson-level reason it exists, not just describing NexusPay from memory, is what shows you actually understand the architecture rather than having memorized a description of it.',
      designChallenge: {
        prompt:
          "Design NexusPay's core transfer flow end to end: an API Gateway in front of a Wallet Service and a Transaction Service, a Redis-backed lock preventing double-spending, RabbitMQ carrying the debit and credit commands between services, and Kafka publishing the completed transaction event for downstream consumers like Analytics.",
        gradingCriteria: [
          'The canvas has an api-gateway component in front of at least a Wallet Service and a Transaction Service, each with its own database',
          'The canvas has a cache component used for the distributed lock, connected to the service that acquires it before touching the wallet balance',
          'The canvas has a queue component carrying the debit and credit commands between Wallet Service and Transaction Service',
          'The canvas has a kafka component that the Transaction Service publishes the completed event to, with at least one downstream consumer such as Analytics connected to it',
        ],
      },
    },
    {
      id: 'as-notification-system',
      title: 'Design a notification system',
      content:
        'A user needs to be told something happened: a payment completed, a message arrived, a price dropped. That single idea, notifying a user, fans out into a genuinely interesting system design problem the moment you consider that a notification can travel through several different channels, push, email, SMS, each with wildly different reliability, cost, and speed, and that the trigger for a notification is almost always some other part of the system finishing its own, unrelated work.\n\nThe very first design decision is how a notification gets triggered in the first place, and it is a direct application of the sync-versus-async lesson from earlier. The service that just completed a payment should not have to wait for a notification to actually be delivered before it can respond to the user who made that payment, since delivery is not something the original caller needs to know about before their own response can return. The correct shape is for the originating service to publish an event, "payment completed," to a queue and move on immediately, with a dedicated Notification Service consuming that event independently and entirely off the critical path of the original request.\n\nOnce the Notification Service picks up an event, it has to decide which channel or channels to actually use, and this decision genuinely benefits from being data-driven rather than hardcoded per event type. A user\'s stored preferences, push enabled, email enabled, SMS disabled, combined with the notification\'s own priority, a security alert might override an SMS-disabled preference where a promotional message never would, determines the final channel selection. This lookup is read-heavy and rarely changes, a textbook cache-aside candidate from the caching lesson, so preferences do not need a fresh database read on every single notification sent.\n\n**Fan-out** is the term for one triggering event turning into several independent delivery attempts, one per channel a user has enabled, each with its own success or failure entirely independent of the others. A push notification failing because a device token is stale should never block or delay the email from also being attempted; the two are separate, independent jobs that happen to share the same originating event, and treating them as separate queue messages or separate worker jobs, rather than one function trying to do all channels sequentially in a single call, is what lets one channel\'s failure stay contained rather than taking the others down with it.\n\nEach individual channel has its own external dependency, an email provider, an SMS gateway, a push notification service, and each of those is exactly the kind of unreliable downstream dependency the circuit breaker lesson covered: a timeout so one slow provider cannot hold a worker thread forever, retries with backoff for a transient failure, and a circuit breaker that stops hammering a provider that is clearly down rather than continuing to retry into a sustained outage. A notification that fails after retries and a tripped breaker are exhausted should be recorded as failed rather than silently disappearing, since knowing whether the user actually got told is exactly the kind of thing worth being able to answer later.\n\nIn an interview, build this system in the order it naturally decomposes: an async trigger off the critical path, a preference lookup that is cache-friendly, fan-out into independent per-channel jobs, and resilience, timeouts, retries, circuit breakers, wrapped around each individual channel\'s external dependency. Say: "the triggering service just publishes an event and returns immediately; the Notification Service consumes it, looks up cached preferences, and fans out into independent per-channel jobs, each protected by its own timeout and circuit breaker so one failing provider cannot take the others down with it." That structure, more than any single piece of it, is what an interviewer is actually listening for.',
      designChallenge: {
        prompt:
          'Design a notification system: a triggering service publishes an event asynchronously, a Notification Service consumes it, looks up cached user preferences, and fans out into independent delivery jobs across at least two channels, for example push and email, each calling its own external provider.',
        gradingCriteria: [
          'The canvas has a triggering service connected to a queue, not directly to the Notification Service',
          'The Notification Service consumes from the queue and is connected to a cache for user preferences',
          'The Notification Service fans out to at least two separate external-api components representing different channels, such as push and email providers',
          'The two channel connections are independent of each other, not merged into a single combined delivery path',
        ],
      },
    },
    {
      id: 'as-realtime-analytics-pipeline',
      title: 'Design a real-time analytics pipeline',
      content:
        'A dashboard needs to show requests per second, right now, or active users, right now, numbers that are meaningfully wrong the instant they are more than a few seconds old. This is a fundamentally different problem from the batch reporting most systems are built around, and it pulls together the event streaming, throughput, and consistency lessons from earlier into one design that has to process a continuous, high-volume firehose of events with latency measured in seconds, not the minutes or hours a nightly batch job would tolerate.\n\nEvery user action, a click, a page view, a purchase, becomes an event published to a stream, Kafka being the natural fit from the event streaming lesson, since analytics is exactly the shape Kafka was built for, multiple independent consumers each needing their own full view of everything that happened, not a one-to-one RabbitMQ command. The **partition key** decision from the sharding and Kafka lessons matters here too: partitioning by something like user id keeps all of one user\'s events in order on the same partition, while still spreading the overall firehose across many partitions that can be processed in parallel.\n\n**Stream processing** is what turns a raw firehose of individual events into an aggregated number a dashboard can actually display, and it is the piece genuinely new to this lesson. A stream processor, conceptually a service consuming continuously from the event stream, maintains running aggregates, a rolling count of requests in the last sixty seconds, for example, updated incrementally as new events arrive rather than recomputed from scratch on every single query. This is the direct, real-time analog of the batching trade-off from the latency-versus-throughput lesson: computing an aggregate incrementally, a little bit of work per event as it arrives, trades some processing complexity for the ability to answer what the number is right now instantly, rather than recomputing the entire aggregate from raw events on every single dashboard refresh.\n\nThe dashboard itself should never query raw events directly, for the same reason a slow, expensive recomputation-per-request was flagged as a latency problem in an earlier lesson. It reads the pre-aggregated, continuously updated numbers the stream processor maintains, typically stored in a fast store like Redis specifically because the dashboard needs sub-second reads on numbers that are changing constantly, not the latency profile of a query against raw event storage scanning millions of rows to answer how many, right now.\n\nConsistency expectations here are explicitly relaxed, and naming that explicitly is exactly the kind of judgment the consistency-models lesson was building toward. A real-time counter that is a couple of seconds behind the true instantaneous value is completely acceptable, arguably unavoidable given the pipeline\'s inherent processing lag, the same eventual-consistency trade-off drawn for a social media like count earlier in the course, now applied to an entire pipeline rather than a single cached value. Demanding strong consistency here would mean blocking the dashboard\'s read on the full, current state of every event still in flight through the pipeline, defeating the entire purpose of a real-time view for a guarantee this specific use case never actually needed.\n\nIn an interview, frame a real-time analytics pipeline as event streaming plus continuous aggregation plus a fast read store, not as a database query problem: "events flow into Kafka, partitioned to keep processing parallelizable, a stream processor maintains rolling aggregates incrementally rather than recomputing from raw events, and the dashboard reads those aggregates from Redis for sub-second response times, accepting a few seconds of lag as the deliberate trade-off for a real-time-feeling pipeline that never blocks on the full, current state of everything in flight." Naming the specific consistency trade-off you are accepting, and why, is what closes out a design like this the way a senior candidate would.',
      designChallenge: {
        prompt:
          'Design a real-time analytics pipeline: events stream in continuously, a stream processor maintains rolling aggregates, and a dashboard reads pre-aggregated numbers from a fast store instead of querying raw events directly.',
        gradingCriteria: [
          'The canvas has a kafka component that services publish raw events to',
          'The canvas has a stream-processing service component consuming from the kafka stream',
          'The canvas has a cache component that the stream processor writes aggregated results to',
          'The dashboard or client component reads from the cache, not directly from the kafka stream or a raw events database',
        ],
      },
    },
  ],
  plannedLessons: [],
};
