import type { ArchitectureModule } from '../../types';

export const communicationPatterns: ArchitectureModule = {
  id: 'communication-patterns',
  title: 'Communication Patterns',
  blurb: 'REST, gRPC, GraphQL, sync vs async, and how message queues and event streams fit in.',
  lessons: [
    {
      id: 'as-rest-vs-grpc-vs-graphql',
      title: 'REST vs gRPC vs GraphQL, picking the right contract',
      content:
        'Your system starts at the edge: something needs to talk to it, a browser, a mobile app, another internal service, or a third-party integration. That something is a **client**, and the way it talks to your system is your **API**, the contract that defines exactly what a caller can ask for and what shape the answer comes back in. Three contracts dominate real systems, and picking the right one for a given caller is a design decision, not a matter of taste.\n\n**REST** organizes everything around resources and HTTP verbs: `GET /users/42` fetches a user, `POST /users` creates one, `PUT /users/42` updates one, `DELETE /users/42` removes one. Status codes communicate outcome without the client needing to parse the response body: 200 for success, 201 for created, 400 for a bad request, 404 for not found. REST is the safe, universally understood default, and it is what most public APIs use, precisely because every client that can make an HTTP request can consume a REST API without any special tooling.\n\n**GraphQL** solves a specific problem REST has: a REST endpoint returns a fixed shape of data, so a mobile client that only needs a user\'s name and avatar still receives every field the endpoint returns, wasted bandwidth on a slow connection. GraphQL lets the client specify exactly which fields it needs in the query itself, and the server returns exactly that, nothing more. This is powerful when different clients, web, iOS, Android, need different slices of the same underlying data, at the cost of a more complex server implementation and the loss of HTTP-level caching that REST gets for free through URLs.\n\n**gRPC** is built for fast, internal, service-to-service communication. It uses a binary format, Protocol Buffers, instead of JSON, which is smaller and faster to parse, and it generates strongly-typed client code automatically from a shared schema file, so a caller and callee agree on the exact shape of every message at compile time rather than discovering a mismatch at runtime. You would not use gRPC for a public API a browser calls directly, since browsers cannot speak gRPC natively without a proxy layer, but it is an excellent choice when one internal service needs to call another thousands of times per second with strict latency requirements.\n\nNone of these three is simply "the best" one. A public API serving unknown third-party developers wants REST, because it needs no special client tooling and is easy for anyone to integrate against. A mobile app juggling several different screens with different data needs benefits from GraphQL\'s flexibility. A payment service calling a wallet service internally, at high volume, wants gRPC for its speed and type safety. The same product can, and often does, use all three at different boundaries.\n\nIn an interview, do not default to REST out of habit, name why it fits the specific boundary you are describing. Say: "I would use REST for the public-facing API, since third-party developers need something they can integrate against with no special tooling, but gRPC between our own internal services, since we control both ends and want the speed and type safety that buys us." Naming the boundary each contract sits at is what shows you understand the trade-off, not just the three acronyms.',
      designChallenge: {
        prompt:
          'A payment platform has a public API that external partners integrate against, and internally the API Gateway calls a Transaction Service thousands of times per second. Design both boundaries: a client-facing side and an internal service-to-service side, and label the connections to show they use different contracts.',
        gradingCriteria: [
          'The canvas has a client component connected to an api-gateway or external-api component representing the public boundary',
          'The canvas has the gateway connected to an internal service representing the service-to-service boundary',
          'The connection representing the public boundary is labeled with a contract like REST or a similar public-facing protocol',
          'The connection representing the internal boundary is labeled with a contract like gRPC, distinct from the public one',
        ],
      },
    },
    {
      id: 'as-sync-vs-async',
      title: 'Synchronous vs asynchronous communication',
      content:
        'When one part of your system calls another, it has exactly two ways to wait for the answer: sit there and block until a response comes back, or fire off the request and move on, picking up the result later or not needing one at all. These are **synchronous** and **asynchronous** communication, and the choice between them, made correctly for each specific call in your system, is one of the most consequential decisions in any design.\n\nA **synchronous** call is the default mental model most people start with: the caller sends a request and waits, doing nothing else, until the response arrives. A checkout page checking whether a credit card is valid is naturally synchronous, because the user is staring at the screen and the very next thing that happens, showing a success or error message, depends entirely on that answer. Synchronous calls are simple to reason about: the code reads top to bottom, and by the time the next line runs, you know the previous step definitely finished, one way or another.\n\nThe cost of synchronous calls compounds the moment you chain several of them together. If step one takes 200 milliseconds, step two takes 300, and step three takes 150, a fully synchronous chain takes 650 milliseconds minimum, because each step waits for the one before it to fully finish before starting. If any single step in that chain is slow or fails, the entire chain is slow or fails with it, and the caller at the very front is stuck waiting on all of it, even for pieces of work that have nothing to do with what they actually needed answered right away.\n\n**Asynchronous** communication breaks that chain. The caller sends a request, typically by publishing a message to a queue, and moves on immediately without waiting for the work to actually complete. A separate worker picks up that message whenever it gets to it and processes it independently. A user submitting a payment does not need to wait for the confirmation email, the fraud-detection check, and the analytics update to all finish before seeing "payment sent," because none of those three things change whether the payment itself succeeded. Publish an event and return immediately; let background workers handle the rest.\n\nThe trade-off is that asynchronous work no longer gives the caller an immediate answer. If a user needs to know right now whether their payment succeeded, that specific check has to stay synchronous: saving the transaction to the database and returning confirmation that it worked. Only the work that genuinely does not need to finish before responding, and that the user is not actively waiting on, is a safe candidate to move behind a queue. Making everything asynchronous "for performance" without checking whether the caller actually needs an immediate answer is its own common mistake.\n\nIn an interview, for every step in a request\'s journey, ask yourself out loud: does the caller need to know the result of this specific step before I respond to them? "Saving the payment to the database has to be synchronous, since the user needs to know it succeeded before I tell them. Sending the confirmation email and updating analytics can be asynchronous, since neither changes what I tell the user right now." Separating these explicitly, one step at a time, is what a senior answer looks like instead of a blanket "I would make it all async for speed."',
      designChallenge: {
        prompt:
          'A user submits a payment. Saving the transaction must finish before you tell the user it succeeded, but sending a confirmation email and updating analytics do not need to block that response. Design the request path so the synchronous save happens directly, while the email and analytics work happens through a queue instead of blocking the response.',
        gradingCriteria: [
          'The canvas has a service component connected directly to a database for the synchronous transaction save',
          'The canvas has a queue component that the service publishes to for the non-blocking work',
          'At least one downstream consumer service reads from the queue, representing the email or analytics work',
          'The queue path is clearly separate from the direct database connection, not merged into a single synchronous chain',
        ],
      },
    },
    {
      id: 'as-message-queues-rabbitmq',
      title: 'Message queues and RabbitMQ patterns',
      content:
        'A message queue is the concrete mechanism that makes asynchronous communication actually work: a durable, ordered buffer sitting between a producer that creates messages and a consumer that processes them, so the two never have to talk directly or be online at the same moment. **RabbitMQ** is one of the most widely used message brokers in production systems, and understanding its core patterns is what turns "just use a queue" into an actual design.\n\nIn the **producer/consumer model**, producers publish messages onto the queue without needing to know anything about who will process them, and consumers read messages off the queue and process them without needing to know who produced them. This decoupling is the entire point: the producer and consumer can be deployed, scaled, and even fail independently of each other, since neither holds a direct connection or dependency on the other\'s availability at the moment a message is sent.\n\nRabbitMQ specifically routes messages through **exchanges** before they reach a queue, rather than a producer publishing directly to a named queue. A **direct exchange** routes a message to exactly one specific queue based on a routing key, useful for a command meant for one particular consumer, like "process this specific refund." A **fanout exchange** ignores the routing key entirely and broadcasts the same message to every queue bound to it, useful when multiple, unrelated consumers each need to react to the same event independently, like both a notification service and an analytics service both needing to know a new user just signed up.\n\nWhat happens when a consumer crashes partway through processing a message is the detail that separates a working queue design from a broken one. RabbitMQ does not remove a message from the queue the instant a consumer picks it up; it waits for an explicit **acknowledgment** that processing finished successfully. If a consumer crashes before sending that acknowledgment, RabbitMQ redelivers the message to another consumer to try again, so work is never silently lost to a crash. This directly implies that consumers must be **idempotent**, meaning processing the exact same message twice produces the exact same result as processing it once, because a redelivered message after a crash could otherwise mean a user receives the same confirmation email twice, or worse, a wallet gets credited twice for one transaction.\n\nA queue that keeps growing because consumers cannot keep up with the rate messages arrive is called **queue buildup**, and it is a warning sign that either the consumer needs to scale out with more instances, or the work being done per message needs to get cheaper. Monitoring queue depth, not just whether the queue exists, is what catches this before it becomes a customer-facing delay, since a growing queue means real user-facing work, an email, a notification, is falling further and further behind in real time.\n\nIn an interview, when you introduce a queue, be specific about the pattern: "I would use a direct exchange routing to a specific queue for this refund command, since exactly one consumer should process it, and I would make sure that consumer is idempotent, since RabbitMQ redelivers a message if the consumer crashes before acknowledging it." Naming the exchange type and the idempotency requirement together is what shows you understand what actually happens when something fails, not just that a queue exists.',
      designChallenge: {
        prompt:
          'A new user signs up, and both a Notification Service and an Analytics Service need to independently react to that event. Design a setup where the signup event is published once and both services receive their own copy of it, rather than the signup service calling each one directly.',
        gradingCriteria: [
          'The canvas has a queue component that the signup service publishes to',
          'The canvas has at least two separate consumer service components, Notification and Analytics, both connected to the queue',
          'The signup service is not connected directly to the Notification or Analytics services, only to the queue',
          'The design reflects a fanout-style pattern where both consumers can receive the same event independently, not a single consumer processing it once',
        ],
      },
    },
    {
      id: 'as-event-streaming-kafka',
      title: 'Event streaming and Kafka patterns',
      content:
        'RabbitMQ is built for directed commands: a message has one intended recipient, and once consumed, it is gone. Some problems need a fundamentally different shape: not "process this one command" but "here is a continuous stream of everything that happened, and multiple independent systems each need their own complete view of it." That is **event streaming**, and **Kafka** is the technology most commonly reached for to build it.\n\nThe core difference starts with how long a message lives. In RabbitMQ, a message is typically deleted once a consumer acknowledges it. In Kafka, an event published to a **topic** persists for a configured retention period, often days, regardless of whether anyone has read it yet. This means a new consumer can be added later and read the entire history of events from the beginning, or a consumer that fell behind can be restarted and pick up exactly where it left off, capabilities that a delete-on-consume queue like RabbitMQ simply cannot offer.\n\nA Kafka topic is split into **partitions**, and this is where a lot of Kafka\'s actual design decisions live. Each partition is an independently ordered, append-only log, and Kafka guarantees strict ordering only within a single partition, not across the whole topic. A producer typically assigns a **partition key**, often something like a user id or transaction id, and every event with the same key always lands on the same partition, which is what guarantees all events for that one entity are processed in the order they happened, even though the topic as a whole is split across many partitions running in parallel.\n\n**Consumer groups** are how Kafka lets multiple independent systems each read the same stream at their own pace. A fraud-detection service and an analytics service can both be separate consumer groups reading the exact same topic, each tracking its own position, called an **offset**, independently. Within a single consumer group, though, each partition is read by only one consumer at a time, which is what lets you scale a single logical consumer horizontally: add more consumer instances to the group, up to the number of partitions, and each instance picks up a subset of the partitions to process in parallel.\n\nChoosing between Kafka and RabbitMQ comes down to the shape of the problem, not which technology sounds more scalable. "Send this specific notification to this specific user" is a directed command with one intended recipient, RabbitMQ\'s shape. "Every transaction that happened, ever" is a stream multiple independent systems need their own full view of, potentially replayed from the beginning, Kafka\'s shape. Reaching for Kafka because it handles higher throughput, while true, is the wrong reason if the actual problem is a simple one-to-one command that RabbitMQ already solves more simply.\n\nIn an interview, justify the choice by the access pattern, not the brand: "I would use Kafka here because multiple independent services, fraud detection and analytics, both need their own full view of every transaction event, and I want new consumers to be able to replay history later. For a one-to-one command like sending this specific refund, I would use RabbitMQ instead, since there is exactly one intended recipient and no need to retain the message after it is processed." That distinction is what tells an interviewer you picked the tool for the problem, not out of habit.',
      designChallenge: {
        prompt:
          'Every completed transaction needs to be seen independently by a Fraud Detection service and an Analytics service, and a new service added later should be able to replay the full history of past transactions. Design this using an event stream that multiple independent consumers can each read at their own pace.',
        gradingCriteria: [
          'The canvas has a Kafka component representing the event stream that the transaction service publishes to',
          'The canvas has at least two separate consumer services, Fraud Detection and Analytics, both connected to the Kafka component',
          'The transaction service is not connected directly to either consumer, only to the Kafka stream',
          'The design uses a Kafka component specifically rather than a RabbitMQ-style queue, reflecting the need for replay and multiple independent full views of the stream',
        ],
      },
    },
    {
      id: 'as-webhooks-vs-polling',
      title: 'Webhooks vs polling',
      content:
        'Your system needs to know when something changes in a third-party service you do not control: a payment processor confirming a charge went through, a shipping provider updating a package\'s status. You cannot put a queue or an API gateway inside their infrastructure, so you are left with exactly two ways to find out: ask them repeatedly until the answer changes, or have them tell you the moment it does. These are **polling** and **webhooks**, and the difference matters far beyond which one feels more modern.\n\n**Polling** means your system calls the third party\'s API on a fixed interval, every ten seconds, every minute, asking whether anything has changed yet. It is simple to implement, requires no inbound connectivity or public endpoint on your side, and works with any API regardless of whether the other side supports anything more sophisticated. Its fundamental weakness is the gap between polls: if you poll every minute and a payment confirms thirty seconds after your last check, your system does not know for up to thirty more seconds, no matter how urgently that information actually matters. Poll more frequently and you shrink that gap, at the direct cost of a much higher volume of mostly-wasted requests asking if anything is new and getting "no" back almost every time.\n\nA **webhook** flips the direction entirely: you register a URL on your own system, and the third party sends an HTTP request to that URL the instant something happens, no polling loop, no wasted requests asking about nothing. This gets you near-instant notification with a fraction of the request volume polling would need, since a message only ever gets sent when there is actually something to report. The cost is that your system now needs a publicly reachable endpoint, and it has to trust and correctly validate that incoming requests genuinely came from the third party and were not forged by an attacker who found or guessed the URL.\n\nWebhook reliability is where a lot of real-world pain shows up. If your endpoint is briefly down when the third party tries to deliver a webhook, that event can be lost entirely unless the sender specifically retries failed deliveries, and even a well-behaved sender that does retry usually gives up after a handful of attempts over a bounded window. This means a webhook-based integration needs its own reconciliation safety net: a periodic, much less frequent poll, once an hour instead of every ten seconds, specifically to catch anything a lost or failed webhook delivery might have silently missed.\n\nSignature verification is not optional for webhooks that trigger anything consequential. Since the endpoint is publicly reachable by definition, anyone who discovers the URL could send a forged request claiming a payment succeeded if there is nothing checking that the request genuinely came from the real payment processor. Providers solve this by signing each webhook payload with a shared secret, and your endpoint must verify that signature before trusting anything in the request body, treating an unsigned or incorrectly signed request exactly the same as one that never should have been trusted in the first place.\n\nIn an interview, prefer webhooks for anything time-sensitive, but never rely on them alone. Say: "I would use webhooks for near-instant payment confirmation, with signature verification on every incoming request, but I would also add a low-frequency reconciliation poll, once an hour, as a safety net in case any individual webhook delivery is ever lost." Mentioning the reconciliation poll alongside the webhook is what shows you understand that webhooks can silently fail, not just that they exist.',
      designChallenge: {
        prompt:
          'Your system needs near-instant notice when a third-party payment processor confirms a charge, but webhook deliveries can occasionally be lost. Design a setup with a webhook endpoint for instant notification, plus a separate low-frequency polling path as a safety net that checks the processor directly.',
        gradingCriteria: [
          'The canvas has an external-api component representing the third-party payment processor',
          'The canvas has a service component with an incoming connection from the external-api representing the webhook delivery',
          'The canvas has a second connection, from the service to the external-api, representing the reconciliation poll',
          'The two connections are labeled differently, distinguishing the webhook push from the periodic poll',
        ],
      },
    },
  ],
  plannedLessons: [],
};
