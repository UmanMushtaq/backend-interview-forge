import type { ArchitectureModule } from '../../types';

export const microservicesDdd: ArchitectureModule = {
  id: 'microservices-ddd',
  title: 'Microservices & DDD',
  blurb: 'When to split a monolith, bounded contexts, sagas, the API Gateway pattern, and service discovery.',
  lessons: [
    {
      id: 'as-monolith-to-microservices',
      title: "Monolith to microservices, when it's actually worth it",
      content:
        'A **monolith** is a single deployable application: one codebase, one build, one deployment, even if internally it is organized into clean modules with clear boundaries between them. **Microservices** split that same functionality into multiple independently deployable services, each with its own codebase, its own deployment pipeline, and often its own database. The industry conversation around this choice is full of hype in both directions, and the actual answer for most teams, most of the time, is more boring than either extreme suggests.\n\nStart with a monolith. This is not a compromise or a starting point you are meant to outgrow quickly, it is very often the correct architecture for a new product or a small team, full stop. A well-organized modular monolith, one with clear internal module boundaries even though everything ships together, is dramatically easier to develop, test, debug, and operate than an equivalent set of microservices. One deployment to reason about. One process to attach a debugger to. One transaction that can span multiple modules with a normal database transaction, no distributed coordination required. Most of the operational complexity people associate with scaling a system simply does not exist yet at this stage.\n\nMicroservices earn their cost when a specific, concrete pressure appears that a monolith genuinely cannot absorb, not as a default upgrade path every growing company is assumed to need. A module with wildly different scaling needs than the rest of the system, a fraud-detection service that needs to burst to ten times its normal capacity during a sale, while the rest of the system stays flat, is a real reason to extract it, so it can scale independently rather than forcing the entire monolith to scale just to keep that one module fed. A module that needs a genuinely different technology, a machine learning service that needs Python\'s ecosystem sitting inside an otherwise all-Node.js codebase, is another real reason. A team that has grown large enough that multiple groups are stepping on each other deploying the same monolith, blocking each other\'s releases, is an organizational reason, not a technical one, and it is just as valid.\n\nWhat microservices cost in return is substantial and easy to underestimate from the outside. A call that used to be a plain in-process function call is now a network call, with its own latency, its own failure modes, and its own need for the timeouts, retries, and circuit breakers covered in an earlier lesson. A transaction that used to be a single database transaction across two tables now has to span two separate services with two separate databases, losing the clean atomicity a monolith gets for free and requiring a pattern like the saga covered in the next lesson. Observability gets harder too: tracing one user\'s request across five separate services to find out which one actually failed is a fundamentally harder debugging problem than reading a single stack trace from a single process.\n\nThe **database-per-service** pattern, one of the core practices that makes microservices actually work rather than just being a monolith split into pieces that all still share one database, enforces that each service owns its own data and nothing reaches into another service\'s tables directly. This is what actually gives you independent deployability, since a service can change its own schema without coordinating with every other service that might otherwise have been directly querying its tables. The cost is that any query needing data owned by two different services can no longer just join across tables, it needs either API composition, calling both services and combining the results in application code, or a read-side projection, one service subscribing to another\'s events and maintaining its own local, denormalized copy of the data it needs.\n\nIn an interview, never present microservices as a strictly better architecture. Say: "I would start with a modular monolith, since this system does not yet have a scaling, technology, or team-size pressure that justifies the operational cost of microservices. I would extract a specific service only once one of those pressures becomes concrete, for example if the fraud-detection module needed to scale independently from the rest of the system." Naming the specific pressure that would justify extraction, rather than assuming microservices by default, is what shows senior judgment.',
      designChallenge: {
        prompt:
          "A modular monolith handles orders, payments, and fraud detection, all in one deployable unit. Fraud detection now needs to burst to ten times its normal capacity during sales, independently of the rest of the system. Design the extraction: pull fraud detection out as its own service with its own database, while orders and payments remain as they are.",
        gradingCriteria: [
          "The canvas has a service component representing the remaining monolith (orders and payments) with its own database",
          "The canvas has a separate Fraud Detection service component with its own separate database, not sharing the monolith's database",
          'The monolith service is connected to the Fraud Detection service, showing they still communicate despite being separately deployable',
          'The design does not merge Fraud Detection\'s database into the monolith\'s database, since database-per-service is the point of the extraction',
        ],
      },
    },
    {
      id: 'as-bounded-contexts-ddd',
      title: 'Bounded contexts and DDD in practice',
      content:
        'Split a system into microservices without a principled way to decide where one service ends and another begins, and you end up with services that are really just a monolith\'s internal files copy-pasted into separate deployables, still tightly coupled, still needing to change together, just now paying network latency for calls that used to be free. **Domain-Driven Design**, DDD, gives you the actual principle for drawing those boundaries correctly, and the core concept behind it is the **bounded context**.\n\nA bounded context is a boundary within which a specific model of the business, and a specific vocabulary, holds true and stays internally consistent. The word "customer" inside a Billing context means something specific: an entity with a payment method, an invoice history, a subscription tier. The word "customer" inside a Support context means something different: an entity with a ticket history, a satisfaction score, a list of past conversations. Both contexts are legitimately talking about the same real-world person, but forcing them to share one single unified Customer model, with every field either context might ever need bolted onto it, produces a bloated, tightly coupled model that nobody can safely change without checking every other context that touches it.\n\nDDD organizes code within a bounded context into distinct layers, each with a specific job. The **domain layer** holds entities, value objects, and domain services, with zero framework dependencies, pure business logic that does not know or care whether it is running inside NestJS, Express, or anything else. The **application layer** orchestrates use cases using those domain objects, calling out through defined **ports**. The **infrastructure layer** provides the **adapters** that actually implement those ports: HTTP controllers, database repositories, message publishers, the concrete, framework-specific code that talks to the outside world.\n\nThis layering, often called **hexagonal architecture** or ports and adapters, is what makes a domain genuinely testable in isolation. Because the domain layer only ever talks to ports, abstract interfaces, rather than concrete infrastructure, you can plug in an in-memory adapter for tests and a real Postgres adapter for production, testing the entire domain and application layer without ever touching a real database, a real message broker, or a real HTTP server. A bug in business logic gets caught by a fast, in-memory unit test; a bug in how you talk to Postgres gets caught by a separate, narrower integration test, and the two never need to be tangled together in the same slow, flaky test suite.\n\nBounded contexts do not need to become microservices to be useful, and this is the detail that gets lost when DDD gets tangled up with microservices in people\'s minds. A modular monolith can have several clean bounded contexts, Billing, Support, Inventory, each with its own model and its own vocabulary, living inside module boundaries within a single deployable, with no network calls between them at all. The moment you do decide to extract microservices, though, bounded contexts are exactly where the seams should go, since a boundary that already represents a coherent, self-contained model of the business is precisely where a network call between two independently deployable services will hurt the least.\n\nIn an interview, use bounded contexts to justify where a service boundary goes, rather than splitting arbitrarily by technical layer, one service for all controllers, another for all database access. Say: "I would draw the Billing and Support boundaries as separate bounded contexts, since they model customer differently and rarely need to change together, and if this ever needs to become microservices, that boundary is where I would split it, rather than splitting by technical layer, which would leave every meaningful change touching multiple services at once." Naming the bounded context, not just saying it would be split up, is what shows you understand where seams actually belong.',
      designChallenge: {
        prompt:
          "Billing and Support both talk about a 'customer,' but each needs a different model of what that means: Billing cares about payment methods and invoices, Support cares about ticket history. Design two separate services, one for each bounded context, each with its own database holding its own model of the customer, connected to each other only where they genuinely need to share information.",
        gradingCriteria: [
          'The canvas has a Billing service component with its own database',
          'The canvas has a separate Support service component with its own separate database',
          'The two services are not sharing a single combined database for customer data',
          'If the two services are connected at all, the connection is labeled to show a specific, narrow reason for the interaction, not an unrestricted shared model',
        ],
      },
    },
    {
      id: 'as-saga-pattern',
      title: 'Saga pattern: orchestration vs choreography (the NexusPay transfer flow)',
      content:
        'A single database transaction guarantees that a set of writes either all succeed or all fail together, with no in-between state ever visible to anyone. The moment you split a system into microservices with a database per service, that guarantee disappears for anything that spans more than one of them, because there is no longer one database to wrap a transaction around. A **saga** is how you get correctness back for a multi-step operation that spans several services, without the atomic transaction a monolith would have given you for free.\n\nA saga breaks a multi-step operation into a sequence of local transactions, each one scoped to a single service\'s own database, with each step publishing an event or message that triggers the next. NexusPay\'s transfer flow is a textbook example: acquire a Redis lock on the source wallet, save the transaction as PENDING, debit the source wallet, publish a message requesting the credit, credit the destination wallet, then publish `transaction.completed`. Each of those is a normal, local transaction against one service\'s own database; the saga is the sequence connecting them together, coordinated across services rather than atomic in the way a single database transaction would be.\n\nThe hard question a saga has to answer is what happens when a step partway through fails. If the debit on the source wallet succeeds but the credit to the destination wallet then fails, you cannot simply roll back the way a database transaction would, since the debit was already committed, is already visible, and may have already been observed by other reads. The fix is a **compensating transaction**: an explicit action that semantically undoes the effect of an earlier step, crediting the source wallet back the amount that was debited, rather than a technical rollback of something that was never wrapped in one shared transaction to begin with. Every step in a saga that has a real-world effect needs a corresponding compensating action defined for it up front, not improvised after something has already gone wrong in production.\n\nThere are two ways to coordinate the steps of a saga, and the choice changes where complexity lives. **Orchestration** puts a central coordinator in charge: it explicitly calls each step in order and explicitly triggers the right compensating transaction if a later step fails, so the entire flow\'s logic lives in one visible, testable place. **Choreography** has no central coordinator at all: each service listens for events from the previous step and reacts independently, publishing its own event for whichever service happens to be listening next, with the overall flow emerging from the sum of these independent reactions rather than being written down anywhere as one sequence.\n\nOrchestration is easier to reason about and debug, since the entire flow is visible in one place, one service, one piece of code you can read top to bottom to understand the whole operation, but it does introduce a new central component that itself needs to be reliable, and every service in the flow becomes coupled to that one orchestrator knowing about them. Choreography avoids that central coupling entirely, each service only needs to know about the events it personally cares about, but the overall flow is not written down anywhere as a single readable sequence, it only exists implicitly as the sum of every service\'s independent event handlers, which becomes genuinely hard to trace once more than a handful of steps are involved.\n\nIn an interview, if asked to design NexusPay\'s transfer flow, name the pattern and the failure path explicitly: "I would orchestrate this from the Transaction Service, since a financial flow benefits from having its entire sequence, and its compensating transactions, visible in one place rather than spread across independent event handlers. If the credit step fails after the debit succeeded, the orchestrator triggers a compensating credit back to the source wallet, rather than attempting an impossible rollback of an already-committed local transaction." Naming both the coordination style and the specific compensating action is what shows you understand why sagas exist at all.',
      designChallenge: {
        prompt:
          "Design NexusPay's transfer flow as a saga: a Transaction Service orchestrates a debit against the source wallet and a credit to the destination wallet, each owned by a Wallet Service with its own database. Show how the flow would compensate if the credit step failed after the debit already succeeded.",
        gradingCriteria: [
          'The canvas has a Transaction Service (orchestrator) connected to a Wallet Service that owns the wallet database',
          'The design shows the debit and credit as distinct steps or connections, not a single merged operation',
          'At least one connection or label references a compensating transaction or rollback path for the case where the credit fails after the debit succeeded',
          "The Wallet Service's database is a distinct component from the Transaction Service, reflecting separate ownership rather than one shared database",
        ],
      },
    },
    {
      id: 'as-api-gateway-pattern',
      title: 'API Gateway pattern',
      content:
        'Split a monolith into ten microservices, and a client now technically needs to know the network address of all ten, handle authentication separately for each, and stitch together a page that needs data from three of them into one coherent response. Nobody wants a mobile app making seven separate authenticated calls to seven separate services just to render one screen. The **API Gateway** pattern solves this by putting one single entry point in front of every service, so the client only ever needs to know about one thing, not the entire internal topology behind it.\n\nThe gateway\'s first job is handling **cross-cutting concerns** once, in one place, instead of every individual service reimplementing the same logic independently and inconsistently. Authentication, verifying a JWT is valid, rate limiting, checking whether this client has exceeded its allowed request volume, and routing, deciding which internal service a given path actually belongs to, all happen at the gateway before a request ever reaches a specific service. Without this, every one of your ten services needs its own JWT validation code, and the moment one of them has a subtle bug in that logic, you have an inconsistent, service-specific security hole rather than one shared, carefully tested implementation everyone relies on.\n\nA gateway can also do **request composition**: a single client request for "show me this order" might genuinely need data from an Order Service, a Payment Service, and a Shipping Service, three separate internal calls. Rather than forcing the client to make three separate authenticated calls and stitch the results together itself, the gateway can call all three internally and return one composed response, which is both simpler for the client and lets the gateway parallelize those three internal calls itself rather than the client\'s request waking them up one at a time.\n\nThe gateway becomes a genuinely critical piece of infrastructure the moment it exists, which cuts both ways. Every single request to every service now flows through it, so it has to be fast and highly available, since a gateway outage is effectively a total outage for the entire system regardless of how healthy every individual service behind it still is. This is exactly why the gateway itself is typically deployed behind a load balancer with multiple instances, the same horizontal scaling pattern covered in an earlier lesson, rather than being a single server that quietly becomes the one component the entire system\'s uptime now depends on.\n\nA common mistake is letting business logic creep into the gateway over time, an "add the shipping fee here since it is convenient" kind of change that seems harmless in the moment. The gateway should stay a thin routing and cross-cutting-concerns layer; real business logic belongs inside the services that own that specific domain. A gateway that has quietly accumulated business logic becomes a second, hidden monolith sitting in front of your microservices, coupling every service\'s behavior to a shared component that was only ever supposed to route and authenticate, defeating a large part of the reason the services were split apart in the first place.\n\nIn an interview, when a design has more than two or three services, introduce the gateway early and say why: "I would put an API Gateway in front of all these services, handling authentication and rate limiting once at the edge rather than duplicating that logic across every service, and composing this order screen from the Order, Payment, and Shipping services internally rather than having the client make three separate calls itself." Naming what specifically the gateway is centralizing is what shows more than knowing the term exists.',
      designChallenge: {
        prompt:
          'A client needs to render one order screen that requires data from an Order Service, a Payment Service, and a Shipping Service. Design an API Gateway that sits in front of all three, handling the client\'s single request and composing the response from the three internal services, rather than the client calling each service directly.',
        gradingCriteria: [
          'The canvas has a client connected only to an api-gateway component, not directly to any of the three backend services',
          'The api-gateway is connected individually to Order, Payment, and Shipping service components',
          'All three backend services are present as distinct components, not merged into one',
          'The design does not show the client bypassing the gateway to reach any service directly',
        ],
      },
    },
    {
      id: 'as-service-discovery',
      title: 'Service discovery',
      content:
        'In a monolith, every function call resolves at compile time or, at worst, by looking something up within the same running process. In a microservices system, a service calling another service first has to answer a much harder question: what is the actual network address of a healthy instance of that service, right now, given that instances are constantly being added, removed, restarted, and rescheduled as the system scales and deploys. **Service discovery** is the mechanism that answers this question without hardcoding IP addresses that are guaranteed to become wrong the moment anything changes.\n\nThe most naive approach, hardcoding a specific IP address for the Payment Service directly into the Order Service\'s configuration, breaks the instant that Payment Service instance restarts, gets rescheduled onto different hardware, or a second instance is added to handle more load. In any environment where instances are dynamic, containers being scheduled onto whichever machine has capacity, autoscaling adding and removing instances based on load, a fixed address is wrong almost immediately and stays wrong until someone manually updates it.\n\n**Client-side discovery** solves this by having a calling service ask a **service registry**, a system that tracks every currently healthy instance of every service, for the current list of addresses before making a call, then picking one itself, often using one of the load balancing algorithms covered in an earlier lesson. Tools like Consul and etcd serve this role: every service instance registers itself on startup and deregisters, or gets removed automatically via a failed health check, on shutdown or crash, so the registry\'s list stays accurate as instances come and go without anyone manually maintaining it.\n\n**Server-side discovery** moves that same lookup behind a load balancer or gateway instead, so the calling service just sends its request to one fixed, stable address, and the load balancer itself consults the registry and picks a healthy instance to route to. This is simpler for the calling service, which no longer needs any discovery logic or a service-registry client of its own baked into it, at the cost of adding another hop, and another piece of shared infrastructure, that every call now depends on being healthy and responsive.\n\nModern container orchestration platforms like Kubernetes bundle service discovery into the platform itself, so most teams running on Kubernetes never build this mechanism by hand. A Kubernetes **Service** provides one stable internal DNS name that automatically resolves to the current set of healthy pods behind it, updating continuously as pods are created, destroyed, and rescheduled, which is effectively server-side discovery provided by the platform rather than something the application code has to implement or even be aware of.\n\nIn an interview, if your design has more than a couple of services calling each other, mention service discovery explicitly rather than implying addresses are simply known: "since instances are added and removed dynamically as the system scales, I would use service discovery rather than hardcoded addresses, either through a registry like Consul that calling services query directly, or through the platform itself if this is running on Kubernetes, where a Service object gives every caller one stable address that always resolves to a current, healthy instance." Naming the mechanism, not assuming addresses are simply known, is what shows you have thought about what happens as the system actually scales and deploys.',
      designChallenge: {
        prompt:
          'An Order Service needs to call a Payment Service, but Payment Service instances are added and removed dynamically as the system scales. Design a setup where the Order Service does not hardcode any specific instance address, instead going through a service registry or discovery mechanism that always points to a currently healthy instance.',
        gradingCriteria: [
          'The canvas has an Order Service component and a Payment Service component that are not directly hardwired to one specific instance',
          'The canvas has a distinct component representing the service registry or discovery mechanism, separate from the Payment Service itself',
          'The Order Service connects to the registry or discovery component before reaching the Payment Service, rather than connecting straight to Payment Service',
          'The design allows for more than one Payment Service instance conceptually, consistent with dynamic discovery rather than one fixed address',
        ],
      },
    },
  ],
  plannedLessons: [],
};
