import type { ArchitectureModule } from '../../types';

export const dataStorage: ArchitectureModule = {
  id: 'data-storage',
  title: 'Data & Storage',
  blurb: 'Choosing the right database, indexing at scale, replication, sharding, and caching.',
  lessons: [
    {
      id: 'as-sql-vs-nosql',
      title: 'SQL vs NoSQL, choosing the right one for the job',
      content:
        'Every system stores data somewhere, and the first real design question you face is almost always the same one: what kind of database. The honest answer is that there is no universally correct choice, only a **SQL** or **NoSQL** database that fits the shape of your specific data and the guarantees your specific problem actually needs.\n\nSQL databases like PostgreSQL and MySQL store structured data in tables with a fixed schema, support relationships between tables through foreign keys, and guarantee **ACID transactions**: a set of writes either all succeed together or all fail together, with no half-finished state ever visible to anyone else. This makes SQL the natural choice for financial data, where "the money left account A but never arrived in account B" is not an acceptable failure mode under any circumstance.\n\nNoSQL databases like MongoDB, Cassandra, and DynamoDB trade some of those guarantees for other properties: flexible or schema-less documents that do not require a migration every time a field changes, easier horizontal scaling across many machines, and consistency models that prioritize availability over every reader seeing identical data at the same instant. A user activity feed, every click, every page view, might use Cassandra instead of Postgres, because it needs to accept an enormous volume of writes per second with no joins between tables and no need for those writes to be transactionally linked to anything else.\n\nThe mistake that separates a junior answer from a senior one is treating NoSQL as simply "the faster, more modern option." It solves different problems for different data shapes, not the same problem better. A payment ledger needs SQL because double-entry bookkeeping requires a debit and its matching credit to succeed or fail as one atomic unit, a guarantee most NoSQL databases do not offer across multiple records by default. Reaching for NoSQL because it sounds more impressive in an interview rather than because your specific access pattern demands it is a common and easily spotted mistake.\n\nThe practical rule of thumb: start with PostgreSQL. With proper indexing, read replicas, and eventually sharding, a single well-run PostgreSQL cluster can handle far more scale than most systems will ever actually need, and it gives you ACID guarantees for free the whole time. Move to a NoSQL database only when you hit a specific, concrete limit that PostgreSQL genuinely cannot solve for your access pattern, a write volume no amount of sharding can sustain, or a document shape that is naturally schema-less and awkward to force into rows and columns.\n\nIn an interview, when asked which database you would use, never just name one, always explain why. Say: "I would use PostgreSQL because we need ACID guarantees for financial transactions, with a Redis cache in front of it to absorb read load so the database only sees the traffic it actually needs to." That single sentence shows you are choosing based on the properties the data requires, not habit or trend.',
      designChallenge: {
        prompt:
          "A social app needs to store two very different kinds of data: users' account balances for an in-app wallet, and a firehose of click events for analytics that nobody needs to query transactionally. Design a setup that uses a different type of database for each, and connect the right service to the right store.",
        gradingCriteria: [
          'The canvas has at least two distinct database or storage components, not one shared database for everything',
          'One database component is clearly intended for the wallet or balance data, the data needing strong consistency',
          'The other database or storage component is clearly intended for the high-volume click events',
          'Each service or gateway component connects to the specific database it actually needs, not a single database serving both purposes',
        ],
      },
    },
    {
      id: 'as-indexing-at-scale',
      title: 'Indexing at scale, what it costs you',
      content:
        'A table with a hundred rows is fast no matter how you query it. A table with a hundred million rows is fast only if the database can find what you asked for without checking every single row along the way. That is the entire purpose of an **index**, and understanding what it actually costs you, not just what it gives you, is what separates candidates who can talk about databases from candidates who have actually operated one under real load.\n\nWithout an index on the `email` column, finding a user by email means a **full table scan**: the database checks every row, one at a time, until it finds a match or reaches the end of the table. On a hundred rows this is instant. On a hundred million rows this can take seconds, and it gets linearly slower as the table grows, forever, with no ceiling. An index on `email` is a separate, sorted data structure, typically a B-tree, that lets the database jump almost directly to the matching row instead of scanning everything, turning an operation that gets slower as the table grows into one that stays fast regardless of table size.\n\nThat speed is not free. Every index has to be updated whenever a row is written, so a table with five indexes pays the cost of five extra updates on every single insert, update, or delete, even though the query itself only touched one row. This is why indexing every column "just in case" is a common and expensive mistake: each additional index makes writes slower and consumes additional disk space, sometimes more space than the actual table data itself, in exchange for a query speedup that only pays off if that specific column is actually filtered or sorted on regularly.\n\nThe columns worth indexing are the ones that appear in your `WHERE` clauses, your `JOIN` conditions, and your `ORDER BY` clauses, not every column that exists. A **composite index** across multiple columns, like `(user_id, created_at)`, can serve a query that filters by user and sorts by date far more efficiently than two separate single-column indexes ever could, because the database can use one ordered structure instead of combining two. Getting the column order right in a composite index matters too: an index on `(user_id, created_at)` speeds up a query filtering by `user_id` alone, but an index on `(created_at, user_id)` generally will not, because the database can only efficiently use the leading columns of the index in order.\n\nAt real scale, indexes are not something you add once and forget. As query patterns shift, an index that used to matter can become dead weight that only slows down writes, while a new hot query path might be running unindexed full scans without anyone noticing until the table grows large enough to make it painfully obvious. Reviewing which indexes actually get used, and dropping the ones that do not, is a routine part of operating a database at scale, not a one-time setup step.\n\nIn an interview, do not just say "I would add an index." Say which column, and why: "I would add a composite index on `user_id` and `created_at`, since the main query here filters by user and sorts by recency, and I would avoid indexing columns we never filter or sort on, since every extra index slows down writes without a matching benefit." Naming the specific columns is what shows you understand the trade-off, not just the vocabulary.',
      designChallenge: {
        prompt:
          'A "find user by email" endpoint is doing a full table scan on a hundred million row table and it is slow. Design the read path for this endpoint: a service that receives the lookup, and a database it queries. Label the connection to make it clear this is a fast, indexed lookup by email, not a full scan.',
        gradingCriteria: [
          'The canvas has a service component that receives the lookup request',
          'The canvas has a database component the service queries',
          'The connection between service and database has a label mentioning an index, indexed lookup, or the specific column, not just a generic label',
          'The design does not introduce unnecessary components like a queue or load balancer that are not part of solving this specific read-path problem',
        ],
      },
    },
    {
      id: 'as-database-replication',
      title: 'Database replication (primary-replica)',
      content:
        'One database server can only accept so many concurrent reads before it starts slowing down, and worse, if that one server disappears, so does your entire application. **Replication** solves both problems at once by keeping multiple copies of your data on multiple machines, with one designated as the **primary** and the rest as **replicas**.\n\nIn the standard **primary-replica** setup, all writes go to the primary. The primary then streams every change to its replicas, typically through the database\'s write-ahead log, so each replica ends up holding the same data the primary has, just slightly behind in time. Reads can then be spread across the replicas instead of all landing on the primary, which is exactly what lets a single logical database absorb far more read traffic than any one machine could handle alone, since you can add replicas horizontally to soak up more read volume the same way you would add application servers behind a load balancer.\n\nThe catch is **replication lag**: the delay between a write landing on the primary and that same write showing up on a replica. This delay is usually milliseconds, but it is never zero, and it creates a real correctness problem candidates often miss. If a user updates their profile and your application immediately reads that profile back from a replica that has not caught up yet, the user sees their old data, even though the write technically succeeded moments earlier. The two common fixes are reading your own writes from the primary specifically right after you write them, or accepting a short window of staleness because the data in question does not need to be perfectly current the instant it changes.\n\nReplicas also give you something almost as valuable as read scaling: automatic failover. If the primary crashes, one of the replicas can be promoted to take over as the new primary, so the system keeps accepting writes with only a brief interruption instead of a total outage. This promotion process needs to be handled carefully though, since a promoted replica that was slightly behind the old primary can mean a handful of recent writes are lost in the handover, a trade-off most systems accept in exchange for not going down completely when a single machine fails.\n\nReplication does not fix every scaling problem on its own. It scales reads, since you can add more replicas to handle more read traffic, but it does nothing for write scaling, because every single write still has to go through the one primary, no matter how many replicas exist behind it. A write-heavy system that has already maxed out its primary needs a different strategy entirely, typically sharding, which splits the data itself across multiple independent primaries rather than just copying all of it everywhere.\n\nIn an interview, when a system is read-heavy, reach for replication early and say why: "I would add read replicas behind the primary to absorb read traffic, and route any read that must see the most recent write straight to the primary to avoid replication lag showing stale data." If the system is write-heavy instead, say explicitly that replication alone will not help, since every write still funnels through one primary, and point toward sharding instead.',
      designChallenge: {
        prompt:
          'A read-heavy endpoint is overwhelming a single database server. Design a setup with one primary database that accepts writes and at least two replica databases that absorb read traffic, with the service correctly routing reads and writes to the right one.',
        gradingCriteria: [
          'The canvas has one database component clearly acting as the primary, receiving writes',
          'The canvas has at least two additional database components acting as replicas',
          'The service component connects to the primary for writes and connects separately to at least one replica for reads, rather than sending everything to a single database',
          'Connections are labeled to distinguish write traffic from read traffic, or replication between primary and replicas is shown',
        ],
      },
    },
    {
      id: 'as-database-sharding',
      title: 'Database sharding and partition keys',
      content:
        'Replication scales reads by copying the same data onto more machines, but it does nothing for writes, since every write still funnels through one primary no matter how many replicas sit behind it. When a single primary can no longer keep up with write volume, or the dataset itself has grown too large for one machine to store, the fix is **sharding**: splitting the data itself across multiple independent databases, each holding a different slice of the total.\n\nEach shard is a complete, independent database that owns a subset of the rows, not a copy of all of them. A system with four shards might split its users so that each shard holds roughly a quarter of all user records, with each shard handling its own reads and writes for the rows it owns, completely independently of the other three. This is what makes sharding scale writes in a way replication cannot: four shards can accept four times the write volume of one database, because each is doing genuinely separate work rather than copying the same write four times.\n\nThe single most important decision in sharding is the **partition key**: the value used to decide which shard a given row belongs to. A common approach hashes the user\'s id and uses that hash to pick a shard, which spreads rows evenly and predictably. Choose a bad partition key and you get a **hot shard**, one shard receiving a wildly disproportionate share of traffic while the others sit nearly idle, which defeats the entire purpose of splitting the data in the first place. Sharding by something like signup country sounds reasonable until you notice that one country has ten times the user base of the others, and that single shard becomes the new bottleneck all over again.\n\nSharding\'s biggest cost shows up the moment you need data that spans multiple shards. A query that used to be a simple join across two tables on one database now has to fan out to multiple shards, collect partial results from each, and merge them in application code, since the database itself has no way to join rows that live on physically separate machines. Transactions that need to touch rows on two different shards atomically become genuinely hard, often requiring a distributed transaction protocol or a redesign that avoids needing cross-shard atomicity at all. This complexity is exactly why sharding should be a last resort, reached for only after replication, caching, and vertical scaling have all been pushed as far as they reasonably go.\n\nResharding, changing the number of shards or the partition key later, is expensive and risky precisely because it means physically moving large amounts of data between machines while the system stays live. Systems that anticipate needing to reshard often build in extra shards up front, more than currently needed, specifically so that growth can be absorbed by moving whole shards to new hardware rather than having to change the partition key and rebalance everything from scratch.\n\nIn an interview, only reach for sharding once you have explicitly ruled out simpler fixes. Say: "before sharding, I would check whether replication and caching solve the read side, and whether a bigger primary solves the write side. If write volume alone is still too high for one machine, I would shard by user id hash rather than something like country, specifically to avoid a hot shard from an uneven distribution." Naming the partition key choice, and defending it, is what shows real understanding of the trade-off you are making.',
      designChallenge: {
        prompt:
          "A single database's write volume has outgrown what one primary can handle, even with replicas in place. Design a sharded setup: a service that routes each request to the correct shard based on a partition key, and at least three separate shard databases, each owning its own slice of the data.",
        gradingCriteria: [
          'The canvas has at least three separate database components representing distinct shards, not one shared database',
          'The canvas has a service or gateway component that sits in front of the shards and decides routing',
          'The service is connected to each shard individually, showing it can reach any of them depending on the partition key',
          'The shards are not connected directly to each other, since each shard should be independent rather than replicating to its peers',
        ],
      },
    },
    {
      id: 'as-caching-strategies',
      title: 'Caching strategies: cache-aside, write-through, write-behind',
      content:
        'Adding a cache to speed up reads is the easy part, drop a Redis instance in front of your database and store the results of expensive queries there. The harder question, the one interviews actually probe, is what happens to that cached value when the underlying data changes. Get this wrong and your cache does not just fail to help, it actively serves incorrect data to users with total confidence. There are three standard patterns for managing this, and each makes a different trade-off between simplicity, freshness, and write latency.\n\n**Cache-aside** is the most common pattern in practice. Your application code checks the cache first; on a hit, it returns the cached value immediately without touching the database at all. On a miss, it falls through to the database, fetches the real value, and writes a copy into the cache before returning it, so the next request for that same data becomes a hit. The cache in this pattern only ever holds data that has actually been requested at least once, and the application, not the cache itself, owns the logic for keeping the two in sync, which usually means explicitly deleting or updating the cached key whenever the underlying row changes.\n\n**Write-through** takes a different approach: every write goes to the cache and the database together, as part of the same operation, so the cache is never allowed to hold stale data in the first place. This guarantees freshness in a way cache-aside cannot, at the direct cost of write latency, since every write now has to succeed against two systems instead of one before it can return to the caller. Write-through fits situations where reading stale data would be a real problem and the extra write latency is an acceptable price for never risking it.\n\n**Write-behind**, sometimes called write-back, flips the order: the application writes to the cache immediately and returns to the caller right away, while the cache flushes that write to the database asynchronously, sometime shortly after. This gives the fastest possible write latency of the three patterns, since the caller never waits on the database at all. It also carries a genuine risk that cache-aside and write-through do not: if the cache crashes after accepting the write but before flushing it to the database, that write is lost permanently, with no record of it ever having happened. Write-behind is reserved for data where losing a small window of very recent writes during a rare crash is an acceptable trade for consistently fast writes.\n\nUnderneath all three patterns sits the same hard problem: **cache invalidation**, deciding when a cached value is no longer trustworthy. A short **TTL**, time to live, is the simplest safety net, since even a value nobody explicitly invalidated will self-correct once it expires, bounding how stale any cached value can ever get. Explicitly deleting a specific key the moment its underlying row changes is more precise but requires remembering to do it at every single place that row can be modified, which becomes error-prone as a codebase grows and more code paths touch the same data.\n\nIn an interview, never just say "I would cache this." Name the pattern and why it fits: "I would use cache-aside with a five-minute TTL for this user profile endpoint, since it is read constantly but changes rarely, so a short staleness window is a small price for removing that read load from the database entirely. For the account balance, though, I would use write-through instead, since serving a stale balance even briefly is not acceptable here." Matching the pattern to what the specific data can tolerate is what shows real judgment, not just knowing three names.',
      designChallenge: {
        prompt:
          'A user profile is read on almost every request but updated rarely, and it can tolerate being a few minutes stale. Design the read and write path using cache-aside: a service checks the cache first on reads, falls back to the database on a miss, and on writes, updates the database and invalidates the cached copy so the next read is not stale.',
        gradingCriteria: [
          'The canvas has a cache component and a database component, both connected to the service',
          'The connection from service to cache is labeled or otherwise clearly represents reads and cache-aside lookups',
          'The connection from service to database is labeled or otherwise clearly represents both the miss path and writes',
          'The design reflects that the cache is checked before the database on reads, not both being hit unconditionally on every request',
        ],
      },
    },
    {
      id: 'as-cdns',
      title: 'CDNs and static asset delivery',
      content:
        'An image, a video, or a JavaScript bundle does not change from one request to the next, and it does not need your application server or your database involved at all to be served. Yet a naive setup routes every single request for that file through your load balancer, your application server, and sometimes your database, for a file that is byte-for-byte identical to the one served a second ago. A **CDN**, content delivery network, exists specifically to take this entire category of traffic off your servers.\n\nA CDN is a network of servers, called **edge nodes**, physically distributed around the world, each holding a cached copy of your static assets. When a user in Tokyo requests an image, the CDN serves it from an edge node in or near Tokyo rather than routing all the way back to your origin server, wherever that happens to be located. This has two separate benefits that are easy to conflate: it removes load from your actual servers entirely for anything the CDN can serve, and it dramatically cuts latency for the user, because the physical distance the data has to travel is much shorter when it comes from a nearby edge node instead of a single origin server on the other side of the planet.\n\nThe first time an edge node receives a request for a file it does not have cached yet, it is a **cache miss**: the edge node fetches the file from your origin server, stores a copy, and serves it, the same cache-aside pattern from the previous lesson in every meaningful way, just applied geographically instead of to a database. Every subsequent request for that file from users near that edge node becomes a cache hit, served directly without ever touching your origin server again, until the cached copy expires or is explicitly invalidated.\n\nCDNs are built for content that changes rarely and is the same for every user: images, videos, CSS, JavaScript bundles, and other static assets. They are a poor fit for content that is personalized per user or changes on every request, like a live account balance or a real-time chat message, because caching that kind of data at the edge risks serving one user\'s private or stale data to someone else, or serving numbers that are already wrong by the time they are displayed. Knowing which category a given piece of content falls into is what determines whether a CDN even applies at all.\n\nCache invalidation shows up here too, in a form specific to CDNs. Deploying a new version of a JavaScript bundle under the exact same filename risks some users continuing to receive the old cached version from an edge node that has not refreshed yet, a subtle bug that can be genuinely painful to track down. The standard fix is **cache busting**: including a content hash or version number in the filename itself, so a new deployment produces a genuinely new filename, and the CDN naturally treats it as new content to fetch rather than serving a stale cached copy under a name it has already seen.\n\nIn an interview, when a design involves images, video, or a frontend bundle, mention a CDN early and explain what it actually buys you: "I would put a CDN in front of static assets, since that removes that traffic from my application servers entirely and serves users from a nearby edge node instead of my single origin, cutting latency for anyone far from where my servers are hosted. For anything personalized or real-time, I would keep it off the CDN, since caching that at the edge risks serving stale or private data to the wrong person."',
      designChallenge: {
        prompt:
          'A media-heavy app serves large images to users all over the world, and right now every image request goes straight to the origin service. Design a setup that puts a CDN in front of the origin, so most requests are served from the edge without ever reaching the origin service.',
        gradingCriteria: [
          'The canvas has a CDN component positioned between the client and the origin service',
          'The client connects to the CDN, not directly to the origin service',
          'The CDN is connected to the origin service, representing the cache-miss fetch path',
          'The design does not route the CDN through a database directly, since the CDN caches assets served by the origin, not raw database rows',
        ],
      },
    },
  ],
  plannedLessons: [],
};
