ABSOLUTE RULE: Never create a branch. Never use git checkout -b. Never use git switch -c. Never open a pull request. Always commit directly to main. If you find yourself about to create a branch, stop and commit to main instead.

# Project instructions

Always commit and push directly to main. Never create a branch. Never use git checkout -b. Never use git switch -c. This is a personal single-developer repo with no branch protection.

Do not run git config commands. Use whatever identity is already configured.
